---
phase: 02-budget-governor-search-presets
plan: 08
type: execute
wave: 3
depends_on: [02-02, 02-05]
files_modified:
  - tests/db/budget-meter.test.ts
  - tests/db/budget-admin-gate.test.ts
  - tests/db/budget-concurrency.test.ts
autonomous: true
requirements: [BUDG-01, BUDG-02]

must_haves:
  truths:
    - "Success criterion 5: with the cap reached, a concurrent burst of 40 workers is refused with no over-spend - exactly 10 granted against a cap that fits 10, reserved equals cap, zero errors"
    - "A denial is ZERO ROWS, not an exception - there is nothing to catch"
    - "bp_not_over refuses a hand-written over-reserve with 23514, and refuses lowering the cap below spent + reserved"
    - "A crashed worker's expired reservations are released by the next reserve, with no scheduler involved"
    - "BUDG-01: settle twice with one request_id produces one ledger row and one balance move"
    - "D-10: a member cannot change the cap; an admin can; both Clerk role spellings resolve to admin"
    - "D-11: one instant in two zones produces opposite month verdicts in SQL, and the reset instant moves with DST"
  artifacts:
    - path: "tests/db/budget-concurrency.test.ts"
      provides: "The 40-way burst against real connections, and the recorded control that proves it discriminates"
      contains: "concurrent burst"
    - path: "tests/db/budget-meter.test.ts"
      provides: "The single-worker meter invariants - denial, CHECK, self-heal, thresholds, settlement"
      contains: "bp_not_over"
  key_links:
    - from: "tests/db/budget-concurrency.test.ts"
      to: "tests/db/_concurrency.ts"
      via: "openTestClients - real pg.Client connections, never withRollback"
      pattern: "openTestClients"
    - from: "tests/db/budget-admin-gate.test.ts"
      to: "app.current_org_role"
      via: "both Clerk claim shapes exercised through actAs"
      pattern: "current_org_role"
---

<objective>
Prove the meter. Not by reading it — by hitting it with 40 genuinely concurrent connections, by refusing it with hand-written SQL, by crashing a worker and watching the next caller reclaim the budget, and by replaying a settlement.

Purpose: ROADMAP success criterion 5 is the only criterion in this phase that a single-threaded test cannot express. RESEARCH measured that the naive check-then-spend body grants all 40 and over-spends 4x under the identical burst — so this test discriminates, which is the property that matters. A green suite proves nothing a mutation check hasn't.
Output: three DB test files, and gate mutations M7, M8, M9 and M10 each watched red against exactly the predicted named test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md
@.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md
@tests/db/_fixtures.ts
@tests/db/_concurrency.ts
@tests/db/rls-isolation.test.ts
@tests/db/event-trigger.test.ts
@tests/db/time.test.ts
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written in full below. `pnpm verify` is NOT runnable here.

If this plan runs in a git worktree: copy `C:/Users/danlo/prospector/.env.local` in, run the launcher with `install`, then `db:migrate` and `db:seed` so the worktree's database matches.

🔴 This is the ONLY wave-3 plan that touches the database. `tests/db/budget-concurrency.test.ts` COMMITS — it is the one test in this repo that cannot use `withRollback` — so do not run it alongside another `test:db` process against the same local PostgreSQL.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: The single-worker meter invariants - denial, the CHECK, self-heal, thresholds, settlement</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Patterns 1 to 4 and section "Code Examples" subsection "The meter, end to end"
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` — the requirement-to-test map rows for BUDG-01 and BUDG-02, and gate mutations M8 and M9
    - `tests/db/_fixtures.ts` — `withRollback`, `actAs`, `actAsOwner`, `seedTwoOrgs`, and the note that `now()` is `transaction_timestamp()` and therefore constant for a whole transaction
    - `tests/db/rls-isolation.test.ts` — the two-assertion message-pinned refusal shape
    - `.planning/CONVENTIONS.md` section Testing
  </read_first>
  <files>tests/db/budget-meter.test.ts</files>
  <action>
Every test inside `withRollback`, two orgs always (`seedTwoOrgs`). `authenticated` holds no INSERT on `budget_periods`, so the fixture row is inserted by `actAsOwner` before switching to `actAs(orgA)`. A shared local helper `seedPeriod(c, orgId, { cap, spent, reserved })` keeps each test to one idea.

Named tests, exactly:

  - `reserve: a grant returns a reservation id and moves reserved` — cap `100000`, spent 0, reserved 0; `select * from app.reserve_budget('places', $period, 10000, null, 'ts_enterprise')` returns one row with a non-null `reservation_id`, `at_100 = false`; the row's `reserved_micro_usd` is now `10000`; a `cost_reservations` row exists with `est_micro_usd = 10000`, `settled_at` null and `expires_at` in the future. This is the positive control every refusal below leans on.
  - `denied returns null: a denial is zero rows, not an exception` — cap `10000`, spent `10000`; the reserve returns a row whose `reservation_id` is **null**, and `at_100` is true, and **nothing throws**. Assert explicitly that the call did not reject — the whole design point is that there is no error to catch. Also assert no `cost_reservations` row was created.
  - `bp_not_over refuses a hand-written over-reserve` — its OWN rolled-back transaction, one refused statement: as the OWNER, `update budget_periods set reserved_micro_usd = cap_micro_usd + 1 where id = ...` and assert `{ code: '23514' }` AND `toThrow(/bp_not_over/)`. The constraint refuses even hand-written SQL and even a bug inside the definer function. Gate mutation **M8** drops this constraint by name.
  - `cap below current spend is refused` — its OWN transaction. With spent `1200` and reserved `400` (total `1600`), lowering the cap to `1600` is ACCEPTED (the positive control, in a separate test named `cap at exactly spent plus reserved is accepted`) while lowering it to `1599` raises `{ code: '23514' }` with `toThrow(/bp_not_over/)`. 🔴 The true floor is `spent + reserved`, which is TIGHTER than UI-SPEC's copy ("at least {spent + $1}") — record that in a comment so a future reader does not "fix" the constraint to match the sentence.
  - `self-heal: a crashed worker's expired reservations are released by the next reserve` — cap `100000`; as the OWNER insert five `cost_reservations` rows totalling `100000` with `expires_at = now() - interval '1 hour'`, `settled_at` null, `released_at` null, and set `reserved_micro_usd = 100000` so the row is fully committed. Then as the tenant call `reserve_budget(..., 10000, ...)`: it must GRANT, the five expired rows must now carry a non-null `released_at`, and `reserved_micro_usd` must be `10000`. Beside it, a separate named test `self-heal does not release a live reservation` — an unexpired reservation is untouched and the reserve is denied if it would breach. Two tests, because a healer that released everything would pass the first alone.
  - `threshold: the 80 percent crossing emits exactly one events row per period` — cap `5000`, spent `3900`; reserve `200`: the returned `pct_after` is `82.0`, `at_80` is true, `at_100` is false, `budget_periods.warned_80_at` is now set, and exactly ONE `events` row exists with `entity_type = 'budget_periods'` and `action = 'budget_80_percent'`. Reserve another `100`: `at_80` is still true but the `events` count is STILL one, and `warned_80_at` did not move. Both halves in one test — the guard is "once per period", and only the second reserve proves it.
  - `threshold: at 100 percent every new reservation is refused` — cap `5000`, spent `5000`; the reserve returns `reservation_id` null and `at_100` true, and `cost_reservations` gains no row.
  - `settlement idempotency: settle twice with one request_id` — reserve `10000`, then `app.settle_reservation(res, 'req-abc', 7000, 1, 'ts_enterprise', 'places')` returns **true**; call it again with the identical `request_id` and it returns **false**. Assert all three consequences: exactly ONE `cost_ledger` row for `request_id = 'req-abc'`; `reserved_micro_usd` went `10000 -> 0` once and not twice; `spent_micro_usd` is `7000` and not `14000`. Gate mutation **M9** drops `cost_ledger_request_id_key` by name.
  - `settlement: reserve the worst case, settle the actual` — reserve `10000`, settle `7000`; `reserved` returns to 0 and `spent` is `7000`, so the `3000` difference went back to the budget. Reserving one page and discovering three is the only way to breach the cap, which is why the reservation is the estimate and the ledger is the actual.
  - `settlement: a zero-cost paid-SKU call still writes a ledger row` — settle with `p_actual_micro = 0` and `p_units = 1`; a `cost_ledger` row exists with `micro_usd = 0` and `units = 1`. 🔴 Without these rows the monthly free allowance is untrackable and every early-month estimate is wrong (RESEARCH Pitfall 4).
  - `cost_cents renders micro-USD exactly` — insert a ledger row with `micro_usd = 35000` through `settle_reservation` and read `cost_cents`: it is `3.50`. Then sum `1428` such rows arithmetically (`select (35000::bigint * 1428) / 10000.0`) and assert `4998.00`, i.e. `$49.98` — the figure that integer cents would render as `$57.12` or `$42.84`.
  - `server version: the test database is at least PostgreSQL 17` — `select current_setting('server_version_num')::int >= 170000`. Production is 17.6 while local and CI are 18; this is the companion to the `pg17` grep test and the reason both exist.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:db -t "settlement idempotency"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:db -t "settlement idempotency"` exits 0 and the output NAMES `settlement idempotency: settle twice with one request_id`
    - `pnpm test:db` exits 0 and its pass list NAMES every test above, including `denied returns null: a denial is zero rows, not an exception`, `bp_not_over refuses a hand-written over-reserve`, `cap at exactly spent plus reserved is accepted`, `self-heal: a crashed worker's expired reservations are released by the next reserve`, `self-heal does not release a live reservation`, `threshold: the 80 percent crossing emits exactly one events row per period`, `settlement: a zero-cost paid-SKU call still writes a ledger row`, `cost_cents renders micro-USD exactly`, `server version: the test database is at least PostgreSQL 17`
    - Gate mutation **M8** recorded verbatim: on the LIVE database `alter table budget_periods drop constraint bp_not_over;` reds `bp_not_over refuses a hand-written over-reserve` AND `cap below current spend is refused`, while the positive control `cap at exactly spent plus reserved is accepted` stays GREEN. Re-add the constraint, confirm green, verify from `pg_constraint`. `git diff --stat` empty.
    - Gate mutation **M9** recorded verbatim: `alter table cost_ledger drop constraint cost_ledger_request_id_key;` reds `settlement idempotency: settle twice with one request_id` and ONLY that test. Re-add, confirm, verify from `pg_constraint`.
    - A third mutation, recorded: remove the self-heal block from `app.reserve_budget`'s body on the LIVE database with `create or replace function`; `self-heal: a crashed worker's expired reservations are released by the next reserve` goes red ALONE and `self-heal does not release a live reservation` stays green. Restore from the migration file's text, verify from `pg_get_functiondef`.
    - Every refusal test sits in its own `withRollback` call — a refusal aborts the transaction and the next statement reports `25P02`, not its own reason
    - Each of the three mutations reds a DIFFERENT set of tests; record the sets side by side so a future reader can see the guards are not coupled
  </acceptance_criteria>
  <done>The meter grants, denies with zero rows, refuses a hand-written breach, heals a crashed worker, crosses 80 percent exactly once, settles idempotently and reports cents exactly — every one proven red first under its own mutation.</done>
</task>

<task type="auto">
  <name>Task 2: The admin gate, both Clerk role spellings, the audited cap change, and the Chicago month in SQL</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Frontend on This Repo" subsection "Admin gate (D-10) - the Clerk role trap" — the real decoded token (`v: 2`, `o.rol = "admin"`, BARE) versus `@clerk/shared`'s `auth().orgRole` (`"org:admin"`, PREFIXED)
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Code Examples" subsection "Chicago month boundary" — the verified SQL pair
    - `tests/db/_fixtures.ts` — the `Claims` type already carries BOTH v1 (`org_id` flat) and v2 (`o: { id, rol, slg }`) shapes; the suite exercises both (Phase 1 D-11)
    - `tests/db/event-trigger.test.ts` — the direct-write acceptance shape, and the note that timestamps are compared IN SQL with `extract(epoch from ...)`, never as JS Dates round-tripped through `pg`
    - `tests/db/time.test.ts` — Phase 1's two-zones-opposite-verdicts pair, which this extends
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` gate mutation M10
  </read_first>
  <files>tests/db/budget-admin-gate.test.ts</files>
  <action>
Every test inside `withRollback`, two orgs always. Named exactly:

  - `current_org_role resolves the v2 bare claim` — `actAs(c, { o: { id: 'org_A', rol: 'admin' }, sub: 'user_danlo', role: 'authenticated' })` then `select app.current_org_role()` returns `'admin'`. 🔴 v2 nests the BARE role under `o.rol`.
  - `current_org_role resolves the v1 prefixed claim` — `actAs(c, { org_id: 'org_A', org_role: 'org:admin', sub: 'user_danlo', role: 'authenticated' })` then `select app.current_org_role()` returns `'admin'` — the `org:` prefix is stripped. 🔴 A naive `coalesce(app.jwt()->'o'->>'rol', app.jwt()->>'org_role')` compares `'admin'` to `'org:admin'` and the admin gate silently passes or silently fails depending on which side was written first. This is the exact sibling of Phase 1's D-11 highest-probability silent failure, and it needs its own test for the same reason.
  - `current_org_role returns null for a member` — `o.rol = 'basic_member'` returns `'basic_member'`, not `'admin'`; and a claim with no role at all returns null. Both, so the function is not simply returning a constant.
  - `set_budget_cap refuses a member` — its OWN rolled-back transaction, one refused statement. As `actAs` with `o.rol = 'basic_member'`, `select app.set_budget_cap('places', 75000000)` raises `{ code: '42501' }` AND `toThrow(/set_budget_cap: admin role required/)`. 🔴 Pin the MESSAGE: `42501` also covers the grant refusal below and an RLS refusal, and only the wording tells the three apart. Gate mutation **M10** rewrites `app.current_org_role()` to return the literal `'admin'` and must red exactly this test.
  - `set_budget_cap accepts an admin` — the positive control, its own test. As `o.rol = 'admin'`, `app.set_budget_cap('places', 75000000)` returns `75000000` and `budget_periods.cap_micro_usd` is `75000000`. Without this, a function that refused everybody would pass the refusal test.
  - `budget_periods holds no direct UPDATE for authenticated` — its OWN transaction, a DIFFERENT invariant from the role check: as `actAs(orgA)`, `update budget_periods set cap_micro_usd = 99999999 where org_id = $1` raises `{ code: '42501' }` AND `toThrow(/permission denied for table budget_periods/)`. This is the GRANT layer, not the function, and the two must not share one refusal — otherwise one mutation reds both and the mutation check stops telling you which guard you broke.
  - `cap change is audited` — the direct-write shape: as the OWNER (no application code in the path) `update budget_periods set cap_micro_usd = 60000000 where id = $1`, then read the latest `events` row for `entity_type = 'budget_periods'` and assert `action = 'update'`, a non-null `actor_id`, `before->>'cap_micro_usd'` and `after->>'cap_micro_usd'` differing, and `occurred_at` within a few seconds — compared IN SQL with `extract(epoch from now() - occurred_at)`, never as a JS `Date` round-tripped through `pg`.
  - `a reservation does NOT write an events row` — the other half, and the reason the trigger is narrowed: reserve `10000` against the same period and assert the `events` count for `entity_type = 'budget_periods'` did not change. At Phase 4 volumes an unnarrowed trigger would write roughly two event rows per paid call, each carrying a full before/after payload — the `source_records` mistake CONVENTIONS already decided against.
  - `two zones: one instant, opposite month verdicts in SQL` — `select (timestamptz '2026-10-01 04:30:00+00' at time zone 'America/Chicago')::date` is `2026-09-30` while `at time zone 'UTC'` is `2026-10-01`, and `date_trunc('month', ... at time zone 'America/Chicago')::date` is `2026-09-01`. 🔴 `America/Chicago` only ever as HALF a pair — the suite runs `TZ=UTC` precisely so a forgotten zone is a red assertion rather than an accident of this Chicago dev machine.
  - `two zones: October's period begins at 05:00Z and March's at 06:00Z` — the DST companion. Assert both instants, so a fixed-offset implementation fails one.
  - `set_budget_cap buckets the period in Chicago, not UTC` — set the transaction time near a month edge if practical, or assert that the function's own period expression contains `at time zone 'America/Chicago'` by reading `pg_get_functiondef('app.set_budget_cap'::regproc)` and matching on it. A bare `::date` silently buckets in UTC and moves every evening change into tomorrow.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:db -t "set_budget_cap"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:db -t "set_budget_cap"` exits 0 and NAMES `set_budget_cap refuses a member`, `set_budget_cap accepts an admin`, and `set_budget_cap buckets the period in Chicago, not UTC`
    - `pnpm test:db -t "current_org_role"` exits 0 and NAMES all three `current_org_role ...` tests
    - `pnpm test:db` exits 0 and its pass list also NAMES `budget_periods holds no direct UPDATE for authenticated`, `cap change is audited`, `a reservation does NOT write an events row`, `two zones: one instant, opposite month verdicts in SQL`, `two zones: October's period begins at 05:00Z and March's at 06:00Z`
    - Gate mutation **M10** recorded verbatim: on the LIVE database, `create or replace function app.current_org_role() returns text language sql stable security definer set search_path = public as $$ select 'admin'::text $$;` reds `set_budget_cap refuses a member` and `current_org_role returns null for a member`, while `set_budget_cap accepts an admin` stays GREEN. Restore the real body from the migration file, verify from `pg_get_functiondef`, `git diff --stat` empty. If it reds two tests rather than one, record that the two properties are independent (one is the gate, one is the resolver) exactly as Phase 1's M3 and M5 were recorded.
    - A second mutation, recorded: `grant update on public.budget_periods to authenticated;` reds `budget_periods holds no direct UPDATE for authenticated` ALONE and NOT `set_budget_cap refuses a member` — proving the two refusals rest on different invariants. Revoke and confirm.
    - The v1 and v2 claim tests each use a DIFFERENT `actAs` shape, and a criterion-level grep confirms both spellings are exercised: `grep -c "org_role" tests/db/budget-admin-gate.test.ts` is at least 1 and `grep -c "rol:" tests/db/budget-admin-gate.test.ts` is at least 2
  </acceptance_criteria>
  <done>The cap is admin-only in the database against both Clerk claim spellings, a direct UPDATE is refused at the grant layer with a distinct message, a cap change is audited while a reservation is not, and the Chicago month is proven by two discriminating pairs.</done>
</task>

<task type="auto">
  <name>Task 3: The 40-way concurrent burst - success criterion 5, with the control that makes it a proof</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Code Examples" subsection "The concurrency proof" — the verified shape, ready to adapt, and the explicit statement that `withRollback` CANNOT express this
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Pattern 1 and its control table: naive `SELECT`-then-`UPDATE` granted 40 of 40 and reserved 400 against a cap of 100
    - `tests/db/_concurrency.ts` (plan 02-02) — `openTestClients`, `closeAll`, `withCommittedFixture`, and why it must never delete an `orgs` row
    - `vitest.db.config.ts` — `fileParallelism: false` and `isolate: false`; serial FILES is orthogonal to what this test needs
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` gate mutation M7
  </read_first>
  <files>tests/db/budget-concurrency.test.ts</files>
  <action>
🔴 This is the one test in the repo that COMMITS. `withRollback` is a single transaction and therefore cannot express two workers seeing each other's commits — that is not a limitation to work around, it is the reason this file exists. Document that at the top of the file so the next reader does not "fix" it back.

Fixture, through `withCommittedFixture` from `tests/db/_concurrency.ts`:
  - one org row `clerk_org_id = 'org_concurrency_fixture'`, created with `on conflict do nothing` and **never deleted** — `app.log_event`'s AFTER DELETE trigger would insert an `events` row referencing the just-deleted org and violate `events_org_id_orgs_id_fk` (RESEARCH Pitfall 8, hit during research).
  - one `budget_periods` row on a dedicated provider/period pair no other test uses, `cap_micro_usd = 100`, `reserved = 0`, `spent = 0`.
  - cleanup in `finally` deletes only `cost_ledger`, `cost_reservations` and `budget_periods` rows for that org and that provider/period.

Named tests, exactly:

  - `concurrent burst: 40 workers against a cap that fits 10` — `N = 40`, `CAP = 100`, `UNIT = 10`. Open 40 real connections with `openTestClients(40)`, let the sockets finish handshaking, then fire all 40 `select reservation_id from app.reserve_budget(<provider>, $period, 10, null, 'ts_enterprise')` through `Promise.all`. Assert, all four:
      1. exactly **10** rows carry a non-null `reservation_id`
      2. exactly **30** carry `null` — a denial is zero rows, not an error
      3. `reserved_micro_usd + spent_micro_usd === 100` — no over-spend AND no under-grant
      4. **zero** errors were thrown by any of the 40 calls
    Assertion 3 is two-sided on purpose: a meter that granted nothing would satisfy "no over-spend" alone.
  - `concurrent burst: open reservations equal the grants` — `select count(*) from cost_reservations where budget_period_id = $1 and settled_at is null and released_at is null` is exactly 10.
  - `concurrent burst survives a crashed worker holding the whole cap` — pre-load five EXPIRED, unsettled reservations totalling the full cap and set `reserved_micro_usd = 100`, then run the same 40-way burst: it must still grant exactly 10, leave `reserved` at 100, and mark the five expired rows released. This is Pattern 2 under concurrency rather than in isolation.

Timeouts: `vitest.db.config.ts` already sets `testTimeout: 60000`. RESEARCH measured the burst at 72 to 103 ms; if a run approaches the ceiling, the cause is connection setup, not the meter — judge by wall clock and re-run the file alone before concluding anything.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:db -t "concurrent burst"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:db -t "concurrent burst"` exits 0 and NAMES all three `concurrent burst ...` tests. Read the NAMES: a `-t` filter that matches nothing exits 0 green.
    - `grep -c 'withRollback' tests/db/budget-concurrency.test.ts` returns 0, and the file's header comment states why
    - `grep -cE 'delete +from +orgs' tests/db/budget-concurrency.test.ts` returns 0
    - Gate mutation **M7**, recorded verbatim and with the numbers: on the LIVE local database only (never in the migration file, so the revert is provable by construction), `create or replace function app.reserve_budget(...)` with the meter's single conditional UPDATE replaced by a `select` of the totals, an `if` in PL/pgSQL, and then an unconditional `update`. Run the suite and record:
        - `concurrent burst: 40 workers against a cap that fits 10` goes RED, and the failure message shows granted 40 (or well above 10) and reserved far above 100 — RESEARCH measured 40 of 40 and 400
        - **every single-worker test in `budget-meter.test.ts` stays GREEN** — that is the point of the mutation, and it is what proves the concurrency test is not redundant with them
      Then restore the real body from `drizzle/0015_budget_meter_functions.sql`, confirm green, verify with `pg_get_functiondef('app.reserve_budget'::regproc)` that the conditional UPDATE is back, and confirm `git diff --stat` is empty.
    - After the run, the committed fixture is cleaned up: `select count(*) from budget_periods where provider = <the dedicated provider>` is 0, while the `org_concurrency_fixture` org row REMAINS (deleting it is forbidden)
    - `pnpm test:db` exits 0 as a whole, with the total test count recorded in the SUMMARY against the Phase 1 baseline of 31
  </acceptance_criteria>
  <done>Forty genuinely concurrent workers hit a cap that fits ten, ten are granted, thirty are refused with zero rows and zero errors, nothing over-spends — and the naive body was shown to grant all forty, so the test discriminates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent workers -> one budget row | The failure mode the phase exists to prevent, and the only one a single-threaded test cannot see |
| a crashed worker -> the org's remaining budget | An unreleased reservation is budget nobody can spend |
| a retried worker -> the ledger | A replayed settlement double-counts |
| a tenant member -> the cap | Raising the cap raises the ceiling on all spend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-04 | Tampering | read-then-spend race | mitigate | `concurrent burst: 40 workers against a cap that fits 10` asserts grants, denials, `reserved + spent == cap` and zero errors against 40 real connections; gate mutation M7 swaps in the naive body and must red it while every single-worker test stays green. Task 3. | high |
| T-2-05 | Tampering | replayed settlement | mitigate | `settlement idempotency` asserts one ledger row, one balance move, and a `false` return on the replay; gate mutation M9 drops `cost_ledger_request_id_key`. Task 1. | high |
| T-2-06 | Denial of Service | crashed worker stranding budget | mitigate | Two self-heal tests — one that the expired rows are released, one that a LIVE reservation is not — plus the same scenario under the 40-way burst. A third mutation removes the self-heal block and must red only the first. Tasks 1, 3. | high |
| T-2-07 | Tampering | breach by hand-written SQL | mitigate | `bp_not_over` is asserted by `23514` with the constraint name pinned, with a boundary-exact positive control at `cap = spent + reserved`. Gate mutation M8. Task 1. | high |
| T-2-02 | Elevation of Privilege | member raises the cap | mitigate | Two independent refusals resting on different invariants — the definer's role check (message `set_budget_cap: admin role required`) and the grant layer (message `permission denied for table budget_periods`) — each with its own mutation and its own positive control. Both Clerk claim spellings tested. Gate mutation M10. Task 2. | high |
| T-2-11 | Repudiation | forged actor on a cap change | mitigate | `cap change is audited` issues a RAW SQL write with no application code in the path and asserts actor and timestamp; `a reservation does NOT write an events row` proves the trigger narrowing did not also disable the audit. Task 2. | high |
| T-2-03 | Tampering | spending outside the meter | mitigate | Every settlement in these tests goes through `app.settle_reservation` with a real reservation id; `cost_ledger.reservation_id` is NOT NULL with an FK, so a test that tried to write a ledger row without one could not. Task 1. | high |
</threat_model>

<verification>
- `pnpm test:db` green, with the total test count recorded against the Phase 1 baseline of 31 and every new test NAME read from the output
- Gate mutations M7, M8, M9 and M10 each applied to the LIVE local database, each watched red against exactly the predicted named test set, each reverted and verified from `pg_get_functiondef` / `pg_constraint` / `information_schema.role_table_grants`, with `git diff --stat` empty after each
- Two supporting mutations recorded: the self-heal removal, and a direct `grant update on budget_periods`
- The committed concurrency fixture is cleaned up, and the `org_concurrency_fixture` org row is deliberately left in place
</verification>

<success_criteria>
- Success criterion 5 is proven: 40 concurrent workers, 10 granted, 30 refused, `reserved + spent == cap`, zero errors, and the naive body shown to grant 40
- A denial is zero rows; a hand-written breach is `23514 bp_not_over`; a replayed settlement moves nothing
- A crashed worker's budget is reclaimed by the next caller with no scheduler involved
- The cap is admin-only against both Clerk role spellings, with the two refusals resting on different invariants
- One instant in two zones gives opposite month verdicts in SQL, and the reset instant moves with DST
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-08-SUMMARY.md` recording: the `pnpm test:db` pass list by NAME with the total count, the M7 burst numbers under the naive body (granted and reserved), and every mutation's red test set side by side so a reader can see the guards are not coupled. Update `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` Status column for the rows this plan closes.
</output>
