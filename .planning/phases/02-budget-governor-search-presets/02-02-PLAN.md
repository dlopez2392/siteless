---
phase: 02-budget-governor-search-presets
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/seed/data/clusters.json
  - src/seed/data/cities.json
  - src/seed/data/counties.json
  - src/seed/data/outlet-counts.json
  - src/seed/data/geo-presets.json
  - src/seed/types.ts
  - tests/unit/outlet-counts.test.ts
  - tests/unit/pg17-compat.test.ts
  - tests/unit/no-google-credential.test.ts
  - tests/unit/fixtures/preset.ts
  - tests/unit/msw/fixtures/census-mcallen.json
  - tests/unit/msw/fixtures/census-rio-grande-city.json
  - tests/unit/msw/fixtures/census-no-match.json
  - tests/unit/msw/fixtures/census-503.json
  - tests/db/_concurrency.ts
autonomous: true
requirements: [SRCH-02, BUDG-03]

must_haves:
  truths:
    - "SRCH-02: the four industry clusters, the 17 RGV cities, the four RGV counties and all 254 Texas counties exist as committed JSON before any code reads them"
    - "Both county numbering systems ship: Comptroller code and FIPS, with county_fips = 2 * comptroller_code - 1 asserted over all 254 rows"
    - "The RGV cluster-by-county matrix in the committed JSON equals the live-measured figures 1452 / 5572 / 977 / 15977"
    - "BUDG-03 / T-2-15: nothing under src/ reads a Google credential, and no test in this repo requires the Google key to exist"
    - "Pitfall 1: no migration in drizzle/ uses PostgreSQL 18-only syntax that production 17.6 would refuse"
    - "The concurrency proof has a harness that opens real connections and is NOT built on withRollback"
  artifacts:
    - path: "src/seed/data/cities.json"
      provides: "The 17 RGV cities, their counties, folded name variants and outlet counts, with the selection rule documented in the file"
      contains: "Rio Grande City"
    - path: "src/seed/data/counties.json"
      provides: "All 254 Texas counties with both FIPS and Comptroller codes"
      contains: "48215"
    - path: "tests/db/_concurrency.ts"
      provides: "openTestClient() - N real pg.Client connections for the 40-way burst"
      contains: "TEST_DATABASE_URL"
  key_links:
    - from: "src/seed/data/outlet-counts.json"
      to: "src/seed/data/counties.json and clusters.json"
      via: "countyFips and clusterKey references resolved by the seed loader"
      pattern: "clusterKey"
    - from: "tests/unit/outlet-counts.test.ts"
      to: "src/seed/data/outlet-counts.json"
      via: "direct JSON import, asserting the measured matrix"
      pattern: "15977"
---

<objective>
Commit the seed data every later plan reads, the Census payloads CI replays instead of calling the network, the real-connection harness the concurrency proof needs, and the three repo-wide guard tests that have no other home.

Purpose: SRCH-02's seed data is the Phase 2 / Phase 3 shared contract — Phase 3's ingest scope reads the cluster and geography definitions written here. And every Wave 0 gap in `02-VALIDATION.md` that needs no new dependency is closed here so no later task lacks an automated verify.
Output: five committed JSON seed files with their provenance, four recorded Census payloads, `tests/db/_concurrency.ts`, and three guard tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md
@.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md
@.planning/CONVENTIONS.md
@tests/db/_fixtures.ts
@src/lib/time.ts
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written out in full below. `pnpm verify` is NOT runnable here.

If this plan runs in a git worktree it has no `node_modules` and no `.env.local`. Copy `C:/Users/danlo/prospector/.env.local` into the worktree root, then run the launcher with `install`.

This plan writes NO migration and runs NO `test:db`. It is safe to run in parallel with any other wave-1 plan.
</machine_notes>

<orchestrator_decision>
The RGV city list is settled (CONTEXT section Claude's Discretion, orchestrator decision 1): **the measured 17 cities with at least 400 active sales-tax outlets**, name variants folded. The committed JSON documents the rule, the coverage and the next five candidates below the line so danlo can add one by editing one file. **This list is the Phase 2 / Phase 3 shared contract — the SUMMARY must flag it for a one-line confirmation from danlo before Phase 3 runs.**
</orchestrator_decision>

<tasks>

<task type="auto">
  <name>Task 1: The committed seed data — 4 clusters, 17 cities, 254 counties, the outlet-count matrix</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Seed Data" — the county table, the FIPS bijection, the 17-city table with outlet counts, the cluster NAICS predicates and the measured matrix, and the Rio Grande City name-variant warning
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Common Pitfalls" Pitfall 6 (`starts_with()` on a Socrata NUMBER column returns HTTP 400) and Pitfall 7 (Comptroller code is not FIPS)
    - `.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md` D-03, D-04, D-05, D-09
    - `src/lib/export/registry.ts` — the house shape for a committed, exported, server-safe constant module
  </read_first>
  <files>src/seed/types.ts, src/seed/data/clusters.json, src/seed/data/cities.json, src/seed/data/counties.json, src/seed/data/outlet-counts.json, src/seed/data/geo-presets.json, tests/unit/outlet-counts.test.ts</files>
  <action>
Write `src/seed/types.ts` first — the TypeScript shapes the JSON must satisfy, so a malformed file fails `tsc` rather than at runtime. Every JSON file is imported with `import x from '../seed/data/x.json'` and structurally checked against these types in the test.

**`src/seed/data/clusters.json`** — four clusters, atomic (D-03). Each: `{ key, displayName, sortOrder, naicsRanges: [{lo, hi}], placesTypes: [string] }` with `lo` inclusive and `hi` exclusive, exactly the measured predicates:
  - `home_services` / "Home services & trades" / `[{lo:230000, hi:240000}]`
  - `food_hospitality` / "Food & hospitality" / `[{lo:721000, hi:723000}]`
  - `personal_care_health` / "Personal care & health" / `[{lo:812100, hi:812200},{lo:621000, hi:622000}]`
  - `auto_retail` / "Auto & retail" / `[{lo:811100, hi:811200},{lo:440000, hi:460000}]`
`placesTypes` seeds a starting list per cluster and carries a `"placesTypesNote"` field on each cluster reading: `"Phase 4 validates every type against the Places API (New) type table before the first paid call. The estimator prices cells = clusters x geography units and never reads this list, so a wrong entry here changes no Phase 2 number."` Suggested starting lists (revisable in Phase 4): home_services `["plumber","electrician","roofing_contractor","general_contractor","painter","locksmith","moving_company"]`; food_hospitality `["restaurant","cafe","bar","bakery","meal_takeaway","hotel"]`; personal_care_health `["hair_salon","barber_shop","nail_salon","spa","dentist","doctor","physiotherapist"]`; auto_retail `["car_repair","car_wash","car_dealer","tire_shop","auto_parts_store","furniture_store","clothing_store"]`.

**`src/seed/data/counties.json`** — all 254 Texas counties. Fetch the authoritative file once (free, no key, static):
`https://www2.census.gov/geo/docs/reference/codes2020/national_county2020.txt`
Keep the 254 rows whose state field is `TX`, and emit per row `{ fips, countyFips, comptrollerCode, name, isRgv, outletCount }` where `fips` is the 5-character GEOID string (`"48215"`), `countyFips` is the integer (215), `comptrollerCode = (countyFips + 1) / 2`, and `name` has the trailing " County" stripped. 🔴 **Do NOT derive the Comptroller code by re-sorting county names — 15 of 254 break under every collation** (El Paso/Ellis, La Salle/Lamar, and the whole `Mc*` block, which FIPS orders before `Ma*`). Set `isRgv: true` and `outletCount` only on the four RGV rows, with the measured values: Cameron `48061`/comptroller 31/12313, Hidalgo `48215`/108/21062, Starr `48427`/214/1226, Willacy `48489`/245/327. Every other county gets `isRgv: false, outletCount: 0`.
If `www2.census.gov` is unreachable, STOP and report — do not hand-type 254 rows.

**`src/seed/data/cities.json`** — the 17, in this exact order and with these exact figures, each `{ name, countyFips, outletCount, nameVariants: [] }`:
Brownsville/48061/6678 · McAllen/48215/5985 · Edinburg/48215/3092 · Mission/48215/3034 · Harlingen/48061/2626 · Pharr/48215/1942 · Weslaco/48215/1551 · San Benito/48061/948 · Donna/48215/821 · San Juan/48215/791 · **Rio Grande City/48427/843** · Alamo/48215/763 · Mercedes/48215/748 · Hidalgo/48215/566 · Los Fresnos/48061/490 · Palmview/48215/465 · South Padre Island/48061/417.
🔴 Rio Grande City is the **folded** total: the Comptroller source spells it three ways — `RIO GRANDE CITY` (776), `RIO GRANDE CY` (35), `RIO GRANDE` (32) — so its row carries `nameVariants: ["RIO GRANDE CITY","RIO GRANDE CY","RIO GRANDE"]` and `outletCount: 843`. Every other row carries `nameVariants` holding its single upper-case source spelling.
The file also carries a top-level `provenance` object, verbatim values:
`{ "rule": "the cities in the four RGV counties with at least 400 active sales-tax outlets, name variants folded", "thresholdOutlets": 400, "selectionBasis": "exact outlet_city spelling", "source": "data.texas.gov/resource/jrea-zgmq.json", "measuredAt": "2026-09-22", "rgvTotalOutlets": 34928, "selectedOutletsBeforeFolding": 31693, "coveragePctBeforeFolding": 90.7, "seededOutletsAfterFolding": 31760, "coveragePctAfterFolding": 90.9, "nextCandidates": [ ... ] }`
`nextCandidates` is the five cities immediately below the 400 line, each `{ name, countyFips, outlets }`, so danlo can promote one by editing this file. **Raymondville (Willacy, `48489`, 221 outlets) must be one of them** — it is Willacy's only town and the only reason a county would be unrepresented in the city picker. Re-query `data.texas.gov/resource/jrea-zgmq.json` for the other four; if Socrata is unreachable, commit Raymondville alone and record the deviation. 🔴 `outlet_naics_code` is a Socrata **number** — `starts_with()` returns HTTP 400 `query.soql.type-mismatch`; use numeric range predicates. `outlet_county_code` and `outlet_city` are text.

**`src/seed/data/outlet-counts.json`** — 16 county rows plus 4 statewide rows, each `{ scope, countyFips, clusterKey, outlets }` with `scope` in `"county" | "state"` and `countyFips: null` on the state rows. The county matrix, exactly as measured (Cameron/Hidalgo/Starr/Willacy):
  - `home_services` 442 / 969 / 32 / 9 — statewide 45612
  - `food_hospitality` 1958 / 3329 / 224 / 61 — statewide 120749
  - `personal_care_health` 303 / 628 / 43 / 3 — statewide 23846
  - `auto_retail` 5861 / 9355 / 606 / 155 — statewide 362071
Plus a top-level `{ "source": "data.texas.gov/resource/jrea-zgmq.json", "measuredAt": "2026-09-22", "caveat": "The Comptroller under-counts pure-service businesses - Texas does not tax most personal services, which is why personal care shows 977 RGV outlets against 15,977 for auto & retail. Expected businesses is a permit-holder count, not a Places result count." }`

**`src/seed/data/geo-presets.json`** — three built-in geographies (D-04, D-05): `rgv_17_cities` (kind `cities`, all 17 city refs by `{name, countyFips}`), `rgv_4_counties` (kind `counties`, the four RGV `fips`), `texas_254_counties` (kind `counties`, all 254 `fips`, displayName exactly `Texas (254 counties)`).

**`tests/unit/outlet-counts.test.ts`** — name the tests exactly:
  - `outlet counts match the measured RGV matrix` — assert each cluster's four county values AND the RGV column totals 1452 / 5572 / 977 / 15977 AND the grand total 23978 AND the statewide total 552278. Two-sided per `tests/unit/no-internal-leak.test.ts`'s discipline: assert the RIGHT numbers are present, never merely that something is not wrong.
  - `254 counties seeded with both numbering systems` — `counties.length === 254`; for every row `countyFips === 2 * comptrollerCode - 1`; the four RGV rows resolve to comptroller 31/108/214/245 and FIPS `48061`/`48215`/`48427`/`48489`; `counties.filter(c => c.isRgv).length === 4`.
  - `the RGV 17-city list and its coverage` — `cities.length === 17`; the folded sum of `outletCount` is `31760`; `provenance.seededOutletsAfterFolding === 31760`; `provenance.coveragePctAfterFolding` equals `31760 / 34928 * 100` rounded to one decimal; `provenance.coveragePctBeforeFolding === 90.7`; every `nextCandidates` entry has `outlets < 400`; Raymondville is present with `outlets: 221` and `countyFips: "48489"`; Rio Grande City has exactly three `nameVariants` and `outletCount === 843`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit -t "outlet counts"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit -t "outlet counts"` exits 0 and the output NAMES `outlet counts match the measured RGV matrix` (read the NAME; a filter that matches nothing exits 0 green)
    - `pnpm test:unit` exits 0 and its pass list also NAMES `254 counties seeded with both numbering systems` and `the RGV 17-city list and its coverage`
    - `node -e "const c=require('./src/seed/data/counties.json');const r=Array.isArray(c)?c:c.counties;if(r.length!==254)throw new Error('counties '+r.length);for(const x of r)if(x.countyFips!==2*x.comptrollerCode-1)throw new Error(x.name);console.log('ok')"` prints `ok`
    - `grep -c '15977\|15,977' src/seed/data/outlet-counts.json` is 0 and the four auto_retail county values `5861` `9355` `606` `155` are each present exactly once
    - `src/seed/data/cities.json` contains `"Rio Grande City"`, `"RIO GRANDE CY"`, `843`, `"Raymondville"`, `221`, `90.7` and `31760`
    - `src/seed/data/counties.json` contains `48215`, `48061`, `48427`, `48489`
    - `src/seed/data/geo-presets.json` contains `texas_254_counties` and the exact string `Texas (254 counties)`
    - `src/seed/data/clusters.json` contains `230000`, `240000`, `721000`, `723000`, `812100`, `812200`, `621000`, `622000`, `811100`, `811200`, `440000`, `460000`
    - `pnpm typecheck` exits 0 (the JSON structurally satisfies `src/seed/types.ts`)
    - The SUMMARY flags the 17-city list for danlo's one-line confirmation BEFORE Phase 3 runs, and states whether `nextCandidates` came from a live Socrata query or from RESEARCH alone
  </acceptance_criteria>
  <done>Every number the estimator and Phase 3 need is committed, provenanced, and pinned by a test that asserts the right values rather than the absence of wrong ones.</done>
</task>

<task type="auto">
  <name>Task 2: Recorded Census payloads, the shared PresetSpec fixture, and the real-connection concurrency harness</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "The Census Geocoder" — the endpoint, the `layers=Counties` size win, the eight failure-mode cases, and the verified zod shape
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Code Examples" subsection "The concurrency proof" — the verified shape, and why `withRollback` cannot express it
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Pitfall 8 — an `orgs` row cannot be deleted, so a committing fixture must not clean up by deleting one
    - `tests/db/_fixtures.ts` — the Supabase-host refusal and the connection-timeout setting to mirror; this new file is NOT built on `withRollback`
  </read_first>
  <files>tests/unit/msw/fixtures/census-mcallen.json, tests/unit/msw/fixtures/census-rio-grande-city.json, tests/unit/msw/fixtures/census-no-match.json, tests/unit/msw/fixtures/census-503.json, tests/unit/fixtures/preset.ts, tests/db/_concurrency.ts</files>
  <action>
1. Record four real Census Geocoder payloads as committed JSON. CI must never touch the network (D-04 / CLAUDE.md), so these are replayed by `msw` in plan 02-07. Fetch each once from:
`https://geocoding.geo.census.gov/geocoder/geographies/onelineaddress?address=<urlencoded>&benchmark=Public_AR_Current&vintage=Current_Current&layers=Counties&format=json`
  - `census-mcallen.json` — `1400 N 10th St, McAllen, TX 78501` (1 match, Hidalgo County, ~26.2161 / -98.2278)
  - `census-rio-grande-city.json` — `101 N Britton Ave, Rio Grande City, TX 78582` (1 match, Starr County)
  - `census-no-match.json` — `Joe's Taqueria, McAllen, TX` (HTTP **200** with `addressMatches: []` — the failure mode is a 200, so a status check is not a success check)
  - `census-503.json` — a hand-written `{ "status": 503, "body": "Service Unavailable" }` envelope the msw handler replays as a 503; the live service will not produce one on demand.
🔴 `layers=Counties` is not optional: the default response carries 11 geography layers at 5,125 bytes; with `layers=Counties` it is 1,154 bytes and still carries `COUNTY` / `GEOID` / `NAME` / `STATE`. Record each payload verbatim, do not trim it by hand.
Add a one-line header comment file `tests/unit/msw/fixtures/README.md` naming the exact URL used for each, the date, and that they are replayed rather than re-fetched.

2. `tests/unit/fixtures/preset.ts` — the shared `PresetSpec` fixture both the estimator tests (02-07) and the component tests (02-10) import, so the two cannot drift. Export at minimum:
  - `RGV_BASELINE_SPEC` — all four cluster keys, geo kind `cities`, the 17 city refs (68 cells)
  - `RGV_COUNTIES_SPEC` — all four cluster keys, geo kind `counties`, the four RGV fips (16 cells)
  - `TEXAS_SPEC` — all four cluster keys, geo kind `counties`, all 254 fips (1016 cells)
  - `RADIUS_SPEC` — one cluster, geo kind `radius`, centred on the McAllen fixture's lat/lng with `countyFips: "48215"`, `radiusMiles: 10`
  - `SINGLE_CLUSTER_CITY_SPEC` — one cluster, one city (1 cell), for the arithmetic tests
Derive the city and county refs FROM the committed seed JSON, never by re-typing them — a fixture that hard-codes a list is a second source of truth.

3. `tests/db/_concurrency.ts` — the harness for success criterion 5. 🔴 It must NOT be built on `withRollback`: that is one transaction and therefore cannot express two workers seeing each other's commits. Export:
  - `openTestClient(): Promise<Client>` — reads `TEST_DATABASE_URL`, throws naming the variable if unset, **refuses a Supabase host** with the same `/supabase\.(co|com)|pooler\.supabase/` test and the same D-04 message `tests/db/_fixtures.ts` uses, sets `connectionTimeoutMillis: 10000`, connects, returns the client.
  - `openTestClients(n: number): Promise<Client[]>` — opens n in parallel, then awaits ~150ms so every socket finishes handshaking before the burst (a client still handshaking is not a concurrent worker).
  - `closeAll(clients: Client[]): Promise<void>` — ends every client, swallowing individual errors so one failure cannot hide the rest; always called from a `finally`.
  - `withCommittedFixture<T>(fn): Promise<T>` — a committing scope: it does NOT open a transaction, it runs `fn`, and in `finally` it deletes only the rows the burst created. 🔴 **It must never delete an `orgs` row** — `app.log_event`'s AFTER DELETE trigger inserts an `events` row referencing the just-deleted org and violates `events_org_id_orgs_id_fk` (verified). So the fixture org is created once with `insert into orgs (...) values ('org_concurrency_fixture', ...) on conflict (clerk_org_id) do nothing` and is left in place; cleanup deletes only `cost_ledger`, `cost_reservations` and `budget_periods` rows for that org and a dedicated `provider`/`period_start` pair that no other test uses.
  Document at the top of the file, in a comment, WHY it diverges from `withRollback` — the next reader will otherwise try to "fix" it back.

This task writes the harness only. Plan 02-05 writes the test that uses it; plan 02-07 writes the msw server that replays the payloads. Both are named here so the Wave 0 checklist in `02-VALIDATION.md` closes.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All four files under `tests/unit/msw/fixtures/` exist; `census-mcallen.json` contains `"COUNTY"`, `"48"` and `"215"`; `census-no-match.json` contains `"addressMatches"` and the array is empty
    - `grep -c 'layers=Counties' tests/unit/msw/fixtures/README.md` returns at least 1
    - `tests/unit/fixtures/preset.ts` exports `RGV_BASELINE_SPEC`, `RGV_COUNTIES_SPEC`, `TEXAS_SPEC`, `RADIUS_SPEC`, `SINGLE_CLUSTER_CITY_SPEC` and imports from `src/seed/data/` rather than restating any city or county list (`grep -c "Brownsville" tests/unit/fixtures/preset.ts` returns 0)
    - `tests/db/_concurrency.ts` contains `TEST_DATABASE_URL`, `pooler.supabase`, `connectionTimeoutMillis`, `openTestClients`, `closeAll` and `org_concurrency_fixture`
    - `grep -c 'withRollback' tests/db/_concurrency.ts` returns 0, and the file carries a comment explaining why
    - `grep -cE 'delete +from +orgs' tests/db/_concurrency.ts` returns 0 (Pitfall 8)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>CI can replay every Census outcome without a network call, the estimator and component tests share one PresetSpec fixture, and the 40-way burst has a harness that can actually express concurrency.</done>
</task>

<task type="auto">
  <name>Task 3: The two repo-wide guard tests — no Google credential in src/, and no PostgreSQL 18-only SQL</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Pitfall 1 — local is 18.6, CI is `postgres:18`, **production is 17.6**; `returning old.` / `returning new.` is accepted locally and does not exist in 17
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Security Domain" T-2-15
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` Executor Rule 14
    - `src/lib/time.ts` — the comment explaining why a bare-token grep guard means the guarded token is deliberately never spelled in the guarded file
    - `tests/db/schema-audit.test.ts` — the catalog-enumeration-as-assertion style these two tests imitate over the filesystem
  </read_first>
  <files>tests/unit/no-google-credential.test.ts, tests/unit/pg17-compat.test.ts</files>
  <action>
**`tests/unit/no-google-credential.test.ts`** — BUDG-03's standing guard, and T-2-15's. The Google Cloud project and the Places API (New) key **do not exist**, and UI-SPEC Executor Rule 14 makes that a contract: nothing on any screen reads a Google credential and no test depends on the key existing. Name the test exactly `no google credential is read anywhere in src`.
Walk every file under `src/` recursively (`.ts`, `.tsx`, `.css`, `.json`) and fail if any matches `GOOGLE_[A-Z_]*KEY`, `GOOGLE_API_KEY`, `NEXT_PUBLIC_GOOGLE`, `X-Goog-Api-Key`, or `maps.googleapis.com`. Report the offending file AND line in the failure message — a guard that says only "failed" costs an hour.
🔴 Two-sided, per `tests/unit/no-internal-leak.test.ts`: also assert the walker actually found files (`expect(scanned.length).toBeGreaterThan(15)`), so a broken path glob cannot pass by scanning nothing.
Add a second named test `src/env.ts declares no Google variable` asserting `src/env.ts` contains neither `GOOGLE` nor `PLACES`.
Exclude nothing from the walk except `src/components/ui/**`? No — include it; generated primitives must be covered too. This test file itself lives under `tests/`, so it may spell the tokens freely.

**`tests/unit/pg17-compat.test.ts`** — Pitfall 1's guard. Name the test exactly `pg17: no migration uses PostgreSQL 18-only syntax`.
Read every `drizzle/*.sql` file. **Strip SQL comments before matching** — a header paragraph that explains the rule would otherwise trip the rule (the self-invalidating grep gate). Concretely: drop every line whose first non-space characters are `--`, and strip `/* ... */` blocks, THEN match case-insensitively for `returning\s+old\.`, `returning\s+new\.`, `uuidv7\s*\(`, and `generated\s+always\s+as\s*\(.*\)\s*virtual`. Any hit fails, naming the file and the matched text.
🔴 Two-sided again: assert the file list is non-empty (`expect(files.length).toBeGreaterThanOrEqual(12)` — Phase 1 shipped twelve migrations), so a wrong directory cannot pass by reading nothing. Add a companion named test `pg17: the comment-stripping guard is not self-invalidating` that runs the same matcher over a synthetic input containing the forbidden token inside a `--` comment and asserts it passes, and over the same token in real SQL and asserts it fails. Without that pair the stripper itself is untested.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit -t "pg17"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit -t "pg17"` exits 0 and the output NAMES both `pg17: no migration uses PostgreSQL 18-only syntax` and `pg17: the comment-stripping guard is not self-invalidating`
    - `pnpm test:unit -t "no google credential"` exits 0 and NAMES `no google credential is read anywhere in src`
    - Watched failing first, both recorded in the SUMMARY: (a) temporarily add `const GOOGLE_API_KEY = process.env.GOOGLE_API_KEY;` to a scratch file under `src/`, confirm the credential test goes red naming that file, delete it, confirm green; (b) temporarily append a file `drizzle/9999_scratch.sql` containing `update t set a = 1 returning old.a;`, confirm the pg17 test goes red naming that file, delete it, confirm green. `git status --short` is clean afterwards.
    - `tests/unit/pg17-compat.test.ts` strips `--` comment lines before matching, and a criterion-level grep confirms it: `grep -c "startsWith('--')\|^\\\\s*--" tests/unit/pg17-compat.test.ts` returns at least 1
    - `pnpm test:unit` overall exits 0
  </acceptance_criteria>
  <done>A Google credential cannot enter `src/` and a PostgreSQL 18-only statement cannot enter a migration without a named test going red, and both guards were proven to discriminate by being watched red first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party data source -> committed repo data | Socrata and the Census static file are fetched once at authoring time and then frozen as committed JSON; nothing at test time reaches the network |
| repo source tree -> deployed bundle | A credential name that enters `src/` reaches the build; `NEXT_PUBLIC_*` reaches the browser |
| local PostgreSQL 18.6 -> production PostgreSQL 17.6 | SQL that passes every local gate can still be refused by production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-15 | Information Disclosure | Google credential in `src/` or the client bundle | mitigate | `tests/unit/no-google-credential.test.ts` walks every file under `src/` for `GOOGLE_*KEY`, `NEXT_PUBLIC_GOOGLE`, `X-Goog-Api-Key` and `maps.googleapis.com`, asserts it scanned a non-empty file set, and is watched red first against a planted credential. No test in this phase requires the key to exist. Task 3. | high |
| T-2-06 | Denial of Service | committing test fixture | mitigate | `withCommittedFixture` cleans up in `finally` and keys off a dedicated provider/period, never a throwaway org — an `orgs` delete would violate `events_org_id_orgs_id_fk` through the audit trigger and leave the suite wedged. Task 2. | medium |
| T-2-13 | Tampering | Census Geocoder response | mitigate | The four payloads are recorded verbatim and replayed; CI never issues the request, so a compromised or changed upstream response cannot alter a CI verdict. The parsing guards themselves land in plan 02-07. Task 2. | medium |
| Pitfall 1 (no STRIDE ref) | Tampering | migration portability | mitigate | `tests/unit/pg17-compat.test.ts` refuses PG18-only syntax in `drizzle/*.sql`, with SQL comments stripped first so the guard cannot invalidate itself, and with a paired test proving the stripper discriminates. Task 3. | high |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint` and `pnpm test:unit` each exit 0 through the store launcher
- The three new test NAMES appear in the `pnpm test:unit` pass list, read from the output
- Both guard tests were watched red first against a planted violation, and `git status --short` is clean afterwards
- `src/seed/data/counties.json` holds exactly 254 rows and every row satisfies `countyFips = 2 * comptrollerCode - 1`
</verification>

<success_criteria>
- Five committed seed JSON files with provenance, satisfying `src/seed/types.ts`
- The RGV matrix, the 254-county bijection and the 17-city coverage are each pinned by a named test asserting exact values
- Four recorded Census payloads plus a README naming their URLs and date
- `tests/db/_concurrency.ts` exists, refuses a Supabase host, and is not built on `withRollback`
- The Google-credential and PG17 guards exist and were proven to discriminate
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-02-SUMMARY.md` recording: the `pnpm test:unit` pass list by NAME, the two watched-red-first outputs, whether `nextCandidates` came from a live Socrata query, and — flagged at the top — **the 17-city list for danlo's one-line confirmation before Phase 3 runs, since it is the Phase 2 / Phase 3 shared contract.**
</output>
