---
phase: 02-budget-governor-search-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - components.json
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/lib/utils.ts
  - src/components/ui/
  - src/components/theme-provider.tsx
  - vitest.config.ts
  - vitest.dom.config.ts
  - eslint.config.mjs
  - tests/unit/_setup-dom.ts
  - tests/unit/jsdom-lane.test.tsx
autonomous: true
requirements: [SRCH-01, SRCH-04, BUDG-04]

must_haves:
  truths:
    - "The repo has a design system: Tailwind v4 compiles, 33 shadcn primitives exist under src/components/ui, and the UI-SPEC palette is painted as literal hex in src/app/globals.css"
    - "The font is Inter bound to --font-sans, not the nova preset's Geist, and no geist package is installed (UI-SPEC Executor Rule 15)"
    - "Light and dark both ship: :root carries the light palette, .dark carries the dark palette, next-themes is mounted with attribute=class and defaultTheme=system"
    - "A .test.tsx file runs under jsdom with React, and tests/unit/suite-zone.test.ts and tests/unit/time.test.ts still run under node with TZ pinned to UTC"
    - "Every new dependency is pinned to the STACK.md version; nothing floats after shadcn init installed it"
  artifacts:
    - path: "src/app/globals.css"
      provides: "The painted design tokens - every hex literal from UI-SPEC section Color"
      contains: "#0F766E"
    - path: "components.json"
      provides: "shadcn config; tailwind.config stays an empty string (Tailwind v4)"
      contains: "zinc"
    - path: "src/components/theme-provider.tsx"
      provides: "next-themes client wrapper mounted inside ClerkProvider"
      contains: "attribute"
    - path: "tests/unit/_setup-dom.ts"
      provides: "@testing-library/jest-dom matchers for the jsdom lane"
      contains: "@testing-library/jest-dom"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import of the stylesheet from the root layout"
      pattern: "globals.css"
    - from: "src/app/layout.tsx"
      to: "next/font/google Inter"
      via: "variable --font-sans"
      pattern: "Inter\\("
---

<objective>
Give this repo a design system. Phase 1 shipped an unstyled shell on purpose; this plan runs `shadcn init`, pins everything it floated, adds the 33 components UI-SPEC names, paints the UI-SPEC palette as literal hex into `globals.css`, swaps the preset's Geist for Inter, mounts `next-themes` and `sonner`, and opens the jsdom lane that `.test.tsx` files need.

Purpose: every screen in Phases 2 through 9 inherits these tokens, these primitives and these rules. Nothing else in this phase can be built until they exist.
Output: a compiling Tailwind v4 setup, 33 shadcn primitives, both palettes, and a component-test lane that does not weaken the node lane's UTC pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-PATTERNS.md
@.planning/CONVENTIONS.md
@src/app/layout.tsx
@vitest.config.ts
@eslint.config.mjs
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script runs through the store launcher, written out in full in every command below. `pnpm verify` itself is NOT runnable here — run typecheck, lint, test:unit, test:db and build individually.

If this plan runs in a git worktree it has no `node_modules` and no `.env.local`. First copy `C:/Users/danlo/prospector/.env.local` into the worktree root, then run the launcher with `install`.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: shadcn init on the Radix/nova preset, pin every floated version, add the 33 components</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section Components (the exact commands, Step 1b's font warning, Step 4's "record, do not fix")
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Frontend on This Repo" (shadcn 4.21.0 flags verified against the installed CLI; the nova preset map; the components.json abort-if-exists)
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Standard Stack" subsection "New in this phase - UI" (the pinned version list)
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Common Pitfalls" Pitfall 10 (init on a dirty tree, or twice)
    - `package.json` - the current dependency set and the exact packageManager pin
    - `src/app/layout.tsx` - what init is about to rewrite with ts-morph
  </read_first>
  <files>components.json, postcss.config.mjs, package.json, pnpm-lock.yaml, src/lib/utils.ts, src/components/ui/*.tsx, src/app/globals.css</files>
  <action>
RED FLAG: `shadcn init` ABORTS if `components.json` already exists, and its edits to `layout.tsx` are ts-morph rewrites that are painful to untangle from unrelated changes. Print `git status --short` BEFORE and AFTER. The tree must be clean before step 2.

1. Record the starting state: `git status --short` (must be empty) and `git rev-parse --short HEAD`.

2. Initialise, non-interactive, exactly this command. There is no `--base-color` flag. Do NOT pass `-t next`, which scaffolds a NEW project:

    npx shadcn@4.21.0 init -b radix -p nova -y --css-variables --pointer --no-monorepo --no-rtl

Record the full stdout in the SUMMARY, and record the file list init actually wrote (`git status --short` after). RESEARCH assumption A8 predicts `postcss.config.mjs`, `src/app/globals.css`, `src/lib/utils.ts`, `components.json` and a rewrite of `src/app/layout.tsx`'s font import. Record what really happened; do not correct the record to match the prediction.

3. The `nova` preset binds `baseColor: "neutral"`; this contract is `zinc`. Run the supported non-interactive migration:

    npx shadcn@4.21.0 migrate base-color --to zinc -y

Then confirm `components.json` has `"baseColor": "zinc"`. If `migrate` refuses, edit `components.json`'s `tailwind.baseColor` to `zinc` by hand and record the deviation.

4. `components.json` -> `tailwind.config` MUST stay the empty string `""`. Tailwind v4 is configured in `globals.css` with `@theme`; no `tailwind.config.ts` is created in this project, ever (UI-SPEC Executor Rule 13). If init wrote a `tailwind.config.*`, delete it and set the field to `""`.

5. init installs floating versions. Pin them all through the store launcher - this is UI-SPEC Executor Rule 12:

    node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs add tailwindcss@4.3.3 @tailwindcss/postcss@4.3.3 lucide-react@1.47.0 class-variance-authority@0.7.1 tailwind-merge@3.7.0 next-themes@0.4.6 sonner@2.0.8 vaul@1.1.2 motion@13.4.0

Every one of those nine must appear in `package.json` with an EXACT version (no caret, no tilde) - the repo pins exactly everywhere else.

6. NEVER add a `geist` package. The `nova` preset binds `font: "geist"` and init writes that import into `layout.tsx`; Task 2 replaces it with Inter. If init added `geist` to `package.json`, remove it with the launcher's `remove geist`.

7. Add the components, exactly this list (33 blocks from the shadcn official registry - UI-SPEC section Registry Safety; no third-party registry is used in this phase):

    npx shadcn@4.21.0 add -y alert avatar badge breadcrumb button card checkbox collapsible command dialog drawer dropdown-menu empty field input input-group item label popover progress radio-group scroll-area select separator sheet sidebar skeleton sonner spinner table tabs toggle-group tooltip

8. Verify and record: `npx shadcn@4.21.0 info`. Record the real `style`, `baseColor` and `iconLibrary` in the SUMMARY. If `style` is not `radix-nova`, record what it actually is and do NOT "fix" it (UI-SPEC Step 4).

9. `postcss.config.mjs` must export `{ plugins: { '@tailwindcss/postcss': {} } }` - Next 16.3.5's own docs prescribe exactly this package name. Create it if init did not.

10. Run the typecheck and lint constituents through the launcher. `src/components/ui/*.tsx` is generated code; if `@typescript-eslint/no-explicit-any` fires inside it, add `'src/components/ui/**'` to the `ignores` array in `eslint.config.mjs` rather than hand-editing a primitive (UI-SPEC: never hand-edit a copy-in beyond the token pass).
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs typecheck &amp;&amp; test $(ls src/components/ui/*.tsx | wc -l) -ge 33 &amp;&amp; echo ok</automated>
  </verify>
  <acceptance_criteria>
    - `components.json` exists and `grep -c '"config": ""' components.json` returns 1 (Tailwind v4: no tailwind.config file)
    - `grep -c 'zinc' components.json` returns at least 1 and `grep -c 'lucide' components.json` returns at least 1
    - `ls tailwind.config.* 2>/dev/null` finds NOTHING
    - `ls src/components/ui/*.tsx | wc -l` is at least 33, and all of `alert.tsx badge.tsx button.tsx card.tsx checkbox.tsx command.tsx dialog.tsx drawer.tsx empty.tsx field.tsx input-group.tsx item.tsx progress.tsx scroll-area.tsx sidebar.tsx sonner.tsx spinner.tsx table.tsx tabs.tsx toggle-group.tsx` exist
    - `postcss.config.mjs` contains `@tailwindcss/postcss`
    - `package.json` contains the exact strings `"tailwindcss": "4.3.3"`, `"@tailwindcss/postcss": "4.3.3"`, `"lucide-react": "1.47.0"`, `"class-variance-authority": "0.7.1"`, `"tailwind-merge": "3.7.0"`, `"next-themes": "0.4.6"`, `"sonner": "2.0.8"`, `"vaul": "1.1.2"`, `"motion": "13.4.0"`
    - `grep -c '"geist"' package.json` returns 0
    - `grep -cE '"(tailwindcss|next-themes|sonner|vaul|motion|lucide-react)": "[\^~]' package.json` returns 0 (no floating ranges)
    - The real output of `npx shadcn@4.21.0 info` is pasted into the SUMMARY with the actual style/baseColor/iconLibrary values
    - `pnpm typecheck` through the store launcher exits 0
  </acceptance_criteria>
  <done>shadcn is initialised on the Radix/nova preset with zinc as the base colour, 33 primitives are copied in, every version is pinned to STACK.md, and no geist package and no tailwind.config file exist.</done>
</task>

<task type="auto">
  <name>Task 2: Paint the UI-SPEC palette into globals.css, swap Geist for Inter, mount ThemeProvider and Toaster</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` sections Design System, Spacing Scale, Typography, Color, Theme, Executor Rules 1-4, 11, 13, 15
    - `src/app/globals.css` as init wrote it (Task 1) - you are ADDING painted values, keeping the `@import "tailwindcss"` line
    - `src/app/layout.tsx` as init rewrote it (Task 1) - the font import to replace
    - `.planning/phases/02-budget-governor-search-presets/02-PATTERNS.md` Group 10 (the "use client" boundary rule and the comment in layout.tsx that IS the pattern)
  </read_first>
  <files>src/app/globals.css, src/app/layout.tsx, src/components/theme-provider.tsx</files>
  <action>
Write `src/app/globals.css` so it keeps init's `@import "tailwindcss";` at the top and then declares these EXACT literal hex values. UI-SPEC Executor Rule 2: painted values, never CSS custom properties, for anything a test pins - the e2e probe asserts `rgb(15, 118, 110)`, so `#0F766E` must literally be in this file.

Light, on `:root`:
  --background: #F4F6F7;  --foreground: #101619;
  --card: #FFFFFF;  --card-foreground: #101619;
  --popover: #FFFFFF;  --popover-foreground: #101619;
  --sidebar: #EAEEEF;  --sidebar-foreground: #101619;
  --muted: #E7EBEC;  --muted-foreground: #5B686D;
  --border: #D8DEE0;  --input: #D8DEE0;
  --primary: #0F766E;  --primary-foreground: #FFFFFF;  --ring: #0F766E;
  --secondary: #E7EBEC;  --secondary-foreground: #101619;
  --accent: #E7EBEC;  --accent-foreground: #101619;
  --destructive: #B42318;  --destructive-foreground: #FFFFFF;  --destructive-surface: #FEE4E2;  --destructive-surface-foreground: #7A271A;
  --warning: #B54708;  --warning-foreground: #FFFFFF;  --warning-surface: #FEF0C7;  --warning-surface-foreground: #B54708;
  --radius: 12px;

Dark, on `.dark`:
  --background: #0E1416;  --foreground: #E7EDEE;
  --card: #161E21;  --card-foreground: #E7EDEE;
  --popover: #161E21;  --popover-foreground: #E7EDEE;
  --sidebar: #121A1C;  --sidebar-foreground: #E7EDEE;
  --muted: #1D272A;  --muted-foreground: #93A1A5;
  --border: #263136;  --input: #263136;
  --primary: #2DD4BF;  --primary-foreground: #04211E;  --ring: #2DD4BF;
  --secondary: #1D272A;  --secondary-foreground: #E7EDEE;
  --accent: #1D272A;  --accent-foreground: #E7EDEE;
  --destructive: #F97066;  --destructive-foreground: #2A0B08;  --destructive-surface: #3A1210;  --destructive-surface-foreground: #F97066;
  --warning: #FDB022;  --warning-foreground: #2A1A04;  --warning-surface: #3B2708;  --warning-surface-foreground: #FDB022;

Then an `@theme inline { ... }` block mapping every one to a Tailwind utility namespace, e.g. `--color-background: var(--background); --color-primary: var(--primary); --color-warning-surface: var(--warning-surface); --radius-lg: var(--radius);` and the font stack:
  --font-sans: var(--font-sans-inter), ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", sans-serif;

Radii: `--radius: 12px` for cards/sheets/drawers, and declare `--radius-control: 8px` (buttons, inputs, chips) and `--radius-pill: 999px` (pills/badges) alongside.

RED FLAG, Executor Rule 3: any token COMPOSED at runtime from another token - anything built with `color-mix(...)` or `var()` of another token - is declared on `*` and `.dark *`, NEVER on `:root`. A composed custom property resolves where it is declared; on `:root` it freezes the light value for the whole document (recorded BIS defect, cost a production bug). If you declare none, say so in the SUMMARY.

RED FLAG, Executor Rule 1: Tailwind v4 arbitrary values are `[var(--x)]`, NEVER `[--x]`. v4 dropped the shorthand and emits invalid CSS silently.
RED FLAG, Executor Rule 4: write unprefixed CSS only. Never hand-write a `-webkit-` twin; Lightning CSS drops the standard property when a hand-written prefixed twin sits behind it.

Now `src/app/layout.tsx`. Replace init's Geist import with Inter and mount the providers. The final file must:
  - `import './globals.css';`
  - `import { Inter } from 'next/font/google';` and
    `const inter = Inter({ subsets: ['latin'], display: 'swap', variable: '--font-sans-inter' });`
  - render `<html lang="en" suppressHydrationWarning className={inter.variable}>` (next-themes requires `suppressHydrationWarning`)
  - keep `<ClerkProvider>` outermost and put `<ThemeProvider>` INSIDE it (UI-SPEC section Theme)
  - keep `<ActivateSoleOrganization />` exactly where it is, imported as a COMPONENT across the client boundary - do not turn it into a data import (the comment in the current file IS the pattern; a "use client" module's exports are client REFERENCES inside a server component and resolve to undefined at runtime with typecheck, lint and build all green - two recorded BIS 500s)
  - mount `<Toaster />` from `@/components/ui/sonner` inside the body
  - export `metadata = { title: 'Siteless' }` and
    `export const viewport = { themeColor: [{ media: '(prefers-color-scheme: light)', color: '#F4F6F7' }, { media: '(prefers-color-scheme: dark)', color: '#0E1416' }] };`

Create `src/components/theme-provider.tsx`:
```
'use client';
import { ThemeProvider as NextThemesProvider } from 'next-themes';
export function ThemeProvider({ children }: { children: React.ReactNode }) {
  return (
    <NextThemesProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
      {children}
    </NextThemesProvider>
  );
}
```

Finally run build through the launcher. Next warns that CSS ordering can differ between dev and `next build`, which is exactly why UI-SPEC Executor Rule 8 requires screenshots of the BUILT app - plan 02-14 takes them.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains every one of these literal strings: `#F4F6F7` `#101619` `#FFFFFF` `#EAEEEF` `#E7EBEC` `#5B686D` `#D8DEE0` `#0F766E` `#B42318` `#FEE4E2` `#B54708` `#FEF0C7` `#0E1416` `#E7EDEE` `#161E21` `#121A1C` `#1D272A` `#93A1A5` `#263136` `#2DD4BF` `#04211E` `#F97066` `#3A1210` `#FDB022` `#3B2708`
    - `src/app/globals.css` contains `@import "tailwindcss"` and `@theme inline` and `--radius: 12px`
    - `grep -cE '\[--[a-z]' src/app/globals.css src/app/layout.tsx` returns 0 (Tailwind v4 shorthand is forbidden - Executor Rule 1)
    - `grep -c -- '-webkit-' src/app/globals.css` returns 0 (Executor Rule 4)
    - `src/app/layout.tsx` contains `Inter(`, `--font-sans-inter`, `suppressHydrationWarning`, `ThemeProvider`, `Toaster`, `globals.css`, and still contains `ActivateSoleOrganization`
    - `grep -c 'Geist' src/app/layout.tsx` returns 0
    - `src/components/theme-provider.tsx` contains `'use client'`, `attribute="class"`, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange`
    - `src/app/layout.tsx` contains `themeColor` with both `#F4F6F7` and `#0E1416`
    - `pnpm build` through the store launcher exits 0 and the route listing still shows the Proxy (Middleware) entry
  </acceptance_criteria>
  <done>Both palettes ship as painted hex, Inter is bound to the sans stack, next-themes and sonner are mounted inside ClerkProvider, and the app builds.</done>
</task>

<task type="auto">
  <name>Task 3: Open the jsdom component-test lane without weakening the node lane's UTC pin</name>
  <read_first>
    - `vitest.config.ts` - the `process.env.TZ = 'UTC'` assignment sits ABOVE every import, in the MAIN process, and that placement is load-bearing
    - `vitest.db.config.ts` - the precedent for a second config file in this repo
    - `tests/unit/suite-zone.test.ts` and `tests/unit/time.test.ts` - both depend on `environment: 'node'` staying the default for `*.test.ts`
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Validation Architecture" harness change 2
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` section "Wave 0 Requirements"
    - `eslint.config.mjs` - it has NO React rules at all today, and this is the first phase with hooks
  </read_first>
  <files>package.json, vitest.config.ts, vitest.dom.config.ts, eslint.config.mjs, tests/unit/_setup-dom.ts, tests/unit/jsdom-lane.test.tsx</files>
  <action>
1. Install the harness, pinned, through the store launcher:

    node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs add -D @testing-library/react@16.3.3 @testing-library/jest-dom@7.0.1 jsdom@30.1.1 @vitejs/plugin-react@6.1.1 msw@2.15.0 eslint-plugin-react-hooks

Pin `eslint-plugin-react-hooks` to the exact latest version whose peer accepts `eslint@10.11.0` and record that version in the SUMMARY. The other five are pinned exactly as written.

2. `tests/unit/_setup-dom.ts` - one line: `import '@testing-library/jest-dom/vitest';`

3. The lane. PREFERRED: vitest 5's `test.projects` inside `vitest.config.ts` - one `node` project (`include: ['tests/unit/**/*.test.ts']`, `environment: 'node'`) and one `dom` project (`include: ['tests/unit/**/*.test.tsx']`, `environment: 'jsdom'`, `plugins: [react()]`, `setupFiles: ['tests/unit/_setup-dom.ts']`), both inheriting the root `resolve.alias` for `@`.

FALLBACK, if vitest 5.0.1 rejects `projects`: create `vitest.dom.config.ts` mirroring `vitest.db.config.ts`'s shape (its own `process.env.TZ = 'UTC'` / `process.env.LANG = 'en_US.UTF-8'` pair ABOVE every import), with `include: ['tests/unit/**/*.test.tsx']`, `environment: 'jsdom'`, `plugins: [react()]`, `setupFiles: ['tests/unit/_setup-dom.ts']`, `pool: 'forks'`; remove `tests/unit/**/*.test.tsx` from `vitest.config.ts`'s `include`; and change the `test:unit` script to `vitest run tests/unit && vitest run --config vitest.dom.config.ts`. Record in the SUMMARY which of the two you used and why.

Either way these three invariants hold, and they are what the acceptance criteria check:
  - `process.env.TZ = 'UTC'` is assigned in the MAIN process, textually BEFORE the first `import` statement, in EVERY vitest config file. vitest's `test.env.TZ` does not change the zone in worker threads, and `TZ=x pnpm test` does nothing at all in PowerShell.
  - `tests/unit/**/*.test.ts` still runs under `environment: 'node'`.
  - `pnpm test:unit` runs BOTH lanes, so a `-t` filter that matches only a `.tsx` test still executes.

4. React lint rules. Add `eslint-plugin-react-hooks`'s recommended config to `eslint.config.mjs`, scoped to `['src/**/*.ts', 'src/**/*.tsx']`, and add `'src/components/ui/**'` to the top-level `ignores` array (generated copy-ins are never hand-edited, so linting them only produces noise). At minimum `react-hooks/rules-of-hooks` must be `error` and `react-hooks/exhaustive-deps` must be `warn` or `error` - plan 02-10's debounced estimate is the first hook-heavy code in this repo and the current config would not catch a single hook mistake.

5. `tests/unit/jsdom-lane.test.tsx` - the smoke test that proves the lane exists. Render the shadcn `Button` from `@/components/ui/button` with the text `Create preset`, and assert BOTH that it is in the document AND that its accessible name is exactly `Create preset` (two-sided: asserting absence alone proves nothing). Name the test exactly `jsdom lane renders a shadcn primitive`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit` through the store launcher exits 0 and the output NAMES `jsdom lane renders a shadcn primitive` in its pass list (read the name, not the exit code - a `-t` filter that matches nothing exits 0 green)
    - `pnpm test:unit` output still NAMES `suite runs in a zone that can discriminate` and the `time.ts` opposite-days test - the node lane was not converted to jsdom
    - This node one-liner prints `ok`: `node -e "const fs=require('fs');for(const f of fs.readdirSync('.').filter(n=>/^vitest.*[.]config[.]ts$/.test(n))){const s=fs.readFileSync(f,'utf8');const tz=s.indexOf(String.fromCharCode(112)+'rocess.env.TZ');const im=s.indexOf('import ');if(tz<0||(im>=0&&tz>im))throw new Error(f);}console.log('ok')"`
    - `package.json` contains the exact strings `"@testing-library/react": "16.3.3"`, `"@testing-library/jest-dom": "7.0.1"`, `"jsdom": "30.1.1"`, `"@vitejs/plugin-react": "6.1.1"`, `"msw": "2.15.0"`
    - `tests/unit/_setup-dom.ts` contains `@testing-library/jest-dom`
    - `eslint.config.mjs` contains `react-hooks/rules-of-hooks` and `src/components/ui/**`
    - `pnpm lint` through the store launcher exits 0
    - The SUMMARY records which lane mechanism was used (`test.projects` or `vitest.dom.config.ts`) and the real `pnpm test:unit` pass list
  </acceptance_criteria>
  <done>A `.test.tsx` file runs under jsdom with React and jest-dom matchers, the node lane keeps its UTC pin and its node environment, and React hook rules are enforced for the first time in this repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry / shadcn registry -> repo | `shadcn init` and `add` copy third-party source into `src/components/ui` and install transitive dependencies at floating versions |
| server module -> client bundle | Anything imported by a `"use client"` module ships to the browser; a credential reaching that side is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-15 | Information Disclosure | client bundle | mitigate | UI-SPEC Executor Rule 14: nothing in `globals.css`, `layout.tsx`, `theme-provider.tsx` or any `components/ui` primitive reads a credential, and no `NEXT_PUBLIC_GOOGLE*` name is introduced. Plan 02-02's `no google credential` grep test is the standing guard over `src/`. Tasks 1-2. | high |
| T-2-16 (new, this plan) | Tampering | shadcn / npm supply chain | mitigate | Only the official shadcn registry `https://ui.shadcn.com/r` is used (UI-SPEC section Registry Safety); no third-party registry, no `--registry` flag. Every floated version `init` installed is re-pinned to an exact STACK.md version in the same task, and a criterion greps `package.json` for any remaining caret or tilde range. Task 1. | medium |
| T-2-17 (new, this plan) | Tampering | test harness | mitigate | The `TZ = UTC` main-process pin is asserted textually above the first import in every vitest config, so the new jsdom lane cannot silently make every `America/Chicago` assertion vacuous on this Chicago dev machine. Task 3. | medium |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint`, `pnpm test:unit` and `pnpm build` each exit 0 through the store launcher (run individually; `pnpm verify` does not run on this machine)
- `npx shadcn@4.21.0 info` output is recorded verbatim in the SUMMARY
- `git status --short` before `init` was empty, and the file list `init` actually wrote is recorded against RESEARCH assumption A8
- `ls tailwind.config.*` finds nothing; `grep -c '"geist"' package.json` returns 0
- Every hex value from UI-SPEC section Color appears literally in `src/app/globals.css`
</verification>

<success_criteria>
- 33 shadcn primitives exist, pinned, from the official registry only
- Both palettes are painted hex in `globals.css`, `--radius: 12px`, Inter bound to the sans stack
- `ThemeProvider` (attribute=class, defaultTheme=system) and `<Toaster />` are mounted inside `ClerkProvider`
- A `.test.tsx` passes under jsdom; `suite-zone.test.ts` and `time.test.ts` still pass under node with TZ=UTC
- React hook lint rules exist for the first time in this repo
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-01-SUMMARY.md` recording: the verbatim `shadcn init` stdout, the verbatim `npx shadcn@4.21.0 info` output with the real style/baseColor/iconLibrary, the file list `init` wrote versus RESEARCH assumption A8's prediction, which jsdom lane mechanism was used, and the `pnpm test:unit` pass list with test names.
</output>
