---
phase: 02-budget-governor-search-presets
plan: 14
type: execute
wave: 6
depends_on: [02-11, 02-12, 02-13]
files_modified:
  - docs/deploy.md
  - docs/runbooks/google-quota.md
autonomous: false
requirements: [BUDG-01, BUDG-02, BUDG-03, SRCH-02]
user_setup:
  - service: google-cloud
    why: "BUDG-03 needs a per-API daily quota on Places API (New) as an independent second wall; the GCP project and the Places key do not exist yet"
    env_vars: []
    dashboard_config:
      - task: "Create the Google Cloud project, enable Places API (New) with billing, then set the quota metric Requests per day to 100"
        location: "Google Cloud console -> Google Maps Platform -> Quotas -> API dropdown -> Places API (New) -> Requests per day -> Edit quota"
  - service: vercel
    why: "A7 is unverified - the Vercel CLI token was invalid during research. Pro decides the Phase 9 scheduler, not this phase"
    env_vars: []
    dashboard_config:
      - task: "Confirm the siteless project's plan is Pro"
        location: "Vercel -> team_8zjV46sJxQDsVzikNQa1JaO2 -> Settings -> Billing"

must_haves:
  truths:
    - "The production Supabase schema carries the same twelve new tables, five functions, constraints, grants and triggers as local, applied ONLY by drizzle-kit"
    - "The reference rows are seeded on production: 254 counties, 17 cities, 4 clusters, 3 geo presets, 20 outlet-count rows"
    - "Production is PostgreSQL 17.6 while local and CI are 18 - every migration applied without a single syntax refusal, which is the claim the pg17 grep test exists to protect"
    - "BUDG-03: a Google Cloud per-API daily quota of 100 requests/day is configured, or is recorded as explicitly blocked on a project that does not exist yet"
    - "No credential is printed, pasted or committed anywhere in this plan's output"
  artifacts:
    - path: "docs/runbooks/google-quota.md"
      provides: "The console path, the derivation, the two Google caveats and the three-timezone warning"
      contains: "100"
    - path: "docs/deploy.md"
      provides: "Extended with the Phase 2 production migration and seed steps"
      contains: "db:seed:prod"
  key_links:
    - from: "local test database"
      to: "production Supabase jahgeqshuesndyscnmjo"
      via: "the same drizzle/ migration files, applied by scripts/db.ts --target=prod"
      pattern: "db:migrate:prod"
---

<objective>
Put this phase's schema and seed data on the real Supabase project, and close BUDG-03's second wall — the half of this phase that no local test can prove.

Purpose: `pnpm build` and `pnpm typecheck` pass WITHOUT the production migration, because Drizzle's types come from the schema files and not from the live database. That is a false-positive verification state, and it is exactly how a deployment builds green and 500s on its first query.
Output: a migrated and seeded production database whose object counts match local side by side, a Google quota runbook, and danlo's recorded confirmations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-10-PLAN.md
@scripts/db.ts
@scripts/seed.ts
@docs/deploy.md
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written in full below.

🔴 This plan is `autonomous: false` and runs SEQUENTIALLY on `main`, never in a worktree. It writes to the real production database.

🔴 Production is **PostgreSQL 17.6**; local and CI are 18. `NULLS NOT DISTINCT` (PG15+), stored generated columns (PG12+) and `for update ... skip locked` are all safe. `returning old.` / `returning new.`, `uuidv7()` and virtual generated columns are NOT, and are refused by `tests/unit/pg17-compat.test.ts`.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight READ against production - no writes</name>
  <read_first>
    - `scripts/db.ts` — `--target=prod` demands a Supabase host AND port 5432 (the SESSION pooler); the transaction pooler cannot run migrations
    - `.planning/phases/01-foundations-tenancy/01-10-PLAN.md` Task 2 — the pre-flight/post-flight discipline this mirrors, and its STOP condition
    - `drizzle/` — every migration file this phase added, in order, so you know exactly what is about to run
    - `.planning/CONVENTIONS.md` section Migrations — drizzle-kit is the single authority (D-09); no Supabase CLI, no MCP `apply_migration`, no dashboard SQL editor for a schema change
  </read_first>
  <files>none - this task only reads production</files>
  <action>
🔴 **A separate pre-flight read, before any write. Never re-run a mutating script to see its output** — that is a recorded hard lesson and it costs a production incident when the script is not idempotent in the way you assumed.

Against `SUPABASE_DB_URL` (session pooler, port 5432), as a READ-ONLY connection, report and record verbatim:
  - `select version()` — expect PostgreSQL 17.6
  - `select name from drizzle.__drizzle_migrations order by created_at` — the migrations already applied (Phase 1 left twelve)
  - `select tablename from pg_tables where schemaname = 'public' order by 1` — expect Phase 1's four and nothing more
  - `select proname from pg_proc p join pg_namespace n on n.oid = p.pronamespace where n.nspname = 'app' order by 1` — expect Phase 1's five
  - `select count(*) from pg_policy`
  - `select count(*) from pg_trigger t join pg_class c on c.oid = t.tgrelid where not t.tgisinternal`

🔴 **STOP conditions.** If `public` already contains any of this phase's twelve tables, or `drizzle.__drizzle_migrations` lists a migration not present in `drizzle/`, STOP and report — something other than drizzle-kit has written to this database and D-09 is already broken.

Run the same six queries against `TEST_DATABASE_URL` and record BOTH sets side by side in a table. That side-by-side is what found the Phase 1 gap that became plan 01-12; it is not a formality.

Then re-run the two portability gates locally so the claim being carried to production is fresh:
  - `pnpm test:unit -t "pg17"`
  - `pnpm test:db -t "server version"`
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit -t "pg17"</automated>
  </verify>
  <acceptance_criteria>
    - `select version()` on production reports PostgreSQL 17.x, recorded verbatim in the SUMMARY
    - The local-versus-production side-by-side table is in the SUMMARY with all six rows for both databases
    - `public` on production contains ONLY `orgs`, `businesses`, `source_records`, `events` before this plan writes anything — if not, the plan STOPPED and said so
    - `drizzle.__drizzle_migrations` on production lists only migrations that exist in `drizzle/`
    - `pnpm test:unit -t "pg17"` and `pnpm test:db -t "server version"` both exit 0, with the test NAMES read from the output
    - No connection string, password or key appears in the SUMMARY (`git grep -nE 'sk_|eyJ|://[^ ]*:[^ ]*@' -- . ':!pnpm-lock.yaml'` returns nothing)
  </acceptance_criteria>
  <done>The exact state of the production database before this phase touches it is recorded, beside local, and the two portability gates are freshly green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [danlo] Approve the production migration and seed, then apply and verify</name>
  <read_first>
    - Task 1's recorded pre-flight side-by-side
    - `scripts/db.ts` and `scripts/seed.ts` — both target gates; `--target=prod` demands a Supabase host on port 5432, `--target=test` refuses one
    - `.planning/phases/01-foundations-tenancy/01-10-PLAN.md` Tasks 2 and 3 — the precedent for how a production write is approved and recorded in this project
    - `docs/deploy.md` — the recipe this task extends
  </read_first>
  <files>docs/deploy.md</files>
  <action>
Present the block below to danlo and STOP. There is no undo on a production migration, and CLAUDE.md is explicit that danlo reviews before anything lands.

**After danlo replies with the go-ahead**, run these four steps in order and record every output:
  1. `pnpm db:migrate:prod` through the store launcher. Every migration this phase added applies in order.
  2. `pnpm db:migrate:prod` a SECOND time and confirm it applies nothing.
  3. `pnpm db:seed:prod` through the store launcher, then a SECOND time — the second run must report 0 inserted for every table. 🔴 `scripts/seed.ts` runs as the migration owner because `authenticated` cannot write an `org_id IS NULL` row at all, by design.
  4. Post-flight verification, as a SEPARATE read, compared against local count for count:
     - all twelve new tables exist in `public` with `relrowsecurity = true`
     - `select count(*) from pg_policy` matches local
     - the named constraints exist: `bp_not_over`, `cost_ledger_request_id_key`, `cr_est_positive`, `counties_fips_identity`, `sv_geo_kind_known`, `runs_status_known`, `oc_scope_target`, `it_naics_range_bounds`
     - the five new `app.*` functions exist: `current_org_role`, `ensure_budget_period`, `reserve_budget`, `settle_reservation`, `set_budget_cap`
     - `search_versions` grants: `authenticated` holds `select, insert` and NOT `update, delete` (read `information_schema.role_table_grants`)
     - `budget_periods` grants: `authenticated` holds `select` only
     - `runs` column grants: `authenticated` holds UPDATE on `status`, `stopped_reason`, `cost_micro_usd`, `calls_count`, `started_at`, `finished_at` and NOT on `search_version_id` (read `information_schema.column_privileges`)
     - the trigger set matches local, including `budget_periods_event_upd` with its `WHEN` clause (read `pg_get_triggerdef`)
     - seeded row counts: `counties` 254, `cities` 17, `industry_clusters` 4, `geo_presets` 3, `outlet_counts` 20
     - `select count(*) from counties where county_fips <> 2 * comptroller_code - 1` returns 0
  Record local and production side by side. A mismatch means dev and production diverged, which is the exact failure D-09 exists to prevent.

  5. Append to `docs/deploy.md`: the Phase 2 migration file names, `pnpm db:seed:prod` and when to run it (after every migration that adds a reference table), and the note that `scripts/refresh-outlet-counts.ts` is NEVER run in CI and never against production without a fresh review of the numbers it rewrites.

🔴 Do NOT use the Supabase MCP `apply_migration`, the Supabase CLI, or the dashboard SQL editor for any schema object. Drizzle-kit is the single migration authority.
  </action>
  <what-built>
    Phase 2's twelve tables, five SQL functions, constraints, grants and triggers are applied and proven on the local PostgreSQL 18.6, with 40-way concurrency, the cap CHECK, settlement idempotency, the admin gate and the reference-row policies all watched red first under their own mutations. The seed data — 254 Texas counties, the 17 RGV cities, four clusters, the outlet-count matrix — is committed and loads idempotently. Nothing has touched production yet.
  </what-built>
  <how-to-verify>
    1. **Read the pre-flight table above.** Production is PostgreSQL 17.6 and currently holds Phase 1's four tables and five functions. Confirm that matches what you expect.
    2. **Approve the migration.** It applies the Phase 2 migration files listed above to `jahgeqshuesndyscnmjo` as the owner, over the SESSION pooler on port 5432. There is no undo.
    3. **Approve the seed.** It inserts reference rows with `org_id IS NULL` — the built-in clusters, cities, counties and geo presets every tenant reads. It is idempotent, so a re-run changes nothing.
    4. **One thing to confirm out loud, because Phase 3 reads it:** the RGV city list is the **17 cities with at least 400 active sales-tax outlets**, covering 90.9% of the RGV outlet universe after folding Rio Grande City's three source spellings. Willacy County's only town, **Raymondville (221 outlets)**, falls below the line and is therefore NOT in the picker, so Willacy is unrepresented among cities though it is present among counties. The next five candidates are listed in `src/seed/data/cities.json` so you can promote one by editing that file. **Is the 17 as measured correct, or do you want Raymondville pinned in?**
    5. **Reply with:**
       `prod migration: approved` / `prod seed: approved` / `city list: 17 as measured` OR `city list: add Raymondville`
  </how-to-verify>
  <acceptance_criteria>
    - danlo's reply is recorded verbatim in the SUMMARY, including his answer on the city list
    - `pnpm db:migrate:prod` exits 0 and a second consecutive run applies zero migrations
    - `pnpm db:seed:prod` exits 0 twice; the second run reports 0 inserted for every table (both summary blocks pasted)
    - The post-flight local-versus-production comparison table is in the SUMMARY with every row above, and every pair matches
    - `select count(*) from counties where county_fips <> 2 * comptroller_code - 1` returns 0 on production
    - `information_schema.role_table_grants` on production shows `authenticated` with no UPDATE or DELETE on `search_versions` and no INSERT/UPDATE/DELETE on `budget_periods`, `cost_reservations` or `cost_ledger`
    - `information_schema.column_privileges` on production shows `authenticated` with no UPDATE on `runs.search_version_id`
    - `ls supabase 2>/dev/null` finds nothing, and the SUMMARY states that no Supabase CLI or MCP migration was used
    - `git grep -nE 'sk_|eyJ|://[^ ]*:[^ ]*@' -- . ':!pnpm-lock.yaml'` returns nothing
    - If danlo answers `add Raymondville`, record it as a FOLLOW-UP for the next plan rather than editing the seed inside this checkpoint — the seed was already applied and changing it mid-plan would desynchronise the SUMMARY from the database
  </acceptance_criteria>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs db:migrate:prod &amp;&amp; node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs db:seed:prod</automated>
  </verify>
  <done>Production carries this phase's schema and seed data, applied entirely by drizzle-kit and `scripts/seed.ts`, matching local object for object and row for row, with danlo's approval recorded.</done>
  <resume-signal>Reply with the three-line status block: `prod migration: approved` / `prod seed: approved` / `city list: 17 as measured` or `city list: add Raymondville`.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [danlo] The Google Cloud daily quota (BUDG-03) and the Vercel plan confirmation</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "The Second Wall" IN FULL — the derivation, the console path with its two alternates, the `limit exceeded` behaviour, the enforcement-latency caveat and the US/Pacific reset
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Pitfall 11 — three timezones all called "monthly"
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` Executor Rule 14 and the second-wall card copy (plan 02-13 already renders the derivation on screen)
    - `.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md` section Claude's Discretion — the 80-to-100/day band and the instruction that nothing may depend on the key existing
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Assumptions Log A7 — the Vercel plan is UNVERIFIED (`vercel teams ls` returned an invalid token)
  </read_first>
  <files>docs/runbooks/google-quota.md</files>
  <action>
First write `docs/runbooks/google-quota.md`, so the runbook exists whether or not danlo can act today:
  - **The value:** `Places API (New) → Requests per day = 100`.
  - **The derivation**, verbatim: a $50.00 cap buys `50,000,000 / 35,000 = 1,428` paid Text Search Enterprise requests at $35.00/1,000, plus the 1,000 free = **2,428/month ≈ 80.9/day**, rounded up to **100/day** so a weekly partition can burst. Runaway bound: `100 × $0.035 = $3.50/day`, against $50 in an hour unbounded.
  - 🔴 **The honesty line:** 100/day × 30 = 3,000 requests = **$70/month, which EXCEEDS the cap**. The quota bounds a runaway DAY; only the app meter bounds the MONTH. That is precisely what "second wall, not the primary meter" means.
  - **The console path:** Google Cloud console → **Google Maps Platform → Quotas** → API dropdown → *Places API (New)* → quota metric **"Requests per day"** → ⋮ → **Edit quota** → uncheck *Unlimited* → enter `100` → **Submit request**. Alternates: *IAM & Admin → Quotas & System Limits*, or *APIs & Services → [API] → Quotas → Edit Quotas*.
  - **Two documented caveats:** exceeding the quota returns a `limit exceeded` ERROR, it does not silently drop traffic; and quota limits are not always entirely precise because there is latency between a quota being surpassed and enforcement beginning — so the quota can overshoot slightly while the meter cannot.
  - 🔴 **Three timezones, all called "monthly":** `America/Chicago` (the app's budget period, D-11), **US/Pacific** (Google's daily quota reset), and Google's billing month (billing-account aligned). They do not coincide. No copy anywhere may imply they do.
  - **A budget alert is not a cap.** Google states verbatim that setting a budget does not automatically cap Google Cloud or Google Maps Platform usage or spending. Keep both, and say which one stops things.

Then present the checkpoint and STOP.
  </action>
  <what-built>
    The second-wall card on `/settings/budget` already renders the recommended quota, its full derivation and its honest limitation, and `docs/runbooks/google-quota.md` now carries the console path. Nothing in `src/` reads a Google credential and no test depends on the key existing — `tests/unit/no-google-credential.test.ts` enforces that, so this checkpoint blocks nothing in the codebase.
  </what-built>
  <how-to-verify>
    1. **The Google Cloud project and the Places API (New) key do not exist yet** (PROJECT.md, "Dependencies not yet created"). If you have not created them, that is fine — answer `blocked` and this becomes the first item of Phase 4's setup. Phase 4 cannot make its first paid call without them anyway.
    2. **If the project exists:** enable Places API (New) with billing, then follow the console path in `docs/runbooks/google-quota.md` and set **Requests per day = 100** on Places API (New). Take a screenshot of the quota page showing the value.
    3. **Understand what you are setting.** 100/day bounds a runaway loop to about $3.50/day instead of $50 in an hour. It does NOT bound the month — 100/day for 30 days is $70, above your $50 cap. The app's own meter is the wall that bounds the month; this is the independent second one.
    4. **Vercel plan (A7).** Research could not verify it — `vercel teams ls` returned "The specified token is not valid". Confirm in the dashboard whether the `siteless` project is on **Pro**. This blocks nothing in Phase 2; the Hobby cron's once-a-day ±59-minute jitter is what would block the Phase 9 scheduler, and Pro's ~$20/mo is infrastructure that is never merged into the $50 data cap.
    5. **Reply with:**
       `google quota: 100/day set` OR `google quota: blocked - no GCP project yet`
       `vercel plan: pro` OR `vercel plan: hobby` OR `vercel plan: unknown`
  </how-to-verify>
  <acceptance_criteria>
    - `docs/runbooks/google-quota.md` exists and contains `100`, `1,428`, `2,428`, `$3.50/day`, `$70`, `US/Pacific`, `America/Chicago`, `limit exceeded`, and the sentence that a budget alert is not a cap
    - danlo's two-line reply is recorded verbatim in the SUMMARY; if the quota was set, the screenshot is referenced
    - If `blocked`, the SUMMARY records it as a **carried-forward blocker for Phase 4** with the exact console path, and states plainly that it blocks nothing in Phase 2
    - `pnpm test:unit -t "no google credential"` exits 0 — nothing in `src/` reads a Google credential and no test requires the key
    - `grep -rn "GOOGLE\|X-Goog\|googleapis" src/ | wc -l` returns 0
    - The Vercel plan answer is recorded and, if not `pro`, flagged as a Phase 9 blocker rather than a Phase 2 one
  </acceptance_criteria>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit -t "no google credential"</automated>
  </verify>
  <done>BUDG-03's quota is either set at 100 requests/day with its derivation documented, or recorded as explicitly blocked on a Google Cloud project that does not exist — and either way nothing in this phase depends on it.</done>
  <resume-signal>Reply with the two-line status block: `google quota: ...` and `vercel plan: ...`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine -> production database | `db:migrate:prod` and `db:seed:prod` run as the owner against the real project; there is no undo |
| local PostgreSQL 18.6 -> production PostgreSQL 17.6 | Every gate in this phase ran on 18; only this plan proves 17 accepts the same SQL |
| repository and SUMMARY -> credentials | A pasted connection string in a SUMMARY is as permanent as a committed one |
| runaway application loop -> Google spend | The app meter is the first wall; the console quota is the independent second |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-03 | Tampering | production spend path | mitigate | The production schema carries `cost_ledger.reservation_id NOT NULL` with its FK and grants `authenticated` SELECT only on all three budget tables — verified from `information_schema.role_table_grants` on production, not assumed from local. Task 2. | high |
| T-2-02 | Elevation of Privilege | production cap | mitigate | Post-flight reads confirm `authenticated` holds no INSERT/UPDATE/DELETE on `budget_periods` on production, so the only write path is `app.set_budget_cap` with its SQL role check. Task 2. | high |
| T-2-09 | Tampering | production reference rows | mitigate | The seed runs as the owner (the only role that can write an `org_id IS NULL` row) behind `scripts/seed.ts`'s target gate, and the post-flight confirms the reference tables' write policies exclude NULL-org rows. Task 2. | high |
| T-2-12 | Tampering | production version and run integrity | mitigate | Post-flight reads `information_schema.role_table_grants` for `search_versions` and `information_schema.column_privileges` for `runs.search_version_id` — the two grant facts SRCH-03 and T-2-12 rest on — rather than trusting that the migration "applied". Task 2. | high |
| T-2-15 | Information Disclosure | Google credential | mitigate | The quota is set in a console by a human; no key enters the repo, no `NEXT_PUBLIC_GOOGLE` name is created, and `tests/unit/no-google-credential.test.ts` is re-run as an acceptance criterion with the key still absent. Task 3. | high |
| Pitfall 1 (no STRIDE ref) | Tampering | PG18-only syntax reaching PG17 | mitigate | Both portability gates are re-run immediately before the production write, and the migration applying cleanly on 17.6 is itself the proof. Tasks 1-2. | high |
| T-1-10 carried forward | Information Disclosure | credentials in the SUMMARY | mitigate | A `git grep` for credential-shaped strings is an acceptance criterion on both write tasks; no value is ever printed, only names and counts. Tasks 1-3. | high |
</threat_model>

<verification>
- The pre-flight and post-flight local-versus-production comparison tables are both in the SUMMARY, every pair matching
- `pnpm db:migrate:prod` and `pnpm db:seed:prod` each green and each idempotent on a second run, with both seed summary blocks pasted
- Production grant facts read from `information_schema`, not inferred: `search_versions` insert-only, budget tables select-only, `runs.search_version_id` not updatable
- `pnpm test:unit -t "pg17"`, `pnpm test:db -t "server version"` and `pnpm test:unit -t "no google credential"` all green
- danlo's two reply blocks recorded verbatim, including his answer on the RGV city list
- `git grep` for credential-shaped strings returns nothing
</verification>

<success_criteria>
- Production carries this phase's twelve tables, five functions, named constraints, grants and triggers, applied only by drizzle-kit, matching local count for count
- The reference rows are seeded on production and a re-run changes nothing
- The 254-county FIPS bijection holds on production
- BUDG-03's quota is set at 100/day, or recorded as blocked with the console path documented and nothing in the phase depending on it
- The RGV city list has danlo's explicit confirmation, because Phase 3 reads it
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-14-SUMMARY.md` recording: the pre-flight and post-flight side-by-side tables, both `db:seed:prod` summary blocks, the `information_schema` grant reads for `search_versions`, the budget tables and `runs.search_version_id`, danlo's two verbatim reply blocks, and — flagged at the top — his answer on the RGV city list, since that is the Phase 2 / Phase 3 shared contract.
</output>
