---
phase: 02-budget-governor-search-presets
plan: 15
type: execute
wave: 7
depends_on: [02-14]
files_modified:
  - .planning/phases/02-budget-governor-search-presets/02-VALIDATION.md
  - docs/deploy.md
autonomous: false
requirements: [BUDG-01, BUDG-02, BUDG-03, BUDG-04, SRCH-01, SRCH-02, SRCH-03, SRCH-04]

must_haves:
  truths:
    - "Criterion 1 carried forward: danlo signs in on the deployed URL and every screen in this phase renders org-scoped, on the real Vercel deployment of a known commit"
    - "Criterion 5: the concurrent-burst refusal and the 80 percent warning are proven, and gate mutations M7 to M12 are recorded AS RUN, each reding exactly the predicted named test"
    - "Every screen was reviewed by danlo on the BUILT app, in both themes, at 390x844 and 1280x800 - design quality is a first-class requirement, not a checkbox"
    - "02-VALIDATION.md's per-task map is filled with real statuses and nyquist_compliant is flipped to true"
    - "The five ROADMAP success criteria for this phase each map to a named, green, mutation-checked test or to a recorded manual verification"
  artifacts:
    - path: ".planning/phases/02-budget-governor-search-presets/02-VALIDATION.md"
      provides: "The completed per-task verification map and the M7-M12 as-run table"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "the deployed Vercel URL"
      to: "production Supabase"
      via: "SUPABASE_DB_POOL_URL as app_user, proven by /api/health db:up"
      pattern: "\"db\":\"up\""
    - from: "tests/e2e/"
      to: "the deployed URL"
      via: "E2E_BASE_URL"
      pattern: "E2E_BASE_URL"
---

<objective>
Deploy, prove it deployed, run every e2e spec against the real URL, let danlo look at the screens, and close the phase: the mutation table as RUN and `02-VALIDATION.md` filled in with real statuses.

Purpose: "it built" is not "it works". A push is never assumed to have deployed — the build log names the commit, and only then does the smoke run. And a green suite proves nothing a mutation check hasn't.
Output: a live deployment of a known commit, a green e2e run against it, danlo's design sign-off, and a closed validation contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md
@.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/01-foundations-tenancy/01-11-PLAN.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@playwright.config.ts
@docs/deploy.md
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written in full below. `pnpm verify` is NOT runnable here — run typecheck, lint, test:unit, test:db and build individually.

🔴 This plan is `autonomous: false` and runs SEQUENTIALLY on `main`, never in a worktree.

🔴 PR-only, no autonomous merges. Open the PR, and merge only when `verify`, `db` and `e2e` are green on the CURRENT head.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: Deploy a known commit, smoke it, and run every e2e spec against the real URL</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-11-PLAN.md` Tasks 1 and 2 — the deploy-and-verify discipline this mirrors, including the sha comparison and the leak scan
    - `docs/deploy.md` — the recipe, the team scope and the production URL
    - `src/app/api/health/route.ts` — the exact response shape and what each field proves
    - `playwright.config.ts` and `tests/e2e/_required-env.ts` — `E2E_BASE_URL` is the only thing pointing the suite at a server, and it must be the DEPLOYED url
    - `.github/workflows/ci.yml` — the e2e job polls `/api/health` until `commit == GITHUB_SHA` before starting Playwright; do the same by hand here
    - every `tests/e2e/*.spec.ts` this phase added: `presets`, `preset-detail`, `spend`, `budget-banner`, `theme-tokens`, `touch-targets`, plus the moved `signed-in`
  </read_first>
  <files>docs/deploy.md</files>
  <action>
1. Confirm the working tree is clean. Record `git rev-parse --short HEAD` and `git rev-parse --abbrev-ref HEAD`. 🔴 A parallel session can move the tree mid-gate and a gate then goes silently green on the wrong commit — print the branch and sha again AFTER every long step.

2. Run the five gate constituents individually through the store launcher and record each result: `typecheck`, `lint`, `test:unit`, `test:db`, `build`. Print `git rev-parse --short HEAD` again afterwards and confirm it is unchanged.

3. Deploy: `vercel --prod --scope team_8zjV46sJxQDsVzikNQa1JaO2`. Record the deployment URL.

4. 🔴 **Never assume a push deployed.** Read the build log and confirm it names the same sha from step 1 (`vercel inspect <url> --scope ...`). If it differs, a parallel session moved the tree — stop, re-read `git log`, redeploy from the intended commit. Confirm the deployment state is READY.

5. Smoke `/api/health` against the DEPLOYED url:
   `curl -fsS "$DEPLOY_URL/api/health"` must return 200 with `"ok":true`, `"db":"up"`, `"proxy":"up"` and a `commit` equal to the recorded sha.
   - `"db":"down"` means the Vercel `SUPABASE_DB_POOL_URL` is wrong — the session pooler (5432) instead of the transaction pooler (6543), the `postgres.` username instead of `app_user.`, or a password never set. Fix the environment variable and redeploy; do not change code.
   - Assert the body leaks nothing: none of `postgres://`, `password`, `sk_`, `jahgeqshuesndyscnmjo`.

6. Smoke the new routes signed out: `GET /presets`, `/spend`, `/settings/budget` must each redirect to sign-in, and none may return a body containing `data-testid="org-id"`.

7. Set `E2E_BASE_URL` to the deployment URL in `.env.local` (gitignored), install the browser (`pnpm exec playwright install --with-deps chromium`), and run `pnpm test:e2e`. 🔴 **Read the test NAMES in the output, not the exit code.** Expect the Phase 1 baseline of 4 plus this phase's specs.
   - If `theme-tokens` fails on a computed colour, do NOT change the assertion — probe `getComputedStyle` by hand on the deployed page first. A hidden or zero-height Chrome window makes every measurement lie; the spec already asserts `innerHeight > 0`.
   - If a spec fails once, re-run that spec ALONE before concluding anything, and judge by wall clock: a regression fails the same test every time, while contention moves between tests.
   - 🔴 Do not port any `setActive` workaround. If a run lands on `/no-access`, the fix is in the app.

8. Append the production URL and the verified sha to `docs/deploy.md`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - The branch and sha are recorded BEFORE and AFTER the five gate constituents, and are identical
    - All five constituents (`typecheck`, `lint`, `test:unit`, `test:db`, `build`) exit 0, each result recorded separately — `pnpm verify` is NOT used, it does not run on this machine
    - The build log names the same commit sha as `git rev-parse --short HEAD` taken before the deploy, both recorded side by side, and the deployment state is READY
    - `curl -fsS "$DEPLOY_URL/api/health"` returns 200 with `"ok":true`, `"db":"up"`, `"proxy":"up"` and the matching `commit`
    - The health body contains none of `postgres://`, `password`, `sk_`, `jahgeqshuesndyscnmjo`
    - Signed-out `GET /presets`, `/spend` and `/settings/budget` each redirect and none returns a body containing `data-testid="org-id"`
    - `pnpm test:e2e` exits 0 against the DEPLOYED url, and EVERY test NAME is pasted into the SUMMARY — including `signs in and is org-scoped`, `create preset: cities`, `create preset: county`, `create preset: radius`, `preset detail: a saved edit shows two versions and the run keeps the old one`, `spend: month-to-date, the gauge and all three providers render`, `budget banner: renders on every route at 80 percent`, `theme tokens: the accent resolves in light`, `theme tokens: the accent resolves in dark`, and the touch-target checks
    - Any spec that failed on its first run is recorded with its solo re-run and its wall-clock times
    - `git status --short` is clean before and after
  </acceptance_criteria>
  <done>A production deployment of a known commit is live, its database and Clerk proxy are both up, no route leaks to a signed-out visitor, and every e2e spec in the phase passed against the real URL with its name read from the output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [danlo] Review the seven screens on the deployed app, both themes, both viewports</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section "Screen Inventory & Layout Patterns" — all seven blocks, which are the contract each screenshot is judged against
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` Executor Rule 8 — screenshots are of the REAL screens, in BOTH themes, on the BUILT app, never a styleguide page and never dev mode
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` section "Manual-Only Verifications" — the visual-quality row, which is exactly this checkpoint
    - CLAUDE.md — world-class UI/UX is a first-class requirement, mobile-first and desk second
    - the SUMMARYs of plans 02-10 through 02-13 — the screenshot inventories each already produced
  </read_first>
  <files>none - this is a review checkpoint</files>
  <action>
Before presenting, capture the full set yourself against the DEPLOYED url with Playwright: **seven screens x two themes x two viewports = 28 images**, plus the four state shots that only appear under specific conditions.

Screens: `/presets` (empty AND populated) · `/presets/new` (settled AND recomputing) · `/presets/[id]` · `/presets/[id]/edit` · `/spend` (empty AND with a run) · `/settings/budget` (admin AND non-admin) · `/settings/organization`.
State shots: the 80% banner, the 100% banner, the run drawer in its REFUSED state, and the assumptions drawer.
Viewports: **390x844** and **1280x800**. Themes: light and dark, driven through the theme switcher, not by a CSS override.

🔴 Assert `window.innerHeight > 0` and that the target element is visible before capturing — a hidden or zero-height Chrome window makes every measurement and every screenshot lie.
🔴 The 80% and 100% states are driven by lowering the cap through `/settings/budget` and are RESTORED afterwards. Record the before and after cap values, and confirm from `budget_periods` that the cap is back where it started.

Then present the checkpoint and STOP.
  </action>
  <what-built>
    The design system born in this phase, on the deployed app: Inter, the teal accent, both palettes, 33 shadcn primitives, a 256px sidebar on desk and a bottom tab bar on phone. Seven screens — preset list, preset editor with the live estimate, preset detail with version history, budget settings, spend, organization settings, and the no-access page. The estimate recomputes 400ms after you stop typing and never blanks. Running a preset takes a real budget reservation, and at 100% of the cap the refusal is a sentence that says nothing was charged.
  </what-built>
  <how-to-verify>
    1. **Open the deployed URL on your phone and sign in.** Work the app for a few minutes: create a preset from the 17 cities, switch it to a county, try the Texas row, switch to a radius and geocode a real McAllen address. Watch the estimate while you do it.
    2. **On the phone specifically:** can you reach every primary action with your thumb? Is the bottom tab bar's active state obvious? Does anything hide behind the tab bar when you scroll to the end?
    3. **Switch the theme to dark** from the user menu and walk the same screens. Both palettes are meant to be first-class; neither is an afterthought.
    4. **Look at the numbers.** The estimate line reads `~N requests · ~$X · ~N businesses · N% of this month's remaining $Y`. Early in the month the dollar figure may legitimately read **$0.00** — the first 1,000 Text Search Enterprise requests each month are free, and the estimator applies that. The Texas row's multiplier is **computed** (roughly ×15 by cells), not the illustrative ×38 in the design document.
    5. **On desk (1280 wide):** the preset editor's estimate rail should stay visible while you scroll the form. The version history table's money and counts should line up in a column.
    6. **Then say which screens need work.** Be specific — "the spend gauge is too quiet" is actionable, "it looks fine" is not. Design quality is a first-class requirement in this project and this is the gate for it.
    7. **Reply with:** `approved` — or a numbered list of the screens and what is wrong with each.
  </how-to-verify>
  <acceptance_criteria>
    - All 28 base images plus the four state shots exist and are inventoried in the SUMMARY as screen x theme x viewport
    - Every screenshot is of the DEPLOYED app, not dev mode and not a styleguide page, and the SUMMARY says so explicitly
    - The cap was restored after driving the banner states, with the before and after values read from `budget_periods` and recorded
    - danlo's reply is recorded VERBATIM. If it is not `approved`, every item becomes a numbered follow-up in the SUMMARY with the screen named, and the phase does NOT close until they are addressed or explicitly deferred by danlo
    - `window.innerHeight > 0` was asserted before capture and the assertion is recorded
  </acceptance_criteria>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:e2e -g "theme tokens"</automated>
  </verify>
  <done>danlo has looked at every screen in both themes on a phone and a desk, on the deployed app, and either approved the phase's design or listed exactly what needs work.</done>
  <resume-signal>Reply `approved`, or a numbered list naming each screen and what is wrong with it.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: The phase gate - M7 to M12 as run, and 02-VALIDATION.md closed</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` — the per-task map to fill and the M7-M12 table to rewrite AS RUN
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` — the format precedent, including "the standing lesson" paragraph about mutation DIRECTION and about a mutation that reds two tests
    - the SUMMARYs of plans 02-02 through 02-13 — every mutation already run, with its failing test names
    - `.planning/CONVENTIONS.md` section Testing
    - `.planning/ROADMAP.md` section Phase 2 — the five success criteria this table must close
  </read_first>
  <files>.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md</files>
  <action>
1. **Re-run every gate mutation that was not already recorded AS RUN in a plan SUMMARY**, and re-verify the ones that were. Each is applied to the **LIVE local database** (never to a migration file, so the revert is provable by `git diff --stat` being empty), the suite is run, the failing test NAMES are read, the mutation is reverted, and the revert is verified from the catalog — `pg_get_functiondef`, `pg_constraint`, `pg_trigger`, or `information_schema.role_table_grants` — not from the fact that a script ran.
   - **M7** replace `app.reserve_budget`'s conditional UPDATE with a SELECT-then-UPDATE → `concurrent burst` ONLY; every single-worker test stays green, which is the point
   - **M8** `alter table budget_periods drop constraint bp_not_over` → `bp_not_over` + `cap below current spend`; the positive control (raising the cap) stays green
   - **M9** `alter table cost_ledger drop constraint cost_ledger_request_id_key` → `settlement idempotency` only
   - **M10** `create or replace function app.current_org_role()` returning the literal `'admin'` → `set_budget_cap refuses a member` only; the admin positive control stays green
   - **M11** append `places.reviews` to `PLACES_TEXT_SEARCH_FIELD_MASK` → the `fieldMaskTier atmosphere` test **and** the ledger-price test — two, and they must be independent
   - **M12** `grant update on public.search_versions to authenticated` → `versions immutable` only; `run keeps its version` stays green

2. 🔴 **Check every mutation for DIRECTION.** Phase 1's standing lesson: a guard that fails CLOSED under the mutation is indistinguishable from a working guard by exit code alone — probe the positive path to tell them apart. M1 in Phase 1 was green for the wrong reason until this was done.
3. 🔴 **A mutation that reds TWO tests is acceptable only when the two properties are proven independent** (Phase 1's M3 and M5); otherwise the tests are coupled and must be decoupled (Phase 1's M1). Where M8 or M11 reds two, prove independence with a narrower mutation and record it.

4. **Rewrite `02-VALIDATION.md`'s M7-M12 table AS RUN**, in Phase 1's format: the mutation as actually applied, the tests that went red by NAME, the suite size, and the SUMMARY that recorded it. Where the planned recipe and the executed recipe differ, the executed one is authoritative and the correction is recorded.

5. **Fill the Per-Task Verification Map.** Every row of the Requirement → Test Map must carry a Task ID (`02-NN-TT`), its plan, its wave, its requirement, its Threat Ref (`T-2-NN` or `—`), the secure behaviour, the test type, the exact automated command through the store launcher, File Exists, and a real Status. Mark manual rows explicitly rather than leaving them pending.

6. **Update the frontmatter:** `status: complete`, `nyquist_compliant: true`, `wave_0_complete: true`, `updated: <today>`. Record the final suite sizes against the Phase 1 baseline of unit 11 / db 31 / e2e 4.

7. **Close the five ROADMAP success criteria explicitly**, one short paragraph each, each citing the named test or the recorded manual verification that proves it:
   1. a named preset over clusters x geography with everything seeded on first run
   2. an estimate with a cost and a result count before saving or running, and a Texas multiplier
   3. an edit creates a version and a past run still points at the version that produced it
   4. a spend view of month-to-date versus the cap, by provider, fed by one ledger row per paid call
   5. a concurrent burst refused at 100% with a warning at 80% and no over-spend, plus the independent Google quota second wall
   🔴 For criterion 5, name BOTH halves: the 40-way burst test AND the BUDG-03 quota's status from plan 02-14 (set, or recorded as blocked on a Google Cloud project that does not exist).

8. **Run the five gate constituents one final time** through the store launcher, individually, and print the branch and sha AFTER the run — a parallel session can move the tree mid-gate and the gate then goes silently green on the wrong commit.

9. Open the PR. 🔴 **PR-only, no autonomous merge.** Merge only when `verify`, `db` and `e2e` are all green on the CURRENT head; if `main` moved during the gates, fetch, merge the remote INTO the branch, and re-run every gate on the combined tree.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:db</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/02-budget-governor-search-presets/02-VALIDATION.md` frontmatter contains `nyquist_compliant: true`, `wave_0_complete: true` and `status: complete`
    - Its Per-Task Verification Map contains NO row reading `_pending planner_` and no Status left as `⬜ pending`; every row carries a Task ID of the form `02-NN-TT`
    - The M7-M12 table is rewritten AS RUN, each row naming the tests that went red and the SUMMARY that recorded it; any divergence from the planned recipe is called out
    - Every mutation's direction was checked and recorded; any mutation reding two tests carries a proof of independence
    - The five success criteria each have a closing paragraph citing a named test or a recorded manual verification
    - All five gate constituents exit 0, each recorded separately, with the branch and sha printed AFTER the run and unchanged from before
    - `git diff --stat` is empty after every mutation revert, and each revert is verified from the catalog rather than from a script having run
    - The final unit / db / e2e suite sizes are recorded against the Phase 1 baseline of 11 / 31 / 4
    - A PR is open and NOT merged; the SUMMARY records that merge waits on danlo and on green CI on the current head
  </acceptance_criteria>
  <done>Every guard in this phase has been shown to discriminate by being watched red under its own mutation, the validation contract is closed with real statuses, and the five success criteria each cite the test that proves them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a push -> a running deployment | A build that succeeded is not a deployment that serves this commit |
| signed-out visitor -> the new routes | Six new routes, each of which must refuse an unauthenticated request |
| a green suite -> a working guard | A test that has never been red proves nothing |
| a parallel session -> the tree under a long gate | A gate can go green on a commit nobody meant to ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-01 | Elevation of Privilege | the six new routes in production | mitigate | Signed-out smokes of `/presets`, `/spend` and `/settings/budget` assert a redirect and the absence of `data-testid="org-id"`; the e2e suite exercises the authenticated paths against the DEPLOYED url with a real Clerk session. Task 1. | high |
| T-2-04 | Tampering | the race-free meter, in production shape | mitigate | Gate mutation M7 is re-run and recorded AS RUN: the naive body reds the concurrent burst while every single-worker test stays green, which is what proves the burst test is not redundant. Task 3. | high |
| T-2-02, T-2-05, T-2-07, T-2-12 | Elevation / Tampering | admin gate, settlement, cap ceiling, version immutability | mitigate | M10, M9, M8 and M12 each re-run against the live local database, each reding exactly the predicted named test, each reverted and verified from the catalog. Task 3. | high |
| T-2-08 | Repudiation | ledger price from the mask | mitigate | M11 appends `places.reviews` to the production field mask and must red TWO independent named tests — the tier and the ledger price. Task 3. | high |
| T-2-15 | Information Disclosure | credentials in the deployed surface | mitigate | The `/api/health` body is scanned for `postgres://`, `password`, `sk_` and the Supabase project ref, and `git grep` for credential-shaped strings is run over the repo. Task 1. | high |
| T-1-35 carried forward | Tampering | a gate green on the wrong commit | mitigate | The branch and sha are printed BEFORE and AFTER every long gate and compared, and the build log's commit is compared against the pre-deploy sha. Tasks 1 and 3. | medium |
</threat_model>

<verification>
- Five gate constituents green individually, with the branch and sha printed after and unchanged
- The deployment's build log names the pre-deploy sha; `/api/health` returns `ok`, `db:up`, `proxy:up` and the matching commit, and leaks nothing
- `pnpm test:e2e` green against the DEPLOYED url with every test NAME pasted
- 28 base screenshots plus four state shots, from the deployed app, both themes, both viewports, with danlo's verbatim reply
- M7 to M12 recorded AS RUN with their red test names, directions checked, reverts verified from the catalog, `git diff --stat` empty
- `02-VALIDATION.md` closed with no pending row and `nyquist_compliant: true`
</verification>

<success_criteria>
- A deployment of a known commit serves the phase's six new routes, org-scoped, with a healthy database and proxy
- Every e2e spec passes against the real URL, read by test name
- danlo has reviewed every screen in both themes at both viewports and approved or listed specifics
- All six gate mutations are recorded as run, each reding exactly the predicted named test, each direction-checked
- The five ROADMAP success criteria each cite a named green test or a recorded manual verification
- A PR is open and unmerged, waiting on danlo and on green CI
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-15-SUMMARY.md` recording: the deploy sha and URL side by side with the build log's commit, the `/api/health` body, the full `pnpm test:e2e` pass list by NAME, the screenshot inventory, danlo's verbatim design reply, the M7-M12 as-run table, the final suite sizes against the Phase 1 baseline, and the PR link with its CI status. Update `.planning/STATE.md` and `.planning/ROADMAP.md` to mark Phase 2 complete only after danlo approves the merge.
</output>
