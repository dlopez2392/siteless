---
phase: 02-budget-governor-search-presets
plan: 11
type: execute
wave: 5
depends_on: [02-09, 02-10]
files_modified:
  - src/app/(app)/presets/page.tsx
  - src/app/(app)/presets/new/page.tsx
  - src/app/(app)/presets/[id]/edit/page.tsx
  - src/components/preset-list/preset-card.tsx
  - src/components/preset-list/presets-empty.tsx
  - src/components/preset-editor/preset-form.tsx
  - src/components/preset-editor/cluster-picker.tsx
  - src/components/preset-editor/geography-picker.tsx
  - src/components/preset-editor/radius-geocoder.tsx
  - src/components/preset-editor/estimate-panel.tsx
  - src/components/preset-editor/assumptions-surface.tsx
  - src/components/preset-editor/use-live-estimate.ts
  - tests/unit/stale-estimate.test.tsx
  - tests/e2e/presets.spec.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-04]

must_haves:
  truths:
    - "SRCH-01: danlo saves a named preset defined as one or more of the four seeded clusters crossed with cities, a county, or a radius around a geocoded address"
    - "SRCH-02: the four clusters, the 17 RGV cities, the four RGV counties and the built-in Texas (254 counties) row are all pickable on first run, with no setup step"
    - "SRCH-04 / D-08: the estimate recomputes 400ms after the last change, with no Estimate button, and never blanks while it recomputes"
    - "D-04: a Texas-wide geography shows a computed multiplier chip, not a hard-coded x38"
    - "Pitfall 5: an out-of-order server-action answer never repaints over a fresher one - the monotonic sequence guard is proven by a named component test"
    - "The save path uses onSubmit plus useTransition, never a form action - React resets a form action even when the action FAILED and Radix Select drives state backwards on that reset"
  artifacts:
    - path: "src/components/preset-editor/use-live-estimate.ts"
      provides: "Debounced estimate hook with the monotonic sequence guard"
      contains: "seq"
    - path: "src/components/preset-editor/estimate-panel.tsx"
      provides: "The loudest thing on the editor - dollars, requests, businesses, percent of remaining, range"
      contains: "preset-editor-estimate"
    - path: "tests/unit/stale-estimate.test.tsx"
      provides: "Two recomputes resolved out of order; the newer value survives"
      contains: "stale estimate"
  key_links:
    - from: "src/components/preset-editor/use-live-estimate.ts"
      to: "src/server/actions/estimate-preset.ts"
      via: "a debounced server action call guarded by a monotonic sequence number"
      pattern: "estimatePreset"
    - from: "src/app/(app)/presets/[id]/edit/page.tsx"
      to: "src/server/actions/save-preset-version.ts"
      via: "onSubmit + useTransition carrying loadedVersion"
      pattern: "loadedVersion"
---

<objective>
Build the two screens SRCH-01 and SRCH-04 live on: the preset list with its empty state, and the preset editor whose focal point is a live estimate that is the loudest thing on the page and never blanks.

Purpose: the estimate IS the product. D-06's line — requests, dollars, expected businesses, and the share of this month's remaining budget — is what makes the cap legible before a cent is spent, and D-08 makes it recompute as danlo types.
Output: `/presets`, `/presets/new`, `/presets/[id]/edit`, the live-estimate hook with its sequence guard, and the first component test in this repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md
@src/lib/ui/copy.ts
@src/lib/estimate/assumptions.ts
@src/server/actions/estimate-preset.ts
@src/server/actions/save-preset-version.ts
@src/server/queries/presets.ts
@src/components/app-shell/budget-banner.tsx
@tests/unit/fixtures/preset.ts
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written in full below. `pnpm verify` is NOT runnable here.

If this plan runs in a git worktree: copy `C:/Users/danlo/prospector/.env.local` in, run the launcher with `install`, then `db:migrate` and `db:seed` — the pickers read the seeded reference rows.

Files are partitioned so plans 02-11, 02-12 and 02-13 can run in parallel: this plan owns `src/components/preset-list/` and `src/components/preset-editor/` and nothing else under `src/components/`.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: The preset list, its empty state, and its loading skeletons</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section "Screen Inventory" item 1 (Preset list) IN FULL — the focal point, the hierarchy, the desk and phone layouts, and the card's five-line anatomy
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section States subsections Loading and Empty, and the Copy Table rows for the preset list
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` Executor Rules 6, 9, 10, 11
    - `src/server/queries/presets.ts` (plan 02-09) — `listPresets` and its return shape
    - `src/lib/ui/copy.ts` — `PRESETS_EMPTY_HEADING`, `PRESETS_EMPTY_BODY`, `PRESETS_EMPTY_CTA`
    - `src/app/(app)/layout.tsx` (plan 02-10) — the shell this page renders inside, and the phone sticky-action-bar slot
  </read_first>
  <files>src/app/(app)/presets/page.tsx, src/components/preset-list/preset-card.tsx, src/components/preset-list/presets-empty.tsx</files>
  <action>
A server component. `requireOrg()` is already enforced by the group layout; read through `listPresets(claims)`.

**Hierarchy:** page title `Search presets` at Heading 20/600, then the count line at Label 14/400 muted using the Copy Table's exact forms — `{n} presets · {m} run this month` and the singular `1 preset · none run this month` — then the card list, then the footer note.

**Desk:** a page header row with the title left and a **"Create preset"** primary button right (`data-testid="presets-create-cta"`), then a 2-column card grid with a 24px gap and 24px card padding.
**Phone:** page header with the title only; cards stacked at a 16px gap with 16px padding; **"Create preset" lives in the sticky bottom action bar** above the tab bar — that is the thumb zone (MOB-01), a full-width 48px-tall primary button.

**`preset-card.tsx`** — the whole card is the link to `/presets/[id]`, and there are NO icon-only actions on it. Anatomy, top to bottom:
```
Hidalgo — home services                            [Built-in]   <- Heading 20/600 + Badge
Home services & trades · Auto & retail                          <- Label 14/400 muted (clusters)
County · Hidalgo                                                <- Label 14/400 muted (geography)
--------------------------------------------------  Separator
Version 3      Last run Sep 18, 2:14 PM      ~$2.40             <- Label 14/600 / Label 14/400 muted / Body 16/600 tabular
```
Money and version numbers carry `tabular-nums`. `Last run {date}` renders through `formatLocal` from `src/lib/time.ts` — zone AND locale pinned — and reads `Never run` when there is none. The cost reads `Est. {cost}` per the Copy Table.

**`presets-empty.tsx`** — the shadcn `Empty` primitive: a 24px lucide icon in a 48px muted circle, Heading 20/600, Body 16/400 muted capped at 60ch, and the action. Copy verbatim from `src/lib/ui/copy.ts`; heading `No search presets yet`; the CTA is `Create your first preset` (`data-testid="presets-empty-cta"`). 🔴 No screen in this product ever shows "No data".

**Loading:** three `Skeleton` cards matching the real card's height and internal rhythm — a 20px title bar, two 14px meta lines, a separator, a footer row. 🔴 Never a centred spinner on a full page.

🔴 Executor Rule 6: every surface here is the shared shadcn primitive. Grep your own output for `bg-card`, `rounded-` and `border border-` on raw `div`s before calling this done.
🔴 Executor Rule 10: exactly ONE filled accent button on this screen.
Every interactive element gets a `data-testid`: `presets-create-cta`, `presets-empty-cta`, `preset-card-{id}`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/presets/page.tsx` contains `Search presets` and `data-testid="presets-create-cta"`
    - `src/components/preset-list/presets-empty.tsx` contains the exact string `No search presets yet` and `data-testid="presets-empty-cta"`
    - `src/components/preset-list/preset-card.tsx` contains `tabular-nums`, `Never run`, and `formatLocal`
    - `grep -rc "Intl.DateTimeFormat\|Intl.NumberFormat" src/components/preset-list/ | grep -v ':0' | wc -l` returns 0 — formatting goes through `src/lib/time.ts` and `src/lib/budget/money.ts`
    - `grep -rnE '<div[^>]*className="[^"]*(bg-card|rounded-(lg|xl|md)|border border-)' src/components/preset-list/ src/app/\(app\)/presets/page.tsx` returns nothing
    - `grep -rc 'variant="default"' src/app/\(app\)/presets/page.tsx src/components/preset-list/` shows at most one filled accent button per rendered screen (Rule 10)
    - `pnpm build`, `pnpm typecheck` and `pnpm lint` each exit 0
    - Screenshots of the BUILT app at 390x844 and 1280x800 in both themes, in BOTH the empty and the populated state, recorded in the SUMMARY
  </acceptance_criteria>
  <done>The preset list reads as a list of costed searches, its empty state teaches what a preset is, and its primary action sits in the thumb zone on a phone.</done>
</task>

<task type="auto">
  <name>Task 2: The preset editor form - name, clusters, and all three geography modes</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section "Screen Inventory" item 2 (Preset editor) IN FULL — the focal point, the desk two-column and phone sticky-bar layouts, and the four form sections in order
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section States subsections Empty and Error (every inline validation string and both Census errors), and the full Copy Table rows for the preset editor
    - `.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md` D-01, D-03, D-04, D-15
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Frontend on This Repo" subsection "The live debounced estimate" — the recorded BIS defect that forbids `<form action>`
    - `src/server/actions/geocode-address.ts` and `save-preset-version.ts` (plan 02-09) — the typed results to branch on
    - `src/lib/ui/copy.ts` — `GEOCODE_NO_MATCH`, `GEOCODE_UNREACHABLE`, `VERSION_NOTICE`
  </read_first>
  <files>src/app/(app)/presets/new/page.tsx, src/app/(app)/presets/[id]/edit/page.tsx, src/components/preset-editor/preset-form.tsx, src/components/preset-editor/cluster-picker.tsx, src/components/preset-editor/geography-picker.tsx, src/components/preset-editor/radius-geocoder.tsx</files>
  <action>
The two pages are thin server components that read the seed tables and (for edit) the current version, then render `preset-form.tsx`, which is `'use client'`.

**Layout.** Desk at 1024px and up: two columns — the form flexible up to 720px, and a **sticky 320px estimate rail** on the right, top-aligned 32px from the top of the content area. Phone: a single-column form with the estimate as a **sticky bar** pinned directly above the sticky action bar, on the `--card` surface with a 1px top border and 16px padding; tapping it opens the assumptions drawer.

**Form, in this order:**
1. **Name** — `Field` + `Input`. Label 14/600 `Preset name`, placeholder `Hidalgo — home services`, help text `You'll see this on the run list and in spend history.` `data-testid="preset-editor-name"`.
2. **Industry clusters** — Label 14/600 `Industry clusters`, help `Pick one or more. Clusters are atomic — the Places types and NAICS ranges inside them are seeded.` Four `Checkbox` rows inside a bordered group, each at least 44px tall: the cluster name at Body 16/400 plus its outlet count at Label 14/400 muted with `tabular-nums`, e.g. `Home services & trades` / `9,412 RGV outlets`. 🔴 Clusters are ATOMIC (D-03) — there is NO per-type toggle inside a cluster in v1. `data-testid="preset-editor-cluster-{key}"`.
3. **Geography** — Label 14/600 `Geography`, then a full-width `ToggleGroup` (single-select, three items, 44px tall): **Cities · County · Radius** (`data-testid="preset-editor-mode-cities|county|radius"`). The panel below swaps:
   - **Cities** — a `Command` search over the seeded 17 with the placeholder `Search the 17 RGV cities` (`data-testid="preset-editor-city-search"`), results in a `ScrollArea` capped at 320px of 44px checkbox rows (city + outlet count, tabular). Selected cities render ABOVE as removable `Badge` pills with an `x` at a 44x44 hit area and `aria-label="Remove McAllen"`. Empty search state: heading `No RGV city matches "{query}"`, body naming the 17, action `Clear search`.
   - **County** — 44px `Checkbox` rows for Cameron · Hidalgo · Starr · Willacy with their outlet counts; then a `Separator`; then ONE pinned built-in row **`Texas (254 counties)`** with a `Built-in` badge and the multiplier chip in the WARNING tone. 🔴 The chip reads `×{computed}` from `texasMultiplier(ctx)` — UI-SPEC's `×38 vs RGV` is illustrative copy and the seeded cell lists give ≈14.9 (Executor Rule 16). Never hard-code it. `data-testid="preset-editor-texas-multiplier"`.
   - **Radius** — `radius-geocoder.tsx`: an `Input` (`Street address, city, ZIP`), a **"Find this address"** button that enters a busy state showing a `Spinner` with the label `Finding…` WITHOUT resizing or dropping below 44px, then the resolved match in an `Item` (matched address, county, lat/lng at Label 14/400 tabular) with a `Use a different address` text link; plus a radius `Select` of 5 · 10 · 25 · 50 miles. Help text: `Geocoded free by the US Census Geocoder. No Google call, no cost.`
     🔴 Render the `matchedAddress` the SERVICE returned, not what the user typed — a bad ZIP is silently corrected upstream (`99999` comes back as `78501`), and showing the typed string would hide that.
     Branch the typed result: `code:'validation'` renders `GEOCODE_NO_MATCH(address)` with the two actions `Try another address` and `Switch to county` (the second actually switches the ToggleGroup — UI-SPEC's escape hatch for the colonia case is a real control, not a sentence); `code:'upstream'` renders `GEOCODE_UNREACHABLE` with `Retry geocoding` and `Switch to cities`.
4. **Assumptions link** — a `How this is estimated` accent text link opening the surface built in Task 3.

**Save.** 🔴 **Never `<form action={...}>`.** Recorded BIS defect: React resets a `<form action>` even when the action FAILED, and Radix `Select` drives state BACKWARDS on that reset. Use `onSubmit` plus `useTransition` plus a `useFormSubmit`-shaped helper. The primary button reads `Create preset` on `/presets/new` and `Save as version {n}` on the edit screen; while saving it shows a `Spinner` and `Saving version 4…` and the form goes `aria-disabled`, NOT removed. `data-testid="preset-editor-save"`.
**Versioning copy (D-15), mandatory:** directly above the save button on the EDIT screen, at Label 14/400 muted: `Saving creates version {n}. Runs that already finished keep pointing at version {n-1}.` from `VERSION_NOTICE(n)`.
Branch `savePresetVersion`'s typed result: `code:'conflict'` renders UI-SPEC's save-conflict copy with `Reload version {n}` and `Copy my changes first`; `code:'validation'` renders the matching inline field error with `aria-invalid` and `aria-describedby` and a `circle-alert` icon — error text is never colour-only. The dismiss action reads `Close without saving`, never `Cancel`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rc "form action=\|<form action" src/components/preset-editor/ src/app/\(app\)/presets/ | grep -v ':0' | wc -l` returns 0
    - `src/components/preset-editor/preset-form.tsx` contains `useTransition` and `onSubmit`
    - `src/components/preset-editor/geography-picker.tsx` contains `Texas (254 counties)`, `preset-editor-mode-cities`, `preset-editor-mode-county`, `preset-editor-mode-radius`, and `preset-editor-texas-multiplier`
    - `grep -rn 'x38\|×38\|"38"' src/components/preset-editor/` returns nothing — the multiplier is computed
    - `src/components/preset-editor/radius-geocoder.tsx` contains `matchedAddress`, `Finding…`, `Switch to county`, and both geocode copy constants
    - `src/components/preset-editor/preset-form.tsx` contains `aria-invalid`, `aria-describedby`, `circle-alert` and `Close without saving`
    - `grep -rc 'Cancel\|"Save"\|"Submit"\|"OK"' src/components/preset-editor/ | grep -v ':0' | wc -l` returns 0 — no generic labels
    - `grep -rnE '<div[^>]*className="[^"]*(bg-card|rounded-(lg|xl|md)|border border-)' src/components/preset-editor/` returns nothing
    - `pnpm build`, `pnpm typecheck` and `pnpm lint` each exit 0, and `pnpm lint` reports no `react-hooks/*` violations
  </acceptance_criteria>
  <done>All three geographies and all four clusters are expressible from one form, the Texas row shows a computed multiplier, the geocoder's every failure ends with a way out, and the save path cannot be reset out from under a Radix Select.</done>
</task>

<task type="auto">
  <name>Task 3: The live estimate - debounce, the sequence guard, the assumptions surface, and the first component test</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Frontend on This Repo" subsection "The live debounced estimate (D-08)" — the full `useTransition` + monotonic `seq` block, with citations, and why there is no `AbortController`
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Pitfall 5
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section "Screen Inventory" item 2 subsection "The live estimate" — the exact five-line block, the debounce, the recomputing treatment, and the Assumptions surface contents; plus States subsection Loading and Executor Rules 11 and 16
    - `.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md` D-06, D-07, D-08 and section Specific Ideas (the estimate line as danlo wants to read it)
    - `src/lib/estimate/assumptions.ts` (plan 02-07) — the constants surfaced verbatim in the drawer
    - `tests/unit/jsdom-lane.test.tsx` (plan 02-01) — the working shape of a `.test.tsx` in this repo
  </read_first>
  <files>src/components/preset-editor/use-live-estimate.ts, src/components/preset-editor/estimate-panel.tsx, src/components/preset-editor/assumptions-surface.tsx, tests/unit/stale-estimate.test.tsx, tests/e2e/presets.spec.ts</files>
  <action>
**`use-live-estimate.ts`** — `'use client'`. The shape RESEARCH worked out, with citations, copied rather than re-derived:
```
const seq = useRef(0);
const [pending, startTransition] = useTransition();
const [estimate, setEstimate] = useState<EstimateRange | null>(null);
const recompute = useDebouncedCallback((spec: PresetSpec) => {
  const mine = ++seq.current;
  startTransition(async () => {
    const next = await estimatePreset(spec);          // "use server"
    if (mine === seq.current) setEstimate(next);      // drop a stale, out-of-order answer
  });
}, 400);
```
🔴 **The monotonic `seq` guard is not optional.** Next.js Server Actions execute SEQUENTIALLY — they are queued, not parallel — and have no `AbortController`, so a queued earlier answer can land after a later one and repaint a stale dollar figure. That is the one bug class the queue behaviour creates.
🔴 Server Actions must be asynchronous while transitions must be synchronous; keep the `async` inside `startTransition`, not around it.
Debounce 400ms after the last change to clusters OR geography (D-08, UI-SPEC OQ 9). There is no Estimate button.
Write `useDebouncedCallback` inline (a `useRef` timer plus `useEffect` cleanup) rather than adding a dependency; it is eight lines and the repo pins every version deliberately.

**`estimate-panel.tsx`** — the FOCAL POINT of the editor, and the loudest thing on the screen. The five-line block, exactly:
```
~$2.40                                          <- Display 28/600, accent, tabular-nums
~68 requests · ~1,100 businesses                <- Label 14/400 muted, tabular
4.8% of this month's remaining $47.60           <- Label 14/400 muted, tabular
Range $1.90–$2.90 · 54–81 requests              <- Label 14/400 muted, tabular
How this is estimated →                         <- accent text link
```
🔴 Executor Rule 16: those figures show the FORMAT, not arithmetic. Every number comes from the `EstimateRange` the action returned, the dollar value already has the monthly free allowance applied (so an early-month preset genuinely reads `$0.00`), and the Texas chip shows `×{computed}`.
Money through `formatUsd`, percentages through `formatPct` (one decimal), and everything comparable carries `tabular-nums`.
🔴 **While recomputing: the previous values STAY on screen at 60% opacity with an inline 16px `Spinner` beside the dollar figure and `aria-busy="true"` on the region. Never a skeleton, never a blank.** A number that blanks while you type is the single worst thing this screen could do (Rule 11).
The region is `aria-live="polite"`. Motion: 150ms opacity 0.6 to 1 with a 2px upward translate on the dollar figure when the value changes; the old value is never removed before the new one paints; under `prefers-reduced-motion` the duration goes to 0 and the transform is dropped but the NUMBER still renders.
Testids: `preset-editor-estimate` on the region, `preset-editor-estimate-dollars` on the figure.
When no cluster is selected, render the inline line `Pick at least one cluster to see an estimate.` rather than a zero.

**`assumptions-surface.tsx`** — a `Drawer` on phone, a `Popover` on desk. Title `How this estimate is built`. A definition list, all values tabular, reading the real constants from `src/lib/estimate/assumptions.ts`:
`Pages per query leg` 1–3 (low–high) · `Query fan-out multiplier` 3.0 — *unmeasured; trued up with real invoice data at the Phase 6 gate* · `Radius reference footprint` 25 miles · `Outlet counts` TX Comptroller active sales-tax permits, measured · `Price` $35.00 per 1,000 Text Search Enterprise requests, first 1,000 free each month. Plus `ASSUMPTIONS_CAVEAT` verbatim. Closing action: `Back to the preset`.
🔴 Read the constants, do not restate them — D-07 makes them a VISIBLE, EDITABLE, committed assumption, and a second copy in JSX is a second source of truth.

**`tests/unit/stale-estimate.test.tsx`** — the first component test in this repo. Named tests, exactly:
  - `stale estimate: an out-of-order answer never repaints over a fresher one` — mock `estimatePreset` so call 1 resolves AFTER call 2; fire two recomputes; assert the rendered dollar figure is call 2's value. Then assert the inverse control in the same file: in-order resolution DOES paint call 2's value, so the guard is not simply ignoring every answer.
  - `stale estimate: the previous value stays visible while recomputing` — assert the old figure is still in the document with `aria-busy="true"` on the region, and that NO skeleton element is present.
  - `stale estimate: no cluster selected renders the prompt, not a zero` — the exact string `Pick at least one cluster to see an estimate.` is present and `$0.00` is not.

**`tests/e2e/presets.spec.ts`** — `data-testid` only, never copy:
  - `create preset: cities` — visit `/presets`, click `presets-create-cta`, fill the name, check two clusters, choose Cities, pick two cities, wait for `preset-editor-estimate` to settle, save, and assert the new preset appears on `/presets`.
  - `create preset: county` and `create preset: radius` — the same flow through the other two modes; the radius flow types a real RGV street address and waits for the geocode result.
  - `spend` and `budget banner` specs are NOT written here — plans 02-13 and 02-10 own them.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit -t "stale estimate"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:unit -t "stale estimate"` exits 0 and the output NAMES all three `stale estimate ...` tests — read the NAMES, a filter that matches nothing exits 0 green
    - Watched failing first, recorded verbatim: delete the `if (mine === seq.current)` guard from `use-live-estimate.ts`; `stale estimate: an out-of-order answer never repaints over a fresher one` goes red while the in-order control stays green; restore; `git diff --stat` empty
    - `src/components/preset-editor/use-live-estimate.ts` contains `seq`, `useRef`, `useTransition`, `400`
    - `src/components/preset-editor/estimate-panel.tsx` contains `aria-busy`, `aria-live="polite"`, `tabular-nums`, `preset-editor-estimate-dollars`, and `opacity` at 60%; `grep -c "Skeleton" src/components/preset-editor/estimate-panel.tsx` returns 0
    - `src/components/preset-editor/assumptions-surface.tsx` imports from `@/lib/estimate/assumptions` and contains no literal `3.0` or `25` of its own (`grep -cE '\b3\.0\b|\b25\b' src/components/preset-editor/assumptions-surface.tsx` returns 0)
    - `tests/e2e/presets.spec.ts` contains `preset-editor-mode-cities`, `preset-editor-mode-county`, `preset-editor-mode-radius` and `preset-editor-estimate`, and contains no literal copy sentence
    - `pnpm build`, `pnpm typecheck`, `pnpm lint` and `pnpm test:unit` each exit 0
    - Screenshots of the BUILT app: the editor at 390x844 and 1280x800 in both themes, in the settled state AND the recomputing state, recorded in the SUMMARY
  </acceptance_criteria>
  <done>The estimate recomputes 400ms after the last keystroke, never blanks, never repaints a stale answer over a fresh one, and shows its own assumptions one tap away — with the guard proven by a test that was watched red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form state -> server action | Every cluster id, city id, county fips, radius and address arrives from a client the user controls |
| user-typed address -> the Census Geocoder | The only user input in this phase that shapes a network call |
| queued server-action responses -> the rendered dollar figure | Server Actions are sequential and uncancellable; an older answer can arrive last |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-14 | Denial of Service | estimate action as a compute oracle | mitigate | 400 ms debounce, one logical in-flight request per editor via the monotonic sequence guard, and the action itself authenticates first and makes no paid call — the blast radius is CPU, not dollars. Task 3. | low |
| T-2-13 | Tampering | geocoder input | mitigate | The address leaves the component only as an argument to an authenticated server action, which zod-bounds it before `src/lib/geocode/census.ts` bounds it again against a hard-coded host. The component renders the SERVICE's `matchedAddress`, never echoing the raw input into a link or a header. Task 2. | high |
| T-2-01 | Elevation of Privilege | editor routes | mitigate | Both pages sit under `(app)`, whose layout calls `requireOrg()` first; every action they call re-checks independently, so a directly POSTed action is still refused. Tasks 1-3. | high |
| Pitfall 5 (no STRIDE ref) | Tampering | stale estimate repainting | mitigate | The monotonic `seq` guard, proven by a named component test whose in-order control stays green, and watched red with the guard deleted. Task 3. | medium |
| Recorded BIS defect (no STRIDE ref) | Tampering | form reset on a failed action | mitigate | No `<form action>` anywhere in the editor; `onSubmit` plus `useTransition`, with a grep criterion. React resets a form action even when the action FAILED and Radix Select drives state backwards on that reset. Task 2. | medium |
</threat_model>

<verification>
- `pnpm build`, `pnpm typecheck`, `pnpm lint` and `pnpm test:unit` each exit 0 through the store launcher
- `stale estimate` watched red with the sequence guard deleted, reverted with `git diff --stat` empty
- Screenshots of the BUILT app for both screens, both themes, both viewports, including the estimate's recomputing state
- No `<form action>`, no hand-rolled surface, no hard-coded Texas multiplier, no generic button label
</verification>

<success_criteria>
- A preset is definable as one or more clusters crossed with cities, a county, or a geocoded radius, and saves as a named version
- The built-in clusters, cities, counties and the Texas row are pickable on first run
- The estimate is live, debounced, never blank, and shows dollars, requests, businesses, percent of remaining and a range
- The Texas chip shows a computed multiplier
- The first component test in this repo passes and was watched red
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-11-SUMMARY.md` recording: the `pnpm test:unit` pass list by NAME, the watched-red-first output for the sequence guard, the screenshot inventory (screen x state x theme x viewport), and the real estimate figures the editor rendered for the RGV baseline so a reader can compare them against `tests/unit/estimate.test.ts`.
</output>
