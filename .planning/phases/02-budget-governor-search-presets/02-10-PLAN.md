---
phase: 02-budget-governor-search-presets
plan: 10
type: execute
wave: 4
depends_on: [02-01, 02-07, 02-09]
files_modified:
  - src/app/(app)/layout.tsx
  - src/app/(app)/settings/organization/page.tsx
  - src/app/page.tsx
  - src/app/no-access/page.tsx
  - src/components/app-shell/app-sidebar.tsx
  - src/components/app-shell/mobile-tab-bar.tsx
  - src/components/app-shell/user-menu.tsx
  - src/components/app-shell/theme-switch.tsx
  - src/components/app-shell/budget-banner.tsx
  - tests/e2e/signed-in.spec.ts
  - tests/e2e/budget-banner.spec.ts
  - tests/e2e/theme-tokens.spec.ts
  - tests/e2e/touch-targets.spec.ts
autonomous: true
requirements: [BUDG-02, BUDG-04]

must_haves:
  truths:
    - "Every screen in Phases 2 to 9 renders inside one shell: a 256px sidebar on desk, a 64px top bar plus a 64px bottom tab bar on phone, with exactly one active accent item"
    - "D-12: at 80% a persistent warning banner renders on EVERY route, is not dismissible and is not a toast; at 100% it is destructive and says nothing more will be charged"
    - "UI-SPEC Rule 7: signed-in-as, org-id and org-row-id move to /settings/organization with those exact names, and tests/e2e/signed-in.spec.ts moves in the SAME task - a retired testid fails silently"
    - "UI-SPEC Rule 2: the computed accent is rgb(15, 118, 110) in light and rgb(45, 212, 191) in dark, probed on the BUILT app"
    - "MOB-01 / Rule 9: every primary control is at least 44x44 CSS px at 390x844"
    - "requireOrg() guards the group layout, so no page under (app) can render without an active organization"
  artifacts:
    - path: "src/app/(app)/layout.tsx"
      provides: "The shell born in this phase - nav, banner, skip link, requireOrg gate"
      contains: "requireOrg"
    - path: "src/components/app-shell/budget-banner.tsx"
      provides: "The persistent 80% / 100% banner, every route, not dismissible"
      contains: "budget-banner-80"
    - path: "tests/e2e/theme-tokens.spec.ts"
      provides: "Computed-style probe proving the painted tokens actually shipped"
      contains: "rgb(15, 118, 110)"
  key_links:
    - from: "src/app/(app)/layout.tsx"
      to: "src/server/queries/budget.ts"
      via: "getCurrentPeriod read once per request to decide the banner"
      pattern: "getCurrentPeriod"
    - from: "src/app/page.tsx"
      to: "/presets"
      via: "redirect"
      pattern: "redirect"
---

<objective>
Build the shell every screen in this project will live in, the persistent budget banner D-12 requires, and the organization settings page Phase 1's e2e hooks move to — then write the four e2e specs that prove the design system actually shipped.

Purpose: UI-SPEC frames this as the phase where the design system is born, and the shell is where it becomes visible. The banner is the only place in the product where the meter reaches danlo before he asks.
Output: `(app)` route group, nav for three breakpoints, the threshold banner, `/settings/organization`, and four e2e specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md
@src/app/page.tsx
@src/app/layout.tsx
@src/app/no-access/page.tsx
@src/lib/auth/require-org.ts
@src/lib/ui/copy.ts
@src/server/queries/budget.ts
@tests/e2e/signed-in.spec.ts
@playwright.config.ts
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written in full below. `pnpm verify` is NOT runnable here.

If this plan runs in a git worktree: copy `C:/Users/danlo/prospector/.env.local` in, run the launcher with `install`, then `db:migrate` and `db:seed`.

🔴 `pnpm test:e2e` runs against `E2E_BASE_URL`, the DEPLOYED url, never localhost. The specs written here are RUN at the deploy gate in plan 02-15; this plan writes them and proves them against a locally BUILT server only where the acceptance criteria say so.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: The (app) route group shell - sidebar, top bar, bottom tab bar, user menu, skip link</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section "Screen Inventory & Layout Patterns" item 0 (App shell) IN FULL — the focal point, the three breakpoints, the exact pixel values, the nav icons, the routes list
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` sections Spacing Scale, Typography, Color (the accent reserved-for list), Accessibility & Touch, and Executor Rules 5, 6, 9, 10
    - `src/app/page.tsx` — `requireOrg()` first, `orgClaims()`, `ensureOrgRow()`, and the three `data-testid` nodes being moved
    - `src/lib/ui/copy.ts` (plan 02-07) — `NAV`, `SKIP_LINK`
    - `src/app/layout.tsx` as plan 02-01 left it — `ClerkProvider` > `ThemeProvider` > `{children}` + `<Toaster />`
  </read_first>
  <files>src/app/(app)/layout.tsx, src/components/app-shell/app-sidebar.tsx, src/components/app-shell/mobile-tab-bar.tsx, src/components/app-shell/user-menu.tsx, src/components/app-shell/theme-switch.tsx, src/app/page.tsx</files>
  <action>
**`src/app/(app)/layout.tsx`** — a server component. 🔴 `const { userId, orgId, orgSlug } = await requireOrg();` then `const claims = await orgClaims();` then `await ensureOrgRow(claims, orgSlug ?? orgId)` — the D-03 just-in-time provisioning moves here from `src/app/page.tsx` so every route under the group is guarded and provisioned, not just one. Then `getCurrentPeriod(claims, 'places')` once per request for the banner (Task 2).

Structure:
  - a skip link as the first focusable element: `<a href="#main" data-testid="skip-link">` with the copy `Skip to main content`
  - the banner slot (Task 2), ABOVE the content area, rendered on every route
  - `<main id="main">` wrapping `{children}`

**Desk, 1024px and up** — `src/components/app-shell/app-sidebar.tsx` built on the shadcn `sidebar` primitive, 256px fixed, `--sidebar` surface, 1px right border.
  - top: org name at Heading 20/600 truncated to one line with a `lucide` `building-2` icon; below it the user's email at Label 14/400 muted
  - nav rows: **Presets** (`list-checks`, `/presets`) · **Spend** (`circle-dollar-sign`, `/spend`) · **Settings** (`settings`, `/settings/budget`). Each row 44px tall, 8px radius, 16px horizontal padding, a 20px icon, an 8px gap, Body 16/400 label. The ACTIVE row gets accent label, accent icon, a 2px accent bar on the left edge, `--muted` background.
  - footer: the user-menu trigger (`Avatar` with initials + name + `chevron-up`) opening a `DropdownMenu` with **Theme** (Light · Dark · System), **Organization details** (`/settings/organization`) and **Sign out**
  - content: `max-width: 1120px`, 32px gutters, 32px top padding

**Tablet, 640 to 1023px** — the sidebar becomes an off-canvas `Sheet` from the left, triggered by a 44x44 `menu` button in a sticky 64px top bar. Content gutters 24px.

**Phone, under 640px** — NO sidebar.
  - a sticky 64px top app bar on the `--sidebar` surface with a 1px bottom border: org name (Heading 20/600, truncated) left, the 44x44 user-menu avatar button right
  - `src/components/app-shell/mobile-tab-bar.tsx`: a sticky bottom tab bar, `64px + env(safe-area-inset-bottom)`, `--sidebar` surface, 1px top border, three equal tabs. Each tab is a 44px-minimum column with a 24px icon OVER a Label 14/400 text label. 🔴 **Never icon-only** — the visible label is the fallback the checker requires. The active tab gets an accent icon, an accent label and a 2px accent bar along the tab's TOP edge.
  - content gutters 16px, top padding 16px, bottom padding 16px PLUS the tab-bar height so nothing hides behind it

**`src/components/app-shell/theme-switch.tsx`** — `'use client'`, three-way Light · Dark · System via `useTheme()` from `next-themes`, `data-testid="theme-switch"`.

**`src/app/page.tsx`** — replace its body with `redirect('/presets')`. Its three `data-testid` nodes move to `/settings/organization` in Task 3; do not leave copies behind.

🔴 **Executor Rule 6 — no hand-rolled surfaces.** Every card, sheet, drawer, popover, input and badge is the shared shadcn primitive. A treatment applied only to the shared `Card` while 25 hand-rolled `bg-card` divs sit beside it reaches almost nothing, and every review passes because the tests pin the `Card`. Before calling this task done, grep for `bg-card`, `rounded-` and `border border-` on raw `div` elements in the files you wrote and convert each to the primitive.
🔴 **Executor Rule 10 — one accent action per screen**, and the only accent in the chrome is the single active nav item plus the focus ring.
🔴 **Executor Rule 5** — `src/lib/ui/copy.ts` and `run-tone.ts` carry no `"use client"` and must stay that way; import them from server components freely, and never re-export a data object THROUGH a client module.
🔴 **Rule 1** — Tailwind v4 arbitrary values are `[var(--x)]`, never `[--x]`.
Every interactive element gets a `data-testid` following `{screen}-{element}-{qualifier}`: at minimum `nav-presets`, `nav-spend`, `nav-settings`, `theme-switch`, `skip-link`, `user-menu`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/layout.tsx` contains `requireOrg`, `orgClaims`, `ensureOrgRow`, `getCurrentPeriod`, `id="main"` and `data-testid="skip-link"`
    - `src/app/page.tsx` contains `redirect('/presets')` and NO `data-testid` at all
    - `src/components/app-shell/app-sidebar.tsx` contains `list-checks`, `circle-dollar-sign`, `settings`, `data-testid="nav-presets"`, `data-testid="nav-spend"`, `data-testid="nav-settings"`
    - `src/components/app-shell/mobile-tab-bar.tsx` contains `env(safe-area-inset-bottom)` and renders a visible text label beside every icon (`grep -c "sr-only" src/components/app-shell/mobile-tab-bar.tsx` returns 0 for the tab labels)
    - `grep -rnE '<div[^>]*className="[^"]*(bg-card|rounded-(lg|xl|md)|border border-)' src/components/app-shell/ src/app/\(app\)/` returns nothing (Executor Rule 6)
    - `grep -rnE '\[--[a-z]' src/components/app-shell/ src/app/\(app\)/` returns nothing (Executor Rule 1)
    - `grep -rc "use client" src/lib/ui/ | grep -v ':0' | wc -l` still returns 0
    - `pnpm build` exits 0 and its route listing shows `/presets`, `/spend`, `/settings/organization` and the Proxy (Middleware) entry
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Every route under `(app)` renders inside one shell that is guarded by `requireOrg`, carries exactly one accent nav item, and is navigable by thumb on a phone and by pointer on a desk.</done>
</task>

<task type="auto">
  <name>Task 2: The persistent budget banner - 80% warning, 100% refusal, every route, not dismissible</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section States subsection Threshold IN FULL — the three rows, their exact copy, their icons, their actions, and the phone collapse behaviour
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section Color subsections "Destructive / warning reserved for" and the note that colour is never the only signal
    - `.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md` D-12 and section Specific Ideas ("The 80% warning is a persistent banner, not a toast")
    - `src/lib/ui/copy.ts` (plan 02-07) — `BUDGET_80_BANNER`, `BUDGET_100_BANNER`
    - `src/lib/budget/money.ts` and `src/lib/budget/period.ts` (plan 02-04) — `formatUsd`, `periodResetInstant`, and `formatLocal` from `src/lib/time.ts` for the reset date
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section Motion — 200ms height and opacity on FIRST MOUNT ONLY, and the reduced-motion rule
  </read_first>
  <files>src/components/app-shell/budget-banner.tsx, src/app/(app)/layout.tsx</files>
  <action>
A server component taking the `getCurrentPeriod` result the layout already read — one query per request, not one per banner.

Three states, and the boundary is `(spent + reserved) * 100 >= cap * 80` computed on `bigint`, matching the SQL exactly. 🔴 Never `cap * 80 / 100` in TypeScript either; keep the two sides of the product on the same side of the comparison so the two tiers cannot drift.

  - **under 80%**: render NOTHING. No empty banner slot, no placeholder.
  - **at least 80%, under 100%**: a warning-surface `Alert` with the `triangle-alert` lucide icon, `role="status"`, `data-testid="budget-banner-80"`. First line Body 16/600, detail Label 14/400. Copy, from `BUDGET_80_BANNER(usedUsd, capUsd)`: `You've used $40.12 of your $50.00 cap — 80%. At 100% Siteless refuses new runs. Nothing is blocked yet.` Actions: **"See spend by provider"** linking to `/spend`, and **"Raise the monthly cap"** linking to `/settings/budget` — the second only when `auth().orgRole === 'org:admin'`. On phone it collapses to one line plus a chevron that expands it (shadcn `Collapsible`).
  - **at 100%**: the same slot in the destructive tone with the `octagon-x` icon, `role="alert"`, `data-testid="budget-banner-100"`. Copy from `BUDGET_100_BANNER(capUsd, resetDate)`: `Your $50.00 monthly cap is spent. Siteless is refusing new runs, so nothing more will be charged. Raise the cap in Budget settings, or wait for the reset on Oct 1 at 12:00 AM America/Chicago.` Actions: **"Raise the monthly cap"** for an admin, **"Ask an admin to raise the cap"** linking to `/settings/organization` for a member.

🔴 **Not dismissible, and not a toast** (D-12). There is no close button and no `sonner` call on this path. Toasts are reserved for reversible successes only.
🔴 **Colour is never the only signal** — the icon and the text label ship with the tone, and the percentage is spelled out.
🔴 The reset date renders through `formatLocal` from `src/lib/time.ts` with the zone AND locale pinned, and the copy keeps saying `12:00 AM America/Chicago` verbatim. There are THREE timezones in this product that are all called "monthly" — the app's Chicago budget period, Google's US/Pacific daily quota reset, and Google's billing month — and no copy may imply they coincide.
🔴 Motion: 200ms height + opacity on FIRST MOUNT ONLY; it must not re-animate on navigation. Under `prefers-reduced-motion` the duration goes to 0 and the transform is dropped, but the banner STILL RENDERS — the recorded 956 Woodworks defect is a naive reduced-motion rule that blanked the content instead of the animation.

Mount it in `src/app/(app)/layout.tsx` above `<main>`, so it is present on `/presets`, `/presets/new`, `/presets/[id]`, `/spend`, `/settings/budget` and `/settings/organization` alike.

Then write **`tests/e2e/budget-banner.spec.ts`**, asserting on `data-testid` only and never on copy:
  - `budget banner: absent under 80 percent` — on a fresh org with no spend, `budget-banner-80` and `budget-banner-100` both have count 0 on `/presets`.
  - `budget banner: renders on every route at 80 percent` — visit `/presets`, `/spend`, `/settings/budget`, `/settings/organization` and assert `budget-banner-80` is visible on each. Drive the state by calling the seeded admin's budget cap down through the UI (`/settings/budget`) rather than by writing SQL from a browser test, then restore it in a `finally`.
  - `budget banner: is not dismissible` — no button inside the banner has an accessible name matching `/dismiss|close/i`.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/app-shell/budget-banner.tsx` contains `budget-banner-80`, `budget-banner-100`, `triangle-alert`, `octagon-x`, `role="status"` and `role="alert"`
    - `grep -cE 'onDismiss|dismissible|toast\(' src/components/app-shell/budget-banner.tsx` returns 0
    - `grep -cE 'cap \* 80 / 100|capMicroUsd \* 80n? / 100n?' src/components/app-shell/budget-banner.tsx` returns 0, and the file contains a comparison of the form `* 100n >= ... * 80n`
    - `src/components/app-shell/budget-banner.tsx` contains `12:00 AM America/Chicago` as prose and imports `formatLocal` (or a helper that does) rather than calling `Intl` directly
    - `tests/e2e/budget-banner.spec.ts` contains `budget-banner-80` and `budget-banner-100` and contains NO literal copy sentence (`grep -c "You've used" tests/e2e/budget-banner.spec.ts` returns 0) — Phase 2 restyles every screen and a copy assertion is a brittle hook
    - `pnpm build`, `pnpm typecheck` and `pnpm lint` each exit 0
    - Recorded in the SUMMARY: a locally BUILT server (`next build && next start`) screenshotted at 390x844 and 1280x800 in BOTH themes with the banner in its 80% state and its 100% state — four images minimum
  </acceptance_criteria>
  <done>The meter is visible before danlo asks: a warning banner at 80% and a destructive one at 100%, on every route, with an icon and a text label beside the tone, and no way to dismiss either.</done>
</task>

<task type="auto">
  <name>Task 3: Organization settings, the testid move, and the three design-system e2e probes</name>
  <read_first>
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section "Screen Inventory" item 6 (Organization settings) and Executor Rules 2, 7, 8, 9
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` section "Route-group and e2e move"
    - `src/app/page.tsx` as it was before Task 1 — the exact three `data-testid` values and what each renders
    - `tests/e2e/signed-in.spec.ts` — its own comment says it asserts on `data-testid`, never on copy, because Phase 2 restyles every one of these screens. It currently visits `/`.
    - `tests/e2e/no-access.spec.ts` and `playwright.config.ts` — `E2E_BASE_URL` is the only thing pointing the suite at a server, and it is the DEPLOYED url
    - `.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md` section Color — the two accent hex values and their `rgb()` forms
  </read_first>
  <files>src/app/(app)/settings/organization/page.tsx, src/app/no-access/page.tsx, tests/e2e/signed-in.spec.ts, tests/e2e/theme-tokens.spec.ts, tests/e2e/touch-targets.spec.ts</files>
  <action>
**`src/app/(app)/settings/organization/page.tsx`** — a `Card` with an `Item` per row: Organization (display name), Tenant ID (the `orgs.id` uuid), Signed in as (the Clerk user id), Your role, Time zone (`America/Chicago`), Theme (the switcher).
🔴 **UI-SPEC Executor Rule 7.** These three `data-testid` values move here VERBATIM from `src/app/page.tsx` and keep those exact names: `signed-in-as`, `org-id`, `org-row-id`. A retired testid fails SILENTLY — the spec keeps passing against nothing. Before calling this task done, `grep -rn "signed-in-as\|org-id\|org-row-id" tests/ src/` and confirm every occurrence points at the new location.
Also add the Settings sub-nav (shadcn `Tabs` on phone, a secondary nav list on desk) with **Budget** and **Organization**; `/settings/budget` is built in plan 02-13, so link to it and let it 404 until that plan lands — record that in the SUMMARY rather than stubbing a page another plan owns.

**`src/app/no-access/page.tsx`** — restyle onto the shadcn `Empty` primitive keeping its existing copy and behaviour. It sits OUTSIDE the `(app)` group (a user with no active org cannot pass `requireOrg`), so it renders without the shell; it still gets the fonts, the tokens and the theme from the root layout.

**`tests/e2e/signed-in.spec.ts`** — change `page.goto('/')` to `page.goto('/settings/organization')`, keep all three `getByTestId` assertions byte-identical, and keep its header comment. Rename the test to `signs in and is org-scoped` if it is not already exactly that (the VALIDATION map cites it by name).

**`tests/e2e/theme-tokens.spec.ts`** — UI-SPEC Executor Rule 2 and Rule 8. 🔴 Probe the COMPUTED value, never a variable name and never `.includes(...)`:
  - `theme tokens: the accent resolves in light` — force light via the theme switcher, read `getComputedStyle` of an element painted with the primary token, and assert the colour is exactly `rgb(15, 118, 110)`.
  - `theme tokens: the accent resolves in dark` — force dark, assert exactly `rgb(45, 212, 191)`.
  - `theme tokens: the page background matches the painted token` — `rgb(244, 246, 247)` light and `rgb(14, 20, 22)` dark.
  🔴 **Assert the window is real first**: `expect(await page.evaluate(() => window.innerHeight)).toBeGreaterThan(0)` and that the probed element is visible. A hidden or zero-height Chrome window makes every measurement lie (recorded BIS defect).
  🔴 These run against the BUILT app. Next's own docs warn that CSS ordering can differ between dev and `next build`, which is exactly why Rule 8 exists.

**`tests/e2e/touch-targets.spec.ts`** — UI-SPEC Rule 9 and MOB-01. At a 390x844 viewport, for each of `nav-presets`, `nav-spend`, `nav-settings`, `user-menu` and `theme-switch`, read the bounding box and assert both width and height are at least 44. Assert first that the viewport really is 390 wide, so a default-viewport run cannot pass vacuously.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(app)/settings/organization/page.tsx` contains all three of `data-testid="signed-in-as"`, `data-testid="org-id"`, `data-testid="org-row-id"`
    - `grep -rn 'data-testid="signed-in-as"' src/ | wc -l` returns exactly 1, and that one occurrence is the new file
    - `tests/e2e/signed-in.spec.ts` contains `/settings/organization` and no longer contains `page.goto('/')`
    - `tests/e2e/theme-tokens.spec.ts` contains the exact strings `rgb(15, 118, 110)`, `rgb(45, 212, 191)`, `rgb(244, 246, 247)`, `rgb(14, 20, 22)` and `innerHeight`
    - `grep -c 'includes(' tests/e2e/theme-tokens.spec.ts` returns 0 — pin the computed value, not a substring
    - `tests/e2e/touch-targets.spec.ts` contains `390` and `44` and asserts the viewport width before measuring
    - `pnpm build` exits 0; `pnpm typecheck` and `pnpm lint` exit 0
    - Recorded in the SUMMARY: the four e2e specs were run against a LOCALLY BUILT server (`next build && next start`, `E2E_BASE_URL=http://localhost:3000`) and their results pasted by test NAME. 🔴 The authoritative run is against the DEPLOYED url in plan 02-15 — this local run is a smoke, and the SUMMARY must say so.
    - `grep -rn "signed-in-as\|org-id\|org-row-id" tests/ src/` output pasted into the SUMMARY, showing no orphaned reference
  </acceptance_criteria>
  <done>Phase 1's e2e hooks live on a real settings screen and their spec moved with them in the same task, and three probes assert the painted tokens and the thumb-zone sizes on a built app rather than on a styleguide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated or org-less session -> any `(app)` route | The group layout is the single gate for every screen in this phase and every phase after it |
| budget state -> the person spending it | A threshold the UI can hide is a threshold that does not exist |
| server component -> client component | A `"use client"` module's exports are client references inside a server component and resolve to `undefined` with every gate green |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-01 | Elevation of Privilege | `(app)` route group | mitigate | `requireOrg()` is the first statement of the group layout, so no page beneath it can render for a signed-out session or one with no ACTIVE organization; `src/proxy.ts` carries no authorization by design and is not relied on. Task 1. | high |
| T-2-10 | Information Disclosure | organization settings page | mitigate | Tenant identifiers are read through `requireOrg` / `ensureOrgRow` from Clerk's server-verified `auth()` only — never from a header, a query parameter or a cookie the client can write. Task 3. | high |
| T-2-02 | Elevation of Privilege | "Raise the monthly cap" affordance | mitigate | The banner shows the admin action only when `auth().orgRole === 'org:admin'`, and a member sees "Ask an admin" instead. This is affordance; the boundary is `app.set_budget_cap` plus the missing UPDATE grant (plan 02-05). Task 2. | medium |
| D-12 (no STRIDE ref) | Repudiation | threshold visibility | mitigate | The banner is a non-dismissible `Alert` in the shell, rendered on every route, with `role="status"` at 80% and `role="alert"` at 100%, and an icon plus a text label so colour is never the only signal. A criterion greps for any dismiss affordance or toast call. Task 2. | high |
| UI-SPEC Rule 7 (no STRIDE ref) | Tampering | retired test hooks | mitigate | The three Phase 1 testids move with their spec in the SAME task, and a repo-wide grep is an acceptance criterion — a retired hook otherwise keeps a green spec asserting nothing. Task 3. | medium |
</threat_model>

<verification>
- `pnpm build`, `pnpm typecheck` and `pnpm lint` each exit 0 through the store launcher
- The four new e2e specs run green against a LOCALLY BUILT server, with results pasted by test NAME and flagged as a smoke rather than the authoritative run
- Screenshots of the BUILT app at 390x844 and 1280x800 in both themes, including the 80% and 100% banner states
- `grep -rn "signed-in-as|org-id|org-row-id" tests/ src/` shows exactly one source location and one spec referencing it
- No hand-rolled `bg-card` / `rounded-` / `border border-` div in the files this plan wrote
</verification>

<success_criteria>
- One shell serves desk, tablet and phone, with exactly one accent nav item and a bottom tab bar that is never icon-only
- The 80% and 100% banners render on every route, are not dismissible, and carry an icon plus a text label
- `/settings/organization` carries Phase 1's three testids and its spec moved with them
- Computed-style probes pin the accent and background in both themes on the built app
- Every primary control is at least 44x44 at 390x844
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-10-SUMMARY.md` recording: the `pnpm build` route listing, the four e2e specs' local-smoke results by test NAME with the explicit note that the authoritative run is plan 02-15's against the deployed url, the screenshot inventory (screen x theme x viewport), and the `grep -rn` output proving no orphaned testid.
</output>
