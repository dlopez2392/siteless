---
phase: 02-budget-governor-search-presets
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/_helpers.ts
  - src/db/schema/clusters.ts
  - src/db/schema/geography.ts
  - src/db/schema/outlet-counts.ts
  - src/db/schema/searches.ts
  - src/db/schema/runs.ts
  - src/db/schema/index.ts
  - drizzle/0012_search_reference_schema.sql
  - drizzle/0013_reference_policies_and_grants.sql
  - drizzle/meta/
  - tests/db/grants-audit.test.ts
  - tests/db/event-trigger.test.ts
autonomous: true
requirements: [SRCH-01, SRCH-02, SRCH-03]

must_haves:
  truths:
    - "SRCH-01: a preset's geography can be persisted as cities, counties or a radius - all three kinds exist as columns and round-trip"
    - "SRCH-02: reference rows ship with org_id IS NULL, a tenant SEES them, and a tenant cannot mutate or forge one"
    - "SRCH-03: search_versions is immutable by GRANT, not by policy, so the refusal reads 42501 permission denied for table search_versions"
    - "D-05: every new table carries org_id, RLS and at least one policy, so tests/db/schema-audit.test.ts passes WITHOUT extending its ALLOW_NO_ORG_ID allow-list"
    - "A duplicate built-in cannot be inserted: UNIQUE NULLS NOT DISTINCT refuses it with 23505, where a plain UNIQUE would accept it"
    - "T-2-12: runs.search_version_id cannot be changed by authenticated - it is outside the column grant, and the FK is on delete no action"
  artifacts:
    - path: "src/db/schema/searches.ts"
      provides: "searches + search_versions, the versioned preset pair"
      contains: "search_versions"
    - path: "drizzle/0013_reference_policies_and_grants.sql"
      provides: "org_id IS NULL read policies, explicit DML grants for all nine tables, search_versions immutability, trigger attachment"
      contains: "revoke update, delete on search_versions"
    - path: "src/db/schema/_helpers.ts"
      provides: "orgScopedNullable and referencePolicies - the NULL-org variant of Phase 1's helpers"
      contains: "orgScopedNullable"
  key_links:
    - from: "runs"
      to: "search_versions"
      via: "search_version_id FK, on delete no action"
      pattern: "no action"
    - from: "searches"
      to: "search_versions"
      via: "current_version_id, nullable, set in a second statement"
      pattern: "current_version_id"
---

<objective>
Create the nine tables the search side of this phase needs — four reference tables that ship built-in with `org_id IS NULL`, and the versioned preset pair plus `runs` — with their RLS policies, their explicit grants, `search_versions`' grant-level immutability, and the audit triggers, all in one generated migration plus one custom migration.

Purpose: SRCH-01 and SRCH-03 are referential-integrity properties, not UI ones. "A past run still points at the version that produced it" is true because of an FK and a revoked grant, and nothing above the database can undo that. SRCH-02's built-ins are reference rows whose immunity from tenant mutation is a policy fact.
Output: nine tables applied to the local test database, `test:db` green, and `TENANT_TABLES` / `EVENT_LOGGED` widened deliberately.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CONVENTIONS.md
@.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md
@.planning/phases/02-budget-governor-search-presets/02-PATTERNS.md
@.planning/phases/02-budget-governor-search-presets/02-CONTEXT.md
@src/db/schema/_helpers.ts
@src/db/schema/businesses.ts
@src/db/schema/events.ts
@src/db/schema/source-records.ts
@src/db/schema/index.ts
@scripts/db.ts
</context>

<machine_notes>
Bare `pnpm` on this machine is the WRONG global 11.9.0. Every script goes through the store launcher, written in full below. `pnpm verify` is NOT runnable here — run typecheck, lint, test:unit, test:db and build individually.

If this plan runs in a git worktree: copy `C:/Users/danlo/prospector/.env.local` in first (it carries `TEST_DATABASE_URL`), then run the launcher with `install`.

🔴 This plan runs `db:migrate` and `test:db` against the ONE shared local PostgreSQL 18.6. It is the only wave-1 plan that touches the database. Do not dispatch a second `test:db` plan in the same wave.
</machine_notes>

<tasks>

<task type="auto">
  <name>Task 1: The nine Drizzle schema modules, with the NULL-org helpers</name>
  <read_first>
    - `src/db/schema/_helpers.ts` — `tstz`, `orgScoped`, `orgPolicies`, and the `(select app.current_org_id())` InitPlan wrapper that is load-bearing
    - `src/db/schema/source-records.ts` — the module shape to copy (imports, `pgTable`, `(t) => [index, check, ...orgPolicies]`)
    - `src/db/schema/events.ts` — the select+insert-only inline policy pair to copy for `search_versions`
    - `src/db/schema/businesses.ts` — `status` column + `.generatedAlwaysAs(sql\`...\`)`, already proven working in this schema
    - `.planning/phases/02-budget-governor-search-presets/02-RESEARCH.md` Pattern 5 (the NULL-org policies and the two surprises) and Pattern 6 (versioned presets, the circular FK, immutability by grant)
    - `.planning/CONVENTIONS.md` sections Tenancy, Grants, Time
  </read_first>
  <files>src/db/schema/_helpers.ts, src/db/schema/clusters.ts, src/db/schema/geography.ts, src/db/schema/outlet-counts.ts, src/db/schema/searches.ts, src/db/schema/runs.ts, src/db/schema/index.ts</files>
  <action>
**`_helpers.ts` — add two exports, do not change the existing four.**
  - `orgScopedNullable` — identical to `orgScoped` except `orgId: uuid('org_id').references(() => orgs.id)` with **no** `.notNull()`. `org_id IS NULL` means "built-in, shipped by the seed" (D-05).
  - `referencePolicies(t: string)` — the four policies from RESEARCH Pattern 5, verified locally. Read admits built-ins; every write policy EXCLUDES them, which is what makes a built-in unmutatable by any tenant:
    - `${t}_select` for select to authenticatedRole using `` sql`org_id is null or org_id = (select app.current_org_id())` ``
    - `${t}_insert` for insert with check `` sql`org_id is not null and org_id = (select app.current_org_id())` ``
    - `${t}_update` for update using AND with check `` sql`org_id is not null and org_id = (select app.current_org_id())` ``
    - `${t}_delete` for delete using `` sql`org_id is not null and org_id = (select app.current_org_id())` ``
  🔴 Do NOT add a `BEFORE UPDATE OR DELETE` trigger to make a built-in mutation loud. RESEARCH verified it does not fire (RLS filters the row before the row trigger runs) AND it breaks the owner's own seed-update path.

**`clusters.ts`** — `industry_clusters`: `...orgScopedNullable`, `key text not null`, `displayName text('display_name') not null`, `sortOrder integer('sort_order') not null default 0`. Constraints: `index('industry_clusters_org_idx').on(t.orgId)`, `unique('industry_clusters_org_key_uniq').on(t.orgId, t.key).nullsNotDistinct()`, `...referencePolicies('industry_clusters')`.
  `industry_terms`: `...orgScopedNullable`, `clusterId uuid('cluster_id').notNull().references(() => industryClusters.id)`, `kind text not null`, `value text not null`, `naicsLo integer('naics_lo')`, `naicsHi integer('naics_hi')`. Constraints: `index('industry_terms_org_idx').on(t.orgId)`, `unique('industry_terms_org_key_uniq').on(t.orgId, t.clusterId, t.kind, t.value).nullsNotDistinct()`, `check('it_kind_known', sql\`kind in ('places_type','naics_range')\`)`, `check('it_naics_range_bounds', sql\`(kind = 'naics_range') = (naics_lo is not null and naics_hi is not null)\`)`, `...referencePolicies('industry_terms')`.

**`geography.ts`** —
  `counties`: `...orgScopedNullable`, `fips text not null` (5-char GEOID, `'48215'`), `countyFips integer('county_fips') not null`, `comptrollerCode integer('comptroller_code') not null`, `name text not null`, `isRgv boolean('is_rgv') not null default false`, `outletCount integer('outlet_count') not null default 0`. Constraints: `index('counties_org_idx').on(t.orgId)`, `unique('counties_org_fips_uniq').on(t.orgId, t.fips).nullsNotDistinct()`, `check('counties_fips_identity', sql\`county_fips = 2 * comptroller_code - 1\`)`, `...referencePolicies('counties')`.
  `cities`: `...orgScopedNullable`, `countyId uuid('county_id').notNull().references(() => counties.id)`, `name text not null`, `nameVariants text('name_variants').array().notNull().default(sql\`'{}'::text[]\`)`, `outletCount integer('outlet_count') not null default 0`, `isRgvSeed boolean('is_rgv_seed') not null default false`. Constraints: `index('cities_org_idx').on(t.orgId)`, `unique('cities_org_county_name_uniq').on(t.orgId, t.countyId, t.name).nullsNotDistinct()`, `...referencePolicies('cities')`.
  `geo_presets`: `...orgScopedNullable`, `key text not null`, `displayName text('display_name') not null`, `kind text not null`, `payload jsonb not null`. Constraints: `index('geo_presets_org_idx').on(t.orgId)`, `unique('geo_presets_org_key_uniq').on(t.orgId, t.key).nullsNotDistinct()`, `check('gp_kind_known', sql\`kind in ('cities','counties','radius')\`)`, `...referencePolicies('geo_presets')`.

**`outlet-counts.ts`** — `outlet_counts`: `...orgScopedNullable`, `scope text not null`, `countyId uuid('county_id').references(() => counties.id)`, `clusterId uuid('cluster_id').notNull().references(() => industryClusters.id)`, `outlets integer not null`, `measuredAt tstz('measured_at') not null`, `source text not null`. Constraints: `index('outlet_counts_org_idx').on(t.orgId)`, `unique('outlet_counts_org_scope_uniq').on(t.orgId, t.scope, t.countyId, t.clusterId).nullsNotDistinct()`, `check('oc_scope_known', sql\`scope in ('county','state')\`)`, `check('oc_scope_target', sql\`(scope = 'county') = (county_id is not null)\`)`, `...referencePolicies('outlet_counts')`.

**`searches.ts`** —
  `searches`: `...orgScoped` (org_id NOT NULL — a preset always belongs to a tenant), `nameInternal text('name_internal') not null`, `displayName text('display_name') not null`, `currentVersionId uuid('current_version_id')` — **nullable, and deliberately declared WITHOUT `.references()` in the TS schema**, `status text not null default 'active'`. Constraints: `index('searches_org_idx').on(t.orgId)`, `check('searches_status_known', sql\`status in ('active','archived')\`)`, `...orgPolicies('searches')`. The FK to `search_versions(id)` is added in Task 2's custom migration, because the pair is circular (`search_versions.search_id -> searches.id`) and drizzle-kit cannot order circular DDL. CONVENTIONS section Naming: `name_internal` is the operator's label and `display_name` is what a screen shows — never interchangeable.
  `search_versions`: `id uuid primaryKey defaultRandom`, `orgId uuid('org_id').notNull().references(() => orgs.id)`, `searchId uuid('search_id').notNull().references(() => searches.id)`, `version integer not null`, `clusterIds uuid('cluster_ids').array().notNull()`, `geoKind text('geo_kind') not null`, `geoPayload jsonb('geo_payload') not null`, `estimateSnapshot jsonb('estimate_snapshot')`, `createdAt tstz('created_at') not null defaultNow`, `createdBy text('created_by')`. **No `updated_at`, no `updated_by`** — it is append-only, exactly like `events`. Constraints: `index('search_versions_org_idx').on(t.orgId)`, `unique('search_versions_search_version_uniq').on(t.searchId, t.version)` (this is the optimistic-concurrency lock: a concurrent save of the same version number raises `23505`), `check('sv_geo_kind_known', sql\`geo_kind in ('cities','counties','radius')\`)`, and an inline select+insert-only policy pair copied from `events.ts` (`search_versions_select` using `org_id = (select app.current_org_id())`, `search_versions_insert` with check the same).

**`runs.ts`** — `runs`: `...orgScoped`, `searchVersionId uuid('search_version_id').notNull().references(() => searchVersions.id, { onDelete: 'no action' })`, `status text not null default 'queued'`, `stoppedReason text('stopped_reason')`, `costMicroUsd bigint('cost_micro_usd', { mode: 'bigint' }) not null default 0`, `callsCount integer('calls_count') not null default 0`, `startedAt tstz('started_at')`, `finishedAt tstz('finished_at')`. Constraints: `index('runs_org_idx').on(t.orgId)`, `index('runs_version_idx').on(t.searchVersionId)`, `check('runs_status_known', sql\`status in ('queued','running','complete','partial','refused','failed')\`)` — exactly the six tones UI-SPEC's status badge map names — and `...orgPolicies('runs')`.
🔴 `onDelete: 'no action'` is what makes SRCH-03 true by construction: a version a run points at cannot be removed.
🔴 Every money column in this phase is `bigint` micro-USD. `(50*1000000)*80/100` raises `22003 integer out of range` on int4 — a CORRECT threshold computation that errors.

**`index.ts`** — add the five new modules to the barrel, keeping the existing four lines.

Then run `pnpm typecheck` through the store launcher. Do NOT run `db:generate` yet — that is Task 2.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/schema/_helpers.ts` exports `orgScopedNullable` and `referencePolicies`, and `grep -c 'org_id is null or org_id' src/db/schema/_helpers.ts` returns 1
    - `grep -c 'notNull' src/db/schema/_helpers.ts` still shows `orgScoped`'s `org_id` NOT NULL unchanged (the existing helper is not weakened)
    - `src/db/schema/clusters.ts` contains `nullsNotDistinct()` and `industry_clusters_org_key_uniq`
    - `src/db/schema/geography.ts` contains `counties_fips_identity`, `county_fips = 2 * comptroller_code - 1`, and `gp_kind_known`
    - `src/db/schema/outlet-counts.ts` contains `oc_scope_target`
    - `src/db/schema/searches.ts` contains `search_versions_search_version_uniq`, `sv_geo_kind_known`, and `current_version_id`; `grep -c 'references' src/db/schema/searches.ts` shows NO reference from `currentVersionId` (the circular FK is added in the migration)
    - `src/db/schema/searches.ts` does NOT declare `updated_at` or `updated_by` on `search_versions` (`grep -c "updatedAt" src/db/schema/searches.ts` counts only the `searches` occurrences from the orgScoped spread, i.e. 0 explicit ones)
    - `src/db/schema/runs.ts` contains `'no action'`, `runs_status_known`, and all six of `queued running complete partial refused failed`
    - `src/db/schema/index.ts` exports all five new modules plus the original four
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>Nine tables are declared in Drizzle with the NULL-org policy variant, the append-only version table, the six run statuses and the no-action FK, and the project typechecks.</done>
</task>

<task type="auto">
  <name>Task 2: The generated migration plus the custom policies, grants, circular FK and trigger migration</name>
  <read_first>
    - `drizzle/0003_policies.sql` — the policy shape drizzle-kit emits
    - `drizzle/0007_event_triggers.sql` — the `do $$ ... foreach t in array ... $$` trigger loop AND its closing `grant select, insert` / `revoke update, delete` pair, both to be copied with different table names
    - `drizzle/0008_revoke_platform_grants.sql` — why a new table inherits NOTHING and must grant its own DML in its own migration
    - `drizzle/0006_retention_constraints.sql` — the hand-written `alter table ... add constraint` and partial-index shapes
    - `.planning/CONVENTIONS.md` sections Migrations (every statement separated by `--> statement-breakpoint`, function bodies kept as ONE statement) and Grants
    - `scripts/db.ts` — `db:generate`, `db:custom --name=...`, `db:migrate --target=test`
  </read_first>
  <files>drizzle/0012_search_reference_schema.sql, drizzle/0013_reference_policies_and_grants.sql, drizzle/meta/</files>
  <action>
1. Generate the schema migration:

    node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs db:generate

Rename nothing; drizzle-kit chooses the file name. If it is not `0012_*`, use whatever number it emitted and update this plan's file list in the SUMMARY. **Commit the `drizzle/meta/NNNN_snapshot.json` it writes alongside the `.sql`** — it is the diff base for the next `generate`, and omitting it makes drizzle-kit re-emit everything. **Never hand-edit generated SQL**: drizzle-kit hashes the text and an applied file that changes is a broken checksum.

Read the generated SQL and confirm it contains no `returning old.`, no `returning new.`, no `uuidv7(`, and no `virtual` generated column. Production is **PostgreSQL 17.6** while local and CI are 18 — plan 02-02's `pg17` test is the standing guard, and it must stay green.

2. Write the custom migration:

    node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs db:custom --name=reference_policies_and_grants

Its contents, with `--> statement-breakpoint` between EVERY statement:

  a. **The circular FK** drizzle-kit could not order:
     `alter table searches add constraint searches_current_version_id_fk foreign key (current_version_id) references search_versions(id) on delete set null;`

  b. **Explicit DML grants — a new table inherits nothing** (migration 0008 retired the platform default ACL; a table created without its grant fails loudly with `42501 permission denied for table <t>` on the first user-role statement, which is the INTENDED failure mode):
     - `grant select, insert, update, delete on industry_clusters, industry_terms, counties, cities, geo_presets, outlet_counts to authenticated;` — the write policies already exclude `org_id IS NULL`, so a built-in is untouchable without a second mechanism
     - `grant select, insert, update, delete on searches to authenticated;`
     - `grant select, insert on search_versions to authenticated;` then `revoke update, delete on search_versions from authenticated;` — **immutability is a GRANT, not a policy.** A policy-only approach is a silent zero-row filter; the grant refusal is `42501 permission denied for table search_versions`, a different message from an RLS refusal's `new row violates row-level security policy` and impossible to mistake for "nothing matched". This is exactly what `drizzle/0007` does for `events`.
     - `grant select, insert on runs to authenticated;` then `grant update (status, stopped_reason, cost_micro_usd, calls_count, started_at, finished_at) on runs to authenticated;` — a **column** grant, following `drizzle/0010`'s `orgs` precedent. A policy's `WITH CHECK` sees the finished row and has no access to `OLD`, so it can express "the row still belongs to me" but never "this column did not change". Leaving `search_version_id` outside the grant is what makes T-2-12 a database fact rather than a convention. Do NOT grant `delete` on `runs` — this phase has no run-deletion path.
     🔴 `authenticated` gets DML and nothing else. Never `truncate`, `references`, `trigger` or `maintain`. `anon` gets nothing, on any table, ever.

  c. **Trigger attachment**, copying `drizzle/0007`'s `do $$ declare t text; begin foreach t in array array[...] loop execute format(...) end loop; end $$;` block:
     - `app.log_event` AFTER INSERT OR UPDATE OR DELETE on **exactly** `['searches','search_versions']`.
       🔴 NOT on any reference table. `app.log_event` resolves `app.current_org_id()`, which is NULL for the seed loader running as the owner, and `events.org_id` is NOT NULL — a trigger there would make the seed fail with `23502`.
       🔴 NOT on `runs`. The `EVENT_LOGGED` enumeration asserts set equality in BOTH directions, so adding one silently is red too. Phase 4 and Phase 9 own run volume; record the exclusion in the migration comment with that reason.
     - `app.touch_updated_at` BEFORE UPDATE on `['searches','runs','industry_clusters','industry_terms','counties','cities','geo_presets','outlet_counts']` — every mutable table. NOT on `search_versions`, which has no `updated_at` and no UPDATE path. It must be `before update`; an `after` attachment recurses because it mutates NEW and returns it.

  d. A closing comment block recording, for the next reader: which tables deliberately have no `log_event` and why.

3. Apply and confirm idempotence:

    node .../pnpm.mjs db:migrate
    node .../pnpm.mjs db:generate

The second `db:generate` must report **no drift** — triggers, functions and grants are not in the Drizzle TS schema, so drizzle-kit must not try to remove them. If it emits a migration, read it: something in Task 1's TS does not match what was applied.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs db:migrate &amp;&amp; node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:unit -t "pg17"</automated>
  </verify>
  <acceptance_criteria>
    - Both migration files exist under `drizzle/` and their `drizzle/meta/*_snapshot.json` siblings are present and staged
    - The custom migration contains `revoke update, delete on search_versions`, `searches_current_version_id_fk`, `grant update (status, stopped_reason, cost_micro_usd, calls_count, started_at, finished_at) on runs`, and `app.touch_updated_at`
    - `grep -c "log_event" drizzle/0013_reference_policies_and_grants.sql` returns at least 1 and the array in that block lists ONLY `searches` and `search_versions`
    - `grep -cE "array\[[^]]*(counties|cities|outlet_counts|runs)[^]]*\].*log_event" drizzle/0013_reference_policies_and_grants.sql` returns 0
    - Every statement in the custom migration is separated by `--> statement-breakpoint` (`grep -c -- '--> statement-breakpoint' drizzle/0013_reference_policies_and_grants.sql` is at least 8)
    - `pnpm db:migrate` exits 0; a second run applies nothing
    - `pnpm db:generate` run immediately after reports no drift (record the exact output in the SUMMARY)
    - `pnpm test:unit -t "pg17"` exits 0 with the test NAME read from the output
    - `grep -cE 'grant .*(truncate|references|trigger|maintain)' drizzle/0013_reference_policies_and_grants.sql` returns 0 and `grep -c 'to anon' drizzle/0013_reference_policies_and_grants.sql` returns 0
  </acceptance_criteria>
  <done>Nine tables, their policies, their explicit grants, the circular FK and the audit triggers are applied to the local test database from drizzle-kit alone, with no drift and no PostgreSQL 18-only syntax.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Widen TENANT_TABLES and EVENT_LOGGED, then prove the whole DB suite green</name>
  <read_first>
    - `tests/db/grants-audit.test.ts` — `TENANT_TABLES` is an array literal IN the test file; test 1 asserts it equals the live contents of `public`, so FORGETTING a table is red rather than silently unguarded
    - `tests/db/event-trigger.test.ts` — `EVENT_LOGGED` is a `Set` literal in the test file; the enumeration asserts set equality in BOTH directions and asserts each trigger is ENABLED (`tgenabled`), because `alter table ... disable trigger` leaves the `pg_trigger` row in place
    - `tests/db/schema-audit.test.ts` — `ALLOW_NO_ORG_ID` must NOT need extending: every new table carries an `org_id` column, merely nullable on the reference ones
    - `.planning/CONVENTIONS.md` section Testing
  </read_first>
  <files>tests/db/grants-audit.test.ts, tests/db/event-trigger.test.ts</files>
  <action>
This task is BLOCKING for everything downstream: `pnpm build` and `pnpm typecheck` pass WITHOUT the migration having been applied, because Drizzle's types come from the schema files and not from the live database. That is a false-positive verification state, and it is why the migration in Task 2 and this proof run before any other plan consumes these tables.

1. In `tests/db/grants-audit.test.ts`, extend `TENANT_TABLES` from
   `['orgs', 'businesses', 'source_records', 'events']`
   to also contain, in the same array literal:
   `'industry_clusters', 'industry_terms', 'counties', 'cities', 'geo_presets', 'outlet_counts', 'searches', 'search_versions', 'runs'`
   — 13 entries. Do NOT move it to a config file; widening it must be a diff a reviewer sees.
   Test 1 asserts the array equals the live catalog. The budget tables (`budget_periods`, `cost_reservations`, `cost_ledger`) do NOT exist yet — plan 02-05 creates them and widens this array again. If you add them now, test 1 goes red for a correct reason.

2. In `tests/db/event-trigger.test.ts`, extend `EVENT_LOGGED` from `new Set(['orgs', 'businesses'])` to `new Set(['orgs', 'businesses', 'searches', 'search_versions'])`. Plan 02-05 adds `budget_periods`. **Deliberately exclude** `runs` and every reference table — the comment above the Set already explains that the exclusion is the decision, not an omission; extend that comment with the two new reasons (the seed loader runs as the owner with no org claim; run volume is Phase 4's).

3. Run the full DB suite through the store launcher and READ THE NAMES in the output, not the exit code:

    node .../pnpm.mjs db:migrate
    node .../pnpm.mjs test:db

Expect the Phase 1 baseline of 31 DB tests still green plus the three enumerations now covering 13 tables. `every public table is org-scoped and has RLS enabled` must pass **without** touching `ALLOW_NO_ORG_ID` — if it fails naming a reference table, the cause is a missing `org_id` column or a missing policy, not a missing allow-list entry. Do not add an allow-list entry to make it green.

4. Watched-failing-first evidence, recorded in the SUMMARY (CONVENTIONS section Testing: a test that has never been red proves nothing). On the LIVE local database, never in a migration file, so the revert is provable by `git diff --stat` being empty:
   - `revoke select on public.counties from authenticated;` → the grants audit goes red naming `counties`. Revert with the matching `grant`, confirm green, and verify from `information_schema.role_table_grants`.
   - `alter table searches disable trigger searches_event;` → `every state-bearing table has an app.log_event after-row trigger` goes red (the enumeration asserts `tgenabled`, so a disabled trigger is caught where a row-count enumeration would report full coverage). Re-enable, confirm green, verify from `pg_trigger`.
   Record the failing test NAMES verbatim, and confirm `git diff --stat` is empty afterwards.
  </action>
  <verify>
    <automated>node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs test:db</automated>
  </verify>
  <acceptance_criteria>
    - `tests/db/grants-audit.test.ts`'s `TENANT_TABLES` has exactly 13 entries and contains `search_versions` and `outlet_counts`; it does NOT contain `budget_periods`
    - `tests/db/event-trigger.test.ts`'s `EVENT_LOGGED` has exactly 4 entries and contains `searches` and `search_versions`; it does NOT contain `runs`, `counties` or `budget_periods`
    - `pnpm test:db` exits 0 and its pass list NAMES `every public table is org-scoped and has RLS enabled`, `every timestamp column in public is timestamptz`, `authenticated holds no TRUNCATE, REFERENCES or TRIGGER on any tenant table`, and `every state-bearing table has an app.log_event after-row trigger`
    - `tests/db/schema-audit.test.ts`'s `ALLOW_NO_ORG_ID` is UNCHANGED (`grep -c "ALLOW_NO_ORG_ID = new Set(\['orgs', '__drizzle_migrations'\])" tests/db/schema-audit.test.ts` returns 1)
    - Both watched-red-first mutations are recorded in the SUMMARY with the verbatim failing test NAME, and `git diff --stat` is empty afterwards
    - `pnpm typecheck`, `pnpm lint` and `pnpm build` each exit 0
  </acceptance_criteria>
  <done>Thirteen tables are guarded by the grants audit, four by the event enumeration, the DB suite is green with names read from the output, and both guards were proven to discriminate on the live database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant session (`authenticated`) -> reference rows | Built-in clusters, counties, cities and geo presets are shared across every tenant; a tenant that could edit one would edit it for everybody |
| tenant session -> another tenant's presets, versions and runs | Standard `org_id` isolation |
| tenant session -> its own audit history | A run re-pointed at a different version, or a version edited after the fact, rewrites what a past run means |
| local PostgreSQL 18.6 -> production PostgreSQL 17.6 | Migration portability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-2-09 | Tampering | reference rows (`org_id IS NULL`) | mitigate | `referencePolicies()` write policies all carry `org_id is not null and org_id = (select app.current_org_id())`, so a built-in is outside every write policy; `UNIQUE NULLS NOT DISTINCT` refuses a duplicate built-in with `23505` where a plain UNIQUE accepts it. The refusal shape (zero rows for UPDATE/DELETE, `42501` only for a forged INSERT) is proven in plan 02-06. Tasks 1-2. | high |
| T-2-10 | Information Disclosure | cross-org presets, versions, runs | mitigate | Phase 1's `orgPolicies` plus a `_org_idx` on every table (an RLS predicate on `org_id` gets no index for free), and the D-10 live-catalog enumeration in `tests/db/schema-audit.test.ts` fails if any table lacks `org_id`, RLS or a policy. Tasks 1, 3. | high |
| T-2-12 | Tampering | `runs.search_version_id`, `search_versions` | mitigate | `search_versions` is immutable by GRANT (`grant select, insert` + `revoke update, delete`), refusing with `42501 permission denied for table search_versions`; `runs.search_version_id` sits OUTSIDE the column grant so it cannot be re-pointed; the FK is `on delete no action` so a cited version cannot be removed. Mutation M12 reds exactly the immutability test. Task 2. | high |
| T-2-11 | Repudiation | actor on a preset or version change | mitigate | `app.log_event` AFTER-ROW triggers on `searches` and `search_versions` resolve the actor themselves from `app.jwt()->>'sub'`; `authenticated` holds no INSERT on `events` (migration 0011), so attribution cannot be forged. `EVENT_LOGGED` asserts `tgenabled`, not mere existence. Tasks 2-3. | high |
| Pitfall 1 (no STRIDE ref) | Tampering | migration portability | mitigate | Generated and custom SQL are read for `returning old.` / `returning new.` / `uuidv7(` / virtual generated columns before applying, and plan 02-02's `pg17` test is re-run as this plan's own verify. Task 2. | high |
</threat_model>

<verification>
- `pnpm db:migrate` is green and idempotent; a following `pnpm db:generate` reports no drift
- `pnpm test:db` is green with the four enumeration test NAMES read from the output
- `pnpm typecheck`, `pnpm lint`, `pnpm build` and `pnpm test:unit -t "pg17"` each exit 0
- Two watched-red-first mutations recorded with verbatim failing test names, `git diff --stat` empty afterwards
- `ALLOW_NO_ORG_ID` in `tests/db/schema-audit.test.ts` is unchanged
</verification>

<success_criteria>
- Nine tables exist with `org_id`, RLS, at least one policy, a `_org_idx`, and an explicit DML grant written in the same migration
- Reference tables admit built-ins on read and exclude them from every write policy
- `search_versions` is append-only by grant; `runs.search_version_id` is outside the column grant and its FK is `on delete no action`
- `TENANT_TABLES` holds 13 names and `EVENT_LOGGED` holds 4, both as literals in their test files
</success_criteria>

<output>
After completion, create `.planning/phases/02-budget-governor-search-presets/02-03-SUMMARY.md` recording: the real migration file names drizzle-kit chose, the verbatim `db:generate` no-drift output, the `pnpm test:db` pass list by NAME with the test count, and the two watched-red-first mutation outputs with `git diff --stat` proven empty.
</output>
