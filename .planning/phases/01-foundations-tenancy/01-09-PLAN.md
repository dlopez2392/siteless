---
phase: 01-foundations-tenancy
plan: 09
type: execute
wave: 4
depends_on: [05, 07]
files_modified:
  - drizzle/0007_event_triggers.sql
  - drizzle/meta/_journal.json
  - tests/db/events-append-only.test.ts
  - tests/db/event-trigger.test.ts
  - .planning/CONVENTIONS.md
autonomous: true
requirements: [FOUND-03, FOUND-01, FOUND-06]
must_haves:
  truths:
    - "D-08: the audit trail is ENFORCED, not conventional — a raw SQL write that no application code made still produces an `events` row with an actor and a timestamp, because the mechanism is an AFTER ... FOR EACH ROW trigger"
    - "D-06: `events` is immutable by GRANT, not by policy — `update events` / `delete from events` as `authenticated` is refused 42501 with 'permission denied for table events', a different message from the RLS refusal"
    - "D-07: an UPDATE touching only a domain column still moves `updated_at` and stamps `updated_by`, so the denormalized columns are not a lie the first time someone writes SQL by hand"
    - "The event-scope boundary is DECIDED here and written down: row triggers on state-bearing tables (`orgs`, `businesses`); bulk-ingest tables (`source_records`) get one run-level event in Phase 3"
    - "Every SECURITY DEFINER function pins `set search_path = public`"
    - "The trigger and grant tests were watched failing first, with the red output recorded"
  artifacts:
    - path: "drizzle/0007_event_triggers.sql"
      provides: "app.log_event(), app.touch_updated_at(), the trigger attachments, and the events grant/revoke"
      contains: "app.log_event"
    - path: "tests/db/event-trigger.test.ts"
      provides: "D-08's acceptance test — a direct write still produces an event — plus the updated_at/updated_by proof and the trigger-coverage enumeration"
    - path: "tests/db/events-append-only.test.ts"
      provides: "Two separate transactions proving UPDATE and DELETE on events are grant refusals"
    - path: ".planning/CONVENTIONS.md"
      provides: "The event-scope boundary and the schema contract Phases 2 and 3 read instead of re-deriving"
  key_links:
    - from: "businesses"
      to: "events"
      via: "AFTER INSERT OR UPDATE OR DELETE FOR EACH ROW EXECUTE FUNCTION app.log_event()"
      pattern: "after insert or update or delete"
    - from: "app.log_event()"
      to: "Clerk actor"
      via: "coalesce(app.jwt()->>'sub', current_setting('app.actor_id', true), 'system')"
      pattern: "app\\.jwt\\(\\)->>'sub'"
---

<objective>
Make attribution a property of the database. An append-only `events` table already exists; this plan adds the trigger that fills it for writes no application code made, the `updated_at`/`updated_by` trigger behind D-07's denormalized columns, and the grant revocation that makes `events` immutable.

Purpose: D-08 requires enforcement rather than convention, and its acceptance test is literally "a direct write still produces an event" — a test a TypeScript write helper cannot pass. BIS had five byte-identical copies of `emit()` and a fix reached only one of them.
Output: `pnpm verify` green with five new named tests, the red output recorded, one mutation check, and the event-scope boundary written down for Phases 2 and 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-RESEARCH.md
@.planning/phases/01-foundations-tenancy/01-PATTERNS.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@src/db/schema/events.ts
@src/db/schema/businesses.ts
@tests/db/_fixtures.ts

<interfaces>
Created here. Phase 7's "status history with actor and timestamp" reads `events`; Phase 3 must honour the scope boundary rather than re-derive it.

app.log_event() returns trigger     SECURITY DEFINER, set search_path = public
  Resolves org from new/old row: org_id, or id when the table IS orgs.
  Actor chain: app.jwt()->>'sub'  ->  current_setting('app.actor_id', true)  ->  'system'
  Writes: org_id, actor_id, entity_type = tg_table_name, entity_id, action = lower(tg_op),
          before (UPDATE/DELETE), after (INSERT/UPDATE), occurred_at default now()

app.touch_updated_at() returns trigger    (plain, not definer — it only edits NEW)
  new.updated_at := now();
  new.updated_by := coalesce(app.jwt()->>'sub', current_setting('app.actor_id', true), 'system');

Trigger attachment (the DECIDED scope boundary — see .planning/CONVENTIONS.md):
  app.log_event()        AFTER INSERT OR UPDATE OR DELETE FOR EACH ROW on: orgs, businesses
  app.touch_updated_at() BEFORE UPDATE          FOR EACH ROW on: orgs, businesses, source_records
  source_records gets NO row-level event trigger: Phase 3 ingests ~10k Comptroller/Overture
  rows and a row trigger there writes 10k event rows with full before/after payloads. Bulk
  ingest writes one run-level event instead.

Grants:
  grant select, insert on events to authenticated;
  revoke update, delete on events from authenticated;   -- immutability is a GRANT, not a policy

Test names (VALIDATION.md filters on these substrings — do not reword):
  'a direct write still produces an event'
  'an UPDATE of only a domain column still moves updated_at and stamps updated_by'
  'every state-bearing table has an app.log_event after-row trigger'
  'events are append-only: UPDATE as authenticated is refused'
  'events are append-only: DELETE as authenticated is refused'

BIS analogs (READ-ONLY, at C:\Users\danlo\bis-platform):
  packages\db\src\test\rls.test.ts  lines 61-68  — the append-only test, with TWO defects to fix
  packages\db\src\events.ts         lines 5-13   — the emit() helper D-08 rejects, and its own comment
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the audit tests — and watch them fail</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 6 (lines 548-595) — the trigger body, the three design notes, and the executed transcript of a raw SQL write producing an event
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Code Examples, Example 5 (lines 914-920) — the executed output
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `tests/db/events-append-only.test.ts` (lines 517-530) — BIS's version and its TWO defects: a bare `.rejects.toThrow()` passes on a SQL typo, and two refusals in one transaction make the second report 25P02
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § Shared Pattern A (lines 726-746) — the message difference between an RLS refusal and a grant refusal, and why the tests must tell them apart
    - `tests/db/_fixtures.ts` — `withRollback`, `actAs`, `actAsOwner`, `seedTwoOrgs`
    - `tests/db/rls-isolation.test.ts` (plan 05) and `tests/db/retention.test.ts` (plan 07) — the house style for mutation-naming headers
  </read_first>
  <files>tests/db/events-append-only.test.ts, tests/db/event-trigger.test.ts</files>
  <action>
Create `tests/db/events-append-only.test.ts`. Header:
```
/**
 * D-06: events is the record of truth for every state change, and its immutability is a
 * GRANT, not a policy. The message matters as much as the code — an RLS refusal says
 * "new row violates row-level security policy" and a grant refusal says "permission denied
 * for table events". Asserting only 42501 cannot tell them apart, and this file is where
 * the difference is the whole point.
 *
 * BIS's version has two defects this port fixes: `.rejects.toThrow()` with no argument
 * passes on any error including a typo in the SQL, and it puts two refusals in ONE
 * transaction — the second reports 25P02, not its own reason.
 *
 * Mutation: delete the `revoke update, delete on events from authenticated` line from
 * drizzle/0007_event_triggers.sql — 'events are append-only: UPDATE as authenticated is
 * refused' goes red, and only that one.
 */
```
Two tests, each in its OWN `withRollback`:
1. `'events are append-only: UPDATE as authenticated is refused'` — seedTwoOrgs, insert a business as owner so an event exists, `actAs(c, { o: { id: 'org_A' }, sub: 'user_danlo', role: 'authenticated' })`, then `update events set action = 'hacked'`. Expect `rejects.toMatchObject({ code: '42501' })` AND the message to match `/permission denied for table events/`.
2. `'events are append-only: DELETE as authenticated is refused'` — identical, with `delete from events`. Separate transaction, because the first refusal aborts the one it is in.

Create `tests/db/event-trigger.test.ts`. Header names the mutation: drop the `businesses_event` trigger — `'a direct write still produces an event'` goes red, and only that one.

Three tests:
1. `'a direct write still produces an event'` — D-08's acceptance test.
   - `seedTwoOrgs`, `actAs(c, { o: { id: 'org_A' }, sub: 'user_danlo', role: 'authenticated' })`
   - Issue a RAW SQL insert: `insert into businesses (org_id, display_name) values ($1, 'direct-write')` with org A's id. No application code is involved — that is the point.
   - `select actor_id, entity_type, action, after->>'display_name' as name, occurred_at from events where entity_type = 'businesses' order by id desc limit 1`
   - Assert `{ actor_id: 'user_danlo', entity_type: 'businesses', action: 'insert', name: 'direct-write' }` and that `occurred_at` is a non-null instant within 60 seconds of `now()`.
   - Then UPDATE that row's `display_name` and assert a second event with `action: 'update'`, a non-null `before` and a non-null `after`.
2. `'an UPDATE of only a domain column still moves updated_at and stamps updated_by'` — D-07.
   - Insert as owner, read `updated_at`, then as `authenticated` with `sub: 'user_danlo'` run `update businesses set city = 'McAllen' where id = $1` — touching NO timestamp column.
   - Assert the new `updated_at` is strictly greater than the old one and `updated_by` equals `'user_danlo'`.
   - Compare timestamps as epoch milliseconds from SQL (`extract(epoch from updated_at)`), not as JS `Date` objects round-tripped through `pg` — a `timestamptz` that arrives as a `Date` is easy to compare wrongly, and this suite runs in UTC on a Chicago machine.
3. `'every state-bearing table has an app.log_event after-row trigger'` — the coverage guard, so Phase 3 cannot add a state table and silently skip attribution.
   - `const EVENT_LOGGED = new Set(['orgs', 'businesses']);` as a literal IN THIS FILE, with a comment naming `source_records` as the deliberate exclusion and why.
   - Query `pg_trigger` joined to `pg_class` and `pg_proc` for triggers whose function is `log_event`, assert the set of table names equals `EVENT_LOGGED` exactly.

WATCH THEM FAIL. Run and paste the verbatim output into the SUMMARY:
```
pnpm test:db -t "append-only"
pnpm test:db -t "direct write still produces an event"
pnpm test:db -t "updated_at"
pnpm test:db -t "after-row trigger"
```
Expected red reasons — a different reason means the test is wrong, not the schema:
- `append-only` — `update events` affects 0 rows and throws NOTHING (RLS filters it; the grant is still in place), so `.rejects` fails
- `direct write still produces an event` — the events query returns no rows
- `updated_at` — `updated_at` is unchanged and `updated_by` is null
- `after-row trigger` — the queried set is empty, not `{orgs, businesses}`

🔴 Read the failing test NAME in every run. A `-t` filter matching nothing exits green.
  </action>
  <verify>
    <automated>pnpm test:db -t "direct write still produces an event" ; test $? -ne 0 &amp;&amp; pnpm test:db -t "append-only" ; test $? -ne 0 &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/db/events-append-only.test.ts` contains exactly two `it(` blocks, each in its own `withRollback`
    - `grep -c 'rejects.toThrow()' tests/db/events-append-only.test.ts` returns 0 — BIS's bare form passes on a SQL typo
    - `grep -c 'permission denied for table events' tests/db/events-append-only.test.ts` returns 2
    - `tests/db/event-trigger.test.ts` contains three `it(` blocks and the literal `new Set(['orgs', 'businesses'])`
    - `tests/db/event-trigger.test.ts` contains `extract(epoch from updated_at)` — timestamps are compared in SQL, not as round-tripped JS Dates
    - Both file headers name their mutation
    - `pnpm test:db -t "direct write still produces an event"` exits NON-ZERO and names its test
    - `pnpm test:db -t "append-only"` exits NON-ZERO and names both its tests
    - The verbatim red output for all four filtered runs is in `01-09-SUMMARY.md` under "Watched failing first"
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Five named tests exist, all five red for exactly the right reason, and the red output is recorded where the phase gate can read it.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] The event and touch triggers, the events grant revocation, and green</name>
  <read_first>
    - The red output recorded in Task 1 — each named test and its failure reason
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 6 (lines 548-595) — the `app.log_event()` body, the `foreach` attachment loop, the three design notes, and the grants
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 7 (lines 599-611) — `app.touch_updated_at()`
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 8 (lines 794-798) — `--> statement-breakpoint`, one function body per statement
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Assumptions Log A7 (line 1161) — the write-amplification boundary this plan decides
    - `drizzle/0000_bootstrap.sql` and `drizzle/0002_tenancy_functions.sql` — the existing `app` schema functions and their grant pattern
    - `src/db/schema/events.ts` (plan 05) — the column names the trigger writes
  </read_first>
  <files>drizzle/0007_event_triggers.sql, drizzle/meta/_journal.json</files>
  <action>
Create the migration with `pnpm db:custom -- --name=event_triggers`. Use the next sequential number after the highest existing migration (expected `0007`; plan 07 deletes its drift-check migration, so confirm the actual highest before assuming).

```
-- D-06 + D-08. Attribution is a property of the database, not of anybody's memory.
-- BIS logged events from the application tier through emit() — which existed as five
-- byte-identical copies, and a fix reached one of them. D-08's acceptance test is
-- literally "a direct write still produces an event"; a helper cannot pass it.

-- SECURITY DEFINER so the trigger can insert into events regardless of the caller's
-- grants (authenticated has no UPDATE/DELETE there, and after the revoke below it has
-- less still). set search_path = public is MANDATORY with it: a definer function without
-- a pinned search_path is a privilege-escalation vector (ASVS V4).
create or replace function app.log_event() returns trigger
language plpgsql security definer set search_path = public as $$
declare v_row jsonb; v_org uuid;
begin
  v_row := to_jsonb(coalesce(new, old));
  -- orgs IS the tenant root, so its own id is the org id.
  v_org := coalesce((v_row->>'org_id')::uuid,
                    case when tg_table_name = 'orgs' then (v_row->>'id')::uuid end);
  insert into events (org_id, actor_id, entity_type, entity_id, action, before, after)
  values (
    v_org,
    -- Clerk sub, then a GUC workers/ETL can set, then 'system'. current_setting with the
    -- missing_ok flag returns NULL rather than erroring on an unset GUC.
    coalesce(app.jwt()->>'sub', current_setting('app.actor_id', true), 'system'),
    tg_table_name,
    (v_row->>'id')::uuid,
    lower(tg_op),
    case when tg_op in ('UPDATE','DELETE') then to_jsonb(old) end,
    case when tg_op in ('INSERT','UPDATE') then to_jsonb(new) end
  );
  return coalesce(new, old);
end $$;
--> statement-breakpoint

-- D-07. Without this, the denormalized columns are a lie the first time someone writes
-- SQL by hand.
create or replace function app.touch_updated_at() returns trigger
language plpgsql as $$
begin
  new.updated_at := now();
  new.updated_by := coalesce(app.jwt()->>'sub', current_setting('app.actor_id', true), 'system');
  return new;
end $$;
--> statement-breakpoint

-- The DECIDED scope boundary (see .planning/CONVENTIONS.md). Row triggers go on
-- state-bearing tables. source_records is bulk-ingest: Phase 3 loads ~10k Comptroller and
-- Overture rows, and a row trigger there would write 10k event rows carrying full
-- before/after payloads. Bulk ingest writes ONE run-level event instead.
do $$ declare t text;
begin
  foreach t in array array['orgs','businesses'] loop
    execute format(
      'create trigger %I_event after insert or update or delete on %I
         for each row execute function app.log_event()', t, t);
  end loop;
  foreach t in array array['orgs','businesses','source_records'] loop
    execute format(
      'create trigger %I_touch before update on %I
         for each row execute function app.touch_updated_at()', t, t);
  end loop;
end $$;
--> statement-breakpoint

-- Immutability is a GRANT, not a policy. With only an RLS filter, an UPDATE is silently
-- filtered to zero rows and reads as "nothing happened"; with the grant revoked it is
-- 42501 "permission denied for table events" — a refusal the caller cannot mistake for
-- an empty result, and a different message from the RLS refusal.
grant select, insert on events to authenticated;
--> statement-breakpoint
revoke update, delete on events from authenticated;
```

Then:
1. `pnpm db:migrate` — the BLOCKING push. Run it twice to confirm no-op.
2. Re-run each previously red filter and confirm GREEN, reading the NAME each time:
   `-t "direct write still produces an event"`, `-t "updated_at"`, `-t "after-row trigger"`, `-t "append-only"`.
3. `pnpm test:db`, then `pnpm verify`.
4. Confirm no schema drift: `pnpm db:generate -- --name=noop` produces a migration with no statements; delete it and its journal entry. Triggers and functions are not in the Drizzle TS schema, so drizzle-kit must not try to remove them — if it emits anything, stop and reconcile before finishing, because Phase 2 and Phase 3 generate on top of this.

If `touch_updated_at` causes an infinite recursion, it is attached as AFTER rather than BEFORE. It must be `before update` — it mutates `NEW` and returns it.
  </action>
  <verify>
    <automated>pnpm db:migrate &amp;&amp; pnpm db:migrate &amp;&amp; pnpm test:db &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - The migration file contains `app.log_event`, `app.touch_updated_at`, `after insert or update or delete`, `before update`, `grant select, insert on events to authenticated`, `revoke update, delete on events from authenticated`
    - Both `create or replace function app.` blocks that are SECURITY DEFINER also carry `set search_path = public` on the same statement; `grep -c 'security definer' <file>` returns 1 and `grep -c 'set search_path = public' <file>` returns 1
    - The attachment loop lists exactly `array['orgs','businesses']` for `log_event` and `array['orgs','businesses','source_records']` for `touch_updated_at`
    - `pnpm db:migrate` exits 0 twice in a row
    - `pnpm test:db` exits 0 and names all five new tests plus every test from plans 05, 06 and 07
    - `pnpm test:db -t "direct write still produces an event"` exits 0 and names its test
    - `pnpm verify` exits 0
    - `select count(*) from pg_trigger t join pg_class c on c.oid=t.tgrelid where not t.tgisinternal` returns 5 (2 event + 3 touch)
    - `pnpm db:generate -- --name=noop` produces a migration with zero statements; the empty file and its journal entry are deleted afterwards
  </acceptance_criteria>
  <done>A raw SQL write no application code made produces an attributed event row, a domain-only UPDATE stamps `updated_at`/`updated_by`, and `events` is immutable by grant.</done>
</task>

<task type="auto">
  <name>Task 3: Mutation check, and write the conventions Phases 2 and 3 read</name>
  <read_first>
    - `01-09-SUMMARY.md` in progress — the red output from Task 1
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Validation Architecture, Sampling Rate (line 1085) — the three phase-gate mutations
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Open Questions 2 and 4 (lines 1173-1182) — what must be written down so Phases 2 and 3 do not re-derive it
    - `.planning/phases/01-foundations-tenancy/01-CONTEXT.md` § Integration Points — Phases 2 and 3 run concurrently on these shapes
    - `CLAUDE.md` — the Conventions section is currently a placeholder sourced from `CONVENTIONS.md`
    - `src/db/schema/*.ts` — the actual column names, to write the contract from the code rather than from memory
  </read_first>
  <files>.planning/CONVENTIONS.md</files>
  <action>
MUTATION CHECK, recorded — a green suite proves nothing a mutation check has not:
1. Against the local database, `drop trigger businesses_event on businesses;` then `pnpm test:db`.
   Exactly one named test must go red: `a direct write still produces an event`. Record the name and the total test count.
2. Restore it with the exact `create trigger` statement from the migration, confirm `pnpm test:db` green.
3. Against the local database, `grant update, delete on events to authenticated;` then `pnpm test:db`.
   Exactly one named test must go red: `events are append-only: UPDATE as authenticated is refused`. Record it.
   (`DELETE` may also go red — if it does, that is two tests for one mutation; note it and split the revoke assertion so each grant has its own test.)
4. Restore with `revoke update, delete on events from authenticated;`, confirm green.
5. Confirm `git diff --stat` shows no file changed — both mutations were applied to the database, not to the migration, precisely so the revert is provable.

Then create `.planning/CONVENTIONS.md`. Write it from the code, not from memory — read `src/db/schema/*.ts` and the applied migrations first. Sections:

**Tenancy** — one Siteless tenant is one Clerk organization (D-01, flat, no agency tier). Every table carries `org_id uuid not null references orgs(id)` except `orgs` itself, which keys on `id`. Every org-scoped table gets `...orgPolicies('<table>')` and `index('<table>_org_idx').on(t.orgId)` in the SAME migration that creates it — the D-10 enumeration test fails otherwise, and a table must never exist un-scoped even for one commit.

**Claims** — org identity comes only from Clerk's server-verified `auth()`. In SQL it is `coalesce(app.jwt()->'o'->>'id', app.jwt()->>'org_id')`, wrapped as `(select app.current_org_id())` inside every policy so it is an InitPlan rather than a per-row call.

**Database access** — `withOrg()` in `src/db/with-org.ts` is the ONLY runtime entry point. The single exception is `/api/health`'s `select 1`, which needs no claims and reads no table. The runtime connects as `app_user` (NOINHERIT, owns nothing); migrations connect as the owner through `scripts/db.ts`.

**Migrations** — drizzle-kit is the single authority (D-09). Generated SQL is committed and never hand-edited; hand-written PL/pgSQL goes through `pnpm db:custom` with `--> statement-breakpoint` between every statement. No Supabase CLI migrations, no MCP `apply_migration`.

**Audit and attribution** — row-level `app.log_event()` triggers on state-bearing tables: currently `orgs` and `businesses`; Phase 7 adds `leads`. Bulk-ingest tables do NOT get row triggers — `source_records` is excluded because Phase 3 loads ~10k rows and a row trigger would write 10k events with full before/after payloads; bulk ingest writes ONE run-level event per run instead. `app.touch_updated_at()` goes on every mutable table. `events` is immutable by grant.

**Naming, three fields** — `orgs.name_internal` vs `orgs.display_name`; `businesses.legal_name` (Comptroller DBA) vs `businesses.display_name` (what the card shows) vs `businesses.internal_notes` (operator annotation, never outbound). Every outbound builder registers in `PAYLOAD_BUILDERS` and accepts `PublicBusiness`.

**Retention** — every `source_records` row carries `retention_class`; Google Places content can only ever be `ephemeral`; ephemeral requires `expires_at` (TTL 21 days, not 30); a durable `businesses` field can only cite a durable source through the composite FK.

**Time** — every timestamp is `timestamptz` (`tstz()` in `_helpers.ts`). Day buckets are always `at time zone 'America/Chicago'` in SQL and `APP_TZ` in TypeScript. Tests run in UTC so a forgotten zone is a red assertion. Zone AND locale are pinned.

**Testing** — `withRollback` wraps every DB test; one refused statement per transaction (the next reports `25P02`); pin the SQLSTATE and the constraint name; two orgs always; watch every isolation/constraint/trigger test fail first and record the red output; one named mutation per criterion test, reverted and diffed back.
  </action>
  <verify>
    <automated>pnpm verify &amp;&amp; node -e "const s=require('fs').readFileSync('.planning/CONVENTIONS.md','utf8');for(const t of ['orgPolicies','withOrg','drizzle-kit','log_event','source_records','retention_class','timestamptz','America/Chicago','withRollback','25P02','internal_notes'])if(!s.includes(t))throw new Error('CONVENTIONS.md missing: '+t);console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records mutation 1: `drop trigger businesses_event`, the ONE test name that went red (`a direct write still produces an event`), and the total test count
    - The SUMMARY records mutation 2: `grant update, delete on events to authenticated`, the test name(s) that went red, and the total test count
    - After both reverts, `pnpm test:db` exits 0 and `git diff --stat` shows zero changed files
    - `.planning/CONVENTIONS.md` exists and contains all of: `orgPolicies`, `withOrg`, `drizzle-kit`, `log_event`, `source_records`, `retention_class`, `timestamptz`, `America/Chicago`, `withRollback`, `25P02`, `internal_notes`
    - `.planning/CONVENTIONS.md` states explicitly that `source_records` is excluded from row-level event triggers and why (Phase 3 must not re-derive this)
    - `.planning/CONVENTIONS.md` lists the exact column names of `orgs`, `events`, `businesses` and `source_records` as they exist in `src/db/schema/`, verified by reading those files
    - `pnpm verify` exits 0
  </acceptance_criteria>
  <done>Two recorded mutations show the audit tests have teeth, and the schema and process contract Phases 2 and 3 depend on is written down instead of re-derived.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any writer → `events` | A trigger must fire for writes the application never made: psql, a migration, a future worker |
| `authenticated` → `events` rows | An attacker who can edit the audit log can erase their own actions |
| trigger function → caller privileges | A `SECURITY DEFINER` trigger runs as the owner on every write to the table it guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-06 | Repudiation | `events` immutability | mitigate | `revoke update, delete on events from authenticated` — a GRANT, not a policy, so the refusal is `42501 permission denied for table events` rather than a silent zero-row filter. Two tests in two transactions pin the code AND the message; a recorded mutation re-grants and confirms a named test goes red. Tasks 1-3. | high |
| T-1-30 | Repudiation | attribution coverage | mitigate | D-08 is enforced by an AFTER ... FOR EACH ROW trigger, so a raw SQL write no application code made still produces an attributed event. A TypeScript helper cannot pass that test — BIS had five byte-identical copies of `emit()` and a fix reached one. The `every state-bearing table has an app.log_event after-row trigger` enumeration stops Phase 3 adding a state table without attribution. Tasks 1-2. | high |
| T-1-07 | Elevation of Privilege | `app.log_event()` | mitigate | `SECURITY DEFINER` is required so the trigger can write `events` regardless of the caller's grants, and `set search_path = public` is pinned on the same statement — a definer function with an attacker-influenced search_path runs as the owner (ASVS V4). Asserted by grep. Task 2. | high |
| T-1-31 | Tampering | actor spoofing | mitigate | The actor chain reads `app.jwt()->>'sub'` from the transaction-local GUC that only `withOrg` sets, falling back to `app.actor_id` and then the literal `'system'`. A client cannot set either GUC; the claims arrive only from Clerk's server-verified `auth()`. Task 2. | medium |
| T-1-32 | Denial of Service | event write amplification | mitigate | The scope boundary is decided and written down: row triggers only on state-bearing tables, never on `source_records`, because Phase 3's ~10k-row ingest would write 10k events carrying full `before`/`after` payloads. Recorded in `.planning/CONVENTIONS.md` so Phase 3 does not re-derive it. Tasks 2-3. | medium |
</threat_model>

<verification>
- `pnpm verify` exits 0 (typecheck + lint + unit + db)
- `pnpm test:db` names all five new tests plus every earlier test and exits 0
- The recorded red output for four named filters appears in `01-09-SUMMARY.md` under "Watched failing first"
- Two mutation records each name the single test that went red, and `git diff --stat` is empty after both reverts
- `pnpm db:generate -- --name=noop` produces no statements
- `.planning/CONVENTIONS.md` passes the content check
</verification>

<success_criteria>
- A raw SQL insert/update on `businesses` produces an `events` row with the Clerk actor, the entity, the action, `before`/`after` and a `timestamptz`
- `update events` and `delete from events` as `authenticated` are both refused 42501 with "permission denied for table events"
- An UPDATE touching only a domain column moves `updated_at` and stamps `updated_by`
- Exactly `orgs` and `businesses` carry an `app.log_event` after-row trigger; `source_records` deliberately does not
- Every SECURITY DEFINER function pins `set search_path = public`
- `.planning/CONVENTIONS.md` records the schema contract, the event-scope boundary and the testing rules for Phases 2 and 3
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-09-SUMMARY.md` with "Watched failing first" (verbatim red output) and "Mutation checks" (both mutations, the test names that went red, the test counts, and the post-revert `git diff --stat`).
</output>
