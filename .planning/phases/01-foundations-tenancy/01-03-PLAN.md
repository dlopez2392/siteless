---
phase: 01-foundations-tenancy
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - vitest.config.ts
  - vitest.db.config.ts
  - playwright.config.ts
  - tests/unit/suite-zone.test.ts
  - tests/db/_fixtures.ts
  - tests/e2e/_required-env.ts
  - tests/e2e/auth.setup.ts
  - .github/workflows/ci.yml
  - .gitignore
autonomous: true
requirements: [FOUND-06, FOUND-02, FOUND-01]
must_haves:
  truths:
    - "The unit suite runs in UTC — deliberately NOT America/Chicago — so any code that forgot an explicit zone renders the wrong day and the Chicago assertion goes red (FOUND-06)"
    - "D-11: withRollback / actAs / actAsOwner are ported from BIS as plain `pg`, upgraded with a v1|v2 Clerk Claims union and seedTwoOrgs"
    - "D-11a: the fixture's doc comment records that a cross-org UPDATE/DELETE is filtered to 0 rows and only a foreign-org INSERT raises 42501, so no test author assumes otherwise"
    - "D-04: withRollback reads TEST_DATABASE_URL and throws on any host matching supabase, so the suite cannot reach the production project"
    - "CI runs pnpm install --frozen-lockfile on actions/setup-node@v4 (never v5) against a postgres:18 service container, and spends no paid API"
  artifacts:
    - path: "vitest.config.ts"
      provides: "TZ + locale pinned in the main process before workers spawn"
      contains: "process.env.TZ = 'UTC'"
    - path: "vitest.db.config.ts"
      provides: "Serial forked DB suite with .env.local loaded"
      contains: "singleFork"
    - path: "tests/db/_fixtures.ts"
      provides: "withRollback, actAs, actAsRole, actAsOwner, seedTwoOrgs, Claims union"
      exports: ["withRollback", "actAs", "actAsRole", "actAsOwner", "seedTwoOrgs"]
    - path: ".github/workflows/ci.yml"
      provides: "verify / db / e2e jobs with a postgres:18 service container"
      contains: "actions/setup-node@v4"
  key_links:
    - from: "tests/db/_fixtures.ts"
      to: "TEST_DATABASE_URL"
      via: "new Client({ connectionString, connectionTimeoutMillis: 10_000 })"
      pattern: "TEST_DATABASE_URL"
    - from: ".github/workflows/ci.yml"
      to: "postgres:18 service container"
      via: "TEST_DATABASE_URL job env"
      pattern: "image: postgres:18"
---

<objective>
Build the test harness every other plan in this phase writes tests into: two vitest configs (unit pinned to UTC, DB serial and forked), the BIS `withRollback`/`actAs` fixtures ported and upgraded, the Playwright config with a fail-loudly env preamble, and the three-job CI workflow.

Purpose: all nine Wave-0 gaps in `01-VALIDATION.md` are test infrastructure. Every "watch it fail first" step in plans 05, 07 and 09 depends on this existing and being trustworthy.
Output: `pnpm test:unit` green on one self-check test; `pnpm test:db` runnable; CI's `db` job red until plan 05 lands, which is expected and stated in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-RESEARCH.md
@.planning/phases/01-foundations-tenancy/01-PATTERNS.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@package.json

<interfaces>
Exported contract. Plans 04, 05, 07 and 09 import these by name. Do not rename.

tests/db/_fixtures.ts exports:
  type Claims =
    | { org_id: string; sub?: string; role?: 'authenticated' }
    | { o: { id: string; rol?: string; slg?: string }; sub?: string; role?: 'authenticated' }
  withRollback(fn: (c: Client) => Promise&lt;void&gt;): Promise&lt;void&gt;
  actAs(c: Client, claims: Claims): Promise&lt;void&gt;          // set_config(..., true) + set local role authenticated
  actAsRole(c: Client, role: 'app_user' | 'anon'): Promise&lt;void&gt;   // set local role, NO claims
  actAsOwner(c: Client): Promise&lt;void&gt;                      // reset role
  seedTwoOrgs(c: Client): Promise&lt;{ a: string; b: string }&gt;  // orgs.id uuids for clerk_org_id org_A / org_B

BIS analogs (outside this repo, at C:\Users\danlo\bis-platform — READ-ONLY, never modify, never import, never add as a dependency):
  packages\db\src\test\db.ts                 whole file, 29 lines
  packages\db\src\test\rls.test.ts           lines 9-19 (seedTwoAccounts)
  apps\web\vitest.config.ts                  30 lines (resolve.alias)
  packages\db\vitest.config.ts               23 lines (raised testTimeout + its comment)
  apps\web\playwright.config.ts              lines 37-63 (env preamble), 95-147 (config)
  apps\web\e2e\auth.setup.ts                 lines 52-82 (password-free sign-in)
  .github\workflows\ci.yml                   lines 128-139 (secrets guard), 141-148 (setup order), 229-237 (artifact upload)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: vitest configs with the timezone pinned in the main process</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 6 (lines 760-786) — `test.env.TZ` does NOT pin the zone in the threads pool, and `TZ=x pnpm test` does nothing in PowerShell
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `vitest.db.config.ts` / `vitest.config.ts` (lines 601-618) — the two-config split, the raised `testTimeout` and its comment, "do NOT port globalSetup's fixture sweep"
    - `C:\Users\danlo\bis-platform\apps\web\vitest.config.ts` (30 lines, READ-ONLY) — the `resolve.alias` `@ -> ./src`
    - `C:\Users\danlo\bis-platform\packages\db\vitest.config.ts` (23 lines, READ-ONLY) — the `testTimeout: 60000` comment about wall-clock failures under contention
    - `package.json` — `test:unit` and `test:db` already name these two files
  </read_first>
  <files>vitest.config.ts, vitest.db.config.ts, tests/unit/suite-zone.test.ts</files>
  <action>
Create `vitest.config.ts`. The two `process.env` assignments must be the first executable statements, physically above every `import`, and the pool must be `forks`:

```
// Line 1, in the MAIN process, before any worker spawns.
//
// vitest's `test.env.TZ` does NOT change the zone in the threads/vmThreads pools —
// Node worker threads lock Intl's zone at thread creation — and `TZ=x pnpm test`
// does nothing at all in PowerShell. Setting it here, plus `pool: 'forks'` so every
// worker is a child process that inherits it, is what actually pins it.
//
// Pinned to UTC, DELIBERATELY NOT America/Chicago: this dev machine IS Chicago, so a
// Chicago-pinned suite cannot discriminate. In UTC, code that forgot an explicit zone
// renders UTC and the Chicago assertion goes red. See tests/unit/suite-zone.test.ts.
process.env.TZ = 'UTC';
process.env.LANG = 'en_US.UTF-8';

import { defineConfig } from 'vitest/config';
import { fileURLToPath } from 'node:url';

export default defineConfig({
  resolve: { alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) } },
  test: {
    include: ['tests/unit/**/*.test.ts', 'tests/unit/**/*.test.tsx'],
    pool: 'forks',
    environment: 'node',
    testTimeout: 15000,
  },
});
```

Create `vitest.db.config.ts`:

```
process.env.TZ = 'UTC';
process.env.LANG = 'en_US.UTF-8';

import { config as loadEnv } from 'dotenv';
import { defineConfig } from 'vitest/config';
import { fileURLToPath } from 'node:url';

// TEST_DATABASE_URL lives in .env.local (gitignored). dotenv/config only reads .env,
// so load .env.local explicitly. In CI neither file exists and TEST_DATABASE_URL comes
// from the job env — override:false keeps CI authoritative.
loadEnv({ path: '.env.local', override: false });

export default defineConfig({
  resolve: { alias: { '@': fileURLToPath(new URL('./src', import.meta.url)) } },
  test: {
    include: ['tests/db/**/*.test.ts'],
    pool: 'forks',
    poolOptions: { forks: { singleFork: true } },
    environment: 'node',
    // 60s, not 20s. In BIS a 20s ceiling made the gate fail on wall clock
    // ("Test timed out in 20000ms") rather than on behaviour, and which file lost the
    // race moved between runs. withRollback opens a real connection per test.
    testTimeout: 60000,
    hookTimeout: 60000,
  },
});
```

`singleFork` is not an optimisation: `withRollback` opens a real `pg.Client` per test and a default `max_connections=100` is easy to exhaust when vitest fans out. Serial is also what makes a `rollback` failure attributable to one test.

Do NOT add a `globalSetup` fixture sweep. BIS needs one because its suite shares a project with production; D-04 removes the need, and a sweep that truncates would defeat `withRollback`.

Create `tests/unit/suite-zone.test.ts` so the pin asserts itself:

```
import { describe, expect, it } from 'vitest';

describe('timezone pinning', () => {
  // If this goes red, vitest.config.ts lost its line-1 assignment or the pool changed
  // back to threads. Every other FOUND-06 assertion becomes vacuous when it does.
  it('the suite runs in a zone that can discriminate', () => {
    expect(Intl.DateTimeFormat().resolvedOptions().timeZone).toBe('UTC');
  });
});
```
  </action>
  <verify>
    <automated>pnpm test:unit -t "suite runs in a zone that can discriminate"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "process.env.TZ = 'UTC'" vitest.config.ts` reports a LOWER line number than `grep -n "^import" vitest.config.ts | head -1`
    - `grep -c "pool: 'forks'" vitest.config.ts` returns 1
    - `vitest.config.ts` assigns `'UTC'` to `process.env.TZ`; the only occurrence of `America/Chicago` in the file is inside the comment explaining why the pin is not Chicago
    - `grep -c "singleFork" vitest.db.config.ts` returns 1
    - `grep -c "testTimeout: 60000" vitest.db.config.ts` returns 1
    - `grep -c "globalSetup" vitest.db.config.ts` returns 0
    - `grep -c "env.local" vitest.db.config.ts` returns 1
    - `pnpm test:unit` exits 0 AND its output prints the test name `suite runs in a zone that can discriminate` — read the NAME, because a `-t` filter that matches nothing exits green
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>The unit suite runs in UTC with a self-asserting pin, and the DB suite config is serial, forked, and loads `.env.local`.</done>
</task>

<task type="auto">
  <name>Task 2: Port withRollback / actAs / actAsOwner and add seedTwoOrgs</name>
  <read_first>
    - `C:\Users\danlo\bis-platform\packages\db\src\test\db.ts` (whole file, 29 lines, READ-ONLY) — port the shape; keep the 10-second-timeout comment
    - `C:\Users\danlo\bis-platform\packages\db\src\test\rls.test.ts` lines 9-19 (READ-ONLY) — `seedTwoAccounts`, the model for `seedTwoOrgs` minus the agency lookup
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 4 (lines 451-493) — the exact Siteless version and the three earned upgrades
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 1 (lines 713-726) — the filtered-vs-refused table that belongs in this file's doc comment
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `tests/db/_fixtures.ts` (lines 391-432) and § Shared Pattern B (lines 748-751)
  </read_first>
  <files>tests/db/_fixtures.ts</files>
  <action>
Create `tests/db/_fixtures.ts` with exactly these exports (the `<interfaces>` contract; plans 04, 05, 07 and 09 import them by name):

```
import { Client } from 'pg';

/**
 * Ten seconds, not pg's default of "forever": an unreachable host otherwise hangs every
 * test to vitest's ceiling and the suite reports a wall of timeouts instead of one
 * connection error naming the cause. (BIS: the direct db.<ref>.supabase.co address is
 * IPv6-only and GitHub-hosted runners have no IPv6.)
 *
 * TEST_DATABASE_URL, never SUPABASE_DB_URL. D-04: the cloud project
 * jahgeqshuesndyscnmjo is production only and is never a test target. A separate name
 * makes "the suite pointed at production" a typo you can see; the guard below makes it
 * one you cannot run.
 *
 * One refused statement per rolled-back transaction. A refusal aborts the transaction;
 * the NEXT statement reports 25P02 ("current transaction is aborted"), not its own
 * reason. Split refusals across tests.
 *
 * Under RLS most cross-org statements are FILTERED, not refused (executed on
 * PostgreSQL 18.3):
 *     select                              -> own rows only, no error
 *     update                              -> rowCount 0, no error
 *     delete                              -> rowCount 0, no error
 *     insert carrying a foreign org_id    -> 42501 "new row violates row-level security policy"
 *     update that MOVES org_id            -> 42501 (WITH CHECK)
 * Assert the zero-row cases with rowCount/length; assert 42501 only on the INSERT.
 */
export async function withRollback(fn: (c: Client) => Promise<void>): Promise<void> {
  const url = process.env.TEST_DATABASE_URL;
  if (!url) {
    throw new Error(
      'tests/db/_fixtures.ts: TEST_DATABASE_URL is not set. Local dev: docs/local-postgres.md. CI: the postgres:18 service container sets it.',
    );
  }
  if (/supabase\.(co|com)|pooler\.supabase/.test(url)) {
    throw new Error(
      'tests/db/_fixtures.ts: TEST_DATABASE_URL points at a Supabase host. D-04: the cloud project is production only and is never a test target.',
    );
  }
  const c = new Client({ connectionString: url, connectionTimeoutMillis: 10000 });
  await c.connect();
  try {
    await c.query('begin');
    await fn(c);
  } finally {
    await c.query('rollback');
    await c.end();
  }
}

/** Clerk session token v1 (flat org_id) and v2 (nested `o`). The suite exercises BOTH (D-11). */
export type Claims =
  | { org_id: string; sub?: string; role?: 'authenticated' }
  | { o: { id: string; rol?: string; slg?: string }; sub?: string; role?: 'authenticated' };

/** Simulate an RLS caller. `true` = set_config is transaction-LOCAL and dies with the tx (D-11b). */
export async function actAs(c: Client, claims: Claims): Promise<void> {
  await c.query("select set_config('request.jwt.claims', $1, true)", [JSON.stringify(claims)]);
  await c.query('set local role authenticated');
}

/** Become a role with NO claims — proves a connection that skipped the wrapper is refused. */
export async function actAsRole(c: Client, role: 'app_user' | 'anon'): Promise<void> {
  if (role !== 'app_user' && role !== 'anon') throw new Error('actAsRole: refusing role ' + role);
  await c.query('set local role ' + role);
}

export async function actAsOwner(c: Client): Promise<void> {
  await c.query('reset role');
}

/**
 * Two tenants, always. With one org every RLS bug is invisible: a policy that returns
 * everything and a policy that returns the caller's rows are the same result set.
 * Returns orgs.id (uuid) for clerk_org_id 'org_A' and 'org_B'.
 *
 * Call this BEFORE actAs — it inserts as the connection owner on purpose. `orgs` has no
 * insert policy for `authenticated`, because JIT provisioning goes through the
 * SECURITY DEFINER function app.ensure_org (D-03).
 */
export async function seedTwoOrgs(c: Client): Promise<{ a: string; b: string }> {
  const a = await c.query<{ id: string }>(
    "insert into orgs (clerk_org_id, name_internal, display_name) values ('org_A','Alpha (test)','Alpha') returning id",
  );
  const b = await c.query<{ id: string }>(
    "insert into orgs (clerk_org_id, name_internal, display_name) values ('org_B','Bravo (test)','Bravo') returning id",
  );
  const aid = a.rows[0]?.id;
  const bid = b.rows[0]?.id;
  if (!aid || !bid) throw new Error('seedTwoOrgs: insert returned no row');
  return { a: aid, b: bid };
}
```

The `orgs` table does not exist until plan 05. This file still typechecks now because every table reference is inside a SQL string.

The `actAsRole` signature is a closed union checked at runtime as well as at compile time — never interpolate a role that came from input. This is the same allow-list rule `src/db/with-org.ts` uses in plan 08.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; node -e "const s=require('fs').readFileSync('tests/db/_fixtures.ts','utf8');for(const n of ['withRollback','actAs','actAsRole','actAsOwner','seedTwoOrgs'])if(!s.includes('export async function '+n))throw new Error('missing export '+n);if(!s.includes('export type Claims'))throw new Error('missing Claims');if(s.includes('SUPABASE_DB_URL'))throw new Error('fixture references SUPABASE_DB_URL');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/db/_fixtures.ts` exports `withRollback`, `actAs`, `actAsRole`, `actAsOwner`, `seedTwoOrgs` and the type `Claims`
    - `grep -c 'TEST_DATABASE_URL' tests/db/_fixtures.ts` returns 2 or more and `grep -c 'SUPABASE_DB_URL' tests/db/_fixtures.ts` returns 0
    - `grep -c 'supabase' tests/db/_fixtures.ts` is at least 1 — the guard regex is present
    - `grep -c 'connectionTimeoutMillis: 10000' tests/db/_fixtures.ts` returns 1
    - `grep -c "await c.query('rollback')" tests/db/_fixtures.ts` returns 1 and it is inside a `finally` block
    - The file's header comment contains the strings `25P02`, `42501`, and `rowCount 0` (D-11a is recorded where a test author will read it)
    - The `Claims` type contains both `org_id: string` and `o: { id: string`
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Every DB test in this phase has one shared, production-proof fixture module with the filtered-vs-refused rule written where its readers are.</done>
</task>

<task type="auto">
  <name>Task 3: Playwright config, e2e auth setup, and the three-job CI workflow</name>
  <read_first>
    - `C:\Users\danlo\bis-platform\apps\web\playwright.config.ts` lines 37-63 and 95-147 (READ-ONLY) — the fail-loudly `REQUIRED_ENV` preamble and the projects/storageState shape
    - `C:\Users\danlo\bis-platform\apps\web\e2e\auth.setup.ts` lines 52-82 (READ-ONLY) — password-free sign-in via `@clerk/testing/playwright`
    - `C:\Users\danlo\bis-platform\.github\workflows\ci.yml` lines 128-139, 141-148, 229-237 (READ-ONLY) — the secrets guard, the setup ordering, the artifact upload
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Example 7 (lines 936-971) — the postgres:18 service container block
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 9 (lines 800-804) — why `actions/setup-node@v4`, not v5
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `playwright.config.ts` (lines 622-664) and § `.github/workflows/ci.yml` (lines 668-720)
  </read_first>
  <files>playwright.config.ts, tests/e2e/_required-env.ts, tests/e2e/auth.setup.ts, .github/workflows/ci.yml, .gitignore</files>
  <action>
Create `tests/e2e/_required-env.ts` — the mechanism, not the config, is the valuable part of BIS's preamble. In BIS a missing `NEXT_PUBLIC_SUPABASE_ANON_KEY` made every in-account page 500, which read on screen as an empty table, and the conclusion drawn was "main is red and the e2e gate is untrustworthy":

```
const REQUIRED_ENV = [
  'E2E_BASE_URL',
  'E2E_ADMIN_EMAIL',
  'CLERK_SECRET_KEY',
  'NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY',
] as const;

export function assertRequiredEnv(): void {
  const missing = REQUIRED_ENV.filter((k) => !process.env[k]);
  if (missing.length > 0) {
    throw new Error(
      'e2e: missing environment variable(s): ' + missing.join(', ') +
        '. Set them in .env.local (local) or repository secrets (CI). Values are never printed.',
    );
  }
}
```
It reports NAMES only, never values.

Create `playwright.config.ts`:

```
import { defineConfig, devices } from '@playwright/test';
import { config as loadEnv } from 'dotenv';
import { assertRequiredEnv } from './tests/e2e/_required-env';

loadEnv({ path: '.env.local', override: false });
assertRequiredEnv();

const AUTH_FILE = 'tests/e2e/.auth/storage-state.json';

export default defineConfig({
  testDir: './tests/e2e',
  fullyParallel: false,
  workers: 1,
  forbidOnly: !!process.env.CI,
  retries: 0,
  reporter: 'list',
  expect: { timeout: 10000 },
  use: {
    baseURL: process.env.E2E_BASE_URL,
    trace: 'retain-on-failure',
  },
  projects: [
    { name: 'setup', testMatch: /auth\.setup\.ts/ },
    {
      name: 'chromium',
      testMatch: /.*\.spec\.ts/,
      use: { ...devices['Desktop Chrome'], storageState: AUTH_FILE },
      dependencies: ['setup'],
    },
  ],
});
```
No `webServer` block: criterion 1 runs this suite against the DEPLOYED Vercel URL via `E2E_BASE_URL` (plan 11). Add `tests/e2e/.auth/` to `.gitignore`.

Create `tests/e2e/auth.setup.ts`:

```
import { clerk, clerkSetup } from '@clerk/testing/playwright';
import { test as setup } from '@playwright/test';

const AUTH_FILE = 'tests/e2e/.auth/storage-state.json';

setup('authenticate', async ({ page }) => {
  await clerkSetup();
  const email = process.env.E2E_ADMIN_EMAIL;
  if (!email) throw new Error('auth.setup: E2E_ADMIN_EMAIL is not set');
  await page.goto('/');
  await clerk.signIn({ page, signInParams: { strategy: 'email_code', identifier: email } });
  await page.context().storageState({ path: AUTH_FILE });
});
```
The Clerk instance is Email-code + Google, so there is no password to type; `@clerk/testing` mints a sign-in token through the Backend API.

Do NOT call `window.Clerk.setActive()` or add an org-selection workaround here. That is the workaround-in-the-test that kept BIS's suite green while the product was broken: a user with a valid invitation was told they had no company. If a Siteless run lands on `/no-access`, the fix is `ActivateSoleOrganization` in the app (plan 08), never `setActive` in the setup.

Create `.github/workflows/ci.yml` with three jobs. Do NOT port BIS's 60-line concurrency block — its premise ("423 live tests against the one Supabase project that also serves production") is exactly what D-04 eliminates:

```
name: CI
on:
  push: { branches: [main] }
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4            # BEFORE setup-node
      - uses: actions/setup-node@v4           # v4, NOT v5 — v5 auto-caches when
        with:                                 # packageManager is present and collides
          node-version: 24                    # with pnpm/action-setup's own caching
          cache: pnpm
      - run: pnpm install --frozen-lockfile   # never npm ci: a Windows-authored lockfile breaks it
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test:unit

  db:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:18
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: siteless_test
        ports: ['5432:5432']
        options: >-
          --health-cmd pg_isready --health-interval 10s
          --health-timeout 5s --health-retries 5
    env:
      TEST_DATABASE_URL: postgres://postgres:postgres@localhost:5432/siteless_test
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm db:migrate
      - run: pnpm test:db

  e2e:
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    env:
      E2E_BASE_URL: ${{ vars.E2E_BASE_URL }}
      E2E_ADMIN_EMAIL: ${{ secrets.E2E_ADMIN_EMAIL }}
      CLERK_SECRET_KEY: ${{ secrets.CLERK_SECRET_KEY }}
      NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - name: Check that the repository secrets are configured
        run: |
          missing=""
          for name in E2E_BASE_URL E2E_ADMIN_EMAIL CLERK_SECRET_KEY NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY; do
            if [ -z "${!name}" ]; then missing="$missing $name"; fi
          done
          if [ -n "$missing" ]; then
            echo "::error::Missing repository secret(s):$missing — the e2e gate cannot run"
            exit 1
          fi
      - run: pnpm exec playwright install --with-deps chromium
      - run: pnpm test:e2e
      - uses: actions/upload-artifact@v4
        if: failure()
        with: { name: playwright-report, path: playwright-report/, retention-days: 7 }
```

Port the secrets guard verbatim in spirit: a gate that quietly skips when it cannot run is how the pre-CI state came about. It fails loudly and names the missing secret; it never prints a value.

CI must never touch the Supabase project and never spend a paid API. The `db` job's `TEST_DATABASE_URL` is the service container only; there is no Supabase URL anywhere in this file.

State plainly in the SUMMARY: the `db` job is EXPECTED to be red until plan 05 lands its first migration, because `pnpm db:migrate` has no migrations to apply yet. Do not disable the job to make it green.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; node -e "const s=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(s.includes('setup-node@v5'))throw new Error('setup-node v5 forbidden');if(s.includes('npm ci'))throw new Error('npm ci forbidden');if(!s.includes('image: postgres:18'))throw new Error('no postgres:18 service');if(!s.includes('pnpm install --frozen-lockfile'))throw new Error('no frozen lockfile');if(/supabase/i.test(s))throw new Error('CI references supabase');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'actions/setup-node@v4' .github/workflows/ci.yml` returns 3 and `grep -c 'setup-node@v5' .github/workflows/ci.yml` returns 0
    - `grep -c 'pnpm/action-setup@v4' .github/workflows/ci.yml` returns 3, and in each job it appears on a line BEFORE the `setup-node` line
    - `grep -c 'npm ci' .github/workflows/ci.yml` returns 0; `grep -c 'pnpm install --frozen-lockfile' .github/workflows/ci.yml` returns 3
    - `grep -c 'image: postgres:18' .github/workflows/ci.yml` returns 1
    - `grep -ci 'supabase' .github/workflows/ci.yml` returns 0 (CI never reaches the production project)
    - `grep -c 'node-version: 24' .github/workflows/ci.yml` returns 3
    - `playwright.config.ts` contains `assertRequiredEnv()` and does NOT contain `webServer`
    - `tests/e2e/auth.setup.ts` does NOT contain `setActive` (`grep -c setActive tests/e2e/auth.setup.ts` returns 0)
    - `tests/e2e/_required-env.ts` reports names only: `grep -c 'process.env\[k\]' tests/e2e/_required-env.ts` returns 1 and the thrown message interpolates `missing.join` and nothing else
    - `.gitignore` contains `tests/e2e/.auth/`
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Playwright is configured to run against a deployed URL with a fail-loudly env guard, and CI has three jobs that between them run typecheck/lint/unit, the DB suite against a postgres:18 container, and e2e — with no Supabase reference and no paid API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → database | `TEST_DATABASE_URL` decides which database the RLS suite issues DDL against |
| CI runner → external services | A workflow that reaches the Supabase project or a paid API spends money and mutates production |
| CI logs → public | Anything echoed in a step is readable by anyone with repo access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-15 | Tampering / Information Disclosure | `tests/db/_fixtures.ts` | mitigate | `withRollback` throws before connecting if `TEST_DATABASE_URL` matches `/supabase\.(co\|com)\|pooler\.supabase/`, and throws a named error if it is unset. D-04 becomes unrunnable-if-violated instead of documented. Task 2. | high |
| T-1-19 | Tampering | `.github/workflows/ci.yml` | mitigate | CI is greppable-clean of `supabase` (asserted) and uses only the `postgres:18` service container, so no workflow run can reach the production project or spend a paid API. Task 3. | high |
| T-1-10 | Information Disclosure | CI logs, `tests/e2e/_required-env.ts` | mitigate | The secrets guard and the env preamble both report variable NAMES via `missing.join(', ')` and never a value; `${{ secrets.* }}` is only ever assigned to `env:`, never echoed. Task 3. | high |
| T-1-14 | Tampering (supply chain) | CI toolchain | mitigate | `actions/setup-node@v4` (asserted; v5 auto-caches on `packageManager` and collides with `pnpm/action-setup`), `pnpm/action-setup@v4` before setup-node, and `pnpm install --frozen-lockfile` rather than `npm ci`, which is a documented repeated failure from a Windows-authored lockfile. Task 3. | medium |
| T-1-20 | Repudiation | `tests/e2e/auth.setup.ts` | mitigate | No `setActive` workaround: the e2e suite must fail when the app cannot activate a sole organization, rather than papering over it the way BIS's fixture did while the product was broken. Asserted by grep. Task 3. | medium |
</threat_model>

<verification>
- `pnpm test:unit` exits 0 and names `suite runs in a zone that can discriminate` in its output
- `pnpm test:db` runs and reports no test files (plan 05 adds the first)
- `pnpm typecheck` and `pnpm lint` exit 0
- `node -e` guards in each task's `<automated>` block all print `ok`
- The SUMMARY states that CI's `db` job is expected red until plan 05
</verification>

<success_criteria>
- Two vitest configs exist, the unit one pinned to UTC in the main process with `pool: 'forks'`
- `tests/db/_fixtures.ts` exports the five functions and the `Claims` union, refuses a Supabase host, and carries the filtered-vs-refused table in its header
- `playwright.config.ts` targets `E2E_BASE_URL` with a fail-loudly preamble and no `setActive` workaround
- `.github/workflows/ci.yml` has `verify`, `db` and `e2e` jobs, uses setup-node v4, a `postgres:18` service container, `--frozen-lockfile`, and contains no reference to Supabase
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-03-SUMMARY.md`
</output>
