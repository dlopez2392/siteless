---
phase: 01-foundations-tenancy
plan: 05
type: execute
wave: 2
depends_on: [03, 04]
files_modified:
  - src/db/schema/_helpers.ts
  - src/db/schema/orgs.ts
  - src/db/schema/events.ts
  - src/db/schema/businesses.ts
  - src/db/schema/index.ts
  - drizzle/0001_tenancy.sql
  - drizzle/0002_tenancy_functions.sql
  - drizzle/0003_policies.sql
  - drizzle/0004_ensure_org.sql
  - drizzle/meta/_journal.json
  - tests/db/rls-isolation.test.ts
  - tests/db/schema-audit.test.ts
  - tests/db/ensure-org.test.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-04, FOUND-06]
must_haves:
  truths:
    - "D-01: one Siteless tenant is one Clerk organization — a flat `orgs` table keyed by a unique `clerk_org_id`, and every other table carries `org_id uuid` referencing it"
    - "D-11a: a foreign-org INSERT raises 42501 while a cross-org SELECT/UPDATE/DELETE is filtered to zero rows — criterion 2 is proven by BOTH halves, in separate transactions"
    - "D-11: the same isolation holds under the Clerk v1 flat `org_id` claim and the v2 nested `o.id` claim, because `app.current_org_id()` coalesces them"
    - "D-11b: a connection that reaches a table without `set local role authenticated` is refused 42501, because `app_user` is NOINHERIT and owns nothing"
    - "D-10: a test enumerates `pg_class` / `pg_policy` and fails on any public table lacking `org_id`, lacking RLS, or lacking a policy — with the allow-list as a Set literal in the test file so adding to it is a diff a reviewer sees"
    - "D-03: `app.ensure_org` provisions the caller's org just-in-time and refuses to provision anybody else's with 42501"
    - "FOUND-04: `legal_name`, `display_name` and `internal_notes` are three distinct columns on `businesses` and `name_internal` / `display_name` are two distinct columns on `orgs`"
    - "FOUND-06: every timestamp column in the public schema is `timestamp with time zone`"
    - "Each of the three RLS criterion tests was watched failing first, and the recorded red output names the test"
  artifacts:
    - path: "src/db/schema/_helpers.ts"
      provides: "orgScoped column spread + orgPolicies() factory — the D-01/D-07/D-10 guarantee as a compile-time artifact"
      exports: ["orgScoped", "orgPolicies", "tstz"]
    - path: "src/db/schema/orgs.ts"
      provides: "The tenant root keyed on clerk_org_id, with name_internal split from display_name"
      contains: "clerk_org_id"
    - path: "src/db/schema/events.ts"
      provides: "Append-only audit table shape (grants revoked in plan 09)"
    - path: "src/db/schema/businesses.ts"
      provides: "legal_name / display_name / internal_notes as three distinct columns"
      contains: "internal_notes"
    - path: "drizzle/0002_tenancy_functions.sql"
      provides: "app.current_org_id() with the Clerk v1|v2 coalesce, plus the authenticated table grants"
      contains: "coalesce(app.jwt()->'o'->>'id', app.jwt()->>'org_id')"
    - path: "drizzle/0004_ensure_org.sql"
      provides: "SECURITY DEFINER JIT org provisioning that validates its argument against the caller claim"
      contains: "ensure_org"
    - path: "tests/db/rls-isolation.test.ts"
      provides: "Six tests covering both halves of criterion 2 under both claim shapes"
    - path: "tests/db/schema-audit.test.ts"
      provides: "D-10 enumeration, the timestamptz enumeration, and the three-name assertion"
  key_links:
    - from: "src/db/schema/_helpers.ts"
      to: "app.current_org_id()"
      via: "pgPolicy using/withCheck wrapped in (select ...)"
      pattern: "select app\\.current_org_id\\(\\)"
    - from: "drizzle/0002_tenancy_functions.sql"
      to: "Clerk session token v2"
      via: "coalesce over the nested o.id claim"
      pattern: "'o'->>'id'"
    - from: "tests/db/rls-isolation.test.ts"
      to: "tests/db/_fixtures.ts"
      via: "withRollback / actAs / seedTwoOrgs import"
      pattern: "from './_fixtures'"
---

<objective>
Build the tenancy spine — `orgs`, `events`, `businesses` — with `org_id` on every table, RLS policies declared next to the tables in TypeScript, and the Clerk-claim resolver that works under both session-token shapes. Prove it the way CLAUDE.md demands: write the isolation tests while the tables are wide open, watch them fail, then add the policies.

Purpose: this is success criterion 2, the criterion the whole phase exists for. It is also the shape Phases 2 and 3 build on concurrently, so the columns decided here are a contract.
Output: `pnpm test:db` green on nine named tests, with the recorded red output for three of them and one recorded mutation check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-RESEARCH.md
@.planning/phases/01-foundations-tenancy/01-PATTERNS.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@tests/db/_fixtures.ts
@drizzle/0000_bootstrap.sql
@drizzle.config.ts

<interfaces>
Contract for plans 07, 08 and 09, and for Phases 2 and 3 (which run concurrently and must not re-derive these).

src/db/schema/_helpers.ts exports:
  tstz(name: string)      -> timestamp(name, { withTimezone: true, mode: 'date' })
  orgScoped               -> { id, orgId, createdAt, updatedAt, updatedBy }  spread into every org-scoped table
  orgPolicies(table)      -> four pgPolicy() entries (select/insert/update/delete) for `authenticated`

Tables created here (columns are a contract — later phases ADD, never rename):
  orgs(id uuid pk, clerk_org_id text unique not null, name_internal text not null,
       display_name text not null, timezone text not null default 'America/Chicago',
       created_at tstz, updated_at tstz, updated_by text)
  events(id bigint generated always as identity pk, org_id uuid not null -> orgs.id,
         actor_id text not null, entity_type text not null, entity_id uuid,
         action text not null, before jsonb, after jsonb, occurred_at tstz not null default now())
  businesses(...orgScoped, legal_name text, display_name text not null,
             internal_notes text, phone_e164 text, city text,
             status text not null default 'active')

SQL functions created here:
  app.current_org_id() returns uuid   SECURITY DEFINER, set search_path = public
  app.ensure_org(p_clerk_org_id text, p_display_name text) returns uuid
                                      SECURITY DEFINER, set search_path = public, granted to authenticated

Three-name rule (FOUND-04, and BIS leaked `accounts.name` to customers three times):
  orgs.name_internal      operator label, never leaves the system
  orgs.display_name       the only org name a human outside could see
  businesses.legal_name   Comptroller DBA, often mistyped
  businesses.display_name what the triage card shows
  businesses.internal_notes operator annotation, never in an export or push payload

Test names (VALIDATION.md filters on these substrings — do not reword):
  'org A sees only its own org rows under a v1 flat claim'
  'a token v2 nested claim resolves the same org as v1'
  'org A cannot INSERT into org B, and the refusal is 42501'
  'a second statement in the same aborted transaction reports 25P02'
  'a cross-org UPDATE and DELETE are filtered, not refused'
  'a connection without set local role authenticated is refused 42501'
  'every public table is org-scoped and has RLS enabled'
  'every timestamp column in public is timestamptz'
  'businesses has three distinct name fields'
  'app.ensure_org is idempotent for the caller own org'
  'app.ensure_org refuses another org with 42501'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Tenancy tables and app.current_org_id(), applied with NO policies yet</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 1 (lines 271-335) — the `orgScoped` spread, the `orgPolicies()` factory, the `(select ...)` InitPlan wrapper, and `entities.roles.provider`
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Code Examples, Example 1 (lines 812-831) — `app.current_org_id()` with the coalesce, executed against both claim shapes
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `drizzle/0001_tenancy.sql` (lines 127-172) — which BIS columns to keep, which to drop, and the `accounts.name` incident that forces the two-name split
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § "Five places BIS is the WRONG pattern" (lines 18-30) — flat tenancy, the coalesce, triggers over `emit()`
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § State of the Art (line 984) — `.enableRLS()`, NOT `.withRLS()`; `withRLS` does not exist in drizzle-orm 0.45.2 and Context7 returns v1-beta snippets
    - `C:\Users\danlo\bis-platform\packages\db\supabase\migrations\0001_tenancy.sql` lines 15-106 (READ-ONLY)
    - `drizzle/0000_bootstrap.sql` — `app.jwt()` already exists; do not redefine it
  </read_first>
  <files>src/db/schema/_helpers.ts, src/db/schema/orgs.ts, src/db/schema/events.ts, src/db/schema/businesses.ts, src/db/schema/index.ts, drizzle/0001_tenancy.sql, drizzle/0002_tenancy_functions.sql, drizzle/meta/_journal.json</files>
  <action>
Write the TypeScript schema, generate migration 0001, hand-write migration 0002, and apply both. Policies are deliberately absent — Task 2 watches the isolation tests fail against wide-open tables.

`src/db/schema/_helpers.ts`:
```
import { sql } from 'drizzle-orm';
import { pgPolicy, text, timestamp, uuid } from 'drizzle-orm/pg-core';
import { authenticatedRole } from 'drizzle-orm/supabase';
import { orgs } from './orgs';

/** timestamptz, no exceptions. mode:'date' returns a JS Date (one instant, no zone);
 *  mode:'string' hands back a session-zone-dependent string, which is how
 *  "it looked right locally" happens. */
export const tstz = (name: string) => timestamp(name, { withTimezone: true, mode: 'date' });

/** D-01 + D-07. Spread into every org-scoped table. */
export const orgScoped = {
  id: uuid('id').primaryKey().defaultRandom(),
  orgId: uuid('org_id').notNull().references(() => orgs.id),
  createdAt: tstz('created_at').notNull().defaultNow(),
  updatedAt: tstz('updated_at').notNull().defaultNow(),
  updatedBy: text('updated_by'),
};

/** Wrapped in (select ...) so Postgres runs it as an InitPlan once per statement
 *  instead of once per row. Unwrapped, app.current_org_id() is re-evaluated per row. */
const CURRENT_ORG = sql`(select app.current_org_id())`;

export function orgPolicies(t: string) {
  return [
    pgPolicy(`${t}_select`, { for: 'select', to: authenticatedRole, using: sql`org_id = ${CURRENT_ORG}` }),
    pgPolicy(`${t}_insert`, { for: 'insert', to: authenticatedRole, withCheck: sql`org_id = ${CURRENT_ORG}` }),
    pgPolicy(`${t}_update`, { for: 'update', to: authenticatedRole,
                              using: sql`org_id = ${CURRENT_ORG}`, withCheck: sql`org_id = ${CURRENT_ORG}` }),
    pgPolicy(`${t}_delete`, { for: 'delete', to: authenticatedRole, using: sql`org_id = ${CURRENT_ORG}` }),
  ];
}
```
`orgs.ts` must NOT import `_helpers.ts` — that would be a cycle. It declares its own columns.

`src/db/schema/orgs.ts` — the tenant root. D-01 is flat: no `agency_id`, no second level. BIS needs one because BIS's customers have customers; Siteless's customer is the agency itself.
```
import { pgTable, text, uuid } from 'drizzle-orm/pg-core';
import { timestamp } from 'drizzle-orm/pg-core';

const tstz = (name: string) => timestamp(name, { withTimezone: true, mode: 'date' });

export const orgs = pgTable('orgs', {
  id: uuid('id').primaryKey().defaultRandom(),
  clerkOrgId: text('clerk_org_id').notNull().unique(),
  // name_internal is the operator's label ("BIS — dogfood"). BIS's single `accounts.name`
  // column leaked to customers three times; that is the entire reason this is two columns.
  nameInternal: text('name_internal').notNull(),
  displayName: text('display_name').notNull(),
  timezone: text('timezone').notNull().default('America/Chicago'),
  createdAt: tstz('created_at').notNull().defaultNow(),
  updatedAt: tstz('updated_at').notNull().defaultNow(),
  updatedBy: text('updated_by'),
});
```
No policies yet — Task 3 adds them (and `orgs` keys on `id`, not `org_id`).

`src/db/schema/events.ts` (D-06 — the record of truth for every state change):
```
import { bigint, index, jsonb, pgTable, text, uuid } from 'drizzle-orm/pg-core';
import { orgs } from './orgs';
import { tstz } from './_helpers';

export const events = pgTable('events', {
  id: bigint('id', { mode: 'bigint' }).generatedAlwaysAsIdentity().primaryKey(),
  orgId: uuid('org_id').notNull().references(() => orgs.id),
  actorId: text('actor_id').notNull(),
  entityType: text('entity_type').notNull(),
  entityId: uuid('entity_id'),
  action: text('action').notNull(),
  before: jsonb('before'),
  after: jsonb('after'),
  occurredAt: tstz('occurred_at').notNull().defaultNow(),
}, (t) => [index('events_org_occurred_idx').on(t.orgId, t.occurredAt)]);
```

`src/db/schema/businesses.ts` — minimal for Phase 1. Phase 3 adds geometry, normalization columns and indexes; plan 07 adds the provenance FK pairs.
```
import { index, pgTable, text } from 'drizzle-orm/pg-core';
import { orgScoped } from './_helpers';

export const businesses = pgTable('businesses', {
  ...orgScoped,
  legalName: text('legal_name'),          // Comptroller DBA, often mistyped
  displayName: text('display_name').notNull(),  // what the triage card shows
  internalNotes: text('internal_notes'),  // operator annotation — never in an export or push
  phoneE164: text('phone_e164'),
  city: text('city'),
  status: text('status').notNull().default('active'),
}, (t) => [index('businesses_org_idx').on(t.orgId)]);
```
The index is not optional: an RLS predicate on `org_id` gets no index for free.

`src/db/schema/index.ts` — replace the placeholder:
```
export * from './orgs';
export * from './events';
export * from './businesses';
```

Generate and apply:
1. `pnpm db:generate -- --name=tenancy` produces `drizzle/0001_tenancy.sql`. Read it. It must create three tables, must NOT contain `drop role`, and must not enable RLS (no policies exist yet).
2. `pnpm db:custom -- --name=tenancy_functions` produces `drizzle/0002_tenancy_functions.sql`. Write:
```
-- BIS reads app.jwt()->>'org_id'. Copied verbatim that returns NULL under a Clerk v2
-- token, because v2 nests org claims under `o` and only when an organization is ACTIVE —
-- which yields zero rows and no error, the single highest-probability silent failure in
-- this build. The coalesce serves both shapes; danlo's instance emits both today.
create or replace function app.current_org_id() returns uuid
language sql stable security definer set search_path = public as $$
  select id from public.orgs
   where clerk_org_id = coalesce(app.jwt()->'o'->>'id', app.jwt()->>'org_id')
$$;
--> statement-breakpoint

-- On bare Postgres `authenticated` has no table privileges at all, so without these every
-- RLS test would fail on the GRANT layer and never reach the policy it is meant to prove.
grant select, insert, update, delete on all tables in schema public to authenticated;
--> statement-breakpoint
grant usage, select on all sequences in schema public to authenticated;
--> statement-breakpoint
alter default privileges in schema public
  grant select, insert, update, delete on tables to authenticated;
--> statement-breakpoint
alter default privileges in schema public grant usage, select on sequences to authenticated;
```
3. Run `pnpm db:migrate`. This is the BLOCKING schema push for the local/CI target — `pnpm typecheck` and `pnpm build` pass without it because Drizzle's types come from the schema file, not the live database, which is exactly how a false-positive verification state gets created.
4. Run `pnpm db:migrate` a second time to confirm it is a no-op.
  </action>
  <verify>
    <automated>pnpm db:generate -- --name=tenancy &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:migrate &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/0001_tenancy.sql` exists and contains `create table "orgs"`, `create table "events"`, `create table "businesses"`
    - `drizzle/0001_tenancy.sql` contains no `drop role` (proof that `entities.roles.provider: 'supabase'` is working)
    - `drizzle/0002_tenancy_functions.sql` contains the literal string `coalesce(app.jwt()->'o'->>'id', app.jwt()->>'org_id')`
    - `drizzle/0002_tenancy_functions.sql` contains `security definer` and `set search_path = public` on the same function
    - `pnpm db:migrate` exits 0 twice in a row
    - After migrating: `select relrowsecurity from pg_class where relname='businesses'` returns `false` — RLS is deliberately still OFF so Task 2 can watch the tests fail
    - `grep -c 'withRLS' src/db/schema/*.ts` returns 0 (it does not exist in drizzle-orm 0.45.2)
    - `grep -c 'agency' src/db/schema/*.ts` returns 0 (D-01 is flat)
    - `src/db/schema/orgs.ts` contains both `name_internal` and `display_name`
    - `src/db/schema/businesses.ts` contains all three of `legal_name`, `display_name`, `internal_notes`
    - `grep -c "withTimezone: true" src/db/schema/_helpers.ts` returns 1 and no schema file contains `timestamp(` without `withTimezone`
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Three tables and the Clerk-claim resolver are live in the local database, with RLS deliberately still off.</done>
</task>

<task type="auto">
  <name>Task 2: Write the isolation, schema-audit and ensure_org tests — and watch them fail</name>
  <read_first>
    - `tests/db/_fixtures.ts` (plan 03) — the exported names, and the filtered-vs-refused table in its header
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Code Examples, Example 3 (lines 858-885) — the executed refusal, the 25P02 follow-up, and the "watched failing first, as a plan step" paragraph
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Code Examples, Example 4 (lines 887-912) — the `pg_class`/`pg_policy` audit query, executed
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 5, the `app.ensure_org` body (lines 526-544)
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `tests/db/rls-isolation.test.ts` (lines 435-498) — copy the SQLSTATE pin from `sites-grants.test.ts:64-73`, NOT the message match in `rls.test.ts:38`
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § Shared Pattern A (lines 726-746) — the per-test SQLSTATE table; using 42501 everywhere is wrong
    - `C:\Users\danlo\bis-platform\packages\db\src\test\sites-grants.test.ts` lines 4-12 and 64-82 (READ-ONLY) — the mutation-check comment header that becomes house style
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` — the exact `-t` filter strings
  </read_first>
  <files>tests/db/rls-isolation.test.ts, tests/db/schema-audit.test.ts, tests/db/ensure-org.test.ts</files>
  <action>
Write three test files. Every test name must match the `<interfaces>` list character-for-character in the parts VALIDATION.md filters on.

`tests/db/rls-isolation.test.ts` — six tests, each in its OWN `withRollback` because one refusal aborts the transaction and the next statement reports 25P02 rather than its own reason. File header carries the mutation:
```
/**
 * Criterion 2. Two seeded orgs, always: with one org a policy that returns everything and
 * a policy that returns the caller's rows are the same result set.
 *
 * Mutation: drop the `with check` clause from the businesses_insert policy in
 * drizzle/0003_policies.sql — 'org A cannot INSERT into org B, and the refusal is 42501'
 * goes red, and only that one.
 */
```
The six tests:
1. `'org A sees only its own org rows under a v1 flat claim'` — seedTwoOrgs, insert one business per org as owner, `actAs(c, { org_id: 'org_A', sub: 'user_danlo', role: 'authenticated' })`, `select display_name from businesses`, expect exactly the org A row.
2. `'a token v2 nested claim resolves the same org as v1'` — identical, with `actAs(c, { o: { id: 'org_A' }, sub: 'user_danlo', role: 'authenticated' })`. Same single row.
3. `'org A cannot INSERT into org B, and the refusal is 42501'` — `await expect(c.query('insert into businesses (org_id, display_name) values ($1,$2)', [b, 'pwned'])).rejects.toMatchObject({ code: '42501' })`, and additionally assert the message matches `/row-level security/` — 42501 also covers "permission denied for table", and plan 09's events tests depend on telling the two apart.
4. `'a second statement in the same aborted transaction reports 25P02'` — same foreign INSERT, then a plain `select 1`, expect `{ code: '25P02' }`.
5. `'a cross-org UPDATE and DELETE are filtered, not refused'` — as org A, `update businesses set display_name='x' where org_id = $1` with org B's id, expect `rowCount === 0` and NO throw; same for `delete`. This is the half no BIS test knows.
6. `'a connection without set local role authenticated is refused 42501'` — `actAsRole(c, 'app_user')` then `select * from businesses`, expect `{ code: '42501' }` and a message matching `/permission denied/`. app_user is NOINHERIT and owns nothing, so it never silently reads every tenant.

`tests/db/schema-audit.test.ts` — three tests:
1. `'every public table is org-scoped and has RLS enabled'` — the executed Example 4 query over `pg_class`/`pg_policy`, filtering to `bad` and asserting `expect(bad).toEqual([])`. The allow-list is a `Set` literal IN THIS FILE, not a config file, so adding to it is a diff a reviewer sees:
   `const ALLOW_NO_ORG_ID = new Set(['orgs', '__drizzle_migrations']);`
2. `'every timestamp column in public is timestamptz'` — `select table_name, column_name, data_type from information_schema.columns where table_schema='public' and data_type like 'timestamp%'`, assert every `data_type` is `timestamp with time zone`. A future phase cannot slip a naked `timestamp` in.
3. `'businesses has three distinct name fields'` — assert `information_schema.columns` for `businesses` contains all of `legal_name`, `display_name`, `internal_notes` as distinct rows, and that `orgs` contains both `name_internal` and `display_name`.

`tests/db/ensure-org.test.ts` — two tests, separate transactions:
1. `'app.ensure_org is idempotent for the caller own org'` — `actAs(c, { o: { id: 'org_JIT' }, sub: 'user_danlo', role: 'authenticated' })`, call `select app.ensure_org('org_JIT','JIT Org')` twice, expect the same uuid both times and exactly one `orgs` row for `org_JIT`.
2. `'app.ensure_org refuses another org with 42501'` — as `org_JIT`, call `select app.ensure_org('org_SOMEONE_ELSE','Nope')`, expect `{ code: '42501' }`.

Now WATCH THEM FAIL. Run, in this order, and paste the verbatim output into the task notes and then into `01-05-SUMMARY.md`:
```
pnpm test:db -t "42501"
pnpm test:db -t "sees only its own org"
pnpm test:db -t "filtered, not refused"
pnpm test:db -t "every public table is org-scoped"
pnpm test:db -t "ensure_org"
```
Expected red reasons, and if a different reason appears the test is wrong, not the schema:
- `42501` — the foreign INSERT SUCCEEDS (no policy exists), so `.rejects` fails
- `sees only its own org` — the select returns BOTH orgs' rows
- `filtered, not refused` — the cross-org UPDATE reports `rowCount: 1`
- `every public table is org-scoped` — `bad` lists `events` and `businesses` with `rls_enabled: false, policy_count: 0`
- `ensure_org` — `function app.ensure_org(unknown, unknown) does not exist`

Two tests are expected GREEN already and that is correct, not a mistake:
- `a connection without set local role authenticated is refused 42501` — it is true because of the GRANT structure from migration 0000, not because of any policy
- `every timestamp column in public is timestamptz` — true from the first migration, and it exists to catch a LATER phase

🔴 Read the failing test NAME in every run. A `-t` filter that matches nothing exits green — that is a recorded BIS cost, and it is the single easiest way to fake a red-then-green record.
  </action>
  <verify>
    <automated>pnpm test:db -t "42501" ; test $? -ne 0 &amp;&amp; pnpm test:db -t "sees only its own org" ; test $? -ne 0 &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/db/rls-isolation.test.ts` contains exactly six `it(` blocks, each wrapped in its own `withRollback`
    - Test names match the `<interfaces>` list verbatim for the substrings `sees only its own org`, `token v2`, `42501`, `25P02`, `filtered, not refused`, `without set local role`
    - `grep -c "toMatchObject({ code: '42501' })" tests/db/rls-isolation.test.ts` returns 2 (the foreign INSERT and the app_user read)
    - `grep -c 'row-level security' tests/db/rls-isolation.test.ts` returns 1 and `grep -c 'permission denied' tests/db/rls-isolation.test.ts` returns 1 — the two different 42501 messages are distinguished
    - `grep -c 'rejects.toThrow()' tests/db/*.test.ts` returns 0 — a bare `toThrow()` passes on a SQL typo
    - `tests/db/schema-audit.test.ts` contains `new Set(['orgs', '__drizzle_migrations'])` as a literal in the test file
    - `tests/db/rls-isolation.test.ts` header comment names the mutation (`with check` on `businesses_insert`)
    - `pnpm test:db -t "42501"` exits NON-ZERO and the output names the test `org A cannot INSERT into org B, and the refusal is 42501`
    - `pnpm test:db -t "sees only its own org"` exits NON-ZERO and names its test
    - `pnpm test:db -t "filtered, not refused"` exits NON-ZERO and names its test
    - The verbatim red output for all five filtered runs is pasted into `01-05-SUMMARY.md` under a "Watched failing first" heading
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Eleven named tests exist, five of them are red for the right reason, and the red output is recorded where the phase gate can read it.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Add the RLS policies and app.ensure_org, turn the tests green, run the mutation check</name>
  <read_first>
    - `src/db/schema/_helpers.ts` (Task 1) — `orgPolicies()` is written and unused; this task wires it in
    - The red output recorded in Task 2 — each named test and its failure reason
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 5, `app.ensure_org` (lines 526-544) — the full SECURITY DEFINER body with the claim check
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 5 (lines 754-758) — the `(select ...)` InitPlan wrapper and the per-table `org_id` index
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Validation Architecture, Sampling Rate (line 1085) — the phase-gate mutation recipe
  </read_first>
  <files>src/db/schema/orgs.ts, src/db/schema/events.ts, src/db/schema/businesses.ts, drizzle/0003_policies.sql, drizzle/0004_ensure_org.sql, drizzle/meta/_journal.json</files>
  <action>
Attach the policies in TypeScript, generate the migration, add `app.ensure_org`, apply, and drive every named test green.

1. Wire the policies into the schema:
   - `businesses.ts` extra-config becomes `[index('businesses_org_idx').on(t.orgId), ...orgPolicies('businesses')]`
   - `events.ts` gets only two policies — select and insert — declared inline, not via `orgPolicies()`, because `events` has no legitimate UPDATE or DELETE path (D-06). Keep the existing index.
     ```
     pgPolicy('events_select', { for: 'select', to: authenticatedRole, using: sql`org_id = (select app.current_org_id())` }),
     pgPolicy('events_insert', { for: 'insert', to: authenticatedRole, withCheck: sql`org_id = (select app.current_org_id())` }),
     ```
   - `orgs.ts` gets two policies keyed on `id`, not `org_id` (it IS the tenant root):
     ```
     pgPolicy('orgs_select', { for: 'select', to: authenticatedRole, using: sql`id = (select app.current_org_id())` }),
     pgPolicy('orgs_update', { for: 'update', to: authenticatedRole,
                               using: sql`id = (select app.current_org_id())`,
                               withCheck: sql`id = (select app.current_org_id())` }),
     ```
     No INSERT policy: provisioning goes through `app.ensure_org`, so `authenticated` never gets blanket INSERT on the tenant root.
   Adding any policy auto-enables RLS in drizzle-orm 0.45.2 — do not call `.enableRLS()` as well, and do not reach for `.withRLS()`, which does not exist in this version.

2. `pnpm db:generate -- --name=policies` produces `drizzle/0003_policies.sql`. Read it and confirm it contains `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` for all three tables and eight `CREATE POLICY` statements.

3. `pnpm db:custom -- --name=ensure_org` produces `drizzle/0004_ensure_org.sql`. Write D-03's JIT provisioning:
```
-- D-03: the orgs row is provisioned on the first authenticated request carrying a Clerk
-- org claim not yet seen. SECURITY DEFINER so `authenticated` never needs blanket INSERT
-- on the tenant root, and the passed org is PROVED to be the caller's own — otherwise a
-- member of org A could provision (and then own) a row for org B.
-- set search_path = public is mandatory on a SECURITY DEFINER function: without it the
-- search_path is attacker-controlled and the body runs as the owner (ASVS V4).
create or replace function app.ensure_org(p_clerk_org_id text, p_display_name text)
returns uuid language plpgsql security definer set search_path = public as $$
declare v_id uuid; v_claim text;
begin
  v_claim := coalesce(app.jwt()->'o'->>'id', app.jwt()->>'org_id');
  if p_clerk_org_id is distinct from v_claim then
    raise exception 'ensure_org: org does not match the caller claim'
      using errcode = '42501';
  end if;
  insert into orgs (clerk_org_id, name_internal, display_name)
       values (p_clerk_org_id, p_display_name, p_display_name)
  on conflict (clerk_org_id) do update set clerk_org_id = excluded.clerk_org_id
  returning id into v_id;
  return v_id;
end $$;
--> statement-breakpoint
grant execute on function app.ensure_org(text, text) to authenticated;
```

4. `pnpm db:migrate` — the BLOCKING push. Then `pnpm db:migrate` again to confirm no-op.

5. Re-run each previously red filter and confirm it is now GREEN, reading the test NAME in the output each time:
   `pnpm test:db -t "42501"`, `-t "sees only its own org"`, `-t "filtered, not refused"`, `-t "every public table is org-scoped"`, `-t "ensure_org"`.
   Then `pnpm test:db` for the whole file set, then `pnpm verify`.

6. MUTATION CHECK — record it, because a green suite proves nothing a mutation check has not:
   - Edit `drizzle/0003_policies.sql`, delete the `WITH CHECK` clause from the `businesses_insert` policy, drop and recreate that one policy against the local database, and run `pnpm test:db`.
   - Exactly one named test must go red: `org A cannot INSERT into org B, and the refusal is 42501`. Record the name and the count.
   - Revert the file, re-apply the original policy, re-run `pnpm test:db` green, and `git diff` to prove the file is byte-identical to before.
   If more than one test goes red, or none does, the tests are wrong — fix them before moving on.
  </action>
  <verify>
    <automated>pnpm db:generate -- --name=policies &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:migrate &amp;&amp; pnpm test:db &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/0003_policies.sql` contains `ENABLE ROW LEVEL SECURITY` for `orgs`, `events` and `businesses`, and exactly 8 `CREATE POLICY` statements
    - `drizzle/0003_policies.sql` contains `(select app.current_org_id())` at least 8 times — every predicate uses the InitPlan wrapper
    - `drizzle/0003_policies.sql` contains no policy named `events_update` or `events_delete`
    - `drizzle/0004_ensure_org.sql` contains `security definer`, `set search_path = public`, `errcode = '42501'` and `on conflict (clerk_org_id)`
    - `pnpm db:migrate` exits 0 twice in a row
    - `pnpm test:db` exits 0 and its output names all eleven tests
    - `pnpm test:db -t "42501"` exits 0 and the output names `org A cannot INSERT into org B, and the refusal is 42501`
    - `pnpm verify` exits 0
    - The mutation check is recorded in `01-05-SUMMARY.md`: the mutation applied, the ONE test name that went red, the test count, and `git diff --stat` showing zero changes after the revert
    - `pnpm db:generate -- --name=noop` immediately after produces a migration with no statements (no schema drift between the TS schema and the live database) — delete that empty migration and its journal entry afterwards
  </acceptance_criteria>
  <done>Criterion 2 is proven by both halves under both Clerk claim shapes, the D-10 enumeration guards every future table, JIT org provisioning refuses to provision anybody else's org, and one recorded mutation shows the test has teeth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Clerk session token → database claims | The `request.jwt.claims` GUC is the only statement of who the caller is; everything downstream trusts it |
| tenant A → tenant B | Two orgs share one database, one connection pool and one set of tables |
| `authenticated` role → tables | RLS policies are the last thing between a signed-in user and every other tenant's rows |
| caller argument → `app.ensure_org` | A text argument decides which org row gets created and who owns it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-02 | Information Disclosure | every org-scoped table | mitigate | `orgPolicies()` puts select/insert/update/delete policies on `businesses`, two on `events` and two on `orgs`, all `to authenticated` and all predicated on `org_id = (select app.current_org_id())`. Proven by `sees only its own org` and `filtered, not refused`; guarded against future drift by the D-10 `pg_class`/`pg_policy` enumeration. Tasks 2-3. | high |
| T-1-01 | Spoofing | `app.ensure_org` | mitigate | The function raises `42501` when `p_clerk_org_id is distinct from coalesce(app.jwt()->'o'->>'id', app.jwt()->>'org_id')`, so a member of org A cannot provision a row for org B. Proven by `app.ensure_org refuses another org with 42501`. Task 3. | high |
| T-1-13 | Spoofing | `app.current_org_id()` | mitigate | The coalesce serves both the Clerk v1 flat `org_id` claim and the v2 nested `o.id` claim. Copied verbatim from BIS it returns NULL under v2 — which yields zero rows and no error, not an error anyone would notice. Both shapes are tested (`token v2`). Tasks 1-2. | high |
| T-1-03 | Elevation of Privilege | runtime connection role | mitigate | `app_user` is NOINHERIT and owns nothing, so a connection that skips `set local role authenticated` is refused `42501 permission denied for table` instead of reading every tenant. Proven by `without set local role`. Task 2. | high |
| T-1-07 | Elevation of Privilege | `app.current_org_id`, `app.ensure_org` | mitigate | Both are `SECURITY DEFINER` with `set search_path = public` pinned on the function itself. A definer function without a pinned search_path is a privilege-escalation vector (ASVS V4). Asserted by grep. Tasks 1, 3. | high |
| T-1-23 | Information Disclosure | `orgs` INSERT | mitigate | `orgs` has no INSERT policy for `authenticated`; the only creation path is the definer function that validates the claim. Asserted: `drizzle/0003_policies.sql` has no `orgs_insert`. Task 3. | medium |
| T-1-24 | Denial of Service | RLS predicate evaluation | mitigate | Every predicate is wrapped in `(select ...)` so it is an InitPlan evaluated once per statement, and every org-scoped table carries `index('<table>_org_idx').on(t.orgId)`. Tasks 1, 3. | low |
</threat_model>

<verification>
- `pnpm verify` exits 0 (typecheck + lint + unit + db)
- `pnpm test:db` names all eleven tests and exits 0
- The recorded red output for five named tests appears in `01-05-SUMMARY.md` under "Watched failing first"
- The mutation check record shows exactly one named test went red and `git diff --stat` is empty after the revert
- `pnpm db:generate -- --name=noop` produces no statements (no drift)
</verification>

<success_criteria>
- `orgs`, `events` and `businesses` exist with `org_id` (or `id` on the tenant root), RLS enabled, and at least one policy each
- A foreign-org INSERT raises 42501 with a row-level-security message; a cross-org UPDATE/DELETE reports rowCount 0; a cross-org SELECT returns only the caller's rows — under BOTH Clerk claim shapes
- A connection that skips `set local role authenticated` is refused 42501
- `app.ensure_org` is idempotent for the caller's own org and refuses any other with 42501
- Every timestamp column in `public` is `timestamp with time zone`
- `legal_name`, `display_name` and `internal_notes` are three distinct columns
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-05-SUMMARY.md` with a "Watched failing first" section (verbatim red output) and a "Mutation check" section (mutation, the one test name that went red, and the post-revert `git diff --stat`).
</output>
