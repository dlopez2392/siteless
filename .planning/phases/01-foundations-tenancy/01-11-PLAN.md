---
phase: 01-foundations-tenancy
plan: 11
type: execute
wave: 6
depends_on: [10]
files_modified:
  - .planning/phases/01-foundations-tenancy/01-VALIDATION.md
  - docs/deploy.md
autonomous: false
requirements: [FOUND-01, FOUND-02, FOUND-03, FOUND-04, FOUND-05, FOUND-06]
user_setup:
  - service: github
    why: "The CI e2e job needs the production URL, which does not exist until this plan deploys"
    env_vars:
      - name: E2E_BASE_URL
        source: "The Vercel production URL printed by this plan's deploy"
    dashboard_config:
      - task: "Set the repository variable E2E_BASE_URL to the production URL"
        location: "GitHub -> dlopez2392/siteless -> Settings -> Secrets and variables -> Actions -> Variables"
must_haves:
  truths:
    - "Criterion 1: danlo signs in with Clerk on the deployed Vercel app and the page shows his user id, his Clerk org id and the provisioned tenant uuid — every request scoped to his org"
    - "D-02: a signed-out visitor never reaches the org-scoped shell, and a session with no active organization lands on /no-access"
    - "`/api/health` on the deployed URL returns 200 with db up and proxy up — the proxy is in the right directory in production, not only locally"
    - "The e2e suite passed against the DEPLOYED URL, not a local server, and without any `setActive` workaround"
    - "All three phase-gate mutation checks are recorded across plans 05, 07 and 09, each turning exactly one named test red"
    - "`01-VALIDATION.md` per-task map is filled in and every row is green or explicitly recorded as manual"
  artifacts:
    - path: ".planning/phases/01-foundations-tenancy/01-VALIDATION.md"
      provides: "The completed per-task verification map with real statuses"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "Vercel production deployment"
      to: "Supabase production database"
      via: "SUPABASE_DB_POOL_URL as app_user, proven by /api/health db:up"
      pattern: "\"db\":\"up\""
    - from: "tests/e2e/signed-in.spec.ts"
      to: "the deployed URL"
      via: "E2E_BASE_URL"
      pattern: "E2E_BASE_URL"
---

<objective>
Deploy, prove it deployed, and close the phase: the e2e suite against the real URL, danlo signing in by hand on the real URL, and the validation map filled in with real statuses.

Purpose: criterion 1 is the only success criterion no local test can prove, and "it built" is not "it works" — BIS's recorded lesson is that a push is never assumed to have deployed, the build log names the commit, and only then does the smoke run.
Output: a live production URL, a green e2e run against it, danlo's recorded confirmation, and a completed `01-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@.planning/CONVENTIONS.md
@playwright.config.ts
@docs/deploy.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy to production, verify the deployment is THIS commit, and smoke /api/health</name>
  <read_first>
    - `docs/deploy.md` (plan 10) — the recipe and the team scope
    - `.vercelignore` (plan 10) — confirm `drizzle/` is excluded so no build step can migrate
    - `src/app/api/health/route.ts` (plan 08) — the exact response shape and what each field proves
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 7 (line 792) — `/api/health` is the canary for a proxy in the wrong directory
    - `01-10-SUMMARY.md` — the `vercel env ls` name column, to confirm all six variables are present before deploying
  </read_first>
  <files>docs/deploy.md</files>
  <action>
1. Confirm the working tree is clean and record `git rev-parse --short HEAD` and `git rev-parse --abbrev-ref HEAD`. Everything below is asserted against that sha.
2. Deploy: `vercel --prod --scope team_8zjV46sJxQDsVzikNQa1JaO2`. Record the deployment URL it prints.
3. 🔴 Never assume a push deployed. Read the build log and confirm it names the same commit sha recorded in step 1: `vercel inspect <deployment-url> --scope ...` and/or `vercel logs <deployment-url> --scope ...`. If the sha differs, a parallel session moved the tree — stop, re-read `git log`, and redeploy from the intended commit.
4. Confirm the deployment state is READY and that the build used Node 24. Record both.
5. Smoke the health route against the DEPLOYED URL:
   `curl -fsS "$DEPLOY_URL/api/health"`
   Assert: HTTP 200, and a JSON body with `"ok":true`, `"db":"up"`, `"proxy":"up"`, and a `commit` field equal to the sha from step 1.
   - `"db":"down"` means `SUPABASE_DB_POOL_URL` on Vercel is wrong — most likely the session pooler (5432) instead of the transaction pooler (6543), the `postgres.` username instead of `app_user.`, or a password that was never set. Fix the environment variable and redeploy; do not change the code.
   - `"proxy":"down"` means `clerkMiddleware()` did not run. `src/proxy.ts` must be at `src/`, level with `app/`. This is the failure whose error text points at Clerk config rather than at the file path.
6. Assert the response body leaks nothing: it must contain none of `postgres://`, `@`, `password`, `sk_`, `jahgeqshuesndyscnmjo`.
7. Also smoke the signed-out path: `curl -sS -o /dev/null -w '%{http_code} %{redirect_url}' "$DEPLOY_URL/"` — a signed-out request must NOT return the org-scoped shell. A 200 whose body contains `data-testid="org-id"` is a failure.
8. Append the production URL and the verified sha to `docs/deploy.md`.
  </action>
  <verify>
    <automated>curl -fsS "$DEPLOY_URL/api/health" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.ok!==true||j.db!=='up'||j.proxy!=='up')throw new Error('health not ok: '+s);for(const bad of ['postgres://','password','sk_','jahgeqshuesndyscnmjo'])if(s.includes(bad))throw new Error('health leaked '+bad);console.log('ok '+j.commit)})"</automated>
  </verify>
  <acceptance_criteria>
    - The deployment URL is recorded in the SUMMARY and in `docs/deploy.md`
    - The build log names the same commit sha as `git rev-parse --short HEAD` taken before the deploy (both recorded side by side)
    - The deployment state is READY and the build used Node 24 (both recorded)
    - `curl -fsS "$DEPLOY_URL/api/health"` returns HTTP 200 with `"ok":true`, `"db":"up"`, `"proxy":"up"`
    - The health body's `commit` field equals the recorded sha
    - The health body contains none of `postgres://`, `password`, `sk_`, `jahgeqshuesndyscnmjo`
    - A signed-out `GET /` does not return a body containing `data-testid="org-id"`
    - `git status --short` is clean before and after
  </acceptance_criteria>
  <done>A production deployment of a known commit is live, its database and its Clerk proxy are both up, and nothing about the connection leaks through the health route.</done>
</task>

<task type="auto">
  <name>Task 2: Run the e2e suite against the deployed URL</name>
  <read_first>
    - `playwright.config.ts` and `tests/e2e/auth.setup.ts` (plan 03) — `E2E_BASE_URL` is the only thing that points the suite at a server
    - `tests/e2e/signed-in.spec.ts` and `tests/e2e/no-access.spec.ts` (plan 08) — the `data-testid` assertions
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `playwright.config.ts` (line 664) — do not port the org-selection workaround; if a run lands on `/no-access`, the fix is in the app
    - Task 1's recorded deployment URL
  </read_first>
  <files>.env.local (gitignored - E2E_BASE_URL)</files>
  <action>
1. Set `E2E_BASE_URL` to the production URL from Task 1 (in `.env.local`, which is gitignored) and confirm `E2E_ADMIN_EMAIL` is `danlopez508@gmail.com` and `CLERK_TESTING_TOKEN` / `CLERK_SECRET_KEY` are present. `tests/e2e/_required-env.ts` will throw naming any that is missing, before a single test runs.
2. Install the browser: `pnpm exec playwright install --with-deps chromium`.
3. Run `pnpm test:e2e`. All three tests must pass:
   - `signs in and is org-scoped`
   - `no access: a signed-out visitor never reaches the org-scoped shell`
   - `no access: /no-access renders the invite-only message`
4. 🔴 If `signs in and is org-scoped` lands on `/no-access`, do NOT add `setActive` to the setup. That is the workaround-in-the-test that kept BIS's suite green while the product was broken — a user with a valid invitation was told they had no company. Diagnose in order:
   a. Does the Clerk dashboard show `danlopez508@gmail.com` as a member of exactly one organization? (Plan 02 Task 3 recorded this.)
   b. Does `ActivateSoleOrganization` run? It is a client component in the root layout — check the browser console in the Playwright trace.
   c. Is the session pending rather than signed in? `auth()` treats a pending session as signed-out by default.
   Fix the app, then re-run.
5. On failure, keep the Playwright trace (`trace: 'retain-on-failure'` is already configured) and attach the failing test NAME — not just the exit code — to the SUMMARY.
6. Record the wall-clock duration of the run. A flaky e2e is judged by wall clock and by whether the failure MOVES between tests; a real regression fails the same test every time. One passing run is evidence, not proof — run it twice and record both durations.
  </action>
  <verify>
    <automated>pnpm test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test:e2e` exits 0 and its output names all three tests as passed
    - The run is repeated and passes a second time; both wall-clock durations are recorded in the SUMMARY
    - `grep -rc 'setActive' tests/e2e/` still returns 0
    - `E2E_BASE_URL` in `.env.local` equals the production URL recorded in Task 1, and `.env.local` is still gitignored (`git status --short` does not list it)
    - The SUMMARY records the deployed URL the suite ran against — not `localhost`
  </acceptance_criteria>
  <done>The e2e suite passes twice against the real production deployment, with no workaround in the test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [danlo] Sign in on the deployed app, then close the phase gate</name>
  <read_first>
    - `.planning/ROADMAP.md` § Phase 1 Success Criteria — the five criteria this checkpoint closes
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` — the per-task map to fill in
    - `01-05-SUMMARY.md`, `01-07-SUMMARY.md`, `01-09-SUMMARY.md` — the three recorded mutation checks
    - Task 1's deployment URL and Task 2's e2e record
  </read_first>
  <files>.planning/phases/01-foundations-tenancy/01-VALIDATION.md</files>
  <action>
Present the checkpoint below to danlo and STOP. Criterion 1 is the one success criterion no automated test can close: it asks whether a human signing in with Clerk on the deployed app sees a page scoped to his org. After danlo replies, fill in `01-VALIDATION.md` (every map row gets a real Task ID, Plan and Status; the Wave 0 checklist is ticked; the Manual-Only table records plan 02's and this task's answers; frontmatter becomes `nyquist_compliant: true` and `wave_0_complete: true`; the Sign-Off checklist is ticked and `**Approval:**` records the date). Then run `pnpm verify` one final time and print `git rev-parse --abbrev-ref HEAD` and `git rev-parse --short HEAD` AFTER the run, not before - a parallel session has moved this machine's tree mid-gate before.
  </action>
  <what-built>
    The Siteless shell is deployed at the URL recorded in Task 1. The production Supabase schema carries `org_id` and RLS on every table, the retention constraints, and the event triggers. `/api/health` reports the database and the Clerk proxy both up, and the e2e suite passed twice against the real URL. What automation cannot prove is that signing in as a human works and shows the right org.
  </what-built>
  <how-to-verify>
    1. Open the production URL in a browser where you are NOT already signed in (a private window).
    2. You should be sent to `/sign-in`. Sign in with `danlopez508@gmail.com` using the email code, or with Google.
    3. After sign-in you should land on `/` and see three lines:
       - `Signed in as user_...` — your Clerk user id
       - `Org org_...` — your Clerk organization id
       - `Tenant <uuid>` — the `orgs` row provisioned just-in-time on this request
       It is unstyled. That is deliberate — the design system is Phase 2's.
    4. Confirm the "Org" line shows the organization you expect, not a blank, not "no access".
    5. Reload the page. The `Tenant` uuid must be the SAME uuid — that proves `app.ensure_org` is idempotent rather than creating a row per request.
    6. Visit `<URL>/no-access` directly. You should see "No access" and a Sign out button.
    7. Visit `<URL>/api/health`. You should see `{"ok":true,"db":"up","proxy":"up","commit":"..."}` and nothing resembling a connection string.
    8. Set the GitHub repository variable `E2E_BASE_URL` to the production URL (Repo → Settings → Secrets and variables → Actions → Variables), so CI's e2e job can run.
    9. Reply with:
       `sign-in: ok` / `org shown: <the org_ id>` / `tenant uuid stable on reload: yes` / `no-access: ok` / `health: ok` / `E2E_BASE_URL: set`
       Do not paste anything else from the page.
  </how-to-verify>
  <acceptance_criteria>
    - danlo's reply confirms sign-in succeeded on the DEPLOYED URL and the page showed a `user_...`, an `org_...` and a tenant uuid
    - danlo's reply confirms the tenant uuid was identical after a reload (JIT provisioning is idempotent, D-03)
    - danlo's reply confirms `/no-access` renders and `/api/health` reports ok
    - danlo's reply confirms the GitHub repository variable `E2E_BASE_URL` is set
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` is updated: every row in the per-task map has a real Task ID, Plan and Status (✅ or a recorded reason), the Wave 0 checklist is all ticked, the Manual-Only table records plan 02's and this task's answers, and the frontmatter reads `nyquist_compliant: true` and `wave_0_complete: true`
    - The Validation Sign-Off checklist in `01-VALIDATION.md` is fully ticked and `**Approval:**` records the date
    - `pnpm verify` exits 0 one final time on the current HEAD, and the branch + sha are printed AFTER the run (a parallel session can move the tree mid-gate and a gate can go green on the wrong commit)
    - The three phase-gate mutation checks are cross-referenced in `01-11-SUMMARY.md` by plan, mutation, and the single test name each turned red
    - `git status --short` is clean and `git log --oneline -1` matches the deployed commit
  </acceptance_criteria>
  <verify>
    <automated>pnpm verify &amp;&amp; git rev-parse --abbrev-ref HEAD &amp;&amp; git rev-parse --short HEAD &amp;&amp; grep -q 'nyquist_compliant: true' .planning/phases/01-foundations-tenancy/01-VALIDATION.md &amp;&amp; echo ok</automated>
  </verify>
  <done>danlo has signed in on the deployed URL and seen his user id, his Clerk org id and a tenant uuid stable across a reload; `01-VALIDATION.md` is complete and signed off; `pnpm verify` is green on a printed, known commit.</done>
  <resume-signal>Reply with the six-line status block. If anything is wrong, describe what you saw instead — especially if the Org line is blank or the Tenant uuid changed on reload.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → deployed app | Everything in this phase is now reachable by anyone who finds the URL |
| CI → deployed app | The e2e job signs in with a real Clerk credential held as a repository secret |
| deployment record → reality | A green build is not a working app, and a build of the wrong commit looks identical to a build of the right one |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-11 | Elevation of Privilege | deployed authorization | mitigate | Verified in production rather than only locally: `/api/health` reports `proxy:"up"` (proving `clerkMiddleware()` ran), a signed-out `GET /` is asserted not to return the org-scoped shell, and an e2e test asserts the same from a real browser. Tasks 1-2. | high |
| T-1-12 | Information Disclosure | `/api/health` in production | mitigate | The deployed response body is scanned for `postgres://`, `password`, `sk_` and the Supabase project ref; the route returns enum values plus a commit sha and never an error string. Task 1. | medium |
| T-1-34 | Spoofing | deployment provenance | mitigate | The build log is read and its commit sha compared against `git rev-parse --short HEAD` taken before the deploy, and `/api/health` echoes `VERCEL_GIT_COMMIT_SHA` so the running code identifies itself. A push is never assumed to have deployed. Task 1. | medium |
| T-1-10 | Information Disclosure | CI secrets, `.env.local` | mitigate | `E2E_BASE_URL` is a repository VARIABLE (a URL, not a secret) while the Clerk credentials are repository SECRETS; `.env.local` is asserted still gitignored; the checkpoint's resume signal asks for a status block and forbids pasting page contents. Tasks 2-3. | high |
| T-1-20 | Repudiation | e2e trustworthiness | mitigate | No `setActive` workaround (asserted zero occurrences), the suite runs against the DEPLOYED URL rather than a local server, and it is run twice with both wall-clock durations recorded — one passing run of a flaky suite is not evidence. Task 2. | medium |
| T-1-35 | Tampering | gate run against the wrong tree | mitigate | The branch and HEAD sha are printed AFTER the final `pnpm verify`, not before: a parallel session has moved this machine's tree mid-gate before, and the gate went green on the wrong commit. Task 3. | medium |
</threat_model>

<verification>
- The deployed `/api/health` returns 200 with `ok:true`, `db:up`, `proxy:up` and the expected commit sha
- `pnpm test:e2e` passes twice against the production URL, with both durations recorded
- danlo's six-line confirmation is recorded verbatim in the SUMMARY
- `01-VALIDATION.md` has every map row filled, the Wave 0 list ticked, and `nyquist_compliant: true`
- `pnpm verify` exits 0, with the branch and sha printed after the run
- All five ROADMAP success criteria are cross-referenced to the evidence that proves each one
</verification>

<success_criteria>
- Criterion 1: danlo signs in with Clerk on the deployed Vercel app and every request is scoped to his org — confirmed by hand and by an e2e run against the real URL
- Criterion 2: a foreign-org INSERT is refused 42501 and cross-org UPDATE/DELETE are filtered to zero rows, under both Clerk claim shapes, watched failing first, one refusal per transaction (plan 05, re-verified in production shape by plan 10's count comparison)
- Criterion 3: Google Places content in a durable field is refused by a database constraint (plan 07)
- Criterion 4: every state change records actor and timestamp, all `timestamptz`, rendered in `America/Chicago` with zone and locale pinned (plans 06 and 09)
- Criterion 5: `legal_name`, `display_name` and internal annotations are three fields, and a test proves the internal one cannot reach an export or push payload (plans 05 and 06)
- `01-VALIDATION.md` is complete and signed off
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-11-SUMMARY.md` containing: the deployment URL and verified sha, the deployed health response, both e2e durations, danlo's six-line confirmation verbatim, the three cross-referenced mutation checks, and a criterion-by-criterion evidence table for all five ROADMAP success criteria.
</output>
