---
phase: 01-foundations-tenancy
plan: 07
type: execute
wave: 3
depends_on: [05, 06]
files_modified:
  - src/db/schema/source-records.ts
  - src/db/schema/businesses.ts
  - src/db/schema/index.ts
  - drizzle/0005_retention.sql
  - drizzle/0006_retention_constraints.sql
  - drizzle/meta/_journal.json
  - tests/db/retention.test.ts
autonomous: true
requirements: [FOUND-05, FOUND-01, FOUND-04]
must_haves:
  truths:
    - "FOUND-05 / criterion 3: persisting Google Places content in a durable field is refused by a DATABASE CONSTRAINT, not by a code review — `source_key='google_places'` with `retention_class='durable'` raises 23514 on `sr_google_is_ephemeral`"
    - "An ephemeral source record with no `expires_at` (and a durable one with one) raises 23514 on `sr_ephemeral_has_expiry` — the TTL is not optional"
    - "A durable business field citing an ephemeral source raises 23503 on the composite FK — a field whose only source is a Google payload simply cannot be set, and the column stays NULL"
    - "D-11a: criterion 3's codes are 23514 (check) and 23503 (foreign key), NOT 42501 — the tests pin the code AND the constraint name"
    - "D-10 stays green: `source_records` carries `org_id`, RLS and four policies from the migration that creates it"
    - "D-09: every one of these constraints arrived through drizzle-kit and nothing else"
    - "The retention test was watched failing first, with the red output recorded"
  artifacts:
    - path: "src/db/schema/source-records.ts"
      provides: "The retention-class table — where the Google Maps Platform Terms live in the schema"
      contains: "retention_class"
    - path: "drizzle/0006_retention_constraints.sql"
      provides: "sr_google_is_ephemeral, sr_ephemeral_has_expiry, sr_durable_uniq and the three composite provenance FKs"
      contains: "sr_google_is_ephemeral"
    - path: "tests/db/retention.test.ts"
      provides: "Three tests pinning 23514 / 23514 / 23503 with their constraint names"
  key_links:
    - from: "src/db/schema/businesses.ts"
      to: "source_records(id, retention_class)"
      via: "composite foreign key over a generated 'durable' column"
      pattern: "businesses_[a-z_]+_src_fk"
    - from: "src/db/schema/businesses.ts"
      to: "src/lib/export/public-business.ts"
      via: "compile-time subset assertion on BusinessLike"
      pattern: "BusinessLike"
---

<objective>
Make the Google Places retention rule a property of the database rather than a convention somebody forgets in month four: a `retention_class` on every source record, a CHECK that Google content can only ever be ephemeral, a CHECK that ephemeral means a TTL, and a composite foreign key that makes "a durable field cites a durable source" impossible to violate.

Purpose: success criterion 3 says the refusal must come from a database constraint, not a code review. PROJECT.md's legal constraint — persist only `place_id`, lat/lng for 30 days and a derived boolean — is unenforceable any other way, and every later phase writes to these tables.
Output: `pnpm test:db` green on three new named tests, the red output recorded, and one mutation check showing the CHECK has teeth.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-RESEARCH.md
@.planning/phases/01-foundations-tenancy/01-PATTERNS.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@src/db/schema/_helpers.ts
@src/db/schema/businesses.ts
@src/lib/export/public-business.ts
@tests/db/_fixtures.ts

<interfaces>
Contract for Phase 3 (entity resolution) and Phase 4 (Places verifier), which run against these tables. Later phases ADD columns; they never rename these.

source_records(
  ...orgScoped,                              -- id, org_id, created_at, updated_at, updated_by
  source_key      text not null check in ('overture','tx_comptroller','osm','county_dba',
                                          'google_places','firecrawl','http_probe','dns_probe','manual'),
  external_id     text,                      -- place_id | GERS id | taxpayer number | osm id | url
  business_id     uuid null -> businesses.id,
  payload         jsonb,
  payload_hash    text,
  retention_class text not null check in ('durable','ephemeral'),
  fetched_at      timestamptz not null default now(),
  expires_at      timestamptz
)

businesses gains, per durable field:
  legal_name_source_id   uuid,  legal_name_src_ret   text generated always as ('durable') stored
  display_name_source_id uuid,  display_name_src_ret text generated always as ('durable') stored
  phone_source_id        uuid,  phone_src_ret        text generated always as ('durable') stored

Constraints added in 0006 (the whole compliance story):
  sr_ephemeral_has_expiry  check ((retention_class = 'ephemeral') = (expires_at is not null))
  sr_google_is_ephemeral   check (source_key <> 'google_places' or retention_class = 'ephemeral')
  sr_durable_uniq          unique (id, retention_class)          -- the FK target
  businesses_legal_name_src_fk   fk (legal_name_source_id, legal_name_src_ret)   -> source_records(id, retention_class)
  businesses_display_name_src_fk fk (display_name_source_id, display_name_src_ret) -> source_records(id, retention_class)
  businesses_phone_src_fk        fk (phone_source_id, phone_src_ret)             -> source_records(id, retention_class)
  index sr_expiry on source_records (expires_at) where expires_at is not null

SQLSTATE table for this plan (they differ, and using 42501 anywhere here is wrong):
  google_places + durable              -> 23514  sr_google_is_ephemeral
  ephemeral with no expires_at         -> 23514  sr_ephemeral_has_expiry
  durable with an expires_at           -> 23514  sr_ephemeral_has_expiry
  durable field citing an ephemeral src-> 23503  businesses_*_src_fk

Test names (VALIDATION.md filters on these substrings — do not reword):
  'google content cannot be durable'
  'an ephemeral source record without expires_at is refused'
  'durable cites durable: a durable field citing an ephemeral source is refused'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] source_records and the provenance columns, applied WITHOUT the retention constraints</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 2 (lines 339-378) — the full DDL, the `generatedAlwaysAs` note, and the drizzle-kit fallback
    - `.planning/research/ARCHITECTURE.md` lines 133-179 — the source-records / businesses sketch and the "durable field can only cite a durable source" paragraph
    - `.planning/research/ARCHITECTURE.md` lines 8-19 — the legal reasoning: `place_id` is the only field exempt from the caching restriction; lat/lng for 30 days; the canonical record must come from non-Google data
    - `src/db/schema/_helpers.ts` (plan 05) — `orgScoped`, `orgPolicies`, `tstz`
    - `src/db/schema/businesses.ts` (plan 05) — the existing columns and its extra-config array
    - `src/lib/export/public-business.ts` (plan 06) — `BusinessLike`, which this task bridges to the real row type
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Assumptions Log A4 (line 1158) — drizzle-kit's differ and a generated column inside a composite FK is the one unverified step
  </read_first>
  <files>src/db/schema/source-records.ts, src/db/schema/businesses.ts, src/db/schema/index.ts, drizzle/0005_retention.sql, drizzle/meta/_journal.json</files>
  <action>
Create the table and the provenance columns, but NOT the three constraints that make criterion 3 true — Task 2 watches the tests fail against them.

`src/db/schema/source-records.ts`:
```
import { sql } from 'drizzle-orm';
import { check, index, jsonb, pgTable, text, uuid } from 'drizzle-orm/pg-core';
import { orgPolicies, orgScoped, tstz } from './_helpers';
import { businesses } from './businesses';

/**
 * Where the Google Maps Platform Terms live in the schema.
 *
 * Places responses may not become the durable record: place_id is the only field exempt
 * from the caching restriction, lat/lng may be cached for 30 consecutive days, and
 * everything else must go. retention_class turns that from a convention somebody forgets
 * in month four into an invariant the database refuses to break. The constraints that
 * enforce it arrive in migration 0006.
 */
export const sourceRecords = pgTable('source_records', {
  ...orgScoped,
  sourceKey: text('source_key').notNull(),
  externalId: text('external_id'),
  businessId: uuid('business_id').references(() => businesses.id),
  payload: jsonb('payload'),
  payloadHash: text('payload_hash'),
  retentionClass: text('retention_class').notNull(),
  fetchedAt: tstz('fetched_at').notNull().defaultNow(),
  expiresAt: tstz('expires_at'),
}, (t) => [
  index('source_records_org_idx').on(t.orgId),
  check('sr_source_key_known', sql`source_key in ('overture','tx_comptroller','osm','county_dba','google_places','firecrawl','http_probe','dns_probe','manual')`),
  check('sr_retention_class_known', sql`retention_class in ('durable','ephemeral')`),
  ...orgPolicies('source_records'),
]);
```
The policies and the `org_id` index ship WITH the table, not later: the D-10 enumeration test from plan 05 is already green and must stay green, so a table can never exist un-scoped even for one commit.

Extend `src/db/schema/businesses.ts` with three provenance pairs (leave the existing columns, index and policies alone):
```
  legalNameSourceId: uuid('legal_name_source_id'),
  legalNameSrcRet: text('legal_name_src_ret').generatedAlwaysAs(sql`'durable'`),
  displayNameSourceId: uuid('display_name_source_id'),
  displayNameSrcRet: text('display_name_src_ret').generatedAlwaysAs(sql`'durable'`),
  phoneSourceId: uuid('phone_source_id'),
  phoneSrcRet: text('phone_src_ret').generatedAlwaysAs(sql`'durable'`),
```
and add the compile-time bridge to the FOUND-04 sentinel at the bottom of the same file:
```
import type { BusinessLike } from '@/lib/export/public-business';

/**
 * Compile-time bridge. tests/unit/no-internal-leak.test.ts scans a structural fixture
 * rather than the Drizzle row, so that the sentinel stays a pure unit test with no
 * database in its import graph. This assertion is what stops the two drifting: rename a
 * column here and tsc fails, instead of the sentinel quietly scanning a stale shape.
 */
type BusinessLikeIsSubset =
  BusinessLike extends Pick<typeof businesses.$inferSelect, keyof BusinessLike> ? true : never;
export const businessLikeBridge: BusinessLikeIsSubset = true;
```

Add both tables to `src/db/schema/index.ts`.

Generate and apply:
1. `pnpm db:generate -- --name=retention` produces `drizzle/0005_retention.sql`. READ IT. It must create `source_records` with its two enum CHECKs, four policies and the org index, and must add six columns to `businesses`. Confirm each generated column is emitted as `GENERATED ALWAYS AS ('durable') STORED`.
2. If drizzle-kit does NOT emit the generated columns correctly (assumption A4), keep the TS declarations and add the three `ALTER TABLE ... ADD COLUMN ... GENERATED ALWAYS AS ('durable') STORED` statements to migration 0006 in Task 3 instead. Record which path was taken in the SUMMARY.
3. `pnpm db:migrate` — the BLOCKING push. Then run it again to confirm no-op.
4. Confirm the D-10 audit is still green: `pnpm test:db -t "every public table is org-scoped"`.
  </action>
  <verify>
    <automated>pnpm db:generate -- --name=retention &amp;&amp; pnpm db:migrate &amp;&amp; pnpm db:migrate &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test:db -t "every public table is org-scoped"</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/0005_retention.sql` contains `create table "source_records"` and four `CREATE POLICY` statements naming `source_records`
    - `drizzle/0005_retention.sql` contains `GENERATED ALWAYS AS ('durable') STORED` three times, OR the SUMMARY records that assumption A4 broke and the statements moved to 0006
    - Ignoring comments, `drizzle/0005_retention.sql` does NOT contain `sr_google_is_ephemeral`, `sr_ephemeral_has_expiry`, or `sr_durable_uniq` — those are Task 3's, after the tests are red
    - `pnpm db:migrate` exits 0 twice in a row
    - `pnpm test:db -t "every public table is org-scoped"` exits 0 and names its test — `source_records` never existed without RLS
    - `select relrowsecurity from pg_class where relname='source_records'` returns `true`
    - `src/db/schema/businesses.ts` contains `businessLikeBridge`
    - `grep -c 'timestamp(' src/db/schema/source-records.ts` returns 0 — every timestamp goes through `tstz`
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>`source_records` and the three provenance column pairs exist, org-scoped and RLS-enabled from birth, with the retention constraints deliberately still missing.</done>
</task>

<task type="auto">
  <name>Task 2: Write the retention tests — and watch them fail</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Code Examples, Example 2 (lines 842-856) — the executed refusal transcript and the line that criterion 3 pins 23514 and 23503, not 42501
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § Shared Pattern A (lines 726-746) — the per-test SQLSTATE table, and "assert the constraint NAME as well as the code — the name is the thing a future migration can rename out from under the test"
    - `tests/db/_fixtures.ts` — `withRollback`, `actAs`, `seedTwoOrgs`; one refused statement per transaction
    - `tests/db/rls-isolation.test.ts` (plan 05) — the house style for a mutation-naming header
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` — the exact `-t` filter strings
  </read_first>
  <files>tests/db/retention.test.ts</files>
  <action>
Create `tests/db/retention.test.ts`. Header first:
```
/**
 * Criterion 3: "An attempt to persist Google Places content into a durable field is
 * refused by a database constraint, not by a code review."
 *
 * The codes here are NOT 42501. A CHECK violation is 23514 and a foreign-key violation is
 * 23503; 42501 is the RLS/grant family and appears nowhere in this file. Pin the constraint
 * NAME as well as the code — the name is the thing a future migration can rename out from
 * under the test.
 *
 * Mutation: drop constraint sr_google_is_ephemeral from source_records —
 * 'google content cannot be durable' goes red, and only that one.
 */
```
Four tests, each in its own `withRollback` because one refusal aborts the transaction:

1. `'google content cannot be durable'`
   - `seedTwoOrgs`, `actAs(c, { o: { id: 'org_A' }, sub: 'user_danlo', role: 'authenticated' })`
   - insert `source_records (org_id, source_key, retention_class, expires_at)` with `('google_places','durable', null)`
   - `rejects.toMatchObject({ code: '23514', constraint: 'sr_google_is_ephemeral' })`

2. `'an ephemeral source record without expires_at is refused'`
   - insert `('overture','ephemeral', null)`
   - `rejects.toMatchObject({ code: '23514', constraint: 'sr_ephemeral_has_expiry' })`

3. `'a durable source record with an expires_at is refused'`
   - insert `('overture','durable', now() + interval '21 days')`
   - `rejects.toMatchObject({ code: '23514', constraint: 'sr_ephemeral_has_expiry' })` — the CHECK is an equivalence, both directions

4. `'durable cites durable: a durable field citing an ephemeral source is refused'`
   - insert an EPHEMERAL google source with `expires_at = now() + interval '21 days'`, capture its id
   - insert a `businesses` row with `phone_source_id` set to that id
   - `rejects.toMatchObject({ code: '23503', constraint: 'businesses_phone_src_fk' })`
   - then, in a SEPARATE `withRollback`, the positive control: insert a DURABLE overture source, cite it from `phone_source_id`, and assert the insert SUCCEEDS. A refusal test with no positive control passes when everything is broken.

Add a fifth test asserting the retention TTL index exists, because Phase 4's purge job depends on it:
`'source_records has a partial index on expires_at'` — query `pg_indexes` for `indexname = 'sr_expiry'` and assert one row. Expected RED now, green after Task 3.

WATCH THEM FAIL. Run and paste the verbatim output into the SUMMARY:
```
pnpm test:db -t "google content cannot be durable"
pnpm test:db -t "ephemeral"
pnpm test:db -t "durable cites durable"
pnpm test:db -t "partial index on expires_at"
```
Expected red reasons — a different reason means the test is wrong, not the schema:
- `google content cannot be durable` — the insert SUCCEEDS, so `.rejects` fails
- `ephemeral` — both inserts succeed
- `durable cites durable` — the business insert citing an ephemeral source SUCCEEDS (there is no FK yet); the positive control passes already, which is correct
- `partial index on expires_at` — `pg_indexes` returns zero rows

🔴 Read the failing test NAME in every run. A `-t` filter matching nothing exits green.
  </action>
  <verify>
    <automated>pnpm test:db -t "google content cannot be durable" ; test $? -ne 0 &amp;&amp; pnpm test:db -t "durable cites durable" ; test $? -ne 0 &amp;&amp; echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/db/retention.test.ts` contains five `it(` blocks plus the separate positive-control block, each wrapped in its own `withRollback`
    - `grep -c "code: '23514'" tests/db/retention.test.ts` returns 3
    - `grep -c "code: '23503'" tests/db/retention.test.ts` returns 1
    - `grep -c "42501" tests/db/retention.test.ts` returns 0
    - `grep -c "constraint: 'sr_google_is_ephemeral'" tests/db/retention.test.ts` returns 1, and every `toMatchObject` with a `code` also carries a `constraint`
    - The file header names the mutation (drop `sr_google_is_ephemeral`)
    - `pnpm test:db -t "google content cannot be durable"` exits NON-ZERO and names its test
    - `pnpm test:db -t "durable cites durable"` exits NON-ZERO and names its test
    - The verbatim red output for all four filtered runs is pasted into `01-07-SUMMARY.md` under "Watched failing first"
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Five named tests exist, four of them red for exactly the right reason, and the red output is recorded where the phase gate can read it.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Add the retention constraints, turn the tests green, run the mutation check</name>
  <read_first>
    - The red output recorded in Task 2 — each named test and its failure reason
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 2 (lines 341-378) — the exact SQL for all three constraints, the FK target unique, and the TTL index
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 8 (lines 794-798) — `--> statement-breakpoint` between every statement
    - `.planning/research/ARCHITECTURE.md` line 158 — the TTL window is 21 days, not 30, so a missed purge run is not a breach; the purge job itself is Phase 4
    - `drizzle/0005_retention.sql` (Task 1) — check whether the generated columns landed, per assumption A4
  </read_first>
  <files>drizzle/0006_retention_constraints.sql, drizzle/meta/_journal.json</files>
  <action>
1. `pnpm db:custom -- --name=retention_constraints` produces `drizzle/0006_retention_constraints.sql`. Write:
```
-- FOUND-05 / criterion 3. Six lines that are the entire compliance story: a durable field
-- can only cite a durable source record. If the only thing that knows this roofer's phone
-- number is a Google payload, the column stays NULL and the UI says "not stored".

alter table source_records add constraint sr_ephemeral_has_expiry
  check ((retention_class = 'ephemeral') = (expires_at is not null));
--> statement-breakpoint

-- Google Places content may never be the durable record. place_id is the only field
-- exempt from the caching restriction; lat/lng may be cached 30 days; everything else
-- goes. This CHECK is what makes that a property of the table rather than a convention.
alter table source_records add constraint sr_google_is_ephemeral
  check (source_key <> 'google_places' or retention_class = 'ephemeral');
--> statement-breakpoint

-- The composite FK target. A plain FK to source_records(id) could cite anything; this
-- pair makes the retention class part of the reference.
alter table source_records add constraint sr_durable_uniq unique (id, retention_class);
--> statement-breakpoint

alter table businesses add constraint businesses_legal_name_src_fk
  foreign key (legal_name_source_id, legal_name_src_ret)
  references source_records (id, retention_class);
--> statement-breakpoint
alter table businesses add constraint businesses_display_name_src_fk
  foreign key (display_name_source_id, display_name_src_ret)
  references source_records (id, retention_class);
--> statement-breakpoint
alter table businesses add constraint businesses_phone_src_fk
  foreign key (phone_source_id, phone_src_ret)
  references source_records (id, retention_class);
--> statement-breakpoint

-- Phase 4's purge job deletes where expires_at < now(). TTL is 21 days, not 30, so a
-- missed run is not a breach. The index ships now; the job is Phase 4's.
create index sr_expiry on source_records (expires_at) where expires_at is not null;
```
If assumption A4 broke in Task 1 (drizzle-kit did not emit the generated columns), prepend the three `ALTER TABLE businesses ADD COLUMN ..._src_ret text GENERATED ALWAYS AS ('durable') STORED` statements here, each with its own breakpoint.

2. `pnpm db:migrate` — the BLOCKING push. Then again, to confirm no-op.

3. Re-run each previously red filter and confirm GREEN, reading the NAME each time:
   `-t "google content cannot be durable"`, `-t "ephemeral"`, `-t "durable cites durable"`, `-t "partial index on expires_at"`.
   Then `pnpm test:db`, then `pnpm verify`.

4. Confirm no schema drift: `pnpm db:generate -- --name=noop` must produce a migration with no statements. Delete that empty migration and its journal entry. If it is NOT empty, the TS schema and the live database disagree — reconcile before finishing, because Phase 3 generates on top of this.

5. MUTATION CHECK, recorded:
   - Against the local database, `alter table source_records drop constraint sr_google_is_ephemeral`, then `pnpm test:db`.
   - Exactly one named test must go red: `google content cannot be durable`. Record the name and the total test count.
   - Restore it with the exact statement from 0006, re-run `pnpm test:db` green, and confirm `git diff --stat` shows no change to any file (the mutation was applied to the database, not to the migration, precisely so the revert is provable).
   If more than one test goes red, or none does, the tests are wrong — fix them before finishing.
  </action>
  <verify>
    <automated>pnpm db:migrate &amp;&amp; pnpm db:migrate &amp;&amp; pnpm test:db &amp;&amp; pnpm verify</automated>
  </verify>
  <acceptance_criteria>
    - `drizzle/0006_retention_constraints.sql` contains all of `sr_ephemeral_has_expiry`, `sr_google_is_ephemeral`, `sr_durable_uniq`, `businesses_legal_name_src_fk`, `businesses_display_name_src_fk`, `businesses_phone_src_fk`, `create index sr_expiry`
    - The file contains at least 7 `--> statement-breakpoint` lines
    - `pnpm db:migrate` exits 0 twice in a row
    - `pnpm test:db` exits 0 and its output names all five retention tests plus every test from plans 05 and 06
    - `pnpm test:db -t "google content cannot be durable"` exits 0 and names its test
    - `pnpm verify` exits 0
    - `pnpm db:generate -- --name=noop` produces a migration with zero statements (no drift); the empty file and its journal entry are deleted afterwards
    - `select count(*) from pg_constraint where conname in ('sr_google_is_ephemeral','sr_ephemeral_has_expiry','sr_durable_uniq','businesses_legal_name_src_fk','businesses_display_name_src_fk','businesses_phone_src_fk')` returns 6
    - The mutation check is recorded in `01-07-SUMMARY.md`: the dropped constraint, the ONE test name that went red, the total test count, and the confirmation that `git diff --stat` is empty
  </acceptance_criteria>
  <done>Criterion 3 holds: the database refuses Google Places content in a durable field with 23514, refuses a durable field citing an ephemeral source with 23503, and a recorded mutation proves the test would notice if it stopped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Places API → durable storage | Maps Platform Terms forbid Places content becoming a business-listings database; only `place_id` is exempt indefinitely and lat/lng for 30 days |
| source record → canonical business field | A `businesses` column citing a source record inherits that source's legal status |
| future phase → these tables | Phases 3, 4 and 5 write here; anything the schema does not forbid, one of them will eventually do |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-08 | Information Disclosure (legal / retention) | `source_records` | mitigate | `sr_google_is_ephemeral` makes `source_key='google_places'` with `retention_class='durable'` a 23514 CHECK violation, and `sr_ephemeral_has_expiry` makes an ephemeral record without a TTL impossible. Criterion 3 requires a database constraint and not a code review; these are it. Tasks 2-3, with a recorded mutation check. | high |
| T-1-27 | Tampering | `businesses` provenance | mitigate | The composite FK over a `GENERATED ALWAYS AS ('durable') STORED` column means a durable field can only reference a durable source record — a Google-only phone number cannot be written at all, so the column stays NULL rather than silently becoming durable. Proven by the 23503 test plus a positive control. Task 3. | high |
| T-1-02 | Information Disclosure | `source_records` RLS | mitigate | The table ships with `org_id`, RLS and four `orgPolicies('source_records')` in the SAME migration that creates it, so it never exists un-scoped even for one commit; the plan-05 D-10 enumeration is re-run as an acceptance criterion in Task 1. | high |
| T-1-28 | Repudiation | constraint naming | mitigate | Every retention test pins the constraint NAME alongside the SQLSTATE, so a future migration that renames a constraint out from under the test fails loudly rather than passing on a coincidental code match. Task 2. | medium |
| T-1-29 | Denial of Service | TTL purge | accept | The `sr_expiry` partial index ships now but the purge job itself is Phase 4's. Until then no Places data exists to expire, because no Places call has been made — the retention exposure is zero for this phase. | low |
</threat_model>

<verification>
- `pnpm verify` exits 0
- `pnpm test:db` names all five retention tests and exits 0
- The recorded red output for four named tests appears in `01-07-SUMMARY.md` under "Watched failing first"
- The mutation record shows exactly one named test went red and `git diff --stat` is empty after restoring the constraint
- `pnpm db:generate -- --name=noop` produces no statements
</verification>

<success_criteria>
- `source_records` exists, org-scoped, RLS-enabled, with both enum CHECKs and the retention CHECKs
- Google content marked durable is refused 23514 on `sr_google_is_ephemeral`
- An ephemeral record with no TTL, and a durable record with one, are both refused 23514 on `sr_ephemeral_has_expiry`
- A `businesses` provenance column citing an ephemeral source is refused 23503; citing a durable source succeeds
- The `sr_expiry` partial index exists for Phase 4's purge
- `businessLikeBridge` compiles, tying the FOUND-04 sentinel's fixture shape to the real row type
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-07-SUMMARY.md` with "Watched failing first" (verbatim red output), "Mutation check" (constraint dropped, the one red test name, post-restore `git diff --stat`), and a note on whether assumption A4 held.
</output>
