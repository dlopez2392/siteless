---
phase: 01-foundations-tenancy
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - .env.example
  - docs/local-postgres.md
autonomous: false
requirements: [FOUND-01, FOUND-02]
user_setup:
  - service: postgresql-18-local
    why: "D-05a — Docker Desktop and WSL are both absent on this machine, so the RLS/grant/constraint suite runs against a native PostgreSQL 18 for Windows install. Tests never touch the Supabase project."
    env_vars:
      - name: TEST_DATABASE_URL
        source: "Local PostgreSQL 18 install — postgres://postgres:<password>@localhost:5432/siteless_test"
      - name: SUPABASE_DB_POOL_URL
        source: "Local dev value only: postgres://app_user:app_user@localhost:5432/siteless_test (the production value is set on Vercel in plan 10)"
    dashboard_config:
      - task: "Install PostgreSQL 18 for Windows with the contrib extensions, then create the siteless_test database"
        location: "EDB installer — https://www.postgresql.org/download/windows/"
  - service: supabase
    why: "drizzle-kit needs the session-pooler URL to migrate the production schema in plan 10"
    env_vars:
      - name: SUPABASE_DB_URL
        source: "Supabase Dashboard -> Project siteless -> Connect -> Session pooler (port 5432, user postgres.jahgeqshuesndyscnmjo)"
    dashboard_config:
      - task: "Confirm Clerk third-party auth is ENABLED with domain equipped-newt-5148.clerk.accounts.dev"
        location: "Supabase Dashboard -> Authentication -> Sign In / Providers -> Third-Party Auth"
  - service: clerk
    why: "D-02 is invite-only; if organization creation is on, a Google sign-in can mint an empty tenant"
    env_vars: []
    dashboard_config:
      - task: "Confirm 'Membership required' is ON and 'Users can create organizations' is OFF"
        location: "Clerk Dashboard -> Siteless -> Configure -> Organizations"
must_haves:
  truths:
    - "D-05a: a native PostgreSQL 18 server is running on this machine with a `siteless_test` database, and `TEST_DATABASE_URL` in `.env.local` points at it"
    - "D-04: `TEST_DATABASE_URL` is not a Supabase host — the production project `jahgeqshuesndyscnmjo` is never a test target"
    - "D-05: the ~$10/mo second-Supabase-project fallback is NOT used; the native install supersedes it"
    - "`SUPABASE_DB_URL` (session pooler, :5432) exists in `.env.local` for drizzle-kit, and is distinct from `SUPABASE_DB_POOL_URL` (runtime)"
    - "D-02: Clerk 'users can create organizations' is confirmed OFF and 'Membership required' is ON, so no signed-in user can mint an empty tenant"
    - "No secret value appears in any committed file"
  artifacts:
    - path: ".env.example"
      provides: "The complete list of variable NAMES this phase needs, with no values"
      contains: "TEST_DATABASE_URL"
    - path: "docs/local-postgres.md"
      provides: "The exact install + database-creation + connection-string recipe for PostgreSQL 18 on Windows"
      contains: "siteless_test"
  key_links:
    - from: ".env.local"
      to: "local PostgreSQL 18"
      via: "TEST_DATABASE_URL"
      pattern: "localhost:5432/siteless_test"
---

<objective>
Stand up the one piece of this phase Claude cannot do: a local PostgreSQL 18 server for the RLS/grant/constraint suite, plus the two Supabase connection strings and the two dashboard settings that must be confirmed by eye.

Purpose: D-04 forbids testing against the production Supabase project, and D-05a (decided 2026-09-21 after Docker Desktop and WSL were both verified absent) names a native PostgreSQL 18 install as the replacement. Every DB test in plans 04, 05, 07 and 09 reads `TEST_DATABASE_URL`; nothing runs until this exists.
Output: a documented local database, `.env.example` in the repo, and four confirmed dashboard/env facts recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .env.example and docs/local-postgres.md</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Environment Availability (lines 994-1034) — the probed machine state, the three DB URLs and their exact pooler modes/ports/users, and the IPv6 warning about `db.<ref>.supabase.co`
    - `.planning/phases/01-foundations-tenancy/01-CONTEXT.md` § D-04 / D-05 / D-05a — why the local database exists and what it must never be
    - `.gitignore` — confirm `!.env.example` is already there so the file you write is actually committable
    - `.env.local` — read ONLY to list which names already exist. Copy no value into any file.
  </read_first>
  <files>.env.example, docs/local-postgres.md</files>
  <action>
Create `.env.example` at the repo root. Names and comments only — never a value, never a placeholder that looks like a value:

```
# Siteless — environment variable NAMES. Copy to .env.local and fill in.
# .env.local is gitignored. Never commit a value.

# --- Clerk (dev instance: equipped-newt-5148.clerk.accounts.dev) ---
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
NEXT_PUBLIC_CLERK_SIGN_IN_URL=
CLERK_FRONTEND_API_URL=
CLERK_JWKS_URL=

# --- Supabase project `siteless` (ref jahgeqshuesndyscnmjo) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Migrations ONLY. Shared pooler, SESSION mode, port 5432, user postgres.jahgeqshuesndyscnmjo.
# Used by `pnpm db:migrate:prod`. Never by the runtime, never by a test.
# Do NOT use the direct db.<ref>.supabase.co:5432 address — it is IPv6-only without the
# IPv4 add-on and GitHub-hosted runners have no IPv6.
SUPABASE_DB_URL=

# Runtime ONLY. Production: shared pooler, TRANSACTION mode, port 6543, user
# app_user.jahgeqshuesndyscnmjo, driver opens with prepare:false.
# Locally: point it at the local Postgres as app_user —
#   postgres://app_user:app_user@localhost:5432/siteless_test
SUPABASE_DB_POOL_URL=

# Tests and local migrations ONLY. Local PostgreSQL 18, as the owner role.
# 🔴 NEVER a *.supabase.co / *.supabase.com / *.pooler.supabase.com host. D-04.
TEST_DATABASE_URL=

# Playwright (plan 03 / plan 11)
E2E_BASE_URL=
E2E_ADMIN_EMAIL=
CLERK_TESTING_TOKEN=
```

Create `docs/local-postgres.md` with the exact recipe below. Write it as instructions danlo follows, not as prose:

1. **Install** — download the PostgreSQL 18 Windows installer from https://www.postgresql.org/download/windows/ (EDB). During setup: keep **PostgreSQL Server**, **Command Line Tools** and **Stack Builder** checked; port `5432`; locale default; choose a superuser password and keep it. No reboot is needed.
2. **Confirm** — open a new PowerShell (so `PATH` is refreshed) and run `psql --version`; it must print `psql (PostgreSQL) 18.x`. If `psql` is not found, add `C:\Program Files\PostgreSQL\18\bin` to `PATH`.
3. **Create the test database** — `psql -U postgres -c "create database siteless_test"`.
4. **Set `.env.local`** — add:
   - `TEST_DATABASE_URL=postgres://postgres:<the superuser password>@localhost:5432/siteless_test`
   - `SUPABASE_DB_POOL_URL=postgres://app_user:app_user@localhost:5432/siteless_test`
     (the `app_user` role and its local password are created by `pnpm db:migrate` in plan 04 — this line is correct before the role exists, it just cannot connect yet)
5. **Why not Docker** — `docker` resolves nowhere on this machine and `wsl --status` reports "not installed" (verified 2026-09-20). D-05a replaces D-04's `supabase start` path with this install plus `drizzle/0000_bootstrap.sql`, which creates the `anon` / `authenticated` / `service_role` / `app_user` roles and the `app` schema so a bare Postgres looks like Supabase. CI's `postgres:18` service container runs the identical bootstrap, which is what makes dev and CI byte-identical.
6. **What this database must never be** — `TEST_DATABASE_URL` pointing at `jahgeqshuesndyscnmjo` would run the RLS suite against production. `scripts/db.ts` (plan 04) refuses any `--target=test` URL whose host matches `supabase`.

Also add a "Do not install" note: do NOT install Supabase CLI or run `supabase db push` in this repo — drizzle-kit is the single migration authority (D-09).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const e=fs.readFileSync('.env.example','utf8');for(const k of ['TEST_DATABASE_URL','SUPABASE_DB_URL','SUPABASE_DB_POOL_URL','CLERK_SECRET_KEY','NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY'])if(!e.includes(k+'='))throw new Error('missing '+k);if(/=\s*\S/.test(e.split('\n').filter(l=>!l.startsWith('#')).join('\n')))throw new Error('.env.example has a value after an =');const d=fs.readFileSync('docs/local-postgres.md','utf8');if(!d.includes('siteless_test'))throw new Error('no siteless_test');if(!/PostgreSQL 18|postgresql-18|18\.x/.test(d))throw new Error('no PG18 reference');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains all of: `TEST_DATABASE_URL=`, `SUPABASE_DB_URL=`, `SUPABASE_DB_POOL_URL=`, `CLERK_SECRET_KEY=`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=`, `E2E_BASE_URL=`
    - Every non-comment line in `.env.example` ends at the `=` — no characters follow it (enforced by the verify script)
    - `docs/local-postgres.md` contains the literal strings `siteless_test`, `create database siteless_test`, and `app_user`
    - `docs/local-postgres.md` contains a "Do not install Supabase CLI" note referencing D-09
    - `git check-ignore -q .env.example` exits non-zero (the file IS committable) and `git check-ignore -q .env.local` exits 0 (still ignored)
  </acceptance_criteria>
  <done>The repo documents every variable name and the exact local-Postgres recipe, with no value committed anywhere.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [danlo] Install PostgreSQL 18 and set TEST_DATABASE_URL</name>
  <read_first>
    - `docs/local-postgres.md` (written in Task 1) — follow it step by step
    - `.planning/phases/01-foundations-tenancy/01-CONTEXT.md` § D-05a
  </read_first>
  <files>.env.local (gitignored - danlo edits; no tracked file changes)</files>
  <action>
Present the checkpoint below to danlo and STOP. Claude cannot perform this step: `docker` resolves nowhere on this machine and `wsl --status` reports "not installed" (verified 2026-09-20), so D-04's `supabase start` path is unavailable and D-05a replaces it with a native PostgreSQL 18 for Windows install driven by a GUI installer. After danlo replies, verify the acceptance criteria with `grep -c '^TEST_DATABASE_URL=' .env.local` and by asking him to paste the `psql --version` output. Do not attempt to install PostgreSQL, do not read or print the password, and do not let plan 04 start until this is confirmed.
  </action>
  <what-built>
    `docs/local-postgres.md` and `.env.example` are committed. Nothing else in this phase can run until a PostgreSQL 18 server exists on this machine — Docker Desktop and WSL are both absent, so there is no scriptable install path.
  </what-built>
  <how-to-verify>
    1. Download and run the PostgreSQL **18** Windows installer from https://www.postgresql.org/download/windows/ (EDB). Keep PostgreSQL Server + Command Line Tools checked. Port `5432`. Choose a superuser password.
    2. Open a NEW PowerShell window and run: `psql --version` — it must print `psql (PostgreSQL) 18.x`. If not found, add `C:\Program Files\PostgreSQL\18\bin` to your PATH and reopen PowerShell.
    3. Create the database: `psql -U postgres -c "create database siteless_test"`
    4. Confirm it exists: `psql -U postgres -l` lists `siteless_test`.
    5. Add these two lines to `C:\Users\danlo\prospector\.env.local` (it is gitignored):
       - `TEST_DATABASE_URL=postgres://postgres:YOURPASSWORD@localhost:5432/siteless_test`
       - `SUPABASE_DB_POOL_URL=postgres://app_user:app_user@localhost:5432/siteless_test`
       Replace `YOURPASSWORD` with the superuser password you chose. URL-encode it if it contains `@ : / ? # [ ] %`.
    6. Reply here with just "postgres ready" — do not paste the connection string or the password.
  </how-to-verify>
  <acceptance_criteria>
    - danlo confirms `psql --version` reported `psql (PostgreSQL) 18.x`
    - danlo confirms `psql -U postgres -l` lists `siteless_test`
    - `grep -c '^TEST_DATABASE_URL=' .env.local` returns 1
    - `grep -c '^SUPABASE_DB_POOL_URL=' .env.local` returns 1
    - `grep -c 'supabase' .env.local | test "$(grep '^TEST_DATABASE_URL=' .env.local | grep -c supabase)" -eq 0` — the TEST_DATABASE_URL line contains no `supabase` substring (D-04)
    - `git status --short` still does not list `.env.local`
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^TEST_DATABASE_URL=' .env.local | grep -qx 1 &amp;&amp; grep -c '^SUPABASE_DB_POOL_URL=' .env.local | grep -qx 1 &amp;&amp; test "$(grep '^TEST_DATABASE_URL=' .env.local | grep -c supabase)" -eq 0 &amp;&amp; echo ok</automated>
  </verify>
  <done>A PostgreSQL 18 server is running locally with a `siteless_test` database, and `.env.local` names it under `TEST_DATABASE_URL` with no `supabase` substring on that line.</done>
  <resume-signal>Type "postgres ready" (or describe what went wrong). Never paste a password or a connection string.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [danlo] Add SUPABASE_DB_URL and confirm the Clerk + Supabase dashboard settings</name>
  <read_first>
    - `.env.example` (Task 1) — the two Supabase URL comments explain which pooler mode each one is
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Environment Availability, "Missing env vars this phase must add" (lines 1015-1020)
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Assumptions Log A5 (line 1159) — the Clerk setting was assumed, never read from the dashboard
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` § Manual-Only Verifications
  </read_first>
  <files>.env.local (gitignored - danlo edits; no tracked file changes)</files>
  <action>
Present the checkpoint below to danlo and STOP. Three of these four facts have no CLI available in this environment: the Supabase session-pooler URI, the Clerk organization settings, and the Supabase third-party-auth state. After danlo replies, verify the URL-shape criteria with grep against `.env.local` (names and shape only, never the value) and copy his five answers verbatim into `01-02-SUMMARY.md` under a "Manual verifications" heading - that record is what `/gsd-verify-work` reads. Do not print any value.
  </action>
  <what-built>
    The repo now knows the names of every variable it needs. Three of the four facts below cannot be read by any CLI available here: the session-pooler URL, the Clerk organization settings, and the Supabase third-party-auth state.
  </what-built>
  <how-to-verify>
    1. **Supabase session-pooler URL** — Supabase Dashboard → project `siteless` → **Connect** → **Session pooler**. Copy the URI. It must use port **5432** and user **`postgres.jahgeqshuesndyscnmjo`**. Add to `.env.local` as `SUPABASE_DB_URL=...`.
       🔴 Do NOT use the "Direct connection" `db.jahgeqshuesndyscnmjo.supabase.co:5432` address — it is IPv6-only without the IPv4 add-on and GitHub-hosted runners have no IPv6.
    2. **Clerk organizations (D-02)** — Clerk Dashboard → Siteless → **Configure → Organizations**. Confirm **"Membership required"** is ON and **"Users can create organizations"** is OFF. If org creation is ON, turn it off — otherwise any Google sign-in can mint an empty tenant. Note the result.
    3. **Clerk organization exists** — Clerk Dashboard → **Organizations**. Confirm at least one organization exists and that `danlopez508@gmail.com` is a member of exactly one. (Exactly one membership is what makes `ActivateSoleOrganization` in plan 08 able to activate it.) If none exists, create one now — this is the org criterion 1 signs in to.
    4. **Supabase third-party auth** — Supabase Dashboard → **Authentication → Sign In / Providers → Third-Party Auth**. Confirm Clerk is listed as **ENABLED** with domain `equipped-newt-5148.clerk.accounts.dev`.
    5. Reply with the four answers in this form (no values, no keys):
       `SUPABASE_DB_URL: set (session pooler, 5432)` / `org creation: off` / `membership required: on` / `orgs: 1, danlo is a member of 1` / `third-party auth: enabled`
  </how-to-verify>
  <acceptance_criteria>
    - `grep -c '^SUPABASE_DB_URL=' .env.local` returns 1
    - `grep '^SUPABASE_DB_URL=' .env.local | grep -c ':5432'` returns 1 (session pooler, not the 6543 transaction pooler)
    - `grep '^SUPABASE_DB_URL=' .env.local | grep -c 'db\.jahgeqshuesndyscnmjo\.supabase\.co'` returns 0 (not the IPv6-only direct address)
    - danlo's reply records: Clerk "users can create organizations" = OFF, "Membership required" = ON
    - danlo's reply records: exactly one Clerk organization with `danlopez508@gmail.com` as a member of exactly one
    - danlo's reply records: Supabase Third-Party Auth for `equipped-newt-5148.clerk.accounts.dev` = ENABLED
    - All five answers are copied verbatim into `01-02-SUMMARY.md` under a "Manual verifications" heading (this is the record `/gsd-verify-work` reads)
    - `git status --short` still does not list `.env.local`
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^SUPABASE_DB_URL=' .env.local | grep -qx 1 &amp;&amp; grep '^SUPABASE_DB_URL=' .env.local | grep -q ':5432' &amp;&amp; test "$(grep '^SUPABASE_DB_URL=' .env.local | grep -c 'db\.jahgeqshuesndyscnmjo\.supabase\.co')" -eq 0 &amp;&amp; echo ok</automated>
  </verify>
  <done>`SUPABASE_DB_URL` is set to the session pooler on port 5432 (not the IPv6-only direct address), and the Clerk + Supabase dashboard facts are recorded verbatim in the SUMMARY.</done>
  <resume-signal>Reply with the five-line status block above, or describe what you found instead.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → database | A test connection with the wrong URL runs destructive DDL against whatever it reaches |
| Clerk dashboard → application | Organization settings decide whether a signed-in stranger can mint a tenant |
| local filesystem → git | `.env.local` holds every credential this project has |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-15 | Tampering / Information Disclosure | `TEST_DATABASE_URL` | mitigate | D-04/D-05a: the variable is a distinct NAME from `SUPABASE_DB_URL` so pointing it at production is a typo you can see; Task 2's acceptance criteria greps the line for `supabase` and requires zero matches; plan 04's `scripts/db.ts` refuses a `--target=test` URL whose host matches `supabase`. | high |
| T-1-17 | Spoofing / Elevation of Privilege | Clerk organization settings | mitigate | D-02: Task 3 requires danlo to confirm "users can create organizations" is OFF and "Membership required" is ON in the dashboard, recorded verbatim in the SUMMARY. Without it a Google sign-in mints an empty tenant and `requireOrg()` is the only thing between that session and the product. | high |
| T-1-10 | Information Disclosure | `.env.local`, `.env.example` | mitigate | `.env.example` carries NAMES only — the Task 1 verify script fails if any non-comment line has characters after the `=`. Every checkpoint's resume-signal explicitly forbids pasting a value. `git status --short` is asserted clean of `.env.local` in both checkpoints. | high |
| T-1-18 | Information Disclosure | Supabase direct connection address | mitigate | `SUPABASE_DB_URL` is asserted to contain `:5432` and NOT to contain `db.jahgeqshuesndyscnmjo.supabase.co` — the direct address is IPv6-only and would make CI hang to vitest's ceiling rather than fail with one nameable connection error. | medium |
</threat_model>

<verification>
- `psql --version` reports PostgreSQL 18.x on this machine
- `psql -U postgres -l` lists `siteless_test`
- `.env.local` contains `TEST_DATABASE_URL`, `SUPABASE_DB_URL`, `SUPABASE_DB_POOL_URL` (names checked with grep; values never read aloud)
- `git status --short` never lists `.env.local`
- The five manual answers are recorded verbatim in `01-02-SUMMARY.md`
</verification>

<success_criteria>
- A PostgreSQL 18 server with a `siteless_test` database is reachable at `localhost:5432`
- `.env.local` holds the three database URLs under the three distinct names
- Clerk organization creation is confirmed OFF, membership required ON, exactly one org with danlo as its sole membership
- Supabase third-party auth for the Clerk dev domain is confirmed ENABLED
- `.env.example` and `docs/local-postgres.md` are committed with zero values
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-02-SUMMARY.md` including a "Manual verifications" section with danlo's five answers verbatim.
</output>
