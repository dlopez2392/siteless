---
phase: 01-foundations-tenancy
plan: 08
type: execute
wave: 3
depends_on: [05, 06]
files_modified:
  - src/proxy.ts
  - src/db/client.ts
  - src/db/with-org.ts
  - src/lib/auth/require-org.ts
  - tests/db/with-org.test.ts
  - src/components/activate-sole-organization.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/no-access/page.tsx
  - src/app/sign-in/[[...sign-in]]/page.tsx
  - src/app/api/health/route.ts
  - tests/e2e/signed-in.spec.ts
  - tests/e2e/no-access.spec.ts
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]
must_haves:
  truths:
    - "FOUND-01 / criterion 1: a signed-in user sees which user and which org the request is scoped to, and the org id comes only from Clerk's server-verified auth()"
    - "D-02: a signed-in user with no active organization lands on /no-access — never on an empty, error-free product"
    - "D-03: the orgs row is provisioned just-in-time on the first authenticated request carrying an unseen Clerk org claim, through app.ensure_org and never through a blanket INSERT"
    - "D-11b: withOrg is the ONE runtime database entry point — it binds the claims as a parameter, allow-lists the role against a Set, and uses set_config(..., true) so nothing survives the transaction onto a pooled connection"
    - "D-11b is PERSISTED, not grepped once: tests/db/with-org.test.ts re-runs in CI, asserting the exact bound SQL and that a second withOrg call on the same pooled connection cannot see the first's claims"
    - "proxy.ts sits at src/proxy.ts, level with app/, and contains no authorization decision — authorization lives next to the data (CVE-2025-29927 class)"
    - "/api/health reports liveness without leaking a database error, a DSN or a claim"
  artifacts:
    - path: "src/proxy.ts"
      provides: "clerkMiddleware() for session context only"
      contains: "clerkMiddleware()"
    - path: "src/db/with-org.ts"
      provides: "The only runtime database entry point"
      exports: ["withOrg", "OrgClaims"]
    - path: "tests/db/with-org.test.ts"
      provides: "The persisted regression for T-1-04 and T-1-05 — bound claims, transaction-local set_config, allow-listed role"
      contains: "die with the transaction"
    - path: "src/lib/auth/require-org.ts"
      provides: "Server guard returning orgId or redirecting, plus JIT org provisioning"
      exports: ["requireOrg", "orgClaims", "ensureOrgRow"]
    - path: "src/app/no-access/page.tsx"
      provides: "D-02 landing for a session with no active organization"
    - path: "src/app/api/health/route.ts"
      provides: "Liveness for the deploy smoke, with a missing-proxy canary"
  key_links:
    - from: "src/db/with-org.ts"
      to: "request.jwt.claims"
      via: "bound set_config with the local flag"
      pattern: "set_config\\('request.jwt.claims'"
    - from: "tests/db/with-org.test.ts"
      to: "src/db/with-org.ts"
      via: "withOrg called twice on the same max:1 pooled connection, plus a spy on db.transaction"
      pattern: "withOrg\\("
    - from: "src/lib/auth/require-org.ts"
      to: "app.ensure_org"
      via: "withOrg transaction calling the SECURITY DEFINER function"
      pattern: "ensure_org"
    - from: "src/app/layout.tsx"
      to: "src/components/activate-sole-organization.tsx"
      via: "component mounted inside ClerkProvider above children"
      pattern: "ActivateSoleOrganization"
---

<objective>
Wire Clerk to the database: `clerkMiddleware()` at `src/proxy.ts` for session context, `requireOrg()` next to the data for authorization, `withOrg()` as the one runtime database entry point, just-in-time `orgs` provisioning, and the unstyled shell that shows who is signed in and which org the request is scoped to.

Purpose: this is success criterion 1's application half. It is also where the phase's two highest-probability silent failures live — a proxy in the wrong directory, and a session with no active organization producing an empty, error-free product.
Output: `pnpm build` green, `/api/health` returning 200 locally, a persisted regression test over `withOrg`, and two e2e specs written for plan 11 to run against the deployed URL.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations-tenancy/01-CONTEXT.md
@.planning/phases/01-foundations-tenancy/01-RESEARCH.md
@.planning/phases/01-foundations-tenancy/01-PATTERNS.md
@.planning/phases/01-foundations-tenancy/01-VALIDATION.md
@src/env.ts
@src/db/schema/index.ts
@src/lib/auth/sole-organization.ts
@playwright.config.ts

<interfaces>
Contract for plans 10 and 11, and for every later phase's server code.

src/db/client.ts        (import 'server-only')
  db          // drizzle over postgres.js on SUPABASE_DB_POOL_URL with prepare:false, max:1
              // db.$client is the underlying postgres.js Sql (used only by the test's teardown)

src/db/with-org.ts      (import 'server-only')
  type OrgClaims = { o: { id: string }; sub: string; role: 'authenticated' }
  withOrg<T>(claims: OrgClaims, fn: (tx) => Promise<T>): Promise<T>
  // THE only runtime database entry point. Opens a transaction, binds the claims,
  // allow-lists the role, runs fn, commits. Nothing outside it may touch `db`
  // except /api/health's `select 1`, which needs no claims and reads no table.

src/lib/auth/require-org.ts   (import 'server-only')
  requireOrg(): Promise<{ userId: string; orgId: string; orgSlug: string | null }>
       // redirect('/sign-in') when !userId, redirect('/no-access') when !orgId
  orgClaims(): Promise<OrgClaims>
  ensureOrgRow(claims: OrgClaims, displayName: string): Promise<string>   // uuid of the orgs row

Routes:
  /                        signed-in shell: "Signed in as {userId} in org {clerk org id} ({display_name})"
  /no-access               D-02 landing, SignOutButton as the only affordance
  /sign-in/[[...sign-in]]  Clerk <SignIn /> — the page @clerk/testing drives
  /api/health              GET -> 200 { ok, db, proxy, commit } | 503 { ok:false, ... }

Test names (VALIDATION.md filters on these substrings — do not reword):
  'die with the transaction'        (tests/db/with-org.test.ts)   <- the M4 phase gate
  'signs in and is org-scoped'      (tests/e2e/signed-in.spec.ts)
  'no access'                       (tests/e2e/no-access.spec.ts)

BIS analogs (READ-ONLY, at C:\Users\danlo\bis-platform):
  apps\web\src\middleware.ts                                 11 lines — port WITH DELETIONS
  apps\web\src\lib\db.ts                                     16 lines — port the DOCTRINE, not the implementation
  apps\web\src\lib\auth.ts                    lines 7-13, 22-40, 100-119
  apps\web\src\components\activate-sole-organization.tsx     63 lines — VERBATIM PORT
  apps\web\src\app\(dashboard)\layout.tsx     lines 107-152 — strip to the skeleton
  apps\web\src\app\(dashboard)\no-access\page.tsx            36 lines — structure only
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: proxy.ts, the postgres.js client, withOrg, requireOrg, and the withOrg regression test</name>
  <read_first>
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 3 (lines 382-447) — the full `withOrg` body, the role allow-list, the bound claims, and the non-owner connection
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pattern 5 (lines 496-522) — the proxy location citation, the matcher, and the `createRouteMatcher` deprecation text
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 7 (lines 788-792) — `app/proxy.ts` never runs and every `auth()` throws `auth_signature_invalid`, an error whose text points at Clerk config rather than at the path
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 4 (lines 744-752) — the owner bypass and the non-local `set_config` leak, both executed
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `src/proxy.ts` (lines 213-245) — the two lines to delete and the matcher to use instead
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `src/db/with-org.ts` (lines 176-210) and § `src/lib/auth/require-org.ts` (lines 309-344) — the doctrine comment to port, and the BIS `serviceDb()` usage NOT to port
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § Shared Pattern E (lines 762-764) — `import 'server-only'` on anything holding a connection or a secret
    - `drizzle/0004_ensure_org.sql` (plan 05) — the exact `app.ensure_org(text, text)` signature
    - `src/env.ts` (plan 01) — `env.SUPABASE_DB_POOL_URL` is already parsed and validated, and `CLERK_SECRET_KEY` is required at module load
    - `tests/db/_fixtures.ts` (plan 03) — `actAs` already issues the same bound `set_config(..., true)`; the new test proves `src/db/with-org.ts` does too, and reuses none of the fixture (it needs the real pooled client, not a fresh `pg.Client`)
    - `vitest.db.config.ts` (plan 03) — loads `.env.local`, `pool: 'forks'`, `singleFork`, and resolves `@` to `./src`
    - `.github/workflows/ci.yml` (plan 03) — the `db` job sets **only** `TEST_DATABASE_URL`; there is no `SUPABASE_DB_POOL_URL` and no `CLERK_SECRET_KEY` in that job, which is why the test file has an env preamble
  </read_first>
  <files>src/proxy.ts, src/db/client.ts, src/db/with-org.ts, src/lib/auth/require-org.ts, tests/db/with-org.test.ts</files>
  <action>
Create `src/proxy.ts` — 🔴 at `src/`, level with `app/`, NEVER inside `app/`. Next 16 renamed `middleware.ts` to `proxy.ts` and the file-convention page is explicit that it lives "at the same level as `pages` or `app`". Placed in `app/`, Next never loads it, `clerkMiddleware()` never runs, and every `auth()` throws `auth_signature_invalid`.

```
import { clerkMiddleware } from '@clerk/nextjs/server';

// Session context ONLY. No authorization here.
//
// Next renamed middleware -> proxy partly in response to CVE-2025-29927, and
// @clerk/nextjs@7.9.4 marks createRouteMatcher @deprecated in the package itself:
// "Use resource-based auth checks instead. Move auth checks into each page, layout,
// API route, or Server Function that accesses protected data." Server Functions are
// POSTs to the page's route, so a matcher refactor can silently remove proxy coverage.
// Authorization is requireOrg(), next to the data.
export default clerkMiddleware();

export const config = {
  matcher: [
    '/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|webmanifest)).*)',
    '/(api|trpc)(.*)',
    '/__clerk/(.*)',
  ],
};
```
Do NOT set `runtime` in a proxy file — proxy defaults to the Node.js runtime in Next 16 and setting it throws.

Create `src/db/client.ts`:
```
import 'server-only';
import { drizzle } from 'drizzle-orm/postgres-js';
import postgres from 'postgres';
import { env } from '@/env';
import * as schema from './schema';

/**
 * The database for anything a signed-in human can reach. RLS applies.
 *
 * Connects as app_user — NOT the owner. A table owner bypasses RLS, so a code path that
 * skips withOrg on an owner connection reads every tenant's rows and nothing complains.
 * As a NOINHERIT non-owner it gets 42501 instead, which is the whole point.
 *
 * prepare:false is mandatory on the transaction pooler and is the most common
 * Supabase + Drizzle production failure. max:1 because each serverless invocation owns
 * its own pool — and because it is what makes tests/db/with-org.test.ts a real
 * pooled-reuse test rather than a lucky one.
 */
const client = postgres(env.SUPABASE_DB_POOL_URL, { prepare: false, max: 1 });

export const db = drizzle({ client, schema });
```

Create `src/db/with-org.ts`:
```
import 'server-only';
import { sql } from 'drizzle-orm';
import { db } from './client';

/** Never interpolate a role that came from input. */
const ROLES = new Set(['authenticated', 'anon']);

export type OrgClaims = { o: { id: string }; sub: string; role: 'authenticated' };

/**
 * THE runtime database entry point. Everything a request touches goes through here.
 *
 * The Drizzle docs' Supabase recipe interpolates the claims with
 * sql.raw(JSON.stringify(token)) — that breaks, or injects, the moment a claim value
 * contains a single quote. Bind the parameter.
 *
 * The `true` on set_config is the LOCAL form: it dies with the transaction. The non-local
 * form survives the commit, and on a transaction-mode pooler the connection is handed to
 * the next request with the previous tenant's claims still set (verified).
 *
 * Both of those are load-bearing and invisible to typecheck, so they are pinned by
 * tests/db/with-org.test.ts, not by a one-time grep. Changing the `true` below to `false`
 * is mutation M4 in 01-VALIDATION.md and must turn that one test red.
 */
export async function withOrg<T>(
  claims: OrgClaims,
  fn: (tx: Parameters<Parameters<typeof db.transaction>[0]>[0]) => Promise<T>,
): Promise<T> {
  if (!ROLES.has(claims.role)) throw new Error('withOrg: refusing role ' + claims.role);
  return db.transaction(async (tx) => {
    await tx.execute(sql`select set_config('request.jwt.claims', ${JSON.stringify(claims)}, true)`);
    await tx.execute(sql.raw('set local role ' + claims.role)); // safe: allow-listed above
    return fn(tx);
  });
}
```

Create `src/lib/auth/require-org.ts`:
```
import 'server-only';
import { auth } from '@clerk/nextjs/server';
import { sql } from 'drizzle-orm';
import { redirect } from 'next/navigation';
import { withOrg, type OrgClaims } from '@/db/with-org';

/**
 * Deny by default, and redirect rather than 403 — never confirm to a wrong-tenant caller
 * that the resource exists.
 *
 * orgId is null until an organization is ACTIVATED on the session, not merely joined.
 * BIS shipped that bug on 2026-09-16: a user who accepted a valid invitation and signed
 * in was told they had no company. Treating null as a routing decision here means no page
 * can ever render a query with a null org.
 */
export async function requireOrg() {
  const { userId, orgId, orgSlug } = await auth();
  if (!userId) redirect('/sign-in');
  if (!orgId) redirect('/no-access?reason=none');
  return { userId, orgId, orgSlug: orgSlug ?? null };
}

export async function orgClaims(): Promise<OrgClaims> {
  const { userId, orgId } = await requireOrg();
  return { o: { id: orgId }, sub: userId, role: 'authenticated' };
}

/**
 * D-03: provision the orgs row just-in-time on the first authenticated request carrying a
 * Clerk org claim not yet seen. app.ensure_org is SECURITY DEFINER and re-checks its
 * argument against the caller's claim, so `authenticated` never needs INSERT on the
 * tenant root and no caller can provision somebody else's org.
 */
export async function ensureOrgRow(claims: OrgClaims, displayName: string): Promise<string> {
  return withOrg(claims, async (tx) => {
    const res = await tx.execute(
      sql`select app.ensure_org(${claims.o.id}, ${displayName}) as id`,
    );
    const row = (res as unknown as Array<{ id: string }>)[0];
    if (!row?.id) throw new Error('ensureOrgRow: app.ensure_org returned no id');
    return row.id;
  });
}
```
Do NOT port BIS's `serviceDb()` usage in the guard. BIS uses the service role there deliberately and carries a 24-line argued exception for it; Siteless has no service-role key in this phase and must not acquire one.

Create `tests/db/with-org.test.ts` — the persisted regression for T-1-04 and T-1-05. Write it VERBATIM; every assertion below is load-bearing and each one corresponds to a row in `01-VALIDATION.md`:

```
import { sql, type SQL } from 'drizzle-orm';
import { PgDialect } from 'drizzle-orm/pg-core';
import { afterAll, afterEach, expect, test, vi } from 'vitest';
import { db } from '@/db/client';
import { withOrg, type OrgClaims } from '@/db/with-org';

/**
 * D-11b, executed instead of grepped.
 *
 * Two invariants live in src/db/with-org.ts and neither is visible to typecheck:
 *   T-1-05  the claims are a BOUND parameter — never sql.raw(JSON.stringify(...)), which
 *           is what the Drizzle Supabase recipe publishes and what injects the moment a
 *           claim value contains a single quote
 *   T-1-04  set_config's third argument is `true`, the transaction-LOCAL form. The
 *           non-local form survives COMMIT, and on a transaction-mode pooler the
 *           connection is handed to the next request with the previous tenant's claims
 *           still set (executed on PostgreSQL 18.3 during research)
 *
 * Until this file existed, both were asserted only by a one-time grep at execution time,
 * so a later refactor could reintroduce either with nothing in CI catching it. This is
 * mutation gate M4 in 01-VALIDATION.md.
 *
 * SCOPE: this connects as whatever the URL names — in CI that is the owner. It proves
 * claim LOCALITY only. The non-owner "a connection without set local role is refused
 * 42501" property belongs to plan 05's test and is deliberately not re-proven here.
 */

// Hoisted above every import by vitest: src/env.ts parses process.env at module load and
// src/db/client.ts opens the pool from it, so this has to run first.
vi.hoisted(() => {
  const url = process.env.SUPABASE_DB_POOL_URL ?? process.env.TEST_DATABASE_URL;
  if (!url) {
    throw new Error(
      'tests/db/with-org.test.ts: neither SUPABASE_DB_POOL_URL nor TEST_DATABASE_URL is set. Local dev: .env.local. CI: the postgres:18 service container sets TEST_DATABASE_URL.',
    );
  }
  if (/supabase\.(co|com)|pooler\.supabase/.test(url)) {
    throw new Error(
      'tests/db/with-org.test.ts: the database URL points at a Supabase host. D-04: the cloud project is production only and is never a test target.',
    );
  }
  process.env.SUPABASE_DB_POOL_URL = url;
  // src/env.ts requires this name to be present; nothing in this file reaches Clerk.
  process.env.CLERK_SECRET_KEY ||= 'sk_test_unused_by_the_db_suite';
});

// `server-only` throws when imported outside a React Server Component graph.
vi.mock('server-only', () => ({}));

const A: OrgClaims = { o: { id: 'org_A' }, sub: 'user_A', role: 'authenticated' };
const B: OrgClaims = { o: { id: 'org_B' }, sub: 'user_B', role: 'authenticated' };
const READ = sql`select current_setting('request.jwt.claims', true) as claims`;

const claimsOf = (rows: unknown): string =>
  String((rows as Array<{ claims: string | null }>)[0]?.claims ?? '');

afterEach(() => {
  vi.restoreAllMocks();
});

afterAll(async () => {
  // postgres.js holds the socket open; without this vitest reports "something prevents
  // the main process from exiting" and the db job burns its timeout.
  await db.$client.end({ timeout: 5 });
});

test('withOrg binds the tenant claims and they die with the transaction', async () => {
  // ---- 1. the SQL withOrg actually issues (T-1-05, and the `true` of T-1-04) ----
  const issued: SQL[] = [];
  const spy = vi.spyOn(db, 'transaction').mockImplementation((async (
    fn: (tx: { execute: (q: SQL) => Promise<unknown> }) => Promise<unknown>,
  ) =>
    fn({
      execute: async (q: SQL) => {
        issued.push(q);
        return [];
      },
    })) as never);
  try {
    await withOrg(A, async () => 'ok');
  } finally {
    spy.mockRestore();
  }

  expect(issued).toHaveLength(2);
  const [setConfigSql, setRoleSql] = issued;
  if (!setConfigSql || !setRoleSql) throw new Error('withOrg issued no SQL');

  const dialect = new PgDialect();
  const setConfig = dialect.sqlToQuery(setConfigSql);
  expect(setConfig.sql).toBe("select set_config('request.jwt.claims', $1, true)");
  expect(setConfig.params).toEqual([JSON.stringify(A)]);
  expect(setConfig.sql).not.toContain('org_A'); // interpolated == injectable
  expect(dialect.sqlToQuery(setRoleSql).sql).toBe('set local role authenticated');

  // A role outside the allow-list is refused before any SQL is issued.
  await expect(
    withOrg({ ...A, role: 'postgres' } as unknown as OrgClaims, async () => 'never'),
  ).rejects.toThrow('refusing role');

  // ---- 2. the behaviour, on the real connection (client.ts pins max: 1) ----
  expect(claimsOf(await withOrg(A, (tx) => tx.execute(READ)))).toContain('org_A');

  // The same pooled connection, outside any transaction. NULL before the GUC has ever
  // been set, '' once a reverted `set local` has defined the placeholder — which is why
  // app.jwt() wraps it in nullif(..., ''). Never a tenant's claims.
  expect(claimsOf(await db.execute(READ))).toBe('');

  // The next request on that connection sees its own org and nothing of the previous one.
  const second = claimsOf(await withOrg(B, (tx) => tx.execute(READ)));
  expect(second).toContain('org_B');
  expect(second).not.toContain('org_A');
});
```

Three notes for the executor:
- `vi.hoisted` and `vi.mock` are hoisted above the `import` statements by vitest's transform even though they appear below them in the file. Do NOT move the imports underneath them to "make it work" — that trips `import/first` and changes nothing.
- The env preamble is not belt-and-braces. CI's `db` job sets only `TEST_DATABASE_URL`, and `src/env.ts` throws at module load without `CLERK_SECRET_KEY` and `SUPABASE_DB_POOL_URL`. The Supabase-host guard is the same D-04 refusal `tests/db/_fixtures.ts` carries, restated because this file reaches the database through a different variable.
- Both halves assert the SAME invariant from two angles, in ONE named test on purpose: mutation M4 (`true` → `false`) must turn **exactly one** named test red, which is what makes the gate specific. Do not split this into two tests.

After the test is green, run mutation M4 once and record it in the SUMMARY: change the `true` in `set_config(..., true)` to `false`, run `pnpm test:db -t "die with the transaction"`, confirm **that named test** is the one that fails (read the name in the output — a `-t` filter matching nothing exits green), revert, and confirm `git diff` is empty.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm build &amp;&amp; pnpm test:db -t "die with the transaction"</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` exists AND `ls src/app/proxy.ts` and `ls proxy.ts` both fail — the file is level with `app/`, not inside it and not at the repo root alongside `src/`
    - `grep -c 'createRouteMatcher' src/proxy.ts` returns 0 and `grep -c 'auth.protect' src/proxy.ts` returns 0
    - `grep -c '__clerk' src/proxy.ts` returns 1 (Clerk's handshake routes are in the matcher)
    - `grep -c 'runtime' src/proxy.ts` returns 0
    - `src/db/client.ts`, `src/db/with-org.ts` and `src/lib/auth/require-org.ts` each have `import 'server-only';` as their first line
    - `grep -c "prepare: false" src/db/client.ts` returns 1
    - `grep -c 'sql.raw(JSON.stringify' src/db/with-org.ts` returns 0 — the claims are bound, never raw
    - `grep -c "set_config('request.jwt.claims'" src/db/with-org.ts` returns 1 and the same call ends with `, true)`
    - `grep -c "new Set(\['authenticated', 'anon'\])" src/db/with-org.ts` returns 1
    - `grep -c 'serviceDb\|SERVICE_ROLE' src/` returns 0 across the whole tree
    - `grep -c "redirect('/no-access" src/lib/auth/require-org.ts` returns 1
    - `tests/db/with-org.test.ts` exists and `grep -c 'die with the transaction' tests/db/with-org.test.ts` returns 1 — the VALIDATION.md filter substring is present verbatim
    - `tests/db/with-org.test.ts` contains the expected SQL literal `select set_config('request.jwt.claims', $1, true)` — check with `grep -F` inside SINGLE quotes so the shell does not expand `$1`. The `, true)` in that literal is what makes mutation M4 detectable
    - `grep -c 'toEqual(\[JSON.stringify(A)\])' tests/db/with-org.test.ts` returns 1 — the claims are asserted to be a bound PARAMETER, not text
    - `grep -c 'vi.hoisted' tests/db/with-org.test.ts` returns 1 and `grep -c 'supabase' tests/db/with-org.test.ts` is at least 1 (the D-04 host guard)
    - `grep -c "vi.mock('server-only'" tests/db/with-org.test.ts` returns 1
    - `grep -c '^test(' tests/db/with-org.test.ts` returns 1 — one named test, so M4 reds exactly one
    - `pnpm test:db -t "die with the transaction"` exits 0 AND its output prints the test name `withOrg binds the tenant claims and they die with the transaction` — read the NAME, because a `-t` filter that matches nothing exits green
    - The SUMMARY records mutation M4: `true` → `false` in `src/db/with-org.ts` turned exactly that one named test red, was reverted, and `git diff` came back empty
    - `pnpm typecheck`, `pnpm lint` and `pnpm build` all exit 0
  </acceptance_criteria>
  <done>Clerk session context is established in the right file; there is exactly one runtime path to the database, connecting as a non-owner with bound, transaction-local claims; and that property is pinned by a test CI re-runs rather than by a grep that ran once.</done>
</task>

<task type="auto">
  <name>Task 2: The unstyled shell — layout, activation, the signed-in page, /no-access, /sign-in and /api/health</name>
  <read_first>
    - `C:\Users\danlo\bis-platform\apps\web\src\components\activate-sole-organization.tsx` (63 lines, READ-ONLY) — port WHOLE, including the three load-bearing details
    - `C:\Users\danlo\bis-platform\apps\web\src\app\(dashboard)\layout.tsx` lines 107-152 (READ-ONLY) — take the skeleton and the placement comment at 139-144, nothing else
    - `C:\Users\danlo\bis-platform\apps\web\src\app\(dashboard)\no-access\page.tsx` (36 lines, READ-ONLY) — the `searchParams: Promise<...>` signature, the two-message branch, `SignOutButton` as the only affordance
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `src/app/layout.tsx` (lines 347-368) and § `src/app/no-access/page.tsx` (lines 372-387)
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 2 (lines 728-736) — the three-layer defence, and `treatPendingAsSignedOut`
    - `.planning/phases/01-foundations-tenancy/01-RESEARCH.md` § Pitfall 7 (line 792) — "add `/api/health` returning `{ ok, orgId: <boolean> }` and hit it in CI after deploy — a missing proxy shows up there immediately"
    - `src/lib/auth/sole-organization.ts` (plan 06) — the pure function this component calls
    - `.planning/ROADMAP.md` § Phase 1 Notes — unstyled shell only
  </read_first>
  <files>src/components/activate-sole-organization.tsx, src/app/layout.tsx, src/app/page.tsx, src/app/no-access/page.tsx, src/app/sign-in/[[...sign-in]]/page.tsx, src/app/api/health/route.ts</files>
  <action>
🔴 Create `src/app/sign-in/[[...sign-in]]/page.tsx` with the Write tool only. Never shell-`cp` or shell-`mkdir -p` a double-bracketed route path — the brackets are glob metacharacters and the path silently lands somewhere else.

`src/components/activate-sole-organization.tsx` — verbatim port. Three details are load-bearing and are exactly what a re-implementation drops:
- the `attempted` ref, because without it the effect fires again on the in-between render and loops (`setActive` resolving does not guarantee the next render sees an `orgId` — the session token has to come back first)
- `window.location.assign('/')` rather than `router.refresh()`, because the new claim arrives in a re-issued session cookie
- `.catch()` logs and leaves the page alone — a failure here must not replace one confusing screen with a blank one

It is `"use client"`, calls `useAuth()` + `useOrganizationList({ userMemberships: true })`, passes `{ signedIn, activeOrgId, memberships }` to `soleOrganizationToActivate`, and renders `null`.

`src/app/layout.tsx` — replace plan 01's placeholder. Skeleton only: no `next/font`, no ThemeProvider, no Toaster, no CSS import, no class names beyond nothing.
```
import { ClerkProvider } from '@clerk/nextjs';
import { ActivateSoleOrganization } from '@/components/activate-sole-organization';

export const metadata = { title: 'Siteless' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body>
          {/* Renders nothing. Inside ClerkProvider and above every page, so it covers
              each surface a client can reach signed-in but with no active organization —
              /, /no-access and /sign-in — instead of only whichever one someone
              remembered. */}
          <ActivateSoleOrganization />
          {children}
        </body>
      </html>
    </ClerkProvider>
  );
}
```
Import the COMPONENT across the client boundary, never a data object: a `"use client"` module's exports become client references inside a server component and resolve to `undefined` at runtime with typecheck, lint and build all green (two recorded occurrences).

`src/app/page.tsx` — the signed-in shell, a server component:
```
import { ensureOrgRow, orgClaims, requireOrg } from '@/lib/auth/require-org';

export const dynamic = 'force-dynamic';

export default async function Home() {
  const { userId, orgId, orgSlug } = await requireOrg();
  const claims = await orgClaims();
  const orgRowId = await ensureOrgRow(claims, orgSlug ?? orgId);   // D-03, just-in-time
  return (
    <main>
      <h1>Siteless</h1>
      <p data-testid="signed-in-as">Signed in as {userId}</p>
      <p data-testid="org-id">Org {orgId}</p>
      <p data-testid="org-row-id">Tenant {orgRowId}</p>
    </main>
  );
}
```
The `data-testid` attributes are how the e2e spec asserts org scoping without depending on copy. Painted values, not CSS custom properties — there is no CSS here at all, which is the point.

`src/app/no-access/page.tsx` — structure only, plain HTML, `SignOutButton` as the only affordance:
```
import { SignOutButton } from '@clerk/nextjs';

export default async function NoAccess({
  searchParams,
}: { searchParams: Promise<{ reason?: string }> }) {
  const { reason } = await searchParams;
  return (
    <main>
      <h1 data-testid="no-access">No access</h1>
      <p>
        {reason === 'none'
          ? 'Your account is not a member of a Siteless organization yet. Siteless is invite-only.'
          : 'You do not have access to this workspace.'}
      </p>
      <SignOutButton>Sign out</SignOutButton>
    </main>
  );
}
```

`src/app/sign-in/[[...sign-in]]/page.tsx`:
```
import { SignIn } from '@clerk/nextjs';

export default function SignInPage() {
  return <main><SignIn /></main>;
}
```
Confirm `.env.local` has `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in`. If it holds a Clerk account-portal URL instead, change it to `/sign-in` locally (the file is gitignored) and record the change in the SUMMARY so plan 10 sets the same value on Vercel.

`src/app/api/health/route.ts` — the deploy smoke target and the missing-proxy canary:
```
import { auth } from '@clerk/nextjs/server';
import { sql } from 'drizzle-orm';
import { NextResponse } from 'next/server';
import { db } from '@/db/client';

export const dynamic = 'force-dynamic';

/**
 * Liveness only. It reports UP or DOWN and nothing else: never the database error text,
 * never the DSN, never a claim payload. The proxy field is the canary from Pitfall 7 —
 * with proxy.ts in the wrong directory, auth() throws auth_signature_invalid and this
 * route is the first place that shows up.
 */
export async function GET() {
  let dbState: 'up' | 'down' = 'down';
  let proxyState: 'up' | 'down' = 'down';
  try {
    await db.execute(sql`select 1`);
    dbState = 'up';
  } catch (e) {
    console.error('health: database probe failed', e instanceof Error ? e.name : 'unknown');
  }
  try {
    await auth();
    proxyState = 'up';
  } catch {
    console.error('health: clerkMiddleware did not run — check that proxy.ts is at src/, level with app/');
  }
  const ok = dbState === 'up' && proxyState === 'up';
  return NextResponse.json(
    { ok, db: dbState, proxy: proxyState, commit: process.env.VERCEL_GIT_COMMIT_SHA ?? 'local' },
    { status: ok ? 200 : 503 },
  );
}
```
`db.execute(sql\`select 1\`)` is the ONE database call outside `withOrg` in the whole codebase — it needs no claims and reads no table. Say so in the comment so the next reader does not treat it as precedent.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `ls "src/app/sign-in/[[...sign-in]]/page.tsx"` succeeds (the double-bracketed directory exists exactly once, not duplicated by a glob mishap)
    - `grep -c 'ActivateSoleOrganization' src/app/layout.tsx` returns 2 (import + mount) and it sits inside `ClerkProvider`, above `{children}`
    - `grep -c 'attempted' src/components/activate-sole-organization.tsx` returns at least 2 (the ref and its set)
    - `grep -c "window.location.assign" src/components/activate-sole-organization.tsx` returns 1 and `grep -c 'router.refresh' src/components/activate-sole-organization.tsx` returns 0
    - `grep -c 'next/font\|ThemeProvider\|Toaster\|className=' src/app/layout.tsx` returns 0 — unstyled shell only
    - `grep -rc 'tailwind\|\.css' src/app/` returns 0
    - `src/app/page.tsx` contains all three of `data-testid="signed-in-as"`, `data-testid="org-id"`, `data-testid="org-row-id"`
    - `src/app/no-access/page.tsx` contains `data-testid="no-access"` and `SignOutButton`
    - `src/app/api/health/route.ts` contains `export const dynamic = 'force-dynamic'`, reports `proxy`, and its JSON body contains no interpolated error: `grep -c 'e.message\|String(e)\|err.stack' src/app/api/health/route.ts` returns 0
    - `grep -c 'db.execute' src/` returns 1 across the whole tree — the only non-`withOrg` database call is the health probe (the `db.execute` in `tests/db/with-org.test.ts` is a test of the leak boundary, not application code, and is outside `src/`)
    - `pnpm typecheck`, `pnpm lint` and `pnpm build` all exit 0, and the build's route listing includes `/`, `/no-access`, `/sign-in/[[...sign-in]]` and `/api/health`
  </acceptance_criteria>
  <done>An unstyled shell exists that shows who is signed in and which tenant the request resolved to, lands a membership-less user on /no-access, and exposes a health route that cannot leak a database error.</done>
</task>

<task type="auto">
  <name>Task 3: E2E specs, and a local production smoke of /api/health</name>
  <read_first>
    - `playwright.config.ts` and `tests/e2e/auth.setup.ts` (plan 03) — the storage-state project, and the rule against a `setActive` workaround
    - `.planning/phases/01-foundations-tenancy/01-PATTERNS.md` § `playwright.config.ts` (lines 622-664) — especially the two things NOT to port
    - `.planning/phases/01-foundations-tenancy/01-VALIDATION.md` — the `-g` filter strings for the two e2e tests
    - `src/app/page.tsx` and `src/app/no-access/page.tsx` (Task 2) — the `data-testid` values the specs assert on
    - `$HOME/.claude/projects/C--Users-danlo/memory/reference_hard_lessons.md` is not available here; the rule that matters is restated in the action below
  </read_first>
  <files>tests/e2e/signed-in.spec.ts, tests/e2e/no-access.spec.ts</files>
  <action>
Create `tests/e2e/signed-in.spec.ts`:
```
import { expect, test } from '@playwright/test';

test('signs in and is org-scoped', async ({ page }) => {
  await page.goto('/');
  await expect(page.getByTestId('signed-in-as')).toContainText('user_');
  await expect(page.getByTestId('org-id')).toContainText('org_');
  await expect(page.getByTestId('org-row-id')).not.toBeEmpty();
});
```

Create `tests/e2e/no-access.spec.ts`:
```
import { expect, test } from '@playwright/test';

// Runs WITHOUT the storage state, so it is a signed-out visitor: / must not render the
// shell. The membership-less case is covered by requireOrg's redirect, which this asserts
// reaches /no-access rather than an empty product.
test.use({ storageState: { cookies: [], origins: [] } });

test('no access: a signed-out visitor never reaches the org-scoped shell', async ({ page }) => {
  await page.goto('/');
  await expect(page.getByTestId('org-id')).toHaveCount(0);
  await expect(page).toHaveURL(/\/sign-in|accounts\.dev/);
});

test('no access: /no-access renders the invite-only message', async ({ page }) => {
  await page.goto('/no-access?reason=none');
  await expect(page.getByTestId('no-access')).toBeVisible();
  await expect(page.getByText('invite-only')).toBeVisible();
});
```
Do NOT call `window.Clerk.setActive()` anywhere, and do NOT add an org-selection workaround. If a run lands on `/no-access` when it should not, the fix is `ActivateSoleOrganization` in the app.

Local production smoke — build, start, probe, stop:
1. `pnpm build`
2. Start the production server in the background: `pnpm start` on port 3000. Record the PID you started.
3. Poll `http://localhost:3000/api/health` until it answers or 30 seconds elapse.
4. Assert the response is HTTP 200 and the JSON body is `{"ok":true,"db":"up","proxy":"up","commit":"local"}` (commit may differ). `db: "up"` proves `SUPABASE_DB_POOL_URL` resolves and `app_user` can connect; `proxy: "up"` proves `src/proxy.ts` is in the right directory.
5. Assert the response body contains none of `postgres://`, `@localhost`, `password`, `sk_`, `jahgeqshuesndyscnmjo`.
6. 🔴 Stop ONLY the process you started, by the PID you recorded, after checking its command line is your own `next start`. Never kill node processes by name — danlo runs other sessions on this machine and a broad `taskkill /IM node.exe` takes them with it.

If `db: "down"`: `SUPABASE_DB_POOL_URL` in `.env.local` must point at the local Postgres as `app_user` (`postgres://app_user:app_user@localhost:5432/siteless_test`), and `pnpm db:migrate` must have run at least once to set that local password. Fix the env, not the code.
If `proxy: "down"`: `src/proxy.ts` is in the wrong place. It belongs at `src/proxy.ts`, level with `app/`.

Do NOT run `pnpm test:e2e` here — `E2E_BASE_URL` points at the deployed Vercel URL, which does not exist until plan 10. Plan 11 runs these specs. Confirm only that Playwright can enumerate them: `pnpm exec playwright test --list`.
  </action>
  <verify>
    <automated>pnpm build &amp;&amp; pnpm exec playwright test --list</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm exec playwright test --list` exits 0 and lists three tests, including one whose title contains `signs in and is org-scoped` and two whose titles contain `no access`
    - `grep -rc 'setActive' tests/e2e/` returns 0
    - The local smoke recorded in the SUMMARY shows `HTTP 200` and a body with `"db":"up"` and `"proxy":"up"`
    - The recorded smoke body contains none of: `postgres://`, `@localhost`, `password`, `sk_`, `jahgeqshuesndyscnmjo`
    - The SUMMARY records the PID that was started and confirms only that PID was stopped
    - `pnpm build` exits 0
    - `pnpm typecheck` and `pnpm lint` exit 0
  </acceptance_criteria>
  <done>Two e2e specs are written and enumerable, and a local production build answers /api/health with both the database and the proxy up, leaking nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → Next.js server | Everything the client sends, including any header, is attacker-controlled |
| proxy → page/handler/server function | Server Functions are POSTs to the page's route; a matcher refactor can silently remove proxy coverage |
| Next.js server → Postgres | One pooled connection is reused across requests and tenants |
| health endpoint → public internet | `/api/health` is unauthenticated by design and reachable by anyone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan | Severity |
|-----------|----------|-----------|-------------|-----------------|----------|
| T-1-11 | Elevation of Privilege | `src/proxy.ts` | mitigate | Proxy establishes session context and makes no authorization decision — no `createRouteMatcher`, no `auth.protect()` (both asserted by grep). Authorization is `requireOrg()` inside each page/handler, as close to the data as possible. This is the CVE-2025-29927 class that prompted the middleware rename and Clerk's own deprecation. Task 1. | high |
| T-1-04 | Information Disclosure | `withOrg` on a pooled connection | mitigate | `set_config('request.jwt.claims', $1, true)` — the LOCAL form. Pinned by a persisted test, not only by a grep: `tests/db/with-org.test.ts` asserts the literal `, true)` in the issued SQL and then calls `withOrg` twice on the same `max:1` connection, asserting the GUC is empty between them and that the second transaction sees `org_B` and never `org_A`. The non-local form survives commit and hands the previous tenant's claims to the next request on a transaction-mode pooler (verified). Mutation gate M4. Task 1. | high |
| T-1-05 | Tampering (SQL injection) | `withOrg` claims/role | mitigate | Claims are a bound parameter, never `sql.raw(JSON.stringify(...))` as the Drizzle docs publish. Pinned by a persisted test, not only by a grep: `tests/db/with-org.test.ts` runs the issued `SQL` through `PgDialect.sqlToQuery()` and asserts the text is exactly `select set_config('request.jwt.claims', $1, true)` with `params === [JSON.stringify(claims)]` and no claim value anywhere in the text. The one unavoidable interpolation, `set local role`, is allow-listed against a `Set` — the same test asserts a role outside the list is refused before any SQL is issued. Task 1. | high |
| T-1-01 | Spoofing | org identity | mitigate | `org_id` is read only from Clerk's server-verified `auth()` — never from a header, query param or cookie the app set — and `app.ensure_org` re-checks it against the claim inside the database. Task 1. | high |
| T-1-03 | Elevation of Privilege | `src/db/client.ts` | mitigate | The runtime connects as `app_user`, not the owner, so a path that skips `withOrg` gets 42501 rather than every tenant's rows. `grep 'db.execute' src/` returns exactly 1 — the health probe's `select 1`, which reads no table. Tasks 1-2. | high |
| T-1-15 | Tampering / Information Disclosure | `tests/db/with-org.test.ts` | mitigate | The test reaches the database through `SUPABASE_DB_POOL_URL`, not `TEST_DATABASE_URL`, so it restates D-04 for itself: a hoisted preamble throws before any import can open a pool if the URL matches `/supabase\.(co\|com)\|pooler\.supabase/`. The production project cannot become a test target by way of `.env.local`. Task 1. | high |
| T-1-12 | Information Disclosure | `/api/health` | mitigate | The response is a fixed shape of enum values plus the commit sha; the database error is logged server-side by `e.name` only, never returned. Asserted: no `e.message`, `String(e)` or `err.stack` in the file, and the smoke body is scanned for `postgres://`, `sk_` and the project ref. Tasks 2-3. | medium |
| T-1-26 | Spoofing | active organization | mitigate | Three layers: the Clerk dashboard setting confirmed in plan 02, `ActivateSoleOrganization` mounted in the root layout so it covers every surface a client can reach, and `requireOrg()` treating `orgId === null` as a routing decision so no page can render a query with a null org. Task 2. | high |
| T-1-10 | Information Disclosure | server modules | mitigate | `import 'server-only'` is line 1 of `client.ts`, `with-org.ts` and `require-org.ts`, so a `"use client"` module cannot pull a connection string into the browser bundle. Asserted by grep. The test's `vi.mock('server-only', () => ({}))` is scoped to the vitest module graph and does not weaken the production guard. Task 1. | high |
</threat_model>

<verification>
- `pnpm typecheck`, `pnpm lint` and `pnpm build` all exit 0, and the route listing includes `/`, `/no-access`, `/sign-in/[[...sign-in]]`, `/api/health`
- `pnpm test:db -t "die with the transaction"` exits 0 and its output NAMES `withOrg binds the tenant claims and they die with the transaction`
- Mutation M4 recorded: `true` → `false` in `src/db/with-org.ts`'s `set_config` reds exactly that one named test; reverted; `git diff` empty
- `pnpm exec playwright test --list` lists three tests with the expected titles
- The local `/api/health` smoke returned 200 with `"db":"up"` and `"proxy":"up"` and leaked nothing
- `grep -rc 'setActive' tests/e2e/` and `grep -c 'serviceDb\|SERVICE_ROLE' src/` both return 0
- `ls src/app/proxy.ts` fails; `ls src/proxy.ts` succeeds
</verification>

<success_criteria>
- `clerkMiddleware()` runs from `src/proxy.ts` and makes no authorization decision
- `withOrg` is the only runtime database entry point, with bound claims, an allow-listed role and transaction-local `set_config`
- Those three `withOrg` properties are pinned by `tests/db/with-org.test.ts`, which CI re-runs, and mutation M4 has been executed and reverted
- A signed-in user with an active org sees their user id, their Clerk org id and the provisioned tenant uuid
- A signed-in user with no active org lands on `/no-access`; a signed-out visitor never reaches the shell
- The `orgs` row is provisioned just-in-time through `app.ensure_org`
- `/api/health` returns `{ ok, db, proxy, commit }` and never a database error, a DSN or a claim
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-tenancy/01-08-SUMMARY.md` recording the local `/api/health` response body, the PID started and stopped, the mutation M4 result (which test went red, and that `git diff` was empty after reverting), and whether `NEXT_PUBLIC_CLERK_SIGN_IN_URL` had to be changed locally.
</output>
