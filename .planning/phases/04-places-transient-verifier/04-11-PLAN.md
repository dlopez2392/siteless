---
phase: 04-places-transient-verifier
plan: 11
type: execute
wave: 2
depends_on: [04-09]
files_modified:
  - drizzle/0028_places_meter_retention.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0028_snapshot.json
  - src/db/with-cron-role.ts
  - tests/db/places-definers.test.ts
  - .planning/CONVENTIONS.md
autonomous: true
requirements: [PLACE-02, PLACE-03]

must_haves:
  truths:
    - "app.release_reservation frees an unsettled hold, writes NO ledger row, is idempotent and tenancy-checked — so the queue-time admission hold is released at run start instead of settled as a phantom Enterprise request (D-15, M53)"
    - "app.plan_run_searches records each planned (type × tile) search idempotently per (run, tile_key) and upserts its place_tiles row; app.mark_run_search moves a search's progress through an allow-listed key set"
    - "app.purge_expired_place_coordinates deletes every expired coordinate across orgs, never an observation, writes one place_purge_runs row per org (zero included), and is executable ONLY by siteless_cron (D-12, M38, M39)"
    - "app.places_transient_stats gives /sources its five figures for the caller's org without granting any read on place_coordinates, and never counts an expired coordinate as held (D-12)"
    - "withCronRole is the one allow-listed path that sets role siteless_cron"
  artifacts:
    - path: "drizzle/0028_places_meter_retention.sql"
      provides: "release_reservation, plan_run_searches, mark_run_search, purge_expired_place_coordinates, places_transient_stats"
      contains: "app.release_reservation"
    - path: "src/db/with-cron-role.ts"
      exports: ["withCronRole"]
    - path: "tests/db/places-definers.test.ts"
      contains: "the purge refuses a tenant session"
  key_links:
    - from: "app.purge_expired_place_coordinates"
      to: "siteless_cron"
      via: "revoke from public, anon, authenticated, service_role; grant execute to siteless_cron"
      pattern: "to siteless_cron"
---

<objective>
The first definer migration of the phase: the meter's missing release, the run-search progress
writers the executor needs, and the two retention functions behind D-12's observable purge.

Purpose: research found there is no explicit release today — only `settle_reservation`, which
refuses `units <= 0` and writes a ledger row, so "settling" the admission hold would count a phantom
Enterprise request against the free 1,000 (Pattern 6). The purge must not depend on a run happening
and is cross-org by nature, so it runs under a dedicated NOLOGIN role that no tenant session holds.

Output: `drizzle/0028_places_meter_retention.sql`, `src/db/with-cron-role.ts`, DB proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:db -t "<name>"`, never `-- -t`; read the PASS-list NAME. drizzle-kit is the only
migration authority (`$PNPM db:custom --name=…`, `$PNPM db:migrate`). PG17-safe SQL only.
</pinned_launcher>

<interfaces>
Definer discipline (0024 L9–15, 0025 L36): `language plpgsql security definer set search_path = public, pg_temp`;
org via `v_org := app.current_org_id(); if v_org is null then raise exception '<fn>: no current org' using errcode = '42501';`;
actor via `coalesce(app.jwt()->>'sub', current_setting('app.actor_id', true), 'system')`; every
caller-supplied uuid re-read with `org_id = v_org`; after every `select … into`, an explicit
`if not found` guard (plpgsql `select into` matching nothing does not raise); one `comment on function` each.
Release analog (`drizzle/0018` L45–70, `app.release_expired_reservations`): sets `released_at`,
decrements `budget_periods.reserved_micro_usd` by `est_micro_usd`, writes no ledger row.
Tenancy re-check analog (`drizzle/0019` L60–72): load as owner, `22023` when missing, `42501` when another org.
Run-ownership message (0020 L46–50): same message for "foreign" and "missing" (no existence oracle).
Named-role revoke (0024 L129–139): `revoke execute on function … from public, anon, authenticated, service_role;`.
Tables from 04-09: `run_searches`, `place_tiles`, `place_coordinates`, `place_observations`,
`place_tile_members`, `place_attachments`, `place_purge_runs`, `cost_reservations (id, org_id,
budget_period_id, run_id, sku, est_micro_usd, expires_at, settled_at, released_at)`,
`budget_periods (id, org_id, provider, period_start, cap_micro_usd, spent_micro_usd, reserved_micro_usd)`.
`src/db/with-org.ts` L5–6 + L54–64: allow-listed role before `sql.raw('set local role ' + …)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0028 — five definers, grants and comments; apply locally</name>
  <files>drizzle/0028_places_meter_retention.sql, drizzle/meta/_journal.json, drizzle/meta/0028_snapshot.json</files>
  <read_first>
    - `drizzle/0018_condemned_luke_cage.sql` L40–95, `drizzle/0019_aromatic_bromley.sql` (whole), `drizzle/0020_yellow_ricochet.sql` L28–110, `drizzle/0024_merge_functions.sql` L1–200 and L400–460, `drizzle/0025_review_fixes_spine.sql` L30–40
    - 04-RESEARCH.md § Pattern 6, § Pattern 10
  </read_first>
  <action>
`$PNPM db:custom --name=places_meter_retention`. `--> statement-breakpoint` between every statement.
1. **`app.release_reservation(p_reservation uuid) returns bigint`** — org check; load `r.org_id, r.budget_period_id, r.est_micro_usd, r.settled_at, r.released_at` from `cost_reservations r` by id (`22023` 'no such reservation' if not found; `42501` 'reservation belongs to another org' if `r.org_id <> v_org`); if already settled or released → return 0; else `update cost_reservations set released_at = now() where id = p_reservation and settled_at is null and released_at is null` and, only if that updated a row, `update budget_periods set reserved_micro_usd = reserved_micro_usd - v_est where id = v_period`; return `v_est`. Writes NO `cost_ledger` row. Grant execute to `authenticated` (revoke from public, anon).
2. **`app.plan_run_searches(p_run uuid, p_searches jsonb) returns table (search_id uuid, tile_key text)`** — org check; run ownership (`42501` "plan_run_searches: run belongs to another org or does not exist") and `status in ('queued','running')` else `55000`; for each element of `p_searches` (keys `tileKey, cellKey, clusterKey, unitKind, unitId, placesType, quadPath, depth, south, west, north, east, parentTileKey, kind`): refuse `kind` inconsistent with the run (`change_check` ⇔ `ids_only`) with `22023`; `insert into place_tiles … on conflict (org_id, tile_key) do update set updated_at = now() returning id`; `insert into run_searches (org_id, run_id, tile_id, tile_key, cell_key, cluster_key, places_type, kind, depth, parent_tile_key) … on conflict (run_id, tile_key) do nothing`; return `(select id, tile_key from run_searches where run_id = p_run and tile_key = el.tileKey)`. Grant to `authenticated`.
3. **`app.mark_run_search(p_search uuid, p_state jsonb) returns void`** — org check; load the search with `org_id = v_org` (`42501` if not found); refuse any key outside `status, saturated, subdivided, truncated, truncated_why, inflight_reservation_id, inflight_request_id` with `22023`; an `inflight_reservation_id` must be a `cost_reservations` row of this org whose `run_id` is the search's run (`42501` otherwise). Apply with `coalesce(new, old)` per key (a JSON `null` for `inflight_*` clears it). When `status` becomes `done`: update the tile — `is_leaf = not subdivided`, `saturated`, `truncated`, and `last_swept_run_id = run_id, last_swept_at = now()` for `enterprise` or `last_checked_at = now()` for `ids_only`. Grant to `authenticated`.
4. **`app.purge_expired_place_coordinates(p_trigger text default 'cron') returns table (purged_org uuid, purged_rows int)`** — no org claim (cross-org by nature); `p_trigger in ('cron','desk')` else `22023`;
```sql
return query
with purged as (delete from place_coordinates pc where pc.expires_at <= now() returning pc.org_id),
counts as (select o.id as oid, count(p.org_id)::int as n from orgs o left join purged p on p.org_id = o.id group by o.id),
ins as (insert into place_purge_runs (org_id, rows_purged, trigger)
        select c.oid, c.n, p_trigger from counts c returning place_purge_runs.org_id, place_purge_runs.rows_purged)
select ins.org_id, ins.rows_purged from ins;
```
   Never touches `place_observations`. Then: `grant usage on schema app to siteless_cron;`, `revoke execute on function app.purge_expired_place_coordinates(text) from public, anon, authenticated, service_role;`, `grant execute on function app.purge_expired_place_coordinates(text) to siteless_cron;`. (The migration owner, used by the desk script, owns the function and can always run it.)
5. **`app.places_transient_stats() returns table (place_ids_held bigint, coordinates_held bigint, oldest_coordinate_ms text, expired_awaiting_purge bigint, last_purge_ms text, last_rows_purged int)`** — org check; `place_ids_held` = count of the union of `place_tile_members.place_id` and `place_attachments.place_id` for the org; `coordinates_held` and `oldest_coordinate_ms` over `expires_at > now()` only (epoch ms as text); `expired_awaiting_purge` over `expires_at <= now()`; last purge row for the org (`ran_at` epoch ms text, `rows_purged`), nulls when never run. Grant to `authenticated`.
6. One `comment on function` per definer (cite D-15 / D-12 / Pattern 6 / M-ids).
Apply with `$PNPM db:migrate`; `$PNPM db:generate` must emit nothing.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM db:migrate && $PNPM db:generate && $PNPM test:unit tests/unit/pg17-compat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "security definer set search_path = public, pg_temp" drizzle/0028_places_meter_retention.sql` = 5
    - `grep -n "grant execute on function app.purge_expired_place_coordinates(text) to siteless_cron" drizzle/0028_places_meter_retention.sql` matches
    - `grep -n "cost_ledger" drizzle/0028_places_meter_retention.sql` matches only comment lines
    - `$PNPM db:generate` emits no file
  </acceptance_criteria>
  <done>0028 applied locally with the five definers and their grants.</done>
</task>

<task type="auto">
  <name>Task 2: withCronRole and the database-access convention</name>
  <files>src/db/with-cron-role.ts, .planning/CONVENTIONS.md</files>
  <read_first>
    - `src/db/with-org.ts` (whole file), `src/db/client.ts`, `src/app/api/health/route.ts` L8–21 (the "one exception" header)
    - `tests/db/with-org.test.ts` (what it greps in with-org.ts)
  </read_first>
  <action>
```ts
import 'server-only';
import { sql } from 'drizzle-orm';
import { db } from './client';
/** D-12. The coordinate purge is cross-org by nature, so it cannot run through withOrg().
 *  This is the second database path outside withOrg (after /api/health's `select 1`), and it
 *  can do exactly one thing: siteless_cron holds EXECUTE on app.purge_expired_place_coordinates
 *  and no privilege on any table. Never interpolate a role that came from input. */
const CRON_ROLE = 'siteless_cron' as const;
type Tx = Parameters<Parameters<typeof db.transaction>[0]>[0];
export async function withCronRole<T>(fn: (tx: Tx) => Promise<T>): Promise<T> {
  return db.transaction(async (tx) => {
    await tx.execute(sql.raw('set local role ' + CRON_ROLE));
    return fn(tx);
  });
}
```
CONVENTIONS.md § Database access: add a bullet — "Two further non-`withOrg` paths exist since Phase 4,
each allow-listed and narrow: `withCronRole()` (`set local role siteless_cron`; execute on the purge
function only) and `withWorkerOrg()` (04-16; claims `{o:{id}}` + `app.actor_id` GUC, no `sub`, no
role claim, for workflow steps)."
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM typecheck && $PNPM lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "set local role ' + CRON_ROLE" src/db/with-cron-role.ts` matches
    - `grep -n "withCronRole" .planning/CONVENTIONS.md` matches
  </acceptance_criteria>
  <done>The cron path exists and is documented as an allow-listed exception.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DB proofs for the five definers</name>
  <files>tests/db/places-definers.test.ts</files>
  <read_first>
    - `tests/db/budget-meter.test.ts` L280–340 and L500–530 (definer tenancy refusal + positive control; zero-cost ledger row)
    - `tests/db/_places-fixtures.ts` (04-09), `tests/db/_fixtures.ts` (`actAs`, `actAsRole('siteless_cron')`, `actAsOwner`)
  </read_first>
  <behavior>
    One refusal per `withRollback`, SQLSTATE and message pinned, each with its positive control:
    - `release_reservation frees the hold and writes no ledger row` (M53): reserve 315_000 µUSD for a seeded run as `CLAIMS_A`; release → returns 315000; `released_at` set; `reserved_micro_usd` back to its prior value; `cost_ledger` row count and `sum(units)` for the period unchanged
    - `release_reservation is idempotent` — second call returns 0 and moves nothing
    - `release_reservation refuses another org's reservation` — `CLAIMS_B` → `42501`
    - `release_reservation will not release a settled reservation` — settle first (units 1), release → 0
    - `plan_run_searches is idempotent per tile` — same payload twice → same `search_id`, one `run_searches` row, one `place_tiles` row
    - `plan_run_searches refuses another org's run` — `42501`
    - `plan_run_searches refuses an ids_only search on a sweep run` — `22023`
    - `mark_run_search refuses an unknown key` — `{ "displayName": "x" }` → `22023`
    - `mark_run_search done marks the tile swept and non-leaf when subdivided`
    - `the purge removes expired coordinates and keeps the observation` (M38): as owner seed one expired (observed now − 31 d, expires now − 1 d) and one fresh coordinate in org A, one expired in org B; `actAsRole(c, 'siteless_cron')`; call → org A 1, org B 1 (and every other org 0); both expired rows gone, fresh kept; `place_observations` count unchanged; `place_purge_runs` gained one row per org
    - `the purge refuses a tenant session` (M39): as `CLAIMS_A` → `42501`, `/permission denied for function purge_expired_place_coordinates/`
    - `the purge refuses an unknown trigger` → `22023`
    - `siteless_cron can read no tenant table`: as `siteless_cron`, `select 1 from runs` → `42501`
    - `transient stats count only what the org holds` — A sees its place ids / coordinates; B sees zeros
    - `transient stats never report an expired coordinate as held` — an expired, not-yet-purged row shows in `expired_awaiting_purge` and not in `coordinates_held` or `oldest_coordinate_ms`
  </behavior>
  <action>Write the tests with the `_places-fixtures.ts` helpers; watch each refusal and the purge test fail first (e.g. run before applying 0028, or with the assertion inverted), quote the red names in the SUMMARY.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "release_reservation" && $PNPM test:db -t "plan_run_searches" && $PNPM test:db -t "mark_run_search" && $PNPM test:db -t "the purge" && $PNPM test:db -t "transient stats" && $PNPM test:db -t "siteless_cron can read no tenant table"</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all fifteen tests
    - `grep -n "permission denied for function purge_expired_place_coordinates" tests/db/places-definers.test.ts` matches
    - Full `$PNPM test:db` green
  </acceptance_criteria>
  <done>Release, planning, progress, purge and stats are proven in the database, including their refusals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant/workflow session → definers | caller-supplied uuids and jsonb cross into owner-privileged code |
| Vercel Cron → purge | cross-org deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Tampering (cross-tenant) | release / plan / mark / stats | mitigate | org from claims, never a parameter; every uuid re-read with `org_id = v_org`; same message for foreign and missing; named refusal tests |
| T-4-07 | Elevation of privilege | cross-org purge | mitigate | execute revoked from public/anon/authenticated/service_role, granted to siteless_cron only; siteless_cron holds no table privilege (named test) |
| T-4-04 | Information disclosure (legal retention) | stats read path | mitigate | counts only; expired rows never reported as held; no row-level coordinate read exists |
| T-4-02 | Denial of service (financial) | admission hold | mitigate | explicit release frees the hold without a phantom ledger unit (M53), so the free allowance stays true |
| T-4-10 | Tampering | `mark_run_search` jsonb | mitigate | allow-listed keys only (22023 otherwise) — no Places text can be smuggled into progress rows |
</threat_model>

<verification>
- `$PNPM db:migrate` + `$PNPM db:generate` (no drift); full `$PNPM test:db`; `$PNPM typecheck`
</verification>

<success_criteria>
- D-15's release and D-12's purge/stats exist as tenancy-checked definers with named proofs
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-11-SUMMARY.md`
</output>
