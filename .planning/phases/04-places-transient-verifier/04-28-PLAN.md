---
phase: 04-places-transient-verifier
plan: 28
type: execute
wave: 8
depends_on: [04-17, 04-23, 04-24, 04-25, 04-27]
files_modified:
  - tests/unit/google-maps-attribution.test.tsx
  - tests/e2e/runs.spec.ts
  - tests/e2e/budget-banner.spec.ts
autonomous: true
requirements: [PLACE-06, PLACE-03]

must_haves:
  truths:
    - "One registry test renders every Places-signal surface and proves each [data-places-content] container holds exactly one Google Maps tag; deleting the tag from any surface reds it by name (PLACE-06, D-11, Rule 28, M44)"
    - "A repo walk proves every file importing places-format also imports GoogleMapsTag (Rule 28)"
    - "A local-only e2e spec renders a real run report from the local database and pins the tag's COMPUTED colour (rgb(94, 94, 94) light, rgb(255, 255, 255) dark) and Roboto font stack, the truncation warning and the 44px controls (Rule 29)"
    - "The budget-banner specs that lower the production cap skip while any run is queued or running, so an e2e run can never stop a live sweep (UI-SPEC OQ 14 hazard)"
    - "The whole phase is green: typecheck, lint, unit, db, workflow, build"
  artifacts:
    - path: "tests/unit/google-maps-attribution.test.tsx"
      contains: "google maps attribution renders wherever a places signal renders"
    - path: "tests/e2e/runs.spec.ts"
      contains: "TARGET_IS_LOCAL"
  key_links:
    - from: "tests/unit/google-maps-attribution.test.tsx PLACES_SIGNAL_SURFACES"
      to: "google-check.tsx, google-listing-card.tsx, tiles-card.tsx, outcomes-card.tsx, changes-card.tsx"
      via: "render each with a fixture"
      pattern: "PLACES_SIGNAL_SURFACES"
---

<objective>
Close the UI side of PLACE-06 with the repo-wide attribution registry, prove the tag's painted styles
on a real rendered screen, guard the production cap specs, and run the full phase gate before any
human checkpoint.

Purpose: Rule 28 requires the tag "wherever" a Places-derived value renders — a per-surface test only
proves the surfaces someone remembered. The registry plus the import walk is what makes "wherever" true.

Output: one registry test, one local-only e2e spec, one e2e guard, a green full suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME.
🔴 e2e here runs against a LOCAL `next build && next start` only (`E2E_BASE_URL=http://localhost:3000`). Kill only the PID of the
`next start` YOU launched; never kill other node processes.
</pinned_launcher>

<interfaces>
Surfaces (props per their plans): `GoogleCheck` (04-25), `GoogleListingCard` (04-24), `TilesCard` / `OutcomesCard` / `ChangesCard` (04-23).
`tests/e2e/preset-detail.spec.ts` L36–45: the `TARGET_IS_LOCAL` self-skip (the two-databases trap: `withDb()` opens the LOCAL test DB).
`tests/e2e/theme-tokens.spec.ts`: computed-style probe pattern (assert `innerHeight > 0` and visibility first — a hidden window lies).
`tests/e2e/budget-banner.spec.ts` L84–90 and L114–120: the two tests that lower the production cap to ~115 % of committed and restore it in `finally`.
04-14: `/spend` By-run rows carry `RunStatusBadge` with `data-status`. 04-09 `_places-fixtures.ts` + 04-15 writers for seeding.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The attribution registry and the import walk (M44)</name>
  <files>tests/unit/google-maps-attribution.test.tsx</files>
  <read_first>
    - The five surface components and their tests (04-23, 04-24, 04-25)
    - 04-UI-SPEC.md Rule 28
  </read_first>
  <behavior>
    - `google maps attribution renders wherever a places signal renders` — `PLACES_SIGNAL_SURFACES` = business Google check signal row, check-history rows (opened), pending (tentative) listing row, review Google listing card, run report Tiles card, Outcomes card, Changes card; each rendered with a fixture; every `[data-places-content]` element in each render contains exactly one `[data-testid="google-maps-attribution"]`, and each surface renders ≥ 1 such container (two-sided). A failure names the surface.
    - `every places formatter import brings the tag with it` — every file under `src/components/` and `src/app/` whose source imports `@/lib/ui/places-format` also imports `GoogleMapsTag`; the walk finds ≥ 3 importers (two-sided).
  </behavior>
  <action>Write both tests (dom lane). Then run M44: delete `<GoogleMapsTag />` from the signal row of `google-check.tsx`, run `$PNPM test:unit -t "google maps attribution renders wherever a places signal renders"`, confirm it goes red NAMING the business-signal surface (and that the second test does not red — the import may remain), revert, `git diff --stat src/` empty. Record both runs.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "google maps attribution renders wherever a places signal renders" && $PNPM test:unit -t "every places formatter import brings the tag with it"</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names both tests
    - `grep -c "name:" tests/unit/google-maps-attribution.test.tsx` ≥ 7 (seven registered surfaces)
    - SUMMARY quotes M44's red run naming the surface, then the clean revert
  </acceptance_criteria>
  <done>"Wherever" is enforced by a registry and an import walk, mutation-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Local-only run-report e2e (computed tag styles) and the budget-banner guard</name>
  <files>tests/e2e/runs.spec.ts, tests/e2e/budget-banner.spec.ts</files>
  <read_first>
    - `tests/e2e/preset-detail.spec.ts` (whole — `withDb`, `tenantId`, `TARGET_IS_LOCAL`, teardown in FK order), `tests/e2e/theme-tokens.spec.ts`, `tests/e2e/budget-banner.spec.ts`
    - 04-UI-SPEC.md Rules 29, 36, 37, OQ 14; § Accessibility (44px)
  </read_first>
  <action>
1. `tests/e2e/runs.spec.ts`: `test.skip(!TARGET_IS_LOCAL, 'seeds the LOCAL database; the deployed app reads another one (two-databases trap)')`.
   `beforeAll` seeds, in the local DB for the signed-in tenant, a `complete` McAllen × home_services run with: two searches (one `truncated/min_size`),
   one attached listing (host `business_site_dead`), one tentative, one unmatched — through `app.plan_run_searches`, `app.record_places_page`
   and `app.mark_run_search` under the tenant's claims, plus two settled ledger rows. `afterAll` deletes them in FK order. Tests (testids and
   numbers only, never copy): `/runs/{id}` shows `run-report-header` with `run-status-badge[data-status="complete"]`; `run-truncation-warning[data-count="1"]`;
   `run-outcome-attached[data-count="1"]`; every `google-maps-attribution` has computed `color` `rgb(94, 94, 94)` in light and `rgb(255, 255, 255)`
   after switching to dark, and computed `font-family` starting with `Roboto` (assert `innerHeight > 0` and visibility first); `run-refresh-now`
   is absent (terminal); at 390×844 the nav item Presets is active and `run-truncation-toggle` is ≥ 44px tall. Also visit `/presets/{id}` and
   assert `preset-recent-runs` lists the run with a link to `/runs/{id}`.
2. `budget-banner.spec.ts`: before each of the two cap-lowering tests, open `/spend` → By run and, if any `[data-testid="run-status-badge"][data-status="running"], [data-status="queued"]` is visible,
   `test.skip(true, 'a Places run is live; lowering the cap now could stop it (04-UI-SPEC OQ 14)')`. Leave their existing `committed ≤ 0` self-skip in place (04-32 decides whether it can become real).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM typecheck && $PNPM lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "TARGET_IS_LOCAL" tests/e2e/runs.spec.ts` matches; `grep -n "getByText(" tests/e2e/runs.spec.ts` returns nothing
    - `grep -n "rgb(94, 94, 94)" tests/e2e/runs.spec.ts` and `grep -n "rgb(255, 255, 255)" tests/e2e/runs.spec.ts` match
    - `grep -n "a Places run is live" tests/e2e/budget-banner.spec.ts` matches twice
    - The local run of `runs.spec.ts` (Task 3) passes and its output names each test
  </acceptance_criteria>
  <done>The tag's painted styles are proven on a real screen; the production cap specs cannot stop a live run.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate, locally</name>
  <files>tests/e2e/runs.spec.ts</files>
  <read_first>
    - `docs/deploy.md` (local build/start), memory rule: judge e2e flake by wall clock; print branch + HEAD after long gates
  </read_first>
  <action>
Run, individually: `$PNPM typecheck`, `$PNPM lint`, `$PNPM test:unit`, `$PNPM test:db`, `$PNPM test:workflow`, `$PNPM build`. Then
`$PNPM start` in the background (record its PID), run `E2E_BASE_URL=http://localhost:3000 $PNPM test:e2e tests/e2e/runs.spec.ts tests/e2e/preset-detail.spec.ts`,
then stop exactly that PID. Record each command's exit code and the e2e PASS names in the SUMMARY, then `git branch --show-current` and
`git rev-parse --short HEAD`. Any red: stop and report — do not proceed to the checkpoints (04-29+) on a red tree.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM typecheck && $PNPM lint && $PNPM test:unit && $PNPM test:db && $PNPM test:workflow && $PNPM build</automated>
  </verify>
  <acceptance_criteria>
    - All six commands exit 0; the local e2e run names `runs.spec.ts` and `preset-detail.spec.ts` tests as passed
    - SUMMARY records branch and HEAD after the gate
  </acceptance_criteria>
  <done>The phase's code is complete and green before any human gate opens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e → production cap | two specs temporarily lower the real cap |
| e2e → local DB | seeds with the owner credential, local only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-13 | Repudiation (legal: attribution) | every Places surface | mitigate | registry + import walk + computed-style probe; M44 mutation-checked |
| T-4-09 | Denial of service (financial / availability) | budget-banner specs | mitigate | skip while any run is queued/running on the deployment |
| T-4-01 | Information disclosure | e2e seeding | mitigate | `runs.spec.ts` self-skips unless local; never uses a production credential (docs/deploy.md § 3) |
</threat_model>

<verification>
- Full suite green locally; local e2e green; M44 recorded
</verification>

<success_criteria>
- PLACE-06 is enforced repo-wide; the tree is green before the human checkpoints
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-28-SUMMARY.md`
</output>
