---
phase: 04-places-transient-verifier
plan: 15
type: execute
wave: 3
depends_on: [04-03, 04-06, 04-09, 04-10, 04-11]
files_modified:
  - src/lib/places/page-record.ts
  - drizzle/0029_places_writers.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0029_snapshot.json
  - tests/unit/page-record.test.ts
  - tests/db/places-writer.test.ts
autonomous: true
requirements: [PLACE-02, PLACE-04, PLACE-05]

must_haves:
  truths:
    - "One definer, app.record_places_page, is the only writer of tile membership, run outcomes, attachments, observations and coordinates; only ids, numbers, enums, booleans and our own keys cross into it (D-05, D-06, D-10, PLACE-02)"
    - "place_attachments.features is CHECKed numeric-only in the database (allow-listed keys; numbers, null, the signal-name array, the rule enum), so Places text is refused at the table even if application code regresses (M36 defence in depth)"
    - "A rejected pair and a human-confirmed attachment are sticky: a later run never changes them (D-05, M40)"
    - "Unmatched results keep only their place_id as a tile member and are counted per cluster as 'unmatched' outcomes; an outside-the-US result is a member and an 'outside' outcome, never an attachment (D-06)"
    - "Each observation's coordinates get expires_at = observed_at + 30 days exactly, and a second page carrying the same place in one run never duplicates its observation (D-10)"
    - "app.record_change_check inserts new members, marks gone ones with gone_at (never deletes), and flags the tile changed; app.decide_place_attachment confirms / rejects / detaches with the pending-only 55000 rule (D-05, D-16)"
  artifacts:
    - path: "src/lib/places/page-record.ts"
      exports: ["PageRecord", "PageRecordPlace", "FEATURE_KEYS", "toPageRecord"]
    - path: "drizzle/0029_places_writers.sql"
      provides: "app.places_features_ok, pa_features_numeric CHECK, app.record_places_page, app.record_change_check, app.decide_place_attachment"
      contains: "app.record_places_page"
    - path: "tests/db/places-writer.test.ts"
      contains: "a rejected pair never re-attaches"
  key_links:
    - from: "src/lib/places/page-record.ts toPageRecord"
      to: "app.record_places_page(p_search, p_record)"
      via: "the jsonb contract — camelCase keys placeId, outOfArea, pureSab, hadWebsiteUri, hostClass, lat, lng, matches[businessId, score, status, reason, tieBusinessId, features]"
      pattern: "hadWebsiteUri"
    - from: "tests/db/places-writer.test.ts"
      to: "src/lib/places/page-record.ts"
      via: "the DB tests build every record through toPageRecord (producer→consumer contract, never a hand-typed JSON)"
      pattern: "toPageRecord"
---

<objective>
The result writers: the TypeScript page-record contract and the three definers that persist what
survives a Places call, plus the table-level numeric-only guard on attachment features.

Purpose: the matcher (04-06) decides in memory; this plan is where those decisions become rows, and
the only place Google-derived data touches disk. D-05 (attach / tentative / sticky reject), D-06
(unmatched keep only the id), D-08 (ties), D-10 (append-only observations with 30-day coordinates),
D-16 (change-check membership diff).

Output: `page-record.ts`, `drizzle/0029_places_writers.sql`, unit + DB tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:db -t "<name>"` / `$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME.
drizzle-kit only (`$PNPM db:custom --name=…`, `$PNPM db:migrate`). PG17-safe SQL only.
</pinned_launcher>

<interfaces>
From 04-06 `src/lib/places/match.ts`:
```ts
export type PlaceFeatures = Features & { city?: 0 | 1; sab?: 0 | 1; listingPhone?: 0 | 1; listingLocation?: 0 | 1 };
export type MatchDecision = { placeId: string; outcome: 'attached' | 'tentative' | 'unmatched' | 'outside';
  matches: Array<{ businessId: string; score: number; status: 'attached' | 'tentative'; reason: 'score' | 'tie'; tieBusinessId: string | null; features: PlaceFeatures }> };
```
`Features` keys: `name, phone, address, distance, cluster, nameSim, distanceM (number|null), signals (('name'|'phone'|'address'|'distance')[]), rule? ('phone_locality_name'|'phone_locality_review'|'over_25km'|'sab_phone_city')`.
From 04-03: `type HostClass`. From 04-09: all Places tables and their CHECKs; `place_attachments_pair_key (org_id, business_id, place_id)`;
`place_observations_run_pair_key (run_id, business_id, place_id)`; `run_place_outcomes_key (run_id, place_id, cluster_key)`;
`place_tile_members_key (tile_id, place_id)`; `run_searches (id, org_id, run_id, tile_id, cluster_key, kind, …)`.
From 04-11: `app.mark_run_search`. Definer discipline and the sticky pending-only UPDATE: `drizzle/0024` L151–199 (`record_merge`) and L403–438 (`record_candidate_decision`: decision enum `22023`, re-read under org `42501`, pending-only UPDATE, `if not found` → `55000` 'already decided').
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The page-record contract (TS)</name>
  <files>src/lib/places/page-record.ts, tests/unit/page-record.test.ts</files>
  <read_first>
    - `src/lib/places/match.ts` (04-06), `src/lib/places/host-class.ts` (04-03)
    - `src/lib/resolve/score.ts` L1–35 (T-3-11: features carry numbers only)
  </read_first>
  <behavior>
    - "page record carries no Places text": a record built from decisions whose inputs contained `SENTINEL Ortiz Plumbing` etc. serializes with none of `tests/unit/msw/places.ts` `PLACES_SENTINELS`
    - "page record drops feature keys outside the allow-list": a features object with an extra `displayName: 'x'` → `toPageRecord` throws `Error('toPageRecord: feature displayName is not allow-listed')` (refuse, never silently drop — a silent drop would make M36 unkillable)
    - "page record refuses a non-numeric feature value": `{ name: 'Ortiz' }` → throws naming `name`
    - "page record keeps the contract keys": the serialized record has exactly the keys `page, sku, resultsSoFar, places` and each place exactly `placeId, outOfArea, pureSab, hadWebsiteUri, hostClass, lat, lng, matches`
  </behavior>
  <action>
```ts
export const FEATURE_KEYS = ['name','phone','address','distance','cluster','nameSim','distanceM','signals','rule','city','sab','listingPhone','listingLocation'] as const;
export type PageRecordPlace = { placeId: string; outOfArea: boolean; pureSab: boolean; hadWebsiteUri: boolean; hostClass: HostClass;
  lat: number | null; lng: number | null; matches: MatchDecision['matches'] };
export type PageRecord = { page: 1 | 2 | 3; sku: 'ts_enterprise' | 'ts_essentials'; resultsSoFar: number; places: PageRecordPlace[] };
export function toPageRecord(i: { page: 1 | 2 | 3; sku: PageRecord['sku']; resultsSoFar: number;
  items: Array<{ decision: MatchDecision; pureSab: boolean; hadWebsiteUri: boolean; hostClass: HostClass; lat: number | null; lng: number | null }> }): PageRecord;
```
`toPageRecord` validates every match's `features`: keys ⊆ `FEATURE_KEYS`; numeric keys are finite numbers
(or `null` for `distanceM`); `signals` is an array of `'name'|'phone'|'address'|'distance'`; `rule` is one of
the four rule strings; `city/sab/listingPhone/listingLocation` ∈ {0,1}. Throws naming the key otherwise. `lat/lng`
are passed through only when both are finite numbers, else both null. PURE, no server import.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "page record" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all four page-record tests
    - `grep -n "export const FEATURE_KEYS" src/lib/places/page-record.ts` matches
  </acceptance_criteria>
  <done>The step→writer contract is a validated TS type that refuses text.</done>
</task>

<task type="auto">
  <name>Task 2: Migration 0029 — the features guard and the three writers; apply locally</name>
  <files>drizzle/0029_places_writers.sql, drizzle/meta/_journal.json, drizzle/meta/0029_snapshot.json</files>
  <read_first>
    - `drizzle/0024_merge_functions.sql` L1–200 and L400–460, `drizzle/0025_review_fixes_spine.sql` L30–40, `drizzle/0028_places_meter_retention.sql` (04-11)
    - 04-RESEARCH.md § Pattern 7 (collisions), § Pattern 8, § Pattern 9
  </read_first>
  <action>
`$PNPM db:custom --name=places_writers`; `--> statement-breakpoint` between every statement.
1. `create or replace function app.places_features_ok(f jsonb) returns boolean language sql immutable` —
   true iff `jsonb_typeof(f) = 'object'` and every key is in the 13 allow-listed keys and: numeric keys
   `jsonb_typeof in ('number')` (`distanceM` also `'null'`); `signals` an array whose elements are all in
   `('name','phone','address','distance')`; `rule` a string in the four rule values; `city`, `sab`,
   `listingPhone`, `listingLocation` in `(0,1)`. Then `alter table place_attachments add constraint pa_features_numeric check (app.places_features_ok(features));`
   (comment: "T-3-11 made a database fact — Places text cannot be stored in features even if the application regresses").
2. `app.record_places_page(p_search uuid, p_record jsonb) returns jsonb` — definer; org from claims (`42501`);
   load `run_searches` by id with `org_id = v_org` (`42501` "record_places_page: search belongs to another org or does not exist"),
   taking `run_id, tile_id, cluster_key`; `v_page := (p_record->>'page')::int` in 1..3 else `22023`; `v_sku` in
   (`ts_enterprise`,`ts_essentials`) else `22023`. For each `pl` in `p_record->'places'`:
   a. `insert into place_tile_members (org_id, tile_id, place_id) … on conflict (tile_id, place_id) do update set last_seen_at = now(), gone_at = null`.
   b. if `outOfArea` → outcome `outside`; skip matches.
   c. for each `m` in `pl->'matches'`: re-read the business `where id = m.businessId and org_id = v_org`
      (`42501` "record_places_page: business belongs to another org or does not exist"); if it is merged
      (`merged_into_id is not null`) skip it (the candidate query excludes merged rows; this is the race);
      upsert: `insert into place_attachments (org_id, business_id, place_id, status, reason, score, features, tie_business_id, first_seen_run_id, last_seen_run_id) values (…) on conflict (org_id, business_id, place_id) do update set status = excluded.status, reason = excluded.reason, score = excluded.score, features = excluded.features, tie_business_id = excluded.tie_business_id, last_seen_run_id = excluded.last_seen_run_id where place_attachments.status <> 'rejected' and place_attachments.reason <> 'confirmed' returning id, status`;
      when no row returns (sticky), `select id, status` of the existing row.
      Final status `attached` or `tentative` → `insert into place_observations (org_id, business_id, place_id, run_id, attachment_id, had_website_uri, host_class, sku, pure_sab) … on conflict (run_id, business_id, place_id) do nothing returning id, observed_at`;
      when inserted and `lat`/`lng` are non-null → `insert into place_coordinates (org_id, observation_id, lat, lng, observed_at, expires_at) values (…, v_obs_at, v_obs_at + interval '30 days')`.
   d. place outcome = `attached` if any final status is attached, else `tentative` if any is tentative, else `unmatched` (a rejected pair is `unmatched`); upsert `run_place_outcomes` on `(run_id, place_id, cluster_key)` keeping the higher rank (`attached` 3 > `tentative` 2 > `unmatched` 1 > `outside` 0).
   Finally `update run_searches set status = 'searching', pages_done = greatest(pages_done, v_page), results_count = (p_record->>'resultsSoFar')::int, inflight_reservation_id = null, inflight_request_id = null where id = p_search`.
   Return `jsonb_build_object('attached', …, 'tentative', …, 'unmatched', …, 'outside', …)` counts for this page.
3. `app.record_change_check(p_search uuid, p_added jsonb, p_gone jsonb, p_verdict text, p_seen int) returns void` — definer; org + search ownership as above; the search's `kind` must be `ids_only` (`22023`); `p_verdict` in the six verdicts (`22023`); insert each added id into `place_tile_members` (on conflict revive: `gone_at = null, last_seen_at = now()`); `update place_tile_members set gone_at = now() where tile_id = v_tile and place_id in (select jsonb_array_elements_text(p_gone)) and gone_at is null` (never delete); `update place_tiles set last_checked_at = now(), changed_at = case when p_verdict in ('new','gone','both','saturated') then now() else changed_at end`; `update run_searches set change_verdict = p_verdict, new_ids = jsonb_array_length(p_added), gone_ids = jsonb_array_length(p_gone), results_count = p_seen, pages_done = greatest(pages_done, 1), inflight_reservation_id = null, inflight_request_id = null`.
4. `app.decide_place_attachment(p_attachment uuid, p_decision text) returns table (business_id uuid, place_id text, status text)` — definer; `p_decision in ('confirm','reject','detach')` else `22023`; re-read the attachment under `org_id = v_org` (`42501`); `confirm`: `update … set status = 'attached', reason = 'confirmed', decided_by = v_actor, decided_at = now() where id = p_attachment and status = 'tentative'`; `reject`: same guard → `status = 'rejected', reason = 'rejected'`; `detach`: guard `status = 'attached'` → `status = 'rejected', reason = 'detached'`; `if not found then raise exception 'decide_place_attachment: already decided' using errcode = '55000'`. The narrowed `log_event` trigger (04-09) records the status change.
5. Grants: execute on the three writers and `places_features_ok` to `authenticated` (revoke from public, anon); one `comment on function` each.
Apply with `$PNPM db:migrate`; `$PNPM db:generate` must emit nothing.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM db:migrate && $PNPM db:generate && $PNPM test:unit tests/unit/pg17-compat.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "add constraint pa_features_numeric check (app.places_features_ok(features))" drizzle/0029_places_writers.sql` matches
    - `grep -n "where place_attachments.status <> 'rejected' and place_attachments.reason <> 'confirmed'" drizzle/0029_places_writers.sql` matches
    - `grep -n "interval '30 days'" drizzle/0029_places_writers.sql` matches
    - `grep -n "delete from place_tile_members" drizzle/0029_places_writers.sql` returns nothing
  </acceptance_criteria>
  <done>0029 applied locally; the writers and the features guard exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DB proofs for the writers (records built through toPageRecord)</name>
  <files>tests/db/places-writer.test.ts</files>
  <read_first>
    - `tests/db/_places-fixtures.ts` (04-09), `tests/db/review-actions.test.ts` L80–120, `tests/db/places-definers.test.ts` (04-11)
  </read_first>
  <behavior>
    Every record is built with `toPageRecord` from `MatchDecision`s (never hand-typed JSON). Seed with `seedPlacesSpine` + `seedPlacesRun` + `seedRunSearch`; act as `CLAIMS_A`.
    - `record_places_page attaches, observes and records outcomes` — ortiz attached (host `none`, lat/lng set): one attachment `attached/score`, one observation, one coordinate with `extract(epoch from expires_at - observed_at) = 2592000`, one member, outcome `attached`, `run_searches.pages_done = 1`
    - `a second page for the same place does not duplicate its observation`
    - `a rejected pair never re-attaches` (M40) — pre-seeded `rejected` pair + a 97 match → stays `rejected`, no observation, outcome `unmatched`
    - `a confirmed attachment is not re-scored by a later run` — `attached/confirmed` + a later 85 → unchanged
    - `a tie writes two tentative rows naming each other` — rio + rioCo
    - `a tentative match is observed but not a signal` — observation exists; `business_place_signal` has no row
    - `an outside listing is a member and an outcome, never an attachment`
    - `an unmatched listing keeps only its place id` — member row + outcome `unmatched`; zero attachments/observations for it
    - `record_places_page refuses another org's search` — `42501`
    - `record_places_page refuses a business of another org` — `42501`
    - `a merged business is never attached`
    - `place attachments refuse text in features` — owner insert with `features = '{"displayName":"x"}'` → `23514`, `pa_features_numeric`
    - `record_places_page refuses Places text in features` — bypass `toPageRecord` ONCE, deliberately, with a record whose features carry `"name": "Ortiz Plumbing"` → `23514`
    - `record_change_check inserts new members and marks gone ones` — verdict `both`: added inserted, gone get `gone_at`, `place_tiles.changed_at` set, `run_searches.change_verdict = 'both'`, counts match
    - `record_change_check never deletes a member`
    - `decide_place_attachment confirms a tentative listing` · `… rejects a tentative listing` · `… detaches an attached listing` · `… refuses a decided listing` (`55000`) · `… refuses another org's attachment` (`42501`)
    - `a listing decision writes one event` — one `events` row with `entity_type = 'place_attachments'` and `action = 'update'` per decision
  </behavior>
  <action>Write the tests; watch each refusal and M40's sticky test fail first (apply the upsert without the `where` guard on the live DB, run, revert from `pg_get_functiondef`), quote the red names in the SUMMARY.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "record_places_page" && $PNPM test:db -t "a rejected pair never re-attaches" && $PNPM test:db -t "record_change_check" && $PNPM test:db -t "decide_place_attachment" && $PNPM test:db -t "place attachments refuse text in features" && $PNPM test:db</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names every test above
    - `grep -c "toPageRecord" tests/db/places-writer.test.ts` ≥ 5
    - Full `$PNPM test:db` green
  </acceptance_criteria>
  <done>What survives a Places call is written by one definer, with stickiness, ties, TTL and text-refusal proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow step → writer definers | derived Places data and our keys cross into owner-privileged code |
| reviewer session → `decide_place_attachment` | a human decision changes attachment state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05 | Information disclosure (legal) | features / all writer inputs | mitigate | `toPageRecord` refuses non-allow-listed keys; `pa_features_numeric` CHECK refuses text at the table; writer columns are booleans/enums/ids/numbers only |
| T-4-06 | Tampering (cross-tenant) | writers | mitigate | org from claims; search, business and attachment re-read under `org_id = v_org`; named 42501 tests |
| T-4-04 | Information disclosure (legal retention) | coordinates | mitigate | `expires_at = observed_at + 30 days` exactly, CHECKed by 04-09; tested by epoch difference |
| T-4-10 | Tampering | jsonb inputs | mitigate | all values read with typed casts inside one definer; enums validated (`22023`) |
| T-4-06 | Repudiation | listing decisions | mitigate | `decided_by` from claims; `log_event` row per status change (named test) |
</threat_model>

<verification>
- `$PNPM db:migrate` + `$PNPM db:generate` (no drift); `$PNPM test:db` and `$PNPM test:unit` green; `$PNPM typecheck`
</verification>

<success_criteria>
- Every Places-derived write goes through one tested definer that refuses text, honours sticky decisions and dates coordinates
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-15-SUMMARY.md`
</output>
