---
phase: 04-places-transient-verifier
plan: 19
type: execute
wave: 4
depends_on: [04-03, 04-05, 04-09, 04-10, 04-12, 04-15, 04-16]
files_modified:
  - src/lib/places/check-tile.ts
  - tests/db/places-check-tile.test.ts
  - scripts/lib/anonymize-places.ts
  - scripts/lib/record-guard.ts
  - scripts/record-places-fixtures.ts
  - tests/unit/anonymize-places.test.ts
  - package.json
autonomous: true
requirements: [PLACE-04, PLACE-02]

must_haves:
  truths:
    - "A change check lists a leaf tile's place ids with the free IDs-only mask through the same builder and meter, ledgers every page at $0 with units 1, and never sends the Enterprise mask (D-16, PLACE-04)"
    - "Its diff against the tile's stored members is written by record_change_check: new ids inserted, gone ids marked gone_at (never deleted), the tile flagged changed; a leaf now at 60 is 'saturated' (D-06, D-16)"
    - "Real Places payloads are anonymized in memory before anything is written: ids, types, SAB flags, host classes, pagination and counts are kept; names, addresses, phones, URL hosts, ratings and coordinates are synthesized (coordinates re-drawn inside the searched rectangle) (D-20)"
    - "The recorder refuses unless PROJECT.md records the D-01 legal decision, refuses any target but the local database, and hard-caps its requests (D-01, D-04)"
  artifacts:
    - path: "src/lib/places/check-tile.ts"
      exports: ["runCheckTile"]
    - path: "scripts/lib/anonymize-places.ts"
      exports: ["anonymizePage", "AnonymizeContext"]
    - path: "scripts/lib/record-guard.ts"
      exports: ["assertLegalRecord", "assertLocalTarget", "MAX_RECORD_REQUESTS"]
    - path: "scripts/record-places-fixtures.ts"
      provides: "the D-04 desk recorder, metered through the real meter against the local DB"
  key_links:
    - from: "src/lib/places/check-tile.ts"
      to: "src/lib/places/change-detect.ts diffTile + app.record_change_check"
      via: "stored members read under RLS, diffed in TS, written by the definer"
      pattern: "diffTile"
    - from: "scripts/record-places-fixtures.ts"
      to: "src/lib/places/meter.ts reservePage + client.searchText"
      via: "the same two primitives the product uses — criterion 5 holds for recordings too"
      pattern: "reservePage"
---

<objective>
The IDs-only change-check body (D-16, PLACE-04's "change detection uses the free IDs-Only SKU") and
the D-20 anonymizing fixture recorder that D-04 (04-32) will run on real payloads.

Purpose: change detection makes Enterprise spend scale with churn, not inventory. The recorder lets
the fixtures become real in structure without a byte of Google text reaching git — and without
bypassing the meter (every recording request is reserved and ledgered like any other).

Output: `check-tile.ts` + DB proofs; `anonymize-places.ts`, `record-guard.ts`, the recorder script + unit proofs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:db -t "<name>"` / `$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME.
🔴 The recorder is NOT run in this plan — it makes real, billed Google calls and is run only at 04-32
after the D-01 and D-03 checkpoints.
</pinned_launcher>

<interfaces>
04-18 (same wave — do NOT edit its file): `src/lib/places/search-tile.ts` will export `SweepInput = { runId; clerkOrgId }`
and `TileStepResult = SearchResult | { kind:'fail'; tileKey; reason: FailReason; retryable: boolean; retryAfterMs? }`.
Declare the identical two types locally in `check-tile.ts` (a later plan, 04-22, may dedupe them) and mirror 04-18's
error-mapping table exactly: `daily_quota` → release → `stopped/google_daily_quota`; `rate_limited` / `unavailable` →
release → fail `places_unavailable` retryable; `rejected` → release → fail `places_request_rejected`; `no_key` → release →
fail `places_key_missing`; `timeout` → settle charged → fail retryable; `bad_shape` → settle charged → fail `places_unavailable` non-retryable.
04-16 meter: `reservePage`, `settleOrRelease`, `settleInFlight`. 04-12: `buildFirstPage({ placesType, rect, mode: 'ids_only' })`
(mask `places.id,nextPageToken`, sku `ts_essentials`), `buildNextPage`, `searchText`, `placesKeyConfigured`.
04-03 `diffTile(stored: ReadonlySet<string>, seen: readonly string[], { hasBaseline }) → { verdict, added, gone }`, `hostClass`.
04-15 `app.record_change_check(p_search uuid, p_added jsonb, p_gone jsonb, p_verdict text, p_seen int)`.
04-11 `app.mark_run_search`. Tables: `place_tiles (last_checked_at, last_swept_run_id)`, `place_tile_members (tile_id, place_id, gone_at)`, `run_searches (tile_id, kind)`.
`server-only` in a tsx desk script: the package's `react-server` export condition resolves to an empty module,
so the recorder runs as `node --conditions=react-server --import tsx scripts/record-places-fixtures.ts` and loads
`.env.local` with `dotenv` BEFORE any `src/` import (env.ts parses at import).
`src/lib/ingest/etl-actor.ts` `resolveEtlOrg` — how a desk script finds the one local org.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: runCheckTile and its DB proofs</name>
  <files>src/lib/places/check-tile.ts, tests/db/places-check-tile.test.ts</files>
  <read_first>
    - `src/lib/places/meter.ts` (04-16), `src/lib/places/change-detect.ts` (04-03)
    - `tests/db/places-meter.test.ts` (04-16 — the `withWorkerOrg` savepoint double and msw harness to copy)
    - 04-RESEARCH.md § Pattern 9
  </read_first>
  <behavior>
    Seed a `running` `change_check` run and an `ids_only` search on a tile with stored members.
    - `a change check against a never-checked tile records a baseline` — verdict `baseline`, every seen id a member
    - `a change check marks new and gone place ids` — stored {a,b,c}, seen {b,c,d} → `both`; `d` inserted; `a.gone_at` set, row NOT deleted; tile `changed_at` set; result `{ kind:'checked', changed:true }`
    - `an unchanged tile is not a paid-sweep candidate` — verdict `unchanged`, `changed:false`, `changed_at` untouched
    - `a change check flags a leaf that now returns 60 as saturated` — 3 IDs-only pages of 20 → verdict `saturated`
    - `a change check never sends the Enterprise mask` — every logged request's mask is exactly `places.id,nextPageToken` and served places carry only `id`
    - `a change check ledgers every page at zero` — each page → one ledger row `sku 'ts_essentials', micro_usd 0, units 1`
    - `a change check stops at Google's daily quota` — `places-429-daily` → `stopped/google_daily_quota`, reservation released
  </behavior>
  <action>
```ts
export async function runCheckTile(input: SweepInput, search: PlannedSearch, deps: { mode: PlacesMode }): Promise<TileStepResult>;
```
Same shape as `runSearchTile` but: `search.kind` must be `ids_only`; `buildFirstPage({ …, mode: 'ids_only' })`; per page
reserve → call → on ok `settleOrRelease(charged:true)` (settles $0, units 1, clears in-flight) and collect ids; error
mapping identical to 04-18. After the loop, one `withWorkerOrg` txn: read the tile row
(`select t.id, t.last_checked_at, t.last_swept_run_id from place_tiles t join run_searches s on s.tile_id = t.id where s.id = ${searchId}`)
and live members (`gone_at is null`); `hasBaseline = last_checked_at !== null || last_swept_run_id !== null || members.length > 0`;
`const d = diffTile(new Set(members), ids, { hasBaseline })`;
`select app.record_change_check(${searchId}, ${JSON.stringify(d.added)}::jsonb, ${JSON.stringify(d.gone)}::jsonb, ${d.verdict}, ${ids.length})`;
`app.mark_run_search(searchId, { status: 'done', saturated: d.verdict === 'saturated' })`.
Return `{ kind: 'checked', tileKey, changed: ['new','gone','both','saturated'].includes(d.verdict) }`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "a change check" && $PNPM test:db -t "an unchanged tile is not a paid-sweep candidate" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all seven tests
    - `grep -n "mode: 'ids_only'" src/lib/places/check-tile.ts` matches; `grep -n "'enterprise'" src/lib/places/check-tile.ts` returns nothing
  </acceptance_criteria>
  <done>The free change check diffs, records and ledgers correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The anonymizer and the recorder's guards (unit-tested; the recorder is not run here)</name>
  <files>scripts/lib/anonymize-places.ts, scripts/lib/record-guard.ts, scripts/record-places-fixtures.ts, tests/unit/anonymize-places.test.ts, package.json</files>
  <read_first>
    - `tests/unit/msw/fixtures/README.md` and `places-recordings.json` (04-10)
    - `scripts/refresh-outlet-counts.ts`, `scripts/resolve.ts` L84–133 (desk-script args and target gate)
    - `.planning/PROJECT.md` § Key Decisions (the table the D-01 row will be written into by 04-29)
    - 04-CONTEXT.md D-01, D-04, D-20, D-21
  </read_first>
  <behavior>
    - "the anonymizer keeps structure and drops every Google string": for a hand-built input page with distinctive strings (`REAL-LOOKING Acme Plumbing`, `4321 N Real Rd, McAllen, TX 78504, USA`, `(956) 686-1234`, `https://acmeplumbing-real.com`, `https://www.facebook.com/acme-real`, a SAB place, 20 places + a `nextPageToken`) the output has the same count, the same ids, the same `pureServiceAreaBusiness` flags, the same `hostClass` per place, a `nextPageToken` iff the input had one (value replaced by `recorded:p<N>`), and `JSON.stringify(output)` contains none of the distinctive strings
    - "the anonymizer places every location inside the searched rectangle" — deterministic per place id; SAB places without a location stay without one
    - "the anonymizer synthesizes ratings" — `rating` → 4.0 and `userRatingCount` → 10 when present (D-21: memory-only fields never reach git)
    - "the anonymizer keeps a foreign listing foreign" — a `…, Mexico` address stays non-USA
    - "the recorder refuses without the D-01 record" — `assertLegalRecord(projectMdText)` throws unless a Key Decisions row contains `D-01` and `Places`
    - "the recorder refuses any target but the local database" — `assertLocalTarget('prod')` throws; a Supabase host URL throws
    - "the recorder caps its requests" — `MAX_RECORD_REQUESTS === 10`; a `--max-requests=11` argument throws
    - "the recorder's ids-only mode is one free call" — `parseRecordArgs(['--ids-only', …])` yields `{ idsOnly: true, maxRequests: 1 }`; `--ids-only --max-requests=2` throws (04-31's D-03 key verification uses exactly one call)
  </behavior>
  <action>
1. `scripts/lib/anonymize-places.ts`:
```ts
export type AnonymizeContext = { rect: Rect; city: string; placesType: string; page: number };
export function anonymizePage(page: unknown, ctx: AnonymizeContext): Record<string, unknown>;
```
   Keep `id`, `types`, `businessStatus`, `pureServiceAreaBusiness` verbatim; `displayName.text` → `Synthetic ${placesType} ${NNN}`;
   `formattedAddress` → `${NNN} Synthetic St, ${city}, TX 78500, USA` (or `${NNN} Calle Sintetica, Synthetic, Mexico` when the original's last segment is not `USA`);
   `location` → a point inside `ctx.rect` from an FNV-1a hash of the id (import `fnv1a32` from `src/lib/places/partition.ts`), 4 decimals;
   `nationalPhoneNumber` → `(956) 555-01NN` iff present; `websiteUri` → a synthetic URL of the SAME host class iff present
   (`none` absent · `business_site_dead` `https://synthetic-NNN.business.site` · `social` `https://www.facebook.com/synthetic-NNN` · `directory` `https://www.yelp.com/biz/synthetic-NNN` · `platform_subdomain` `https://synthetic-NNN.wixsite.com/site` · `other` `https://synthetic-NNN.example`), using `hostClass` from `src/lib/places/host-class.ts`;
   `rating` → 4.0, `userRatingCount` → 10 iff present; every other key dropped; `nextPageToken` → `recorded:p${page + 1}` iff present.
2. `scripts/lib/record-guard.ts`: `MAX_RECORD_REQUESTS = 10`; `assertLegalRecord(projectMd: string)`; `assertLocalTarget(target: string, url: string)` (only `test`; refuses a Supabase / non-localhost host, the `scripts/db.ts` rule); `parseRecordArgs(argv): { version; type; unit; quad?: string; maxRequests: number; out?: string; idsOnly: boolean }` (`--ids-only` forces `maxRequests = 1` and refuses any other explicit value).
3. `scripts/record-places-fixtures.ts` (desk-only; header states "makes REAL, BILLED Places calls; run only at 04-32"):
   args `--version=<uuid> --type=<placesType> --unit=<unitKind:unitIdDbSafe> [--quad=r0] [--max-requests=6] [--out=<basename>] [--ids-only]`.
   `--ids-only` (04-31's D-03 key verification): the run is `kind 'change_check'`, the search `ids_only`, the request built with
   `mode: 'ids_only'` (sku `ts_essentials`), exactly ONE request, and no fixture is written unless `--out` is also given.
   Loads `.env.local`, reads PROJECT.md → `assertLegalRecord`; `assertLocalTarget('test', url)`; resolves the local org;
   inserts, as the owner, a `runs` row `{ kind:'full_sweep', status:'running', ceiling_requests: maxRequests, search_version_id: --version }`
   and plans the root search with `app.plan_run_searches`; for each page ≤ 3 and within the cap: `reservePage` → `searchText` →
   `settleOrRelease(charged: true)` → `anonymizePage(raw, ctx)` → keep in memory; the raw payload is never written or logged.
   Writes `tests/unit/msw/fixtures/places-recorded-<out>-p<N>.json` and updates `places-recordings.json` entries with
   `{ synthetic: false, anonymized: true, recordedAt, requests }`; finally sets the local run `complete`. Prints request count and cost.
4. `package.json`: `"record:places": "node --conditions=react-server --import tsx scripts/record-places-fixtures.ts"`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the anonymizer" && $PNPM test:unit -t "the recorder" && $PNPM typecheck && $PNPM lint</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all eight tests
    - `grep -n "writeFileSync" scripts/record-places-fixtures.ts` appears only after an `anonymizePage(` call in the same function
    - `grep -n "console.log(raw\|JSON.stringify(raw" scripts/record-places-fixtures.ts` returns nothing
    - `grep -n '"record:places"' package.json` matches
  </acceptance_criteria>
  <done>The recorder exists, is metered, anonymizes in memory, and refuses without the legal record — ready for 04-32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google response → git | the recorder is the only path from a real payload to a committed file |
| desk script → Google's bill | real requests outside the product UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05 | Information disclosure (legal) | committed fixtures | mitigate | in-memory anonymization with a structure-preserving, text-dropping unit test; raw payload never written or logged; sidecar marks `anonymized: true` (D-20) |
| T-4-04 | Information disclosure (legal retention) | fixture coordinates | mitigate | coordinates re-drawn from a hash inside the searched rectangle — no real lat/lng persists in git beyond 30 days |
| T-4-02 | Denial of service (financial) | recorder | mitigate | goes through `reservePage` (reserved + ledgered, criterion 5); hard cap 10 requests; local target only; refuses without the D-01 record |
| T-4-02 | Denial of service (financial) | change check | mitigate | IDs-only mask by construction; still reserved and ledgered at $0 (D-16) |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM test:db`, `$PNPM typecheck`, `$PNPM lint` green
- The recorder is NOT executed in this plan
</verification>

<success_criteria>
- The change check is proven; the recorder is ready, metered and guarded
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-19-SUMMARY.md`
</output>
