---
phase: 04-places-transient-verifier
plan: 29
type: execute
wave: 9
depends_on: [04-28]
files_modified:
  - docs/legal/places-persistence.md
  - .planning/PROJECT.md
  - .planning/STATE.md
autonomous: false
requirements: [PLACE-02, PLACE-06]

must_haves:
  truths:
    - "Before any real Places call of any SKU, PROJECT.md Key Decisions records either counsel's answer on Maps Platform Terms §3.2.3(c) and §3.2.3(d)(iii) or danlo's own written risk call (D-01)"
    - "The decision covers the REAL persistence list, generated from the live schema — every column that holds a Places-derived value, the coordinate TTL and purge, the SAB flag, tile membership, match features, anonymized fixtures, the memory-only rating fields and the future review-volume bucket, the link-out, and the attributions question (D-01, D-05, D-06, D-10, D-13, D-20, D-21)"
    - "A 'no' is a configuration outcome: PLACES_MODE stays off and 04-31/04-32 do not run; nothing in code changes"
  artifacts:
    - path: "docs/legal/places-persistence.md"
      provides: "the enumeration the legal call covers, generated from information_schema"
      contains: "place_coordinates"
    - path: ".planning/PROJECT.md"
      provides: "the D-01 Key Decisions row"
      contains: "D-01"
  key_links:
    - from: ".planning/PROJECT.md D-01 row"
      to: "scripts/lib/record-guard.ts assertLegalRecord"
      via: "the recorder (04-32) refuses to run until this row exists"
      pattern: "D-01"
---

<objective>
The D-01 legal gate: danlo records counsel's answer or his own written risk call, against an
enumeration of exactly what Siteless persists from Google — before the first real Places call of any
SKU (including 04-31's free verification call).

Purpose: STATE.md's standing blocker ("Legal read owed before Phase 4's first production Places
call"). The kill switch (D-02) makes a "no" a config change; this plan makes the question concrete.

Output: `docs/legal/places-persistence.md`, the PROJECT.md Key Decisions row, STATE.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/research/DATA-SOURCES.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
🔴 No Places call of any SKU, anywhere, in this plan.
</pinned_launcher>

<tasks>

<task type="auto">
  <name>Task 1: Generate the persistence enumeration from the live schema</name>
  <files>docs/legal/places-persistence.md</files>
  <read_first>
    - `.planning/research/DATA-SOURCES.md` § (a) Google Places API (New) (~L82–281) and § "Google attribution, and the non-Google-map trap" (~L896–903)
    - 04-CONTEXT.md D-01, D-05, D-06, D-09, D-10, D-12, D-13, D-20, D-21; 04-UI-SPEC.md Open Questions 5 and 10
    - `drizzle/0026_*.sql` … `drizzle/0029_places_writers.sql`
  </read_first>
  <action>
Query the LOCAL test database (read-only transaction) for every column of `place_attachments, place_observations, place_coordinates,
place_tiles, place_tile_members, run_searches, run_place_outcomes, place_purge_runs` from `information_schema.columns`, and write
`docs/legal/places-persistence.md` with:
1. **Persisted, by table and column** — each column marked `Google-derived` or `ours` (keys, timestamps, counts), with its retention:
   `place_id` (indefinite — the Terms' exempt field); `lat`/`lng` in `place_coordinates` (`expires_at ≤ observed_at + 30 days`, unreadable by any
   tenant session, deleted by a daily purge; on disk ≤ ~1 day past expiry, unreadable — say so plainly); `had_website_uri` (derived boolean);
   `host_class` (derived six-value class; the URL itself discarded at call time — D-09); `pure_sab` (derived flag — D-13); `sku`, `observed_at`;
   `place_attachments.score` + `features` (numbers only, CHECK-enforced — D-05); tile membership (`place_id` per tile, first/last seen, gone — D-06);
   per-cluster unmatched counts (D-06).
2. **Never persisted** — name, address, phone, `websiteUri`, types, business status, rating, review count, reviews, photos (D-05, D-09, D-21), and how
   that is enforced (the numeric-only CHECK, the sentinel scans, the step-return scan).
3. **Transient sinks** — the Vercel workflow event log holds tile keys, counts and our ids only (M45); logs and error messages carry status codes
   and our keys only; committed fixtures are synthetic or anonymized in memory (D-20).
4. **Requested but memory-only** — `rating`, `userRatingCount` (D-21), and the planned Phase 6 "derived review-volume bucket" so the call covers it now.
5. **Display** — "Google Maps" text attribution wherever a derived value renders; no map; a link OUT to Google Maps by `place_id` (UI OQ 5).
6. **Open questions for the call** — §3.2.3(c): is a derived value (boolean / host class / SAB flag / score) "Content"? §3.2.3(d)(iii): is a
   lead tool a "listings or directory service"? Does a derived-signal display trigger third-party attributions (`places.attributions` is not
   requested — UI OQ 10)? Is the link-out acceptable?
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && grep -c "place_coordinates\|host_class\|pure_sab\|§3.2.3" docs/legal/places-persistence.md</automated>
  </verify>
  <acceptance_criteria>
    - Every column of the eight tables appears in the document (count them against the query and record both numbers in the SUMMARY)
    - The document names D-01, D-05, D-06, D-09, D-10, D-13, D-20, D-21 and UI OQ 5 and 10
  </acceptance_criteria>
  <done>The legal call can be made against the real list.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: danlo records the D-01 legal decision</name>
  <files>docs/legal/places-persistence.md</files>
  <read_first>
    - `docs/legal/places-persistence.md` (Task 1)
  </read_first>
  <decision>May Siteless make real Google Places API (New) calls and persist the enumerated derived data?</decision>
  <context>
Present `docs/legal/places-persistence.md` in full. The four questions in its § 6 are the ones counsel (or danlo) must answer.
Nothing Places-derived exists anywhere yet — production has `PLACES_MODE` unset (= `off`), and CI never spends.
The answer is recorded verbatim; Claude writes no legal conclusion of its own.
  </context>
  <options>
    <option id="counsel-yes">
      <name>Counsel's answer recorded — proceed</name>
      <pros>The strongest footing; the answer text goes into Key Decisions verbatim with its date</pros>
      <cons>Requires counsel's written answer now</cons>
    </option>
    <option id="danlo-risk-call">
      <name>danlo's own written risk call — proceed</name>
      <pros>Unblocks D-03/D-04 without waiting on counsel; the kill switch keeps a later "no" a config change</pros>
      <cons>The risk is danlo's; the rationale must be written, not implied</cons>
    </option>
    <option id="no">
      <name>No (or not yet) — PLACES_MODE stays off</name>
      <pros>Zero exposure; the phase's code still ships behind the switch</pros>
      <cons>04-31 (GCP + BUDG-03) and 04-32 (first real run) do not run; BUDG-03 stays open; the phase closes on synthetic fixtures only</cons>
    </option>
  </options>
  <acceptance_criteria>
    - danlo has answered one of `counsel-yes`, `danlo-risk-call`, `no`, and supplied the text to record (counsel's answer, or his rationale)
  </acceptance_criteria>
  <resume-signal>Reply `counsel-yes: <answer text + date>`, `danlo-risk-call: <rationale>`, or `no`</resume-signal>
  <action>Present docs/legal/places-persistence.md in full with the three options and wait. Record the reply verbatim; draw no legal conclusion.</action>
  <verify>Human checkpoint — the reply is recorded verbatim in the SUMMARY; no automated command applies.</verify>
  <done>danlo's reply is recorded and the plan proceeds (or stops) exactly as it directs.</done>
</task>

<task type="auto">
  <name>Task 3: Record the decision in PROJECT.md and STATE.md</name>
  <files>.planning/PROJECT.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/PROJECT.md` § Key Decisions (table format), `.planning/STATE.md` § Blockers/Concerns
    - `scripts/lib/record-guard.ts` (04-19 — what `assertLegalRecord` looks for)
  </read_first>
  <action>
Add a Key Decisions row: Decision "D-01 Places legal gate — <counsel-yes | danlo-risk-call | no>", Rationale = the verbatim text from Task 2
plus "covers docs/legal/places-persistence.md as of <date>", Outcome = "PLACES_MODE may be set to ids_only/enterprise" or "PLACES_MODE stays off".
STATE.md: replace the "Legal read owed before Phase 4's first production Places call" blocker with a line pointing at the new row.
If the answer was `no`: record it, and write in STATE.md "04-31 and 04-32 skipped by D-01; BUDG-03 remains open" — the executor then stops this
phase's checkpoint chain after 04-30 and reports.
Verify the recorder's guard accepts the file: run `node --conditions=react-server --import tsx -e "import('./scripts/lib/record-guard.ts').then(m => m.assertLegalRecord(require('fs').readFileSync('.planning/PROJECT.md','utf8')))"` — exit 0 for a yes, non-zero for `no`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && grep -n "D-01" .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "D-01 Places legal gate" .planning/PROJECT.md` matches
    - STATE.md no longer lists the legal read as an open blocker
    - The guard check's exit code matches the answer (0 for a yes, non-zero for no)
  </acceptance_criteria>
  <done>The legal gate is recorded where the recorder and every later plan can see it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Siteless ↔ Google Maps Platform Terms | the legal boundary every later real call crosses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05 | Information disclosure (legal: Terms §3.2.3) | everything persisted | mitigate | the decision is taken against a schema-generated enumeration; a "no" keeps `PLACES_MODE=off` |
| T-4-02 | Denial of service (financial) | premature calls | mitigate | 04-31/04-32 depend on this plan; the recorder refuses without the row (tool contract, not a prompt rule) |
</threat_model>

<verification>
- The Key Decisions row exists with verbatim text; STATE.md updated; the recorder guard agrees
</verification>

<success_criteria>
- D-01 is recorded before any real call, against the real persistence list
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-29-SUMMARY.md`
</output>
