---
phase: 04-places-transient-verifier
plan: 24
type: execute
wave: 5
depends_on: [04-06, 04-07, 04-08, 04-21]
files_modified:
  - src/app/(app)/review/page.tsx
  - src/components/review/review-filter.tsx
  - src/components/review/candidate-pair.tsx
  - src/components/review/google-listing-card.tsx
  - src/components/review/reject-dialog.tsx
  - src/components/review/review-actions.tsx
  - src/components/review/review-empty.tsx
  - src/components/review/signal-chips.tsx
  - tests/unit/review-google.test.tsx
  - tests/unit/reject-dialog.test.tsx
  - tests/unit/places-chips-contract.test.ts
autonomous: true
requirements: [PLACE-06, PLACE-05]

must_haves:
  truths:
    - "/review shows tentative Google listings as a second item kind in one score-ordered queue with an All · Duplicates · Google filter reflected in ?kind= (D-05, UI OQ 3)"
    - "A Google item shows the spine record (the Phase 3 side card, extracted as SpineRecordCard, not forked), the chips, the tentative reason (score band or tie naming the other business) and one Google Maps tag in its bordered card — and no Google text (D-05, D-08, D-11, Rules 28, 30)"
    - "'Open this listing on Google Maps' links out by place_id with our own name/city, in a new tab with noopener noreferrer; no map is rendered (UI OQ 5, Rule 31)"
    - "'Same business' confirms without a dialog; 'Not this business' opens a destructive confirmation and calls no server action until confirm; the queue never advances before the write lands (Rules 20, 42)"
    - "The matcher's numeric features render as chips (producer→consumer contract between 04-06 and 04-07)"
  artifacts:
    - path: "src/components/review/google-listing-card.tsx"
      exports: ["GoogleListingCard"]
    - path: "src/components/review/reject-dialog.tsx"
      exports: ["RejectDialog"]
    - path: "src/components/review/candidate-pair.tsx"
      exports: ["CandidatePair", "SpineRecordCard"]
  key_links:
    - from: "src/components/review/reject-dialog.tsx"
      to: "src/server/actions/record-listing-decision.ts"
      via: "onClick inside useTransition, only on confirm"
      pattern: "recordListingDecision"
    - from: "src/components/review/google-listing-card.tsx"
      to: "src/lib/ui/places-format.ts placesChips + mapsUrlFor"
      via: "imports alongside GoogleMapsTag (Rule 28's import rule)"
      pattern: "GoogleMapsTag"
---

<objective>
The review queue's Google listing kind (UI-SPEC § Screen 3): filter, item card, actions and the reject
confirmation, reusing Phase 3's pair card and chip band rather than forking them.

Purpose: D-05 — a tentative attachment is excluded from every verdict until a human confirms it; the
queue is where that human decision is made, without Siteless ever showing Google's own text.

Output: the page change, six components (one new filter, one new card, one new dialog, three edited), three test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/03-free-data-spine-entity-resolution/03-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. Never run `npx shadcn init`.
</pinned_launcher>

<interfaces>
04-21: `listReviewQueue(claims, filter)` → `{ top: (CandidatePairView & { kind:'pair' }) | GoogleListingView | null; remaining; counts: { pairs, google }; ingested }`;
`GoogleListingView = { kind:'google'; attachmentId; placeId; score; reason: 'score'|'tie'; features: unknown; business: CandidateSideView; tie: { businessId; displayName; score } | null }`;
`recordListingDecision({ attachmentId, decision: 'attached'|'rejected'|'skip' })` → `ActionResult<{ remaining; businessName }>`.
04-07: `placesChips(features): PlacesChip[]`, `mapsUrlFor(placeId, name, city)`, copy `REVIEW_GOOGLE_*`, `REVIEW_FILTER_*`, `REJECT_*`, `TOAST_*`.
04-08: `GoogleMapsTag`. 04-06: `scoreSab`, `toPlaceForMatch`, `decide` (for the contract test).
Existing: `src/app/(app)/review/page.tsx` L25–135 (one `withOrg`, `review-remaining` live region, `ReviewAdvance` keyed by item id);
`src/components/review/candidate-pair.tsx` L55–129 (`Side` + `CandidatePair`; the first heading has `tabIndex={-1}` and `data-review-focus`);
`src/components/review/review-actions.tsx` (ThumbBar, focus move L72–77, never advance before the write — Rule 20);
`src/components/review/signal-chips.tsx` L21–58; `src/components/business-detail/unmerge-dialog.tsx` (the destructive confirmation analog:
`useIsDesk` from `run-drawer.tsx`, `Dialog` desk / `Drawer` phone, `useTransition`, no `<form action>`, toast on success only, `Alert` on refusal).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpineRecordCard extraction, GoogleListingCard, chips widening, and the chips contract test</name>
  <files>src/components/review/candidate-pair.tsx, src/components/review/google-listing-card.tsx, src/components/review/signal-chips.tsx, tests/unit/review-google.test.tsx, tests/unit/places-chips-contract.test.ts</files>
  <read_first>
    - `src/components/review/candidate-pair.tsx`, `signal-chips.tsx` (whole files), `tests/unit/review-actions.test.tsx`
    - 04-UI-SPEC.md § Screen 3 "The Google-listing item", Accessibility (external link), Rules 28, 30, 31
  </read_first>
  <behavior>
    - `the pair card is unchanged by the extraction` — the existing Phase 3 review tests pass untouched
    - `the google listing card shows the spine side, chips, reason and one Google Maps tag` — `review-google-side` holds the `SpineRecordCard`; `review-google-listing` is a `Card` with `data-places-content` containing exactly one `google-maps-attribution`, `review-chips`, and `review-tentative-reason` (`data-reason`)
    - `the tie reason names the other business as a link` — `data-reason="tie"`; a link to `/businesses/{tie.businessId}` with the tie's display name and score
    - `the open-on-Google-Maps link is built from the place id and opens safely` — `review-google-open-maps` href = `mapsUrlFor(placeId, business.displayName, business.city)`, `target="_blank"`, `rel="noopener noreferrer"`, a visually hidden "(opens Google Maps)" suffix, `external-link` icon `aria-hidden`
    - `the google listing card renders no Google text` — the card's text contains only copy strings, the spine fields and numbers
    - `the matcher's features render as chips` (contract): `scoreSab(...)` for the SENTINEL SAB listing vs the Valley Locksmith side → `placesChips(result.features)` yields labels including "service-area business", "city match", "no location on the listing"
  </behavior>
  <action>
Extract `Side` from `candidate-pair.tsx` as exported `SpineRecordCard` (same markup; the focus hook `tabIndex={-1}` + `data-review-focus`
stays on its first heading); `CandidatePair` uses it. `SignalChips` accepts `PlacesChip[]` (a superset of `ReviewChip`, same variants:
agreement `secondary`, disagreement/absence `outline` + muted). `GoogleListingCard({ item }: { item: GoogleListingView })`: desk two equal
columns `gap-6`, phone stacked `gap-4`; right card: title "Google Maps listing", the body sentence, the outline 44px Maps link, "How it
compared" eyebrow, `SignalChips` from `placesChips(item.features)`, the reason line (score / tie), and `<GoogleMapsTag />` bottom-right desk,
bottom-left phone. Server-safe component (no client directive) — the link is a plain `<a>`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the google listing card" && $PNPM test:unit -t "the tie reason names the other business" && $PNPM test:unit -t "the open-on-Google-Maps link" && $PNPM test:unit -t "the matcher's features render as chips" && $PNPM test:unit tests/unit/review-actions.test.tsx tests/unit/review-chips.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names every test above; Phase 3 review tests green
    - `grep -n "function Side\b" src/components/review/candidate-pair.tsx` returns nothing; `grep -n "export function SpineRecordCard" src/components/review/candidate-pair.tsx` matches
    - `grep -n "iframe\|maps/embed" src/components/review/google-listing-card.tsx` returns nothing
  </acceptance_criteria>
  <done>The Google item renders from stored data only, attributed, with the pair card reused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RejectDialog, the Google actions, the filter and the page</name>
  <files>src/components/review/reject-dialog.tsx, src/components/review/review-actions.tsx, src/components/review/review-empty.tsx, src/components/review/review-filter.tsx, src/app/(app)/review/page.tsx, tests/unit/reject-dialog.test.tsx</files>
  <read_first>
    - `src/components/business-detail/unmerge-dialog.tsx` + `tests/unit/unmerge-dialog.test.tsx` (whole files)
    - `src/components/review/review-actions.tsx`, `review-empty.tsx`, `src/app/(app)/review/page.tsx` (whole files)
    - 04-UI-SPEC.md § Screen 3 actions/helpers/remaining count/empty states, § Error table, Rules 20, 42
  </read_first>
  <behavior>
    - `the reject trigger calls no server action until confirm` (Rule 42) — clicking `review-action-not-this` opens `review-reject-dialog`; `recordListingDecision` has 0 calls; clicking `review-reject-confirm` → 1 call with `decision: 'rejected'`
    - `the reject dialog stays open until the write lands` — confirm shows "Recording…"; a failed result shows the reject-failed `Alert` inside the dialog and does not advance
    - `keep it pending dismisses without recording` — `review-reject-dismiss` → 0 calls
    - `same business on a google item records a confirm without a dialog` — `review-action-same` → 1 call `decision: 'attached'`, no dialog
    - `the review filter reflects the URL` — `?kind=google` selects `review-filter-google` (accessible name "Google listings"); choosing Duplicates navigates to `?kind=duplicates`
    - `the remaining count names both kinds` — all: "{n} left to review · {d} duplicate pairs, {g} Google listings"; google: "{n} Google listings left to review" / singular
    - `the google empty state offers the duplicates` — Google filter with none left and duplicates remaining → "No Google listings to review" + "Show duplicates"
  </behavior>
  <action>
`reject-dialog.tsx` (`'use client'`): copy `unmerge-dialog.tsx`'s structure (desk `Dialog` / phone `Drawer` via `useIsDesk`; destructive `Alert`
body; confirm `review-reject-confirm` destructive filled 48px on phone; dismiss `review-reject-dismiss` outline "Keep it pending"; `useTransition`;
try/catch around the action; toast `TOAST_REJECTED(name)` on success only; on success close, then call the `onRecorded(remaining)` the
parent passes so `ReviewActions` advances AFTER the write). `review-actions.tsx` gains `kind: 'pair' | 'google'`; the Google kind wires
Same business → `recordListingDecision({ decision:'attached' })` (toast `TOAST_ATTACHED(name)`), Not this business → opens `RejectDialog`,
Skip → `decision:'skip'`; helpers per kind; focus move and busy states inherited. `review-filter.tsx` (`'use client'`): `ToggleGroup`
single-select, full width on phone, 44px, `review-filter` + `review-filter-{all|duplicates|google}`, `router.push('?kind=…')`.
`review-empty.tsx` gains the two per-filter empty states. `page.tsx`: read `searchParams.kind` (default `all`, anything else → `all`),
`listReviewQueue(claims, filter)` in the page's ONE `withOrg`, render the filter under the title, the remaining line per filter, and
dispatch `top.kind` to `CandidatePair` or `GoogleListingCard`; `ReviewAdvance` keyed by `pairId` / `attachmentId`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the reject trigger calls no server action until confirm" && $PNPM test:unit -t "the reject dialog" && $PNPM test:unit -t "same business on a google item" && $PNPM test:unit -t "the review filter reflects the URL" && $PNPM test:unit -t "the remaining count names both kinds" && $PNPM typecheck && $PNPM lint && $PNPM build</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names every test above
    - `grep -n "<form" src/components/review/reject-dialog.tsx` returns nothing
    - `grep -n "gap-3" src/components/review/*.tsx "src/app/(app)/review/page.tsx"` returns nothing in lines this plan touched
    - `$PNPM build` exits 0
  </acceptance_criteria>
  <done>danlo can confirm, reject (with confirmation) or skip a Google listing from the queue on a phone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → listing actions | decisions and ids are untrusted; server re-checks (04-21) |
| app → Google Maps (link out) | a URL built from stored values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-11 | Tampering | Maps link | mitigate | `mapsUrlFor` from place_id + spine name/city, every part `encodeURIComponent`-ed; `rel="noopener noreferrer"` |
| T-4-13 | Repudiation (legal) | attribution / no map | mitigate | one `GoogleMapsTag` in the bordered listing card; link-out only, no embed (Rule 31) |
| T-4-05 | Information disclosure (legal) | Google text | mitigate | the card renders stored ids, numbers and spine fields only (named test) |
| T-4-06 | Tampering | irreversible reject | mitigate | confirmation required; no action before confirm (Rule 42 test); no advance before the write lands |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint`, `$PNPM build` green
</verification>

<success_criteria>
- Tentative listings are reviewable on /review with the Phase 3 muscle memory and Google's attribution
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-24-SUMMARY.md`
</output>
