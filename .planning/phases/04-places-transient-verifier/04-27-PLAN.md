---
phase: 04-places-transient-verifier
plan: 27
type: execute
wave: 7
depends_on: [04-03, 04-04, 04-07, 04-13, 04-14, 04-26]
files_modified:
  - src/components/preset-detail/places-mode-notice.tsx
  - src/components/preset-detail/run-actions.tsx
  - src/components/preset-detail/other-runs.tsx
  - src/components/preset-detail/recent-runs.tsx
  - src/components/preset-detail/summary-card.tsx
  - src/app/(app)/presets/[id]/page.tsx
  - tests/unit/preset-actions.test.tsx
  - tests/e2e/preset-detail.spec.ts
autonomous: true
requirements: [PLACE-04]

must_haves:
  truths:
    - "Preset detail offers Run full sweep · Run this week's partition · Check for changes (free), each with its cost line, all through the same meter and kill switch (D-16)"
    - "PLACES_MODE is read on the server and passed as a prop; off shows the notice and three aria-disabled (focusable, described) actions with zero accent; ids_only makes the free check primary; enterprise makes the full sweep primary (D-02, Rules 33, 34)"
    - "Exactly one element exists per run-action testid in every mode (Rule 40; touch-target invariant)"
    - "A 'Recent runs' card lists the five newest runs across versions with kind, cost and status, each linking to its report; the summary's last-run line links to the report (UI § 2)"
    - "The partition action names this ISO week and its date range and prices only the partition's cells (PLACE-04)"
  artifacts:
    - path: "src/components/preset-detail/run-actions.tsx"
      exports: ["RunActions"]
    - path: "src/components/preset-detail/places-mode-notice.tsx"
      exports: ["PlacesModeNotice"]
    - path: "tests/unit/preset-actions.test.tsx"
      contains: "the accent goes to the first enabled run action"
  key_links:
    - from: "src/app/(app)/presets/[id]/page.tsx"
      to: "src/env.ts PLACES_MODE"
      via: "server read → mode prop (never NEXT_PUBLIC_)"
      pattern: "env.PLACES_MODE"
    - from: "src/components/preset-detail/run-actions.tsx"
      to: "src/components/preset-detail/run-drawer.tsx"
      via: "kind 'full' | 'partition' | 'check'"
      pattern: "kind="
---

<objective>
The preset page's three ways to run, the Places-mode notice, and recent runs (UI-SPEC § Screen 2).

Purpose: D-16 closes PLACE-04 in this phase — "Run this week's partition" and "Check for changes
(free)" exist as manual actions; Phase 9 only puts them on a clock. D-02's kill switch becomes visible
and explained rather than a mystery-disabled button.

Output: four components (three new, one edited), the page, a dom test, the local e2e rewrite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. Never run `npx shadcn init`.
</pinned_launcher>

<interfaces>
`src/app/(app)/presets/[id]/page.tsx` L152–199 (the single `withOrg`, the last-run query), L306–322 (the `run-preset` trigger: one DOM element moved by CSS into the phone sticky bar), `gap-3` at L310.
04-26 `RunDrawer({ kind: 'full' | 'partition' | 'check', partition?, trigger, … })`. 04-14 `RunStatusBadge`. 04-07 copy `PRESET_*`, `PLACES_MODE_NOTICE_*`, `RUN_*`, `STOPPED_REASON`, `RUN_KIND_LABEL`.
04-04 `estimatePreset(spec, ctx, { onlyCells })`; 04-13 `cellsForRun(spec, seed, 'partition', now)` (+ `partitionIndex`); 04-03 `isoWeekOf(now)`.
`src/env.ts` `env.PLACES_MODE`. Analogs: `version-history.tsx` rows, `summary-card.tsx`, `src/components/ui/item.tsx`.
`tests/e2e/touch-targets.spec.ts` (exactly one visible match per testid). `tests/e2e/preset-detail.spec.ts` self-skips unless `E2E_BASE_URL` is local.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RunActions, the Places-mode notice, and the other-runs card (accent by mode)</name>
  <files>src/components/preset-detail/run-actions.tsx, src/components/preset-detail/places-mode-notice.tsx, src/components/preset-detail/other-runs.tsx, tests/unit/preset-actions.test.tsx</files>
  <read_first>
    - `src/components/preset-detail/run-drawer.tsx` (after 04-26), `summary-card.tsx`
    - 04-UI-SPEC.md § Screen 2 (actions table, accent-by-mode table, notice copy, disabled semantics), Rules 33, 34, 40
  </read_first>
  <behavior>
    - `the accent goes to the first enabled run action` (Rule 34): enterprise → exactly one `variant="default"` button and it is `run-preset`; ids_only → exactly one, `run-check-changes`; off → zero
    - `exactly one element per run testid in every mode` — `run-preset`, `run-partition`, `run-check-changes` each appear exactly once in enterprise, ids_only and off renders
    - `disabled run actions are aria-disabled and focusable` — in off: each action has `aria-disabled="true"`, no `disabled` attribute, `aria-describedby` = the notice's id, `data-enabled="false"`, and clicking opens nothing
    - `the places mode notice renders per mode` — off: `places-mode-notice` `data-mode="off"` with the `power-off` icon; ids_only: `data-mode="ids_only"`; enterprise: no notice
    - `the check action reads as free through words, not colour` — its cost line is "$0.00 · IDs only, no website data · ~{r} requests" and its classes carry no accent
  </behavior>
  <action>
`places-mode-notice.tsx`: muted `Alert` (`power-off` / `circle-dashed`), heading-weight first line + body from copy, stable `id` for `aria-describedby`.
`run-actions.tsx` (`'use client'`): props `{ mode: 'off' | 'ids_only' | 'enterprise'; estimates: { full: CostLine; partition: CostLine & { isoWeek; mondayIso; sundayIso; cells; totalCells }; check: CostLine }; noticeId: string; drawerProps }`.
Primary slot = first enabled action in order full → partition → check (off: the disabled "Run full sweep"); the other two go to the
`OtherRuns` card rows in fixed order. Enabled buttons open `RunDrawer` with their kind; disabled ones use `aria-disabled` + no-op + 50% opacity +
`cursor-not-allowed`, never `disabled`. Phone: the primary element moves into the sticky bar by CSS (one DOM element), plus in off mode the
line "Places is switched off — see the note at the top."
`other-runs.tsx`: `Card` `preset-other-runs`, `Item` rows: title, description, cost line (Label 14/400 muted tabular), 44px outline button right-aligned desk / full-width phone.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the accent goes to the first enabled run action" && $PNPM test:unit -t "exactly one element per run testid in every mode" && $PNPM test:unit -t "disabled run actions are aria-disabled and focusable" && $PNPM test:unit -t "the places mode notice renders per mode" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all five tests
    - `grep -n " disabled={\| disabled " src/components/preset-detail/run-actions.tsx` returns nothing (aria-disabled only)
    - `grep -rn "NEXT_PUBLIC_" src/components/preset-detail/` returns nothing
  </acceptance_criteria>
  <done>The three actions obey the mode table, the accent rule and the one-element-per-testid rule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recent runs, the summary's report link, the page wiring and the local e2e</name>
  <files>src/components/preset-detail/recent-runs.tsx, src/components/preset-detail/summary-card.tsx, src/app/(app)/presets/[id]/page.tsx, tests/unit/preset-actions.test.tsx, tests/e2e/preset-detail.spec.ts</files>
  <read_first>
    - `src/app/(app)/presets/[id]/page.tsx` (whole), `summary-card.tsx`, `version-history.tsx`
    - `tests/e2e/preset-detail.spec.ts` (whole, after 04-26)
    - 04-UI-SPEC.md § Screen 2 (recent runs, summary link), § States (recent-runs empty per mode)
  </read_first>
  <behavior>
    - `recent runs list the five newest with kind, cost and status` — six fixture runs → five `preset-run-row-{runId}` rows, newest first, each a link to `/runs/{id}` ≥ 44px, with `RunStatusBadge` and, for partial, the `STOPPED_REASON` sentence
    - `recent runs empty state names the off mode` — zero runs + off → the "No runs yet" body's off-mode second sentence
    - `the summary links the last run's report` — `summary-last-run-link` href `/runs/{lastRunId}`
  </behavior>
  <action>
`recent-runs.tsx`: `Card` `preset-recent-runs`; desk rows / phone `Item` rows; footer link "See all runs on the spend view" → `/spend`; empty state per UI-SPEC.
`summary-card.tsx`: the last-run line gains "· Open the run report" (`summary-last-run-link`, accent inline link) and the full-sweep cost line.
`page.tsx`: read `env.PLACES_MODE` on the server; inside the page's ONE `withOrg`, add the five-recent-runs query (the existing last-run query's
shape, `limit 5`, selecting `r.id, r.kind, r.status, r.stopped_reason, cost, at_ms, v.version_number`) and compute the three cost lines:
full = `estimatePreset(spec, ctx)`, partition = `estimatePreset(spec, ctx, { onlyCells })` over `cellsForRun(spec, seed, 'partition', now)` with
`isoWeekOf(now)`, check = the same type-search count at $0.00. Render notice → summary → `OtherRuns` → `RecentRuns` → version history.
Fix `gap-3` at L310 → `gap-4`.
`preset-detail.spec.ts` (local-only): replace 04-26's interim block with the off-state assertions — `places-mode-notice[data-mode="off"]` visible;
`run-preset`, `run-partition`, `run-check-changes` each visible, `data-enabled="false"`, `aria-disabled="true"`, and focusable (`focus()` then
`toBeFocused()`); `preset-recent-runs` visible. No `run-confirm` click remains in any spec.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "recent runs" && $PNPM test:unit -t "the summary links the last run's report" && $PNPM typecheck && $PNPM lint && $PNPM build</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names the three tests; `$PNPM build` exits 0
    - `grep -n "env.PLACES_MODE" "src/app/(app)/presets/[id]/page.tsx"` matches
    - `grep -rn "run-confirm" tests/e2e` returns nothing (the only remaining click was replaced)
    - `grep -n "gap-3" "src/app/(app)/presets/[id]/page.tsx"` returns nothing
  </acceptance_criteria>
  <done>danlo can run a full sweep, this week's partition, or a free change check from a preset — or see exactly why not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server env → browser | the mode reaches the client only as a rendered prop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information disclosure | `PLACES_MODE` | mitigate | server-read prop; never `NEXT_PUBLIC_` (grep) |
| T-4-02 | Denial of service (financial) | disabled actions | mitigate | UI is an affordance only; `queueRun` refuses independently (04-26) |
| T-4-09 | Denial of service (financial) | e2e | mitigate | no spec clicks `run-confirm` any more (grep acceptance) |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint`, `$PNPM build` green
</verification>

<success_criteria>
- PLACE-04's two manual operations exist on the preset page, gated by the kill switch and explained in every mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-27-SUMMARY.md`
</output>
