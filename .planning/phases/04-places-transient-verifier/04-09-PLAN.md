---
phase: 04-places-transient-verifier
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema/runs.ts
  - src/db/schema/place-attachments.ts
  - src/db/schema/place-observations.ts
  - src/db/schema/place-coordinates.ts
  - src/db/schema/place-tiles.ts
  - src/db/schema/place-tile-members.ts
  - src/db/schema/run-searches.ts
  - src/db/schema/run-place-outcomes.ts
  - src/db/schema/place-purge-runs.ts
  - src/db/schema/index.ts
  - drizzle/0026_places_tables.sql
  - drizzle/0027_places_grants_triggers.sql
  - drizzle/meta/_journal.json
  - drizzle/meta/0026_snapshot.json
  - drizzle/meta/0027_snapshot.json
  - tests/db/grants-audit.test.ts
  - tests/db/event-trigger.test.ts
  - tests/db/_fixtures.ts
  - tests/db/_places-fixtures.ts
  - tests/db/places-schema.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/CONVENTIONS.md
autonomous: true
requirements: [PLACE-02]

must_haves:
  truths:
    - "Eight new org-scoped tables exist with org_id, RLS policies, an _org_idx, explicit grants in the creating migration and a TENANT_TABLES row"
    - "place_observations is append-only by grant (UPDATE/DELETE 42501 'permission denied for table place_observations') and by a raising BEFORE trigger even for the owner; one immutable row per (run, business, place_id) holds had_website_uri, host_class, sku, pure_sab, observed_at (D-10, D-13)"
    - "Coordinates live in place_coordinates, which authenticated cannot read at all (42501 'permission denied for table place_coordinates'), with expires_at CHECKed to ≤ observed_at + 30 days; purging deletes coordinate rows and never an observation (D-10, D-12)"
    - "The host class persists beside the boolean and PLACE-02 plus ROADMAP criterion 2 are amended in this plan (D-09)"
    - "business_place_signal (security_invoker) is true if any ATTACHED listing's latest observation is true, and excludes tentative and rejected attachments (D-05, D-08, M43)"
    - "One active run per org is a unique partial index; stale queued runs are failed as never_started before it is created (so production's Phase 2 queued rows cannot block the migration)"
    - "runs gains kind, partition_index, estimates, ceiling_requests, workflow_run_id, requested_by, heartbeat_at; heartbeat_at and workflow_run_id join the UPDATE column grant, ceiling_requests does not"
    - "businesses gains no Places-derived column and no google_places provenance pair, so the Phase 1 durable-cites-durable composite FK (M18) is untouched"
  artifacts:
    - path: "drizzle/0027_places_grants_triggers.sql"
      provides: "grants, revokes, triggers, siteless_cron role, signal view, unique active-run index, latlng index"
      contains: "security_invoker"
    - path: "tests/db/places-schema.test.ts"
      contains: "authenticated cannot read place coordinates"
    - path: "tests/db/_places-fixtures.ts"
      exports: ["PLACES_SPINE", "seedPlacesSpine", "seedPlacesRun", "seedRunSearch", "seedAttachmentWithObservation"]
  key_links:
    - from: "tests/db/grants-audit.test.ts TENANT_TABLES"
      to: "live public schema"
      via: "set equality (test 1)"
      pattern: "place_coordinates"
    - from: "tests/db/_places-fixtures.ts PLACES_SPINE"
      to: "tests/unit/msw/fixtures/places-match-*.json (04-10)"
      via: "identical phones/cities/names — the producer→consumer contract for matching"
      pattern: "\\+19566310001"
---

<objective>
The Phase 4 schema: eight tables, the `runs` extension, grants, triggers, the cron role, the signal
view and the retention barrier — applied to the local test database with its DB proofs.

Purpose: D-10 (append-only observations), D-12 (a database-level proof that no coordinate older than
30 days can be read), D-09 (host class beside the boolean), D-05/D-08 (the signal excludes tentative),
and the tenancy obligations every table carries (CONVENTIONS § Tenancy, § Grants). Coordinates go
in a grant-less side table — research Open Question 6's recommendation, adopted: it satisfies D-10
("never deletes the observation") and D-12 with zero UPDATE path on observations.

Output: schema files, migrations 0026 (generated) + 0027 (custom), fixtures, `places-schema.test.ts`,
audit-list updates, the D-09 requirement/roadmap amendment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/CONVENTIONS.md
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:db -t "<name>"`, never `-- -t`; read the PASS-list NAME. drizzle-kit is the single
migration authority: `$PNPM db:generate`, `$PNPM db:custom --name=<n>`, `$PNPM db:migrate` (local
test DB only). Never `drizzle-kit push`, never Supabase MCP `apply_migration`. PG17-compatible SQL only
(`tests/unit/pg17-compat.test.ts`: no `returning old.`/`new.`, no `uuidv7()`, no virtual columns).
</pinned_launcher>

<interfaces>
`src/db/schema/_helpers.ts`: `orgScoped = { id uuid pk defaultRandom, orgId uuid not null → orgs.id, createdAt, updatedAt tstz not null defaultNow, updatedBy text }`, `tstz(name)`, `orgPolicies(t)` (auto-enables RLS; never `.enableRLS()`).
`src/db/schema/runs.ts`: `status` CHECK `runs_status_known`; bigint defaults are `.default(sql\`0\`)`, never `0n` (drizzle-kit cannot serialize BigInt).
`drizzle/0013` L90: `grant update (status, stopped_reason, cost_micro_usd, calls_count, started_at, finished_at) on runs to authenticated;`
`drizzle/0015` L66–93: the two-trigger narrowed `log_event` shape (insert/delete arm + `when (old.x is distinct from new.x)` update arm).
`drizzle/0023` L104–177: SELECT-only grant + belt-and-braces revoke; the `do $$ … foreach t in array array[...]` touch-trigger loop; one `comment on table` per table excluded from `log_event`.
`drizzle/0000_bootstrap.sql` L26–33: conditional role creation (`if not exists (select 1 from pg_roles …)`).
`drizzle/0024` L129–139: named-role revoke — `from public` alone does nothing because 0000 L54 sets default execute to `authenticated, anon, service_role`.
`tests/db/_fixtures.ts` L80–88 `actAsRole` (explicit allow-list; `set role` takes no bound parameter).
`tests/db/_merge-fixtures.ts`: `CLAIMS_A` / `CLAIMS_B` (`{ o: { id: 'org_A', rol: 'admin' }, sub: 'user_reviewer_A', role: 'authenticated' }`), `seedOvertureSide(c, orgId, { name, street, zip, city, lat, lng, phone?, basicCategory? })`.
`tests/db/events-append-only.test.ts` L29–55: grant refusal pinned by code AND message; one refused statement per `withRollback` (the next statement reports `25P02`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle schema for eight tables + runs extension, generated migration 0026</name>
  <files>src/db/schema/runs.ts, src/db/schema/place-attachments.ts, src/db/schema/place-observations.ts, src/db/schema/place-coordinates.ts, src/db/schema/place-tiles.ts, src/db/schema/place-tile-members.ts, src/db/schema/run-searches.ts, src/db/schema/run-place-outcomes.ts, src/db/schema/place-purge-runs.ts, src/db/schema/index.ts, drizzle/0026_places_tables.sql, drizzle/meta/_journal.json, drizzle/meta/0026_snapshot.json</files>
  <read_first>
    - `src/db/schema/runs.ts`, `src/db/schema/business-merges.ts`, `src/db/schema/merge-candidates.ts` (L54–59 unique + queue index), `src/db/schema/budget.ts` L112–190 (`cost_reservations`, append-only `cost_ledger`)
    - `src/db/schema/_helpers.ts`, `src/db/schema/index.ts`
    - 04-RESEARCH.md § Pattern 8
  </read_first>
  <action>
Every table: `...orgScoped`, `index('<t>_org_idx').on(t.orgId)`, `...orgPolicies('<t>')`. Header doc-block per file citing its D-ID.

- **`runs.ts` (MOD)** adds: `kind text not null default 'full_sweep'` + CHECK `runs_kind_known` (`kind in ('full_sweep','partition','change_check')`); `partitionIndex integer` + CHECK `runs_partition_index_range` (`partition_index is null or partition_index between 0 and 3`); `estimateRequestsLo integer`, `estimateRequestsHi integer`; `estimateMicroUsdLo bigint`, `estimateMicroUsdHi bigint` (mode bigint, nullable); `ceilingRequests integer not null default 0` (doc: "immutable after insert — deliberately outside the UPDATE column grant; 0 on legacy rows means any reservation is refused"); `workflowRunId text`; `requestedBy text`; `heartbeatAt tstz`.
- **`place_attachments`**: `businessId uuid not null → businesses.id`, `placeId text not null`, `status text not null` CHECK `pa_status_known` (`attached`,`tentative`,`rejected`), `reason text not null` CHECK `pa_reason_known` (`score`,`tie`,`confirmed`,`rejected`,`detached`), `score integer not null` CHECK `pa_score_range` (0..100), `features jsonb not null`, `tieBusinessId uuid → businesses.id`, `firstSeenRunId uuid → runs.id`, `lastSeenRunId uuid → runs.id`, `decidedBy text`, `decidedAt tstz`. `unique('place_attachments_pair_key').on(orgId, businessId, placeId)`; `index('place_attachments_queue_idx').on(orgId, status, score.desc())`; `index('place_attachments_place_idx').on(orgId, placeId)`.
- **`place_observations`** (append-only): `businessId → businesses.id not null`, `placeId text not null`, `runId → runs.id not null`, `attachmentId → place_attachments.id not null`, `hadWebsiteUri boolean not null`, `hostClass text not null` CHECK `po_host_class_known` (six values), `sku text not null` CHECK `po_sku_known` (`ts_essentials`,`ts_pro`,`ts_enterprise`,`ts_enterprise_atmosphere`), `pureSab boolean not null`, `observedAt tstz not null defaultNow`. CHECK `po_host_class_agrees`: `had_website_uri = (host_class <> 'none')`. `unique('place_observations_run_pair_key').on(runId, businessId, placeId)`; `index('place_observations_business_idx').on(orgId, businessId, observedAt.desc())`.
- **`place_coordinates`** (grant-less): `observationId uuid not null → place_observations.id` + `unique('place_coordinates_observation_key')`, `lat double precision not null`, `lng double precision not null`, `observedAt tstz not null`, `expiresAt tstz not null`. CHECKs `pc_expiry_within_30_days` (`expires_at <= observed_at + interval '30 days'`) and `pc_expiry_after_observed` (`expires_at > observed_at`). `index('place_coordinates_expiry_idx').on(expiresAt)`.
- **`place_tiles`**: `tileKey text not null`, `unitKind text not null`, `unitId text not null` (DB-safe form), `placesType text not null`, `quadPath text not null`, `depth integer not null` CHECK ≥ 0, `south/west/north/east double precision not null`, `isLeaf boolean not null default true`, `saturated boolean not null default false`, `truncated boolean not null default false`, `lastSweptRunId uuid → runs.id`, `lastSweptAt tstz`, `lastCheckedAt tstz`, `changedAt tstz`. `unique('place_tiles_key').on(orgId, tileKey)`.
- **`place_tile_members`**: `tileId → place_tiles.id not null`, `placeId text not null`, `firstSeenAt tstz not null defaultNow`, `lastSeenAt tstz not null defaultNow`, `goneAt tstz`. `unique('place_tile_members_key').on(tileId, placeId)`; `index('place_tile_members_place_idx').on(orgId, placeId)`.
- **`run_searches`**: `runId → runs.id not null`, `tileId → place_tiles.id not null`, `tileKey text not null`, `cellKey text not null`, `clusterKey text not null`, `placesType text not null`, `kind text not null` CHECK (`enterprise`,`ids_only`), `depth integer not null`, `parentTileKey text`, `status text not null default 'planned'` CHECK (`planned`,`searching`,`done`,`stopped`), `pagesDone integer not null default 0`, `resultsCount integer not null default 0`, `saturated`/`subdivided`/`truncated boolean not null default false`, `truncatedWhy text` CHECK null or (`max_depth`,`min_size`,`novelty`), `changeVerdict text` CHECK null or (`baseline`,`unchanged`,`new`,`gone`,`both`,`saturated`), `newIds integer not null default 0`, `goneIds integer not null default 0`, `inflightReservationId uuid → cost_reservations.id`, `inflightRequestId text`. `unique('run_searches_key').on(runId, tileKey)`; `index('run_searches_run_idx').on(orgId, runId)`. (No page-token column: tokens stay in memory — data minimization.)
- **`run_place_outcomes`**: `runId → runs.id not null`, `placeId text not null`, `clusterKey text not null`, `outcome text not null` CHECK (`attached`,`tentative`,`unmatched`,`outside`). `unique('run_place_outcomes_key').on(runId, placeId, clusterKey)`; `index('run_place_outcomes_run_idx').on(orgId, runId)`.
- **`place_purge_runs`**: `ranAt tstz not null defaultNow`, `rowsPurged integer not null` CHECK ≥ 0, `trigger text not null` CHECK (`cron`,`desk`). `index('place_purge_runs_recent_idx').on(orgId, ranAt.desc())`.
Export all from `index.ts`. Then `$PNPM db:generate --name=places_tables` (`scripts/db.ts` forwards
extra args to drizzle-kit) → `drizzle/0026_places_tables.sql`. Never rename a generated file by hand;
if drizzle-kit emits a different name, keep it and record the real name in the SUMMARY. Commit the SQL + `meta/`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM typecheck && $PNPM db:generate</automated>
  </verify>
  <acceptance_criteria>
    - One new generated migration exists and `drizzle/meta/_journal.json` gains one entry
    - `grep -ci "enable row level security" drizzle/0026_*.sql` ≥ 8 (drizzle emits RLS enablement per table via policies)
    - `grep -n "pc_expiry_within_30_days" drizzle/0026_*.sql` and `grep -n "po_host_class_agrees" drizzle/0026_*.sql` match
    - A second `$PNPM db:generate` emits nothing (no drift)
  </acceptance_criteria>
  <done>The tables and columns exist in the Drizzle schema and in one generated migration.</done>
</task>

<task type="auto">
  <name>Task 2: Custom migration 0027 — data fix, indexes, grants, triggers, cron role, signal view; apply locally</name>
  <files>drizzle/0027_places_grants_triggers.sql, drizzle/meta/_journal.json, drizzle/meta/0027_snapshot.json, tests/db/grants-audit.test.ts, tests/db/event-trigger.test.ts, tests/db/_fixtures.ts</files>
  <read_first>
    - `drizzle/0023_spine_constraints_grants.sql` L90–177, `drizzle/0015_budget_grants_and_triggers.sql` L40–110, `drizzle/0000_bootstrap.sql` L20–60, `drizzle/0013_reference_policies_and_grants.sql` L85–95
    - `tests/db/grants-audit.test.ts` L30–120 (TENANT_TABLES, and whether its enumeration reads `information_schema.tables` with or without `table_type = 'BASE TABLE'`)
    - `tests/db/event-trigger.test.ts` L55–110 (`EVENT_LOGGED`, `LOG_EVENT_TRIGGER_ROWS`)
    - `tests/db/schema-audit.test.ts` (does it enumerate views? a view has no org_id column and no RLS)
  </read_first>
  <action>
`$PNPM db:custom --name=places_grants_triggers`; write the body with `--> statement-breakpoint`
between EVERY statement and each function body as one statement. Header comment: what and why (D-10,
D-12, D-15 one-active-run, research Pattern 8). Statements, in order:
1. `update runs set status = 'failed', stopped_reason = 'never_started', finished_at = coalesce(finished_at, now()) where status = 'queued' and created_at < now() - interval '15 minutes';` — comment: production holds Phase 2 queued rows that never ran (03-21 SUMMARY: 5 runs); without this the unique index below fails on apply.
2. `create unique index runs_one_active_per_org on runs (org_id) where status in ('queued','running');`
3. `create index businesses_latlng_idx on businesses (org_id, lat) where lat is not null and merged_into_id is null;` (research Pattern 7 B4)
4. `grant select on place_attachments, place_observations, place_tiles, place_tile_members, run_searches, run_place_outcomes, place_purge_runs to authenticated;`
5. `revoke insert, update, delete on place_attachments, place_observations, place_tiles, place_tile_members, run_searches, run_place_outcomes, place_purge_runs from authenticated;`
6. `revoke all on place_coordinates from authenticated, anon;` — comment: D-12's database-level proof; any read is `42501 permission denied for table place_coordinates`; counts come from a definer (04-11).
7. `grant update (heartbeat_at, workflow_run_id) on runs to authenticated;` — comment: `ceiling_requests` is deliberately NOT granted (immutable after insert, T-4-12).
8. The touch-trigger loop over `place_attachments, place_tiles, place_tile_members, run_searches, run_place_outcomes` (`before update … execute function app.touch_updated_at()`). None on `place_observations`, `place_coordinates`, `place_purge_runs` (append-only / purge-only, the `cost_ledger` precedent).
9. `create trigger place_attachments_event_upd after update on place_attachments for each row when (old.status is distinct from new.status) execute function app.log_event();` — comment: status changes are the state-bearing events (confirm / reject / detach / re-score); per-run INSERT volume is audited at run level by 04-22's finishRun step via `app.emit_event`, the `budget_periods` precedent.
10. `create or replace function app.refuse_place_observation_change() returns trigger language plpgsql set search_path = public, pg_temp as $$ begin raise exception 'place_observations is append-only' using errcode = '55000'; end $$;` then `create trigger place_observations_append_only before update or delete on place_observations for each row execute function app.refuse_place_observation_change();`
11. `do $$ begin if not exists (select 1 from pg_roles where rolname = 'siteless_cron') then create role siteless_cron nologin; end if; end $$;` then `grant siteless_cron to app_user;` — comment: the one role that may run the cross-org coordinate purge (04-11 grants it execute on exactly that function).
12. The signal view:
```sql
create view business_place_signal with (security_invoker = true) as
select a.org_id, a.business_id,
       bool_or(o.had_website_uri) as had_website_uri,
       (array_agg(o.host_class order by o.had_website_uri desc, o.observed_at desc, o.id desc))[1] as host_class,
       max(o.observed_at) as observed_at,
       count(*)::int as listings
  from place_attachments a
  join lateral (
    select po.id, po.had_website_uri, po.host_class, po.observed_at
      from place_observations po
     where po.org_id = a.org_id and po.business_id = a.business_id and po.place_id = a.place_id
     order by po.observed_at desc, po.id desc
     limit 1
  ) o on true
 where a.status = 'attached'
 group by a.org_id, a.business_id;
```
    then `grant select on business_place_signal to authenticated;` and a `comment on view` citing D-05/D-08 ("tentative and rejected are never a verdict input; true if ANY attached listing's latest observation lists a website — leans against a false 'no website' lead").
13. `comment on table` for each new table not carrying `log_event`, naming why.
Apply: `$PNPM db:migrate`, then `$PNPM db:generate` must emit nothing.
Audit lists: `TENANT_TABLES` gains the eight tables, each with a comment naming its grant line in 0027
(`place_coordinates`: "no grant at all — D-12"). If the audit's enumeration includes views, add
`business_place_signal` with the comment "security_invoker view; SELECT only" — otherwise do not.
If `schema-audit.test.ts` enumerates views and fails on the view's missing RLS, add an explicit,
commented `EXPECTED_VIEWS = ['business_place_signal']` exception — never a broad filter.
`EVENT_LOGGED` gains `place_attachments`; `LOG_EVENT_TRIGGER_ROWS` gains the one update-arm row.
`tests/db/_fixtures.ts` `actAsRole`'s allow-list gains `'siteless_cron'`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM db:migrate && $PNPM db:generate && $PNPM test:db -t "grants" && $PNPM test:db tests/db/event-trigger.test.ts tests/db/schema-audit.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c -- '--> statement-breakpoint' drizzle/0027_places_grants_triggers.sql` ≥ 15
    - `grep -n "security_invoker = true" drizzle/0027_places_grants_triggers.sql` matches
    - `grep -n "grant update (heartbeat_at, workflow_run_id) on runs" drizzle/0027_places_grants_triggers.sql` matches and `grep -n "ceiling_requests" drizzle/0027_places_grants_triggers.sql` matches only a comment line
    - `grep -c "place_" tests/db/grants-audit.test.ts` ≥ 6 and the TENANT_TABLES set-equality test passes
    - `$PNPM test:unit tests/unit/pg17-compat.test.ts` green (the new SQL is PG17-safe)
  </acceptance_criteria>
  <done>0027 applied locally; audits widened; no drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The DB proofs, the shared Places fixtures, and the D-09 requirement amendment</name>
  <files>tests/db/_places-fixtures.ts, tests/db/places-schema.test.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/CONVENTIONS.md</files>
  <read_first>
    - `tests/db/_merge-fixtures.ts` (whole file), `tests/db/_fixtures.ts`
    - `tests/db/events-append-only.test.ts` (whole file)
    - `tests/db/versioned-presets.test.ts` (how a search + search_version + run are seeded as the owner)
    - `.planning/REQUIREMENTS.md` L36 (PLACE-02), `.planning/ROADMAP.md` L192 (criterion 2)
  </read_first>
  <behavior>
    Each refusal is its own `withRollback`, paired with a positive control, SQLSTATE and message pinned:
    - `place observations are append-only: update is refused` — as `CLAIMS_A`, `update place_observations set had_website_uri = true` → `42501`, `/permission denied for table place_observations/`
    - `place observations are append-only: delete is refused` — same, `delete` → `42501`
    - `the append-only trigger refuses even the owner` — as owner, `update place_observations …` → `55000`, `/append-only/`
    - `authenticated cannot read place coordinates` (M37) — as `CLAIMS_A`, `select * from place_coordinates` → `42501`, `/permission denied for table place_coordinates/`
    - `a coordinate cannot be kept past 30 days` — owner insert with `expires_at = observed_at + interval '31 days'` → `23514`, constraint `pc_expiry_within_30_days`; positive control at exactly 30 days succeeds
    - `an observation's host class agrees with its boolean` — owner insert `had_website_uri = false, host_class = 'social'` → `23514` `po_host_class_agrees`
    - `one active run per org` — a second `queued` run in org A → `23505` `runs_one_active_per_org`; positive control: a queued run in org B succeeds
    - `a tentative attachment is excluded from the signal` (M43) — business with a tentative attachment whose latest observation is `true` → no row in `business_place_signal` as `CLAIMS_A`
    - `the signal is true if any attached listing's latest observation is true` (D-08) — two attached listings, latest observations `false` and `true` → `had_website_uri = true`; and a listing whose OLDER observation was true but latest false contributes false
    - `the signal view is tenant-scoped` — as `CLAIMS_B`, org A's signal row is invisible (zero rows)
    - `grants: runs heartbeat_at is updatable and ceiling_requests is not` — `has_column_privilege('authenticated','runs','heartbeat_at','UPDATE') = true`, `…'ceiling_requests'… = false`
    - `businesses gains no Places-derived column` — `information_schema.columns` for `businesses` has no column matching `%website%`, `%place%`, `%host_class%`, `%sab%`
  </behavior>
  <action>
1. `tests/db/_places-fixtures.ts` — the shared contract with 04-10's msw fixtures; values are FIXED:
```ts
export const PLACES_SPINE = {
  ortiz:   { name: 'Ortiz Plumbing',   street: '1200 N 10th St',        zip: '78501', city: 'McAllen', lat: 26.2159, lng: -98.2336, phone: '+19566310001' },
  garza:   { name: 'Garza Electric',   street: '4100 N 23rd St',        zip: '78504', city: 'McAllen', lat: 26.2489, lng: -98.2388, phone: '+19566310002' },
  rio:     { name: 'Rio Roofing',      street: '900 S Main St',         zip: '78501', city: 'McAllen', lat: 26.1960, lng: -98.2300, phone: '+19566310003' },
  rioCo:   { name: 'Rio Roofing Co',   street: '2200 W Nolana Ave',     zip: '78504', city: 'McAllen', lat: 26.2400, lng: -98.2450, phone: '+19566310003' },
  valley:  { name: 'Valley Locksmith', street: '500 E Expressway 83',   zip: '78503', city: 'McAllen', lat: 26.1900, lng: -98.2200, phone: '+19566310004' },
} as const;
```
   `seedPlacesSpine(c, orgId)` seeds all five through `seedOvertureSide` with a `basicCategory` that
   maps to `home_services` in the `overture_category_map` built-ins (read them; name the one used) and
   returns `{ ortiz, garza, rio, rioCo, valley }` business ids. `seedPlacesRun(c, orgId, opts: { kind?, status?, ceilingRequests?, clusterKey?, unit? })`
   seeds a search + version (McAllen × home_services by default) + a `runs` row as the owner and
   returns ids. `seedRunSearch(c, orgId, runId, { tileKey, placesType, depth?, parentTileKey?, kind? })`
   upserts a `place_tiles` row and inserts a `run_searches` row. `seedAttachmentWithObservation(c, { orgId, businessId, placeId, runId, status, hadWebsiteUri, hostClass, observedAt? , withCoordinates? })`.
   All inserts as the owner, tens of rows, never thousands. Re-export `CLAIMS_A`, `CLAIMS_B`.
2. `tests/db/places-schema.test.ts` with the twelve tests above. Watch every refusal fail first by
   temporarily inverting its assertion or running before the relevant 0027 statement exists; quote the
   red names in the SUMMARY.
3. D-09 amendment, in this plan as CONTEXT requires:
   - `.planning/REQUIREMENTS.md` PLACE-02 → "System persists only `place_id` (indefinite), lat/lng (30-day TTL, enforced), and derived signals per business — a `had_website_uri` boolean, a host class computed from `websiteUri` at call time (`none | business_site_dead | social | directory | platform_subdomain | other`; the URL itself is discarded), and the `pureServiceAreaBusiness` flag; every other Places field is discarded after the call (amended by 04-CONTEXT D-09/D-13)"
   - `.planning/ROADMAP.md` Phase 4 criterion 2 → "After a run, the only Places-derived data on disk is `place_id`, lat/lng younger than 30 days, and derived signals — the `had_website_uri` boolean, its host class (the URL itself discarded at call time), and the service-area flag — the TTL purge is observable — and wherever a Places-derived signal is displayed, Google attribution is displayed with it."
   - `.planning/CONVENTIONS.md` § Retention gains a paragraph: Places observations are append-only; coordinates live in `place_coordinates`, which `authenticated` cannot read (no grant), with `expires_at ≤ observed_at + 30 days`; a daily purge deletes expired coordinate rows and never an observation; no `source_records` row is ever written for `google_places` (the observation rows are the Places record).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "place observations are append-only" && $PNPM test:db -t "authenticated cannot read place coordinates" && $PNPM test:db -t "a tentative attachment is excluded from the signal" && $PNPM test:db -t "one active run per org" && $PNPM test:db -t "businesses gains no Places-derived column" && $PNPM test:db</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all twelve tests in `places-schema.test.ts`
    - `grep -n "host class computed from" .planning/REQUIREMENTS.md` and `grep -n "derived signals" .planning/ROADMAP.md` match
    - `grep -n "+19566310001" tests/db/_places-fixtures.ts` matches
    - Full `$PNPM test:db` green (retention M18 and every Phase 1–3 test untouched)
  </acceptance_criteria>
  <done>The schema's guarantees are proven in the database, and the requirement text matches what persists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tenant session (`authenticated`) → Places tables | SELECT only; every write is a definer (04-11, 04-15) |
| tenant session → `place_coordinates` | no privilege at all |
| migration owner → production | 0026/0027 go to production only through 04-30's human gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Information disclosure (legal retention) | `place_coordinates` | mitigate | `revoke all` from authenticated/anon; `pc_expiry_within_30_days` CHECK; named tests (M37) |
| T-4-06 | Tampering (cross-tenant) | new tables | mitigate | org_id + orgPolicies + SELECT-only grants + revoke; TENANT_TABLES set equality; view is `security_invoker` so RLS applies through it; tenant-scoped view test |
| T-4-05 | Information disclosure (legal) | observations | mitigate | columns hold booleans, enums, ids and timestamps only; `businesses` gains no Places column (named test) |
| T-4-12 | Tampering | `runs.ceiling_requests` | mitigate | outside the UPDATE column grant (named test); `calls_count` stays tenant-updatable — accepted, the monthly meter still bounds spend |
| T-4-02 | Denial of service (financial) | concurrent runs | mitigate | `runs_one_active_per_org` unique partial index (23505) |
| T-4-07 | Elevation of privilege | `siteless_cron` | mitigate | NOLOGIN, held only by `app_user`, will hold execute on exactly one function (04-11) |
</threat_model>

<verification>
- `$PNPM db:migrate` then `$PNPM db:generate` reports no drift
- `$PNPM test:db` fully green; `$PNPM typecheck`; `$PNPM test:unit tests/unit/pg17-compat.test.ts`
- SUMMARY lists the real generated migration name and the red runs of each refusal
</verification>

<success_criteria>
- The retention barrier, append-only history, signal view and tenancy of every new table are database facts with named proofs
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-09-SUMMARY.md`
</output>
