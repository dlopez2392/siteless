---
phase: 04-places-transient-verifier
plan: 12
type: execute
wave: 2
depends_on: [04-02, 04-04, 04-05, 04-10]
files_modified:
  - src/lib/budget/field-mask-tier.ts
  - src/lib/places/request.ts
  - src/lib/places/response.ts
  - src/lib/places/reserved-call.ts
  - src/lib/places/client.ts
  - tests/unit/field-mask-tier.test.ts
  - tests/unit/no-google-credential.test.ts
  - tests/unit/no-network.test.ts
  - tests/unit/places-request.test.ts
  - tests/unit/places-client.test.ts
autonomous: true
requirements: [PLACE-01, PLACE-05, BUDG-03]

must_haves:
  truths:
    - "One builder owns the request: the field mask, includePureServiceAreaBusinesses: true, strictTypeFiltering, pageSize 20, regionCode US and languageCode en are module constants no call site can omit (PLACE-01, PLACE-05, M26)"
    - "Pages 2 and 3 are the first page's body plus pageToken, never a rebuilt body (Google's INVALID_ARGUMENT rule, M49)"
    - "places.pureServiceAreaBusiness is in fieldMaskTier's PRO list and in the Enterprise mask, whose tier stays ts_enterprise; rating and userRatingCount stay in the mask, memory-only (D-13, D-21, M27)"
    - "The IDs-only mask is ['places.id','nextPageToken'] and prices ts_essentials (D-16)"
    - "src/lib/places/client.ts is the ONLY module that names the Places host, the API-key header, the field-mask header and the key variable; searchText requires a ReservedCall and is the only exported call — Place Details is unreachable (criterion 1, D-03, M51)"
    - "The client never throws and never echoes a response body: every outcome is a named reason, and a daily-quota 429 is told apart from a per-minute 429 by the error's quota metadata, ambiguous → daily (D-19, Pitfall 3)"
  artifacts:
    - path: "src/lib/places/client.ts"
      exports: ["searchText", "placesKeyConfigured", "SearchTextOutcome"]
    - path: "src/lib/places/request.ts"
      exports: ["buildFirstPage", "buildNextPage", "PlacesRequest", "SearchTextBody"]
    - path: "src/lib/places/reserved-call.ts"
      exports: ["ReservedCall", "mintReservedCall"]
    - path: "src/lib/places/response.ts"
      exports: ["PlaceSchema", "SearchTextResponse", "GoogleErrorEnvelope", "ParsedPlace", "classifyGoogleError"]
  key_links:
    - from: "tests/unit/no-google-credential.test.ts"
      to: "src/lib/places/client.ts"
      via: "the single allow-listed file for the key, key header and host patterns"
      pattern: "src/lib/places/client.ts"
    - from: "tests/unit/field-mask-tier.test.ts HEADER_MAY_BE_NAMED_IN"
      to: "src/lib/places/client.ts"
      via: "the field-mask header's one-module rule moves to the module that sends it"
      pattern: "HEADER_MAY_BE_NAMED_IN"
---

<objective>
The Places adapter: the one request builder, the zod response schema, the one sanctioned client,
and the reservation-token type that makes "no call without a reservation" a type error.

Purpose: criterion 1 (hard-coded mask, `fieldMaskTier` refuses unknown fields, Place Details
unreachable), PLACE-05 (SAB flag on every search), D-13 (the SAB field in the mask), D-03 (one
sanctioned key reader — this plan also delivers BUDG-03's code half: the key is server-only and
API-restricted by runbook; the quota itself is set at 04-31's checkpoint).

Output: five `src/lib/places` / budget modules and five test files, all against msw.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. No scratch `.ts` in the repo.
</pinned_launcher>

<interfaces>
`src/lib/budget/field-mask-tier.ts` (current): `ESSENTIALS = ['places.id','places.name','nextPageToken']`,
`PRO = ['places.displayName','places.formattedAddress','places.location','places.types','places.businessStatus']`,
`ENTERPRISE = ['places.websiteUri','places.nationalPhoneNumber','places.rating','places.userRatingCount']`,
`ATMOSPHERE = ['places.reviews']`, `PLACES_TEXT_SEARCH_FIELD_MASK` (11 fields, tier `ts_enterprise`),
`fieldMaskTier(mask)` refuses unknown/empty. Its header comment currently SPELLS the field-mask header
name — the header grep would then flag it once the rule moves to `client.ts`; reword it ("the field-mask request header").
`tests/unit/field-mask-tier.test.ts`: `FIELD_TIERS` table (L41–55), set equality at L73, `HEADER_MAY_BE_NAMED_IN` (L57–59) and the repo grep (L119–139). Gate M11 appends `places.reviews` to the mask and must red the tier test and `price-book.test.ts`'s ledger-price test.
`src/lib/geocode/census.ts`: the client analog — `import 'server-only'`, hard-coded origin + path constants, `fetch` with `AbortSignal.timeout`, never throws, named failure reasons, zod `safeParse`, never echoes a body.
From 04-04: `isTableAType(t)`. From 04-05: `type Rect = { south; west; north; east }`.
From 04-10: `setPlacesRoutes`, `resetPlaces`, `placesRequests`, `PLACES_SENTINELS`, fixtures `places-saturated-p*.json`, `places-429-daily.json`, `places-429-minute.json`, `places-400-invalid.json`, `places-503.json`.
Unit lane forces `process.env.GOOGLE_PLACES_API_KEY = 'test-key-not-real'` (04-01).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-13 mask change, the IDs-only mask, and the one builder</name>
  <files>src/lib/budget/field-mask-tier.ts, src/lib/places/request.ts, tests/unit/field-mask-tier.test.ts, tests/unit/places-request.test.ts</files>
  <read_first>
    - `src/lib/budget/field-mask-tier.ts` and `tests/unit/field-mask-tier.test.ts` (whole files)
    - `tests/unit/price-book.test.ts` (M11's second target)
    - 04-RESEARCH.md § Pattern 4 (request shape and the "all parameters must be the same" rule)
  </read_first>
  <behavior>
    - `fieldMaskTier maps every known field to its tier` gains the row for `places.pureServiceAreaBusiness` → `ts_pro` (M27 target — red until PRO gains it)
    - "the Places mask stays enterprise with the service-area flag": `PLACES_TEXT_SEARCH_FIELD_MASK` includes `places.pureServiceAreaBusiness`, `places.rating`, `places.userRatingCount`; `fieldMaskTier(mask) === 'ts_enterprise'`
    - "the ids-only mask is free": `PLACES_IDS_ONLY_FIELD_MASK` deep-equals `['places.id','nextPageToken']`; tier `ts_essentials`
    - "every places request carries includePureServiceAreaBusinesses" (M26): `buildFirstPage({ placesType:'plumber', rect, mode:'enterprise' }).body.includePureServiceAreaBusinesses === true`, same for `ids_only`, and for `buildNextPage(first,'t')`
    - "a page request repeats the first request's body" (M49): `buildNextPage(first,'tok').body` deep-equals `{ ...first.body, pageToken: 'tok' }` and `mask`/`sku` are the first's
    - "the builder refuses a type Google does not accept": `placesType: 'general_contractor'` throws naming it
    - "the builder sends a rectangle south-west low and north-east high"
    - "the builder derives textQuery from the type": `roofing_contractor` → `'roofing contractor'`
  </behavior>
  <action>
1. `field-mask-tier.ts`: add `'places.pureServiceAreaBusiness'` to `PRO`; insert it into
   `PLACES_TEXT_SEARCH_FIELD_MASK` after `places.businessStatus` (D-13; comment: "Pro tier — free at
   the margin under Enterprise; persisted only as the derived pure_sab flag"); keep `places.rating`
   and `places.userRatingCount` with the comment "D-21: requested, memory-only — nothing in Phase 4
   uses or persists them". Export `PLACES_IDS_ONLY_FIELD_MASK: readonly PlacesField[] = ['places.id', 'nextPageToken']`.
   Reword the header so it no longer spells the header name.
2. `src/lib/places/request.ts` (pure; no `server-only`, no I/O):
```ts
export type SearchTextBody = {
  textQuery: string; includedType: string; strictTypeFiltering: true;
  locationRestriction: { rectangle: { low: { latitude: number; longitude: number }; high: { latitude: number; longitude: number } } };
  includePureServiceAreaBusinesses: true; pageSize: 20; regionCode: 'US'; languageCode: 'en'; pageToken?: string;
};
export type PlacesRequest = { readonly body: Readonly<SearchTextBody>; readonly mask: readonly PlacesField[]; readonly sku: TextSearchSku };
export function buildFirstPage(a: { placesType: string; rect: Rect; mode: 'enterprise' | 'ids_only' }): PlacesRequest;
export function buildNextPage(first: PlacesRequest, pageToken: string): PlacesRequest; // { ...first, body: { ...first.body, pageToken } }
```
   Constants (`INCLUDE_PURE_SERVICE_AREA_BUSINESSES = true`, `STRICT_TYPE_FILTERING = true`, `PAGE_SIZE = 20`,
   `REGION_CODE = 'US'`, `LANGUAGE_CODE = 'en'`) are module-private; `sku = fieldMaskTier(mask)`.
   Header: "PLACE-01 / PLACE-05 / Pitfalls 1 and 8. The ONE place a Places request is shaped. Categorical
   query only: `includedType` + `strictTypeFiltering`, textQuery = the type with '_' → ' ' (Claude's
   Discretion, research A7). Returns body + mask; the HEADERS are spelled only in client.ts."
3. Tests per the behavior block; red first (the FIELD_TIERS row and the SAB flag), then green.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "fieldMaskTier" && $PNPM test:unit -t "every places request carries includePureServiceAreaBusinesses" && $PNPM test:unit -t "a page request repeats the first request's body" && $PNPM test:unit -t "the ids-only mask is free" && $PNPM test:unit tests/unit/price-book.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "'places.pureServiceAreaBusiness'" src/lib/budget/field-mask-tier.ts` matches twice (PRO list and the mask)
    - `grep -n "export const PLACES_IDS_ONLY_FIELD_MASK" src/lib/budget/field-mask-tier.ts` matches
    - PASS list names `every places request carries includePureServiceAreaBusinesses` and `a page request repeats the first request's body`
  </acceptance_criteria>
  <done>The mask carries the SAB field at the same tier; one builder shapes every request.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: response schema, the ReservedCall brand, and the one sanctioned client</name>
  <files>src/lib/places/response.ts, src/lib/places/reserved-call.ts, src/lib/places/client.ts, tests/unit/places-client.test.ts</files>
  <read_first>
    - `src/lib/geocode/census.ts` (whole file)
    - 04-RESEARCH.md § Pattern 3, § Code Examples → zod response schema, Pitfalls 1(d), 3, 9
    - `tests/unit/census.test.ts` (msw usage in the unit lane)
  </read_first>
  <behavior>
    - "searchText sends the key and mask headers and parses the page": against route `saturated`, outcome `{ ok: true }` with 20 places and `nextPageToken === 'saturated:p2'`; the logged request has `hasKey` true and `mask` equal to the joined mask
    - "searchText classifies a daily quota 429" → `{ ok:false, reason:'daily_quota', status:429 }`
    - "searchText classifies a per-minute 429" → `reason:'rate_limited'`, `retryAfterMs` a positive number
    - "searchText treats an ambiguous 429 as daily" (429 without quota metadata) → `daily_quota`
    - "searchText classifies a 400 as rejected" · "a 503 as unavailable" · "a network error as timeout" (msw `HttpResponse.error()`; unknown outcome → caller settles as charged) · "an unparseable 200 as bad_shape"
    - "searchText refuses without a key and sends nothing": key env deleted → `reason:'no_key'`, `placesRequests.length === 0`
    - "a failed request's outcome never contains response text": `JSON.stringify(outcome)` for each error fixture contains none of the fixture's `error.message` strings
    - "place details is unreachable": the client module's exports are exactly `placesKeyConfigured`, `searchText` (plus types); its source matches `:searchText` exactly once and never matches `/v1\/places\/|places:get|placeDetails|:autocomplete|:searchNearby/`
    - "no module but the meter mints a reserved call": every file under `src/` that imports `mintReservedCall` is `src/lib/places/meter.ts` (none exist yet; 04-16 adds the meter and turns this into an equality)
  </behavior>
  <action>
1. `response.ts` — the research zod schemas verbatim (`PlaceSchema` with optional `displayName`, `formattedAddress`,
   `location`, `types`, `businessStatus`, `pureServiceAreaBusiness`, `websiteUri`, `nationalPhoneNumber`, `rating`,
   `userRatingCount`; `SearchTextResponse` with `places` defaulting to `[]`; `GoogleErrorEnvelope`), `type ParsedPlace`,
   and `classifyGoogleError(status: number, json: unknown): 'daily_quota' | 'rate_limited' | 'rejected' | 'unavailable'`:
   429 → scan `error.details[*].metadata.quota_limit` (and `quota_metric`) for `/PerDay/i` → `daily_quota`, `/PerMinute/i` → `rate_limited`, else `daily_quota`; 400 → `rejected`; 5xx and any other status → `unavailable`.
2. `reserved-call.ts`:
```ts
declare const reservedBrand: unique symbol;
export type ReservedCall = { readonly [reservedBrand]: true; readonly reservationId: string; readonly requestId: string; readonly sku: TextSearchSku };
/** Minted ONLY by src/lib/places/meter.ts, after the mode check and a granted reservation (criterion 5). */
export function mintReservedCall(reservationId: string, requestId: string, sku: TextSearchSku): ReservedCall {
  return { reservationId, requestId, sku } as ReservedCall;
}
```
3. `client.ts`:
```ts
import 'server-only';
const PLACES_ORIGIN = 'https://places.googleapis.com';
const SEARCH_TEXT_PATH = '/v1/places:searchText';
const PLACES_TIMEOUT_MS = 10_000;
const DEFAULT_RETRY_AFTER_MS = 60_000;
export function placesKeyConfigured(): boolean { return Boolean(process.env.GOOGLE_PLACES_API_KEY); }
export type SearchTextOutcome =
  | { ok: true; places: ParsedPlace[]; nextPageToken: string | null }
  | { ok: false; reason: 'daily_quota' | 'rate_limited' | 'rejected' | 'unavailable' | 'timeout' | 'bad_shape' | 'no_key'; status: number | null; retryAfterMs?: number };
export async function searchText(call: ReservedCall, req: PlacesRequest): Promise<SearchTextOutcome>;
```
   Read the key per call; absent → `no_key` without fetching. `fetch(PLACES_ORIGIN + SEARCH_TEXT_PATH, { method:'POST',
   signal: AbortSignal.timeout(PLACES_TIMEOUT_MS), headers: { 'Content-Type':'application/json', 'X-Goog-Api-Key': key,
   'X-Goog-FieldMask': req.mask.join(',') }, body: JSON.stringify(req.body) })`. A thrown fetch → `timeout`
   (unknown outcome; the caller settles as charged, Pitfall 9). Non-OK → parse JSON best-effort → `classifyGoogleError`;
   for `rate_limited`, `retryAfterMs` from a numeric `Retry-After` header else `DEFAULT_RETRY_AFTER_MS`.
   OK → `SearchTextResponse.safeParse` → `bad_shape` on failure. `call` is required by the signature
   (the type proof) and otherwise only read for its `sku` sanity check (`call.sku !== req.sku` → `rejected`, status null).
   🔴 Never log, never throw, never put `json`, `message` or any response text in the outcome.
   Header: "D-03 — the ONE sanctioned reader of the Places key and the only module that names the host,
   the API-key header and the field-mask header. Server-only; the key is API-restricted to Places API (New)
   and never NEXT_PUBLIC_ (Vercel functions have no fixed egress IP, so no application restriction)."
4. Tests per the behavior block (msw via the shared server; `resetPlaces()` after each).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "searchText" && $PNPM test:unit -t "place details is unreachable" && $PNPM test:unit -t "a failed request's outcome never contains response text" && $PNPM test:unit -t "no module but the meter mints a reserved call"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "process.env.GOOGLE_PLACES_API_KEY" src/lib/places/client.ts` ≥ 1 and `grep -rn "GOOGLE_PLACES_API_KEY" src --include=*.ts --include=*.tsx | grep -v "src/lib/places/client.ts"` returns nothing
    - `grep -n "console\.\|throw " src/lib/places/client.ts` returns nothing
    - PASS list names all ten client tests
  </acceptance_criteria>
  <done>The client is the one reader, requires a reservation token by type, and returns only named reasons.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move the three repo guards onto client.ts (amended, never deleted)</name>
  <files>tests/unit/no-google-credential.test.ts, tests/unit/no-network.test.ts, tests/unit/field-mask-tier.test.ts</files>
  <read_first>
    - The three test files (after 04-02 moved them onto `_walk.ts`)
    - 04-PATTERNS.md § tests/unit/no-google-credential.test.ts · no-network.test.ts · field-mask-tier.test.ts, blast-radius notes 2 and 3
  </read_first>
  <behavior>
    - `no google credential is read anywhere in src` stays green with `src/lib/places/client.ts` as the ONLY allow-listed file, and gains a positive control: the allow-listed file IS scanned and DOES contain `GOOGLE_PLACES_API_KEY` (proves the allow-list is exercised, not dead). FORBIDDEN gains `{ label: 'places.googleapis.com host', re: /places\.googleapis\.com/i }`.
    - `no-network`'s host list gains the Places host (as a constructed string, so the test file itself does not trip other guards) and `ALLOWED_FILES` gains `src/lib/places/client.ts`.
    - `field-mask-tier.test.ts` `HEADER_MAY_BE_NAMED_IN` becomes `src/lib/places/client.ts`; the grep asserts the header is spelled in exactly that one file.
  </behavior>
  <action>Amend each test as above. Then mutation-check M51 locally: add `const k = process.env.GOOGLE_PLACES_API_KEY;` to `src/lib/places/request.ts`, run `$PNPM test:unit -t "no google credential is read anywhere in src"` — it must go red naming `request.ts`; revert; `git diff --stat src/` empty. Record both runs in the SUMMARY.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "no google credential is read anywhere in src" && $PNPM test:unit tests/unit/no-network.test.ts tests/unit/field-mask-tier.test.ts && $PNPM typecheck && $PNPM lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "src/lib/places/client.ts\|'src', 'lib', 'places', 'client.ts'" tests/unit/no-google-credential.test.ts` matches
    - The SUMMARY quotes M51's red run naming `request.ts`, then the clean revert
    - `$PNPM test:unit` fully green
  </acceptance_criteria>
  <done>The one-reader rule is enforced by the existing guards, amended to the real sanctioned module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → places.googleapis.com | the key and the mask leave here; the response enters here |
| Google response → our code | untrusted, versioned third-party JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information disclosure | Places key | mitigate | read only in `client.ts` (`server-only`); guard allow-lists exactly that file with a positive control; M51 mutation-checked |
| T-4-02 | Denial of service (financial) | mask / SKU | mitigate | mask is a module constant; `fieldMaskTier` refuses unknown fields; IDs-only mask is a separate constant priced `ts_essentials` |
| T-4-05 | Information disclosure (legal) | error outcomes | mitigate | outcomes carry reason + status only; named test proves no response text survives |
| T-4-10 | Tampering | response parsing | mitigate | zod `safeParse` (strip unknown keys); `bad_shape` on failure; no string from the response reaches SQL here |
| T-4-02 | Denial of service (financial) | retry on 429 | mitigate | daily quota vs per-minute by metadata; ambiguous → daily (stop, the safe direction); retries are the step's job, bounded (04-18, 04-22) |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint` green; M51 red-then-revert recorded
</verification>

<success_criteria>
- Criterion 1 holds by type and by test; PLACE-05's flag is on every request by construction
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-12-SUMMARY.md`
</output>
