---
phase: 04-places-transient-verifier
plan: 23
type: execute
wave: 5
depends_on: [04-07, 04-08, 04-14, 04-20]
files_modified:
  - src/app/(app)/runs/[id]/page.tsx
  - src/app/(app)/runs/[id]/loading.tsx
  - src/app/(app)/runs/[id]/error.tsx
  - src/app/(app)/runs/[id]/not-found.tsx
  - src/components/runs/run-report-view.tsx
  - src/components/runs/run-header.tsx
  - src/components/runs/run-alerts.tsx
  - src/components/runs/requests-card.tsx
  - src/components/runs/tiles-card.tsx
  - src/components/runs/outcomes-card.tsx
  - src/components/runs/changes-card.tsx
  - tests/unit/run-report.test.tsx
autonomous: true
requirements: [PLACE-03, PLACE-06]

must_haves:
  truths:
    - "/runs/[id] is a live, ledger-style report: status and stopped reason, requests by SKU, cost versus the estimate and the ceiling, tiles searched/saturated/subdivided/still truncated, and attached/tentative/unmatched per cluster with the website split (D-17)"
    - "Any still-truncated tile renders an explicit warning in EVERY status, including complete — never a silent partial (criterion 3)"
    - "Every stopped reason renders as a sentence through the copy map; a refused run shows only its alert (Rule 35, UI-SPEC § States)"
    - "The Tiles, Outcomes and Changes cards are Places-derived and each carries exactly one Google Maps tag in its [data-places-content] container; the Requests card (our own ledger) carries none (D-11, PLACE-06, Rule 28)"
    - "The ceiling line shows the dollar ceiling and the request ceiling (D-18); counts are tabular and never animate; the page refreshes itself every 5 s only while live (D-17)"
  artifacts:
    - path: "src/app/(app)/runs/[id]/page.tsx"
      provides: "RSC: uuid guard, one read, notFound for unknown/foreign"
      contains: "isUuid"
    - path: "src/components/runs/run-report-view.tsx"
      exports: ["RunReportView"]
    - path: "tests/unit/run-report.test.tsx"
      contains: "the run report shows the truncation warning in every status including complete"
  key_links:
    - from: "src/app/(app)/runs/[id]/page.tsx"
      to: "src/server/queries/run-report.ts getRunReport"
      via: "one withOrg read per render"
      pattern: "getRunReport"
    - from: "src/components/runs/run-report-view.tsx"
      to: "src/components/runs/run-auto-refresh.tsx"
      via: "status + renderedAtMs + truncatedCount props"
      pattern: "RunAutoRefresh"
---

<objective>
The run report screen (UI-SPEC § Screen 1; D-17), rendering 04-20's read with 04-14's chrome.

Purpose: answer, in order, *is it done? what did it cost against the estimate? did Google truncate
anything? what did we find?* — the place where criterion 3's truncation is shown, never swallowed.

Output: the `/runs/[id]` route (page, loading, error, not-found), seven components, one dom test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
@.planning/phases/02-budget-governor-search-presets/02-UI-SPEC.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. Never run `npx shadcn init`; no `add` is needed.
</pinned_launcher>

<interfaces>
04-20 `RunReport` (run, requests, tiles, outcomes, changes) and `getRunReport(claims, runId)`.
04-14 `RunStatusBadge({ status })`, `RunAutoRefresh({ status, renderedAtMs, truncatedCount })`.
04-08 `GoogleMapsTag`. 04-07 copy: `RUN_REPORT_*`, `RUN_ESTIMATE_LINE(lo, hi, ceilingUsd, ceilingRequests)`, `RUN_STOP_*`, `RUN_STOP_DAILY_QUOTA`,
`RUN_FAILED_ERROR`, `RUN_NEVER_STARTED`, `RUN_ABANDONED`, `RUN_TRUNCATION_HEADING(n)`, `STOPPED_REASON`, `RUN_KIND_LABEL`; `places-format.ts` `hostClassLabel`, `HOST_CLASS_ORDER`.
04-04 `RUN_CEILING_MULTIPLIER`. `src/lib/time.ts` `formatLocal`, `formatCount`; money `formatUsd`. `src/lib/ids.ts` `isUuid`.
Analogs: `src/app/(app)/businesses/[id]/{page,loading,error,not-found}.tsx` (uuid guard, one read, `notFound()`, desk breadcrumb L104–121,
`dynamic = 'force-dynamic'`); `src/components/sources/source-ledger.tsx` (count grids, desk Table vs phone label/value rows, testid per count);
`src/components/spend/by-run.tsx` L28–31 (distinct testids on the desk and phone elements); `CopyCommandButton` for "Copy the tile list".
`tests/unit/ids.test.ts` discovers every `[id]/page.tsx` and requires `isUuid`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The /runs/[id] route files</name>
  <files>src/app/(app)/runs/[id]/page.tsx, src/app/(app)/runs/[id]/loading.tsx, src/app/(app)/runs/[id]/error.tsx, src/app/(app)/runs/[id]/not-found.tsx</files>
  <read_first>
    - `src/app/(app)/businesses/[id]/page.tsx`, `loading.tsx`, `error.tsx`, `not-found.tsx` (whole files)
    - 04-UI-SPEC.md § 0 (breadcrumb, back link), § Screen 1, § States → Loading/Error
  </read_first>
  <action>
`page.tsx`: `export const dynamic = 'force-dynamic'`; `const { id } = await params;` — invalid uuid renders the bad-id error body
(`RUN_REPORT_BAD_ID` + "Open spend view") inline; else `const claims = await orgClaims(); const report = await getRunReport(claims, id); if (!report) notFound();`
then `<RunReportView report={report} renderedAtMs={Date.now()} canRaiseCap={…} />` where `canRaiseCap` is the org-admin check the
budget settings page already uses (find it with `grep -rn "org:admin\|isOrgAdmin\|current_org_role" src/app src/lib/auth`). Desk breadcrumb
"Presets / {preset name} / Run {Sep 23, 2:14 PM}"; phone 44px back link "← {preset name}" to `/presets/{presetId}`; `document.title` via
`generateMetadata` → "Run report — {preset name}". `loading.tsx`: title + breadcrumb immediately, header-card skeleton and three card
skeletons (never a centred spinner). `error.tsx`: the load-failed copy with "Try again" (reset) and "Open spend view". `not-found.tsx`:
`RUN_REPORT_NOT_FOUND` + "Open spend view".
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit tests/unit/ids.test.ts && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isUuid" "src/app/(app)/runs/[id]/page.tsx"` matches; `ids.test.ts` green (it discovers the new route)
    - `grep -n "export const dynamic = 'force-dynamic'" "src/app/(app)/runs/[id]/page.tsx"` matches
  </acceptance_criteria>
  <done>The route exists with the uuid guard, one read, and every state's shell.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunReportView, the header and the alerts (stop / refused / failed / queued-long / truncation)</name>
  <files>src/components/runs/run-report-view.tsx, src/components/runs/run-header.tsx, src/components/runs/run-alerts.tsx, tests/unit/run-report.test.tsx</files>
  <read_first>
    - 04-UI-SPEC.md § Screen 1 (header card, stop/refusal/failure alerts table, truncation warning), § Running/terminal table, § Accessibility (live region), Rules 11, 35, 36, 41
    - `src/components/runs/run-auto-refresh.tsx`, `run-status-badge.tsx` (04-14)
  </read_first>
  <behavior>
    Render `RunReportView` with hand-built `RunReport` fixtures (dom lane; `next/navigation` mocked as in 04-14's test):
    - `the run report shows the truncation warning in every status including complete` — for each of running / complete / partial / failed with `stillTruncated 3`, `run-truncation-warning` exists with `data-count="3"` and `role="status"`; with 0 it is absent
    - `the run report never renders a raw stopped reason` — for each of the eight `STOPPED_REASONS` (with the matching status), the DOM text contains no `_`-joined key and `run-stop-alert` has `data-reason` = the key
    - `the run report renders the refused state alone` — `refused/budget_cap_reached` → the refused alert; no `run-requests`, no `run-tiles-*`, no `run-outcome-*`
    - `the ceiling line shows the dollar and request ceilings` — `estimateMicroUsdLo 1_900_000`, `Hi 2_900_000`, `ceilingRequests 108` → `run-estimate-line` contains `$1.90`, `$2.90`, `$5.80` and `108`
    - `the header shows the above-estimate line only between the top of the estimate and the ceiling`
    - `the header's live parts appear only while the run is live` — `run-refresh-now` present for running, absent for complete (which shows the finished line)
    - `the cost is display-sized and foreground` — `run-cost` has `data-micro-usd`, class includes `text-[28px]`/the Display class the 02 spec uses, and not `text-primary`
  </behavior>
  <action>
`run-report-view.tsx` (server component) composes: title/subtitle → `RunHeader` → `RunAlerts` → `RequestsCard` → `TilesCard` →
`OutcomesCard` or `ChangesCard` (change_check) → footnote. Desk: single column, max 1120px, `gap-6` between cards, `p-6`; phone `gap-4`,
`p-4`. Refused: header + alert only. Stable keys by section id (a refresh must not re-mount an opened Collapsible).
`run-header.tsx`: `data-testid="run-report-header"`; `RunStatusBadge` + `run-kind` (`data-kind`, muted Label text) + "started …";
`run-status-line` per the summary-line table; `run-cost` (Display 28/600, foreground, tabular, `data-micro-usd`); `run-estimate-line`
via `RUN_ESTIMATE_LINE(lo, hi, formatUsd(RUN_CEILING_MULTIPLIER × hi), ceilingRequests)`; the above-estimate muted line; the change-check
cost note; `RunAutoRefresh` for queued/running, else the "Finished … · took …" line.
`run-alerts.tsx`: `run-stop-alert` (`data-reason`) per the alerts table plus the Amendment-1 reasons (`google_daily_quota` warning;
`places_request_rejected` / `places_unavailable` / `places_key_missing` in the failed alert's `{error}` clause; `never_started` / `abandoned`
destructive); queued > 2 min muted `clock` alert; the truncation warning (`run-truncation-warning`, `data-count`, `role="status"`,
`triangle-alert`, heading via `RUN_TRUNCATION_HEADING(n)`, a Collapsible `run-truncation-toggle` listing "{geography} · {type} · tile {quadPath}",
and `run-truncation-copy` via `CopyCommandButton` with one tile per line). Alerts stack: stop alert first, then truncation.
Spacing only `gap-1/2/4/6/8`; badges with `FLAG_BADGE_SIZING`; icons + words, never colour alone.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the run report shows the truncation warning in every status including complete" && $PNPM test:unit -t "the run report never renders a raw stopped reason" && $PNPM test:unit -t "the ceiling line" && $PNPM test:unit -t "the header" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all seven tests
    - `grep -rn "gap-3\|text-xs" src/components/runs/` returns nothing
  </acceptance_criteria>
  <done>The report's top half answers "done? cost? truncated?" in the specified order and tone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Requests, Tiles, Outcomes and Changes cards</name>
  <files>src/components/runs/requests-card.tsx, src/components/runs/tiles-card.tsx, src/components/runs/outcomes-card.tsx, src/components/runs/changes-card.tsx, tests/unit/run-report.test.tsx</files>
  <read_first>
    - 04-UI-SPEC.md § Screen 1 (Requests / Tiles / Outcomes / Changes), § States → Empty, Rules 28, 30
    - `src/components/sources/source-ledger.tsx` L318–349, L405–424
  </read_first>
  <behavior>
    - `the requests card renders both SKUs even at zero` — `run-sku-row-ts_enterprise` and `run-sku-row-ts_essentials` present with 0; `run-sku-row-refused` only when `refusedByMeter > 0`; totals row
    - `the tiles card warns on still truncated` — `run-tiles-truncated` with `data-count="2"` carries the warning icon; at 0 it is plain
    - `the outcomes card links tentative listings to the Google review filter` — `run-review-link` href `/review?kind=google` when `tentative > 0`, absent at 0
    - `the outcomes card lists every cluster including zero rows` — `run-cluster-row-{key}` for each cluster in the fixture, zeros rendered
    - `the website block keeps its fixed order and one colour` — `run-website-listed`, `run-website-none`, then `run-host-class-{other,social,directory,platform_subdomain,business_site_dead}` in that DOM order; none carries a colour class other than foreground/muted
    - `a change check shows changes instead of matching` — `run-changes-{checked,unchanged,new,gone}` present; no `run-outcome-*`
    - `places-derived cards carry the Google Maps tag` — each `[data-places-content]` container (Tiles, Outcomes, Changes) contains exactly one `google-maps-attribution`; `run-requests` contains none
    - `the outcomes card says no places yet while live` / `…says Google returned none when finished empty`
  </behavior>
  <action>
Each card is a shadcn `Card`; desk `Table` with right-aligned `tabular-nums` cells; phone label/value rows with DISTINCT testids from desk (never one testid on two elements).
Places-derived cards wrap their content in `<div data-places-content>` with `<GoogleMapsTag />` in the footer (Google's placement rule: top or bottom of the container).
The Requests card is our ledger: no tag. The website block: `HOST_CLASS_ORDER` rows indented under "Listed a website", every row the same colour; an
optional share bar, if added, uses one `--muted-foreground` fill for all rows (`Progress` + painted divs, no chart library).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the requests card" && $PNPM test:unit -t "the tiles card" && $PNPM test:unit -t "the outcomes card" && $PNPM test:unit -t "the website block" && $PNPM test:unit -t "places-derived cards carry the Google Maps tag" && $PNPM typecheck && $PNPM lint && $PNPM build</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names every test above
    - `grep -c "GoogleMapsTag" src/components/runs/tiles-card.tsx src/components/runs/outcomes-card.tsx src/components/runs/changes-card.tsx` ≥ 1 each; `grep -n "GoogleMapsTag" src/components/runs/requests-card.tsx` returns nothing
    - `$PNPM build` exits 0
  </acceptance_criteria>
  <done>The report's cards render every D-17 number with attribution exactly where Places-derived values appear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL → page | `[id]` is untrusted |
| DB keys → rendered text | stopped reasons, host classes, kinds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Information disclosure (cross-tenant) | `/runs/[id]` | mitigate | `isUuid` guard; `orgClaims()`; RLS read; unknown and foreign both `notFound()` (no existence oracle) |
| T-4-13 | Repudiation (legal: attribution) | Places-derived cards | mitigate | one `GoogleMapsTag` per Places-derived container (named test; the repo-wide registry lands in 04-28) |
| T-4-05 | Information disclosure | stopped reasons | mitigate | copy-mapped; named test over all eight keys |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint`, `$PNPM build` green
</verification>

<success_criteria>
- The run report exists and renders every state the UI-SPEC defines, with truncation always visible
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-23-SUMMARY.md`
</output>
