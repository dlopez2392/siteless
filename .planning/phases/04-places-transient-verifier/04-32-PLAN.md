---
phase: 04-places-transient-verifier
plan: 32
type: execute
wave: 12
depends_on: [04-29, 04-30, 04-31]
files_modified:
  - tests/unit/msw/fixtures/places-recorded-mcallen-plumber-p1.json
  - tests/unit/msw/fixtures/places-recorded-mcallen-plumber-p2.json
  - tests/unit/msw/fixtures/places-recorded-mcallen-plumber-p3.json
  - tests/unit/msw/fixtures/places-recordings.json
  - tests/unit/msw/fixtures/README.md
  - tests/db/places-recorded-replay.test.ts
  - docs/measurements/04-first-run.md
  - .planning/phases/04-places-transient-verifier/deferred-items.md
autonomous: false
requirements: [PLACE-03, PLACE-04, PLACE-05, PLACE-02]

must_haves:
  truths:
    - "The first real run is one city × one cluster (McAllen × home services unless danlo names another), sized inside the free 1,000 Enterprise requests (54 requests high, ceiling 108) and started by danlo from the deployed app (D-04)"
    - "The run proves on real RGV density what the fixtures could only assert: saturation and subdivision (or their absence, measured), service-area businesses in results (pure_sab observations), truncation reporting, per-cluster unmatched counts, and a ledger row per request with its SKU (criteria 3, 4, 5)"
    - "Anonymized fixtures recorded from real payloads replace nothing and add structure: the recorder anonymizes in memory, the sidecar says anonymized: true, and a replay test proves no Google text reached git (D-20)"
    - "Whether the Phase 2 banner specs can become real is decided on the arithmetic, not assumed: inside the free allowance committed spend stays $0 (D-04 note)"
    - "Measured numbers — requests, saturated/subdivided/truncated tiles, SAB count, matched/unmatched per cluster, host-class split, duration, daily-quota headroom — are committed, and any tiling-constant change is proposed, never made silently (Claude's Discretion: constants tuned on D-04)"
  artifacts:
    - path: "docs/measurements/04-first-run.md"
      provides: "the D-04 numbers and the tiling/quota recommendations"
      contains: "pure_sab"
    - path: "tests/db/places-recorded-replay.test.ts"
      contains: "recorded fixtures replay through the tile search"
  key_links:
    - from: "scripts/record-places-fixtures.ts"
      to: "tests/unit/msw/fixtures/places-recorded-*.json"
      via: "anonymizePage in memory before writeFileSync"
      pattern: "anonymized"
---

<objective>
The D-04 first real run and the fixture recording, as a human-gated checkpoint: danlo chooses where it
runs and flips the switch; Claude records anonymized fixtures, verifies the run from the database, and
commits the measurements.

Purpose: saturation tuning is empirical (ROADMAP research flag). Until real RGV density hits the
quadtree, MAX_DEPTH / MIN_TILE_SIDE_M / NOVELTY_MAX_OVERLAP, SAB inclusion under locationRestriction
(research A6) and per-page billing (A1) are assertions.

Output: recorded anonymized fixtures + replay test, `docs/measurements/04-first-run.md`, deferred items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@docs/runbooks/places.md
@docs/runbooks/ingest.md
@docs/deploy.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
🔴 Abort if 04-29 recorded `no`. Every production read here is a separate `begin read only` transaction. Never re-run a paid operation for its output.
🔴 Production `businesses` is EMPTY (Phase 3 loaded the spine locally only — 03-21). A production run would match nothing unless the spine is loaded there first; Task 1 decides.
</pinned_launcher>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Where and what the first real run is, and the banner-spec question</name>
  <files>docs/measurements/04-first-run.md</files>
  <read_first>
    - `docs/runbooks/ingest.md` (the `--target=prod` ingest + resolve procedure), 04-CONTEXT.md D-04, D-19
  </read_first>
  <decision>How the D-04 run executes, on which cell, and what happens to the two Phase 2 banner specs</decision>
  <context>
Sizing (committed estimator, D-18): McAllen × home services = 6 Places types → 54 requests high, `ceiling_requests` 108; the month's first 1,000
Text Search Enterprise requests are free, so the run costs $0.00. The GCP quota is 100 requests/day (IDs-only included): a saturated
trade can push the run past 100 and stop it `partial / google_daily_quota` — that is D-19 working, not a failure; the remaining tiles run the next day.
Fixture recording (before the run): ≤ 6 real requests through the recorder, against the LOCAL database, ledgered locally (they do not appear in the
production ledger; they count against the same free 1,000 and the same 100/day — recorded here so the first invoice reconciles).
Banner specs: `budget-banner.spec.ts` self-skips while production committed spend ≤ 0. Inside the free allowance committed spend STAYS $0 after
this run (reservations settle at $0), so the 80 %/100 % shots cannot become real from D-04 alone.
  </context>
  <options>
    <option id="prod-with-spine">
      <name>Production, after loading the RGV spine into production (recommended)</name>
      <pros>Real matching, drivable from danlo's phone, the real cron purge and /sources card, and the Phase 6 slice already in place</pros>
      <cons>Runs the Phase 3 ingest + resolve against production first (desk scripts, `--target=prod`, 6–12 min resolve); a bigger change than the run itself</cons>
    </option>
    <option id="prod-no-spine">
      <name>Production without the spine</name>
      <pros>Proves saturation, subdivision, SAB inclusion, ledger and quota behaviour on real density with no ingest</pros>
      <cons>Every result is "matched nothing"; attachments, signals and the review queue stay synthetic until Phase 6</cons>
    </option>
    <option id="local">
      <name>Locally (`next start`, local world, local spine, PLACES_MODE=enterprise in .env.local)</name>
      <pros>Real matching with no production data change</pros>
      <cons>Not drivable from the phone; the production cron/card stay synthetic; the local ledger, not production's, records the spend</cons>
    </option>
  </options>
  <acceptance_criteria>
    - danlo chose the venue, confirmed or named the city × cluster, chose the post-run `PLACES_MODE` (`enterprise` / `ids_only` / `off`), and chose for the banner specs: `re-defer to the first billed month` or `accept synthetic`
  </acceptance_criteria>
  <resume-signal>Reply e.g. `prod-with-spine mcallen home_services after=off banners=re-defer`</resume-signal>
  <action>Present the three venues with the sizing and ledger notes; wait for the venue, cell, post-run mode and banner choice.</action>
  <verify>Human checkpoint — the reply is recorded verbatim in the SUMMARY; no automated command applies.</verify>
  <done>danlo's reply is recorded and the plan proceeds (or stops) exactly as it directs.</done>
</task>

<task type="auto">
  <name>Task 2: Record anonymized fixtures, prepare the venue, and add the replay test</name>
  <files>tests/unit/msw/fixtures/places-recorded-mcallen-plumber-p1.json, tests/unit/msw/fixtures/places-recorded-mcallen-plumber-p2.json, tests/unit/msw/fixtures/places-recorded-mcallen-plumber-p3.json, tests/unit/msw/fixtures/places-recordings.json, tests/unit/msw/fixtures/README.md, tests/db/places-recorded-replay.test.ts</files>
  <read_first>
    - `scripts/record-places-fixtures.ts`, `scripts/lib/anonymize-places.ts` (04-19), `tests/db/places-search-tile.test.ts` (04-18)
  </read_first>
  <action>
1. Record: `$PNPM record:places --version=<local McAllen × home_services version> --type=plumber --unit=city:48215/McAllen --max-requests=3 --out=mcallen-plumber`
   (≤ 3 pages of the root). If the root was saturated, optionally one child `--quad=r0 --max-requests=3 --out=mcallen-plumber-r0` (total ≤ 6).
   Inspect every written file BEFORE committing, structurally: every `displayName.text` starts with `Synthetic `; every `formattedAddress`
   contains `Synthetic`; every `nationalPhoneNumber` matches `^\(956\) 555-01\d\d$`; every `websiteUri` host is `synthetic-NNN.*`, `www.facebook.com`,
   `www.yelp.com` or ends in `.example`/`.business.site`/`.wixsite.com` with a `synthetic-` label; no other string keys exist besides `id`, `types`,
   `businessStatus`, `nextPageToken`. Confirm the sidecar entries say `"anonymized": true`. Any deviation: delete the file and stop.
2. `tests/db/places-recorded-replay.test.ts` — `recorded fixtures replay through the tile search`: serve the recorded pages via msw routes and run
   `runSearchTile` on a seeded local run: pagination and counts match the sidecar; every place id round-trips into `place_tile_members`; a
   `pure_sab` observation exists iff the recording held a SAB; and the recorded files' own string values (all synthetic) never appear in any
   Places table (the sentinel scan, with the sentinels computed from the recorded files).
3. README: a "Recorded (anonymized) — D-04" subsection with the recording date, request count and the ledger note.
4. Venue prep per Task 1:
   - `prod-with-spine`: separate read-only pre-flight (`select count(*) from businesses` = 0), then the Phase 3 procedure from
     `docs/runbooks/ingest.md` with `--target=prod` (ingest Comptroller, ingest Overture, resolve), recording counts; post-flight counts.
   - `local`: set `PLACES_MODE=enterprise` in `.env.local`, `$PNPM build && $PNPM start` (record the PID).
   - production venues: danlo sets `PLACES_MODE=enterprise` in Vercel Production (or approves Claude doing it in Task 1's reply) and Claude redeploys;
     the build log names the sha; `vercel env ls production` shows `PLACES_MODE`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "recorded fixtures replay through the tile search" && node -e "const r=require('./tests/unit/msw/fixtures/places-recordings.json');const rec=Object.values(r.files).filter(f=>f.anonymized===true);if(rec.length<1)process.exit(1);console.log('anonymized',rec.length)"</automated>
  </verify>
  <acceptance_criteria>
    - At least one `places-recorded-*.json` exists with a sidecar entry `anonymized: true`; total recording requests ≤ 6 (SUMMARY lists them)
    - The replay test passes by name
    - Venue prep recorded (ingest counts, or the local PID, or the env listing + build sha)
  </acceptance_criteria>
  <done>Real-structure fixtures exist without Google text; the venue is ready.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: danlo starts the run and watches the report</name>
  <files>docs/measurements/04-first-run.md</files>
  <read_first>
    - The chosen venue's URL
  </read_first>
  <what-built>The executor, meter, report, review and business screens, live on the chosen venue with `PLACES_MODE=enterprise`.</what-built>
  <how-to-verify>
1. On your phone, open the McAllen × home services preset (create it if needed: McAllen, Home services & trades).
2. Check the three actions: "Run full sweep" is the accent; the cost line shows ~54 requests and $0.00.
3. Tap "Run full sweep" → "Reserve budget & start the sweep". You land on the run report.
4. Watch it: the badge moves Queued → Running; counts update every ~5 s without flicker; it ends Complete or Partial.
5. If Partial: read the stop alert — `google_daily_quota` is expected if the run passed 100 requests today.
6. If any tiles are still truncated, the warning names them.
7. Open /review (Google filter) and one attached business's detail: the "Google Maps" tag sits beside every Google-derived value.
  </how-to-verify>
  <acceptance_criteria>
    - danlo replied `approved` (or described issues), and named the post-run `PLACES_MODE` if it differs from Task 1
  </acceptance_criteria>
  <resume-signal>Type `approved` or describe issues</resume-signal>
  <action>Hand the numbered steps to danlo and wait; do not start the run on his behalf.</action>
  <verify>Human checkpoint — the reply is recorded verbatim in the SUMMARY; no automated command applies.</verify>
  <done>danlo's reply is recorded and the plan proceeds (or stops) exactly as it directs.</done>
</task>

<task type="auto">
  <name>Task 4: Verify the run from the database, measure, restore the mode, record</name>
  <files>docs/measurements/04-first-run.md, .planning/phases/04-places-transient-verifier/deferred-items.md</files>
  <read_first>
    - `src/server/queries/run-report.ts` (the numbers the page showed), `src/lib/places/tiling.ts` constants
  </read_first>
  <action>
In one read-only transaction on the venue's database, for the run: status/stopped_reason; `calls_count` vs `count(cost_ledger)` (must be equal)
and every ledger row's `sku` (`ts_enterprise`) and `units` (1); no reservation of the run left unsettled/unreleased; `run_searches` totals
(searched, saturated, subdivided, truncated by `truncated_why`); `count(*) filter (where pure_sab)` over the run's observations (criterion 4's SAB
evidence; 0 is a finding — record it against research A6); outcomes per cluster; host-class split; `min/max(expires_at - observed_at)` on the
run's coordinates (= 30 days); `/sources` transient figures via `app.places_transient_stats()`.
Write `docs/measurements/04-first-run.md`: venue, cell, date, duration, the numbers above, the recording's requests, daily-quota headroom,
and recommendations — change a tiling constant (with the observed truncation pattern) or raise the quota (D-19's deferred decision) — as
PROPOSALS for danlo, never applied here. Restore `PLACES_MODE` per danlo's choice (Vercel env edit + redeploy + sha check; if `local`, restore
`.env.local` and stop exactly the recorded PID). Append to `deferred-items.md`: the banner-spec decision, any constant proposals, research
assumptions A1/A2 ("verify per-page billing and error-response billing on the first invoice"), and A6's measured outcome.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && grep -c "pure_sab\|calls_count\|truncated\|ts_enterprise" docs/measurements/04-first-run.md</automated>
  </verify>
  <acceptance_criteria>
    - `calls_count` equals the run's ledger row count, recorded; no open reservation remains for the run
    - The SAB count, truncation counts and per-cluster unmatched counts are recorded with the query that produced them
    - The post-run `PLACES_MODE` matches danlo's choice (env listing + deployed sha recorded)
  </acceptance_criteria>
  <done>D-04 is done: real density measured, fixtures recorded, the switch where danlo wants it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| production app → places.googleapis.com | the first billed-SKU calls (inside the free allowance) |
| desk → production database | the optional spine ingest |
| real payload → git | the recorder |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Denial of service (financial) | the run | mitigate | one cell (54 high, ceiling 108), inside the free 1,000; meter + ceiling + 100/day quota; daily-quota stop is expected and non-retried |
| T-4-05 | Information disclosure (legal) | recorded fixtures | mitigate | in-memory anonymization, manual inspection before commit, replay sentinel test, sidecar `anonymized: true` |
| T-4-03 | Elevation / Tampering | mode flip | mitigate | danlo chooses the mode before and after; redeploy + sha check; runbook's cancel procedure available |
| T-4-16 | Tampering | production ingest (optional) | mitigate | Phase 3's own desk procedure with its pre-flight and `--target=prod` gate, only on danlo's explicit option |
</threat_model>

<verification>
- Ledger ↔ calls equality, SAB count, truncation counts, coordinate TTL, and the mode restoration recorded; replay test green
</verification>

<success_criteria>
- The verifier has run for real on RGV density, measured and recorded, with fixtures that carry no Google text
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-32-SUMMARY.md`
</output>
