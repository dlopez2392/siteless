---
phase: 04-places-transient-verifier
plan: 14
type: execute
wave: 3
depends_on: [04-07, 04-09]
files_modified:
  - src/components/runs/run-status-badge.tsx
  - src/components/runs/run-auto-refresh.tsx
  - src/components/spend/by-run.tsx
  - src/server/queries/budget.ts
  - src/components/app-shell/app-sidebar.tsx
  - tests/unit/run-auto-refresh.test.tsx
  - tests/unit/run-chrome.test.tsx
autonomous: true
requirements: [PLACE-03]

must_haves:
  truths:
    - "One RunStatusBadge (lifted from /spend, sized with FLAG_BADGE_SIZING) is used by /spend, recent runs and the run report (UI-SPEC § Reused, Rule 41)"
    - "/spend By-run renders a stopped reason through STOPPED_REASON, never the stored key, and links each run to /runs/{id} via spend-run-link-{runId} (Rule 35 leak fix; UI-SPEC § 0 route 3)"
    - "RunAutoRefresh polls router.refresh() every 5000 ms only while queued/running, pauses when hidden, refreshes immediately on return, and leaves zero timers after a terminal status or unmount (D-17, Rule 36)"
    - "A refresh that does not land within 10 s keeps the previous numbers and shows the refresh-failed line; numbers never blank (Rule 11)"
    - "The polite live region announces only transitions (status changes, first truncation) — never the 5-second counts"
    - "/runs/[id] lights the Presets nav item via alsoActiveUnder: ['/runs']"
  artifacts:
    - path: "src/components/runs/run-auto-refresh.tsx"
      exports: ["RunAutoRefresh"]
    - path: "src/components/runs/run-status-badge.tsx"
      exports: ["RunStatusBadge"]
  key_links:
    - from: "src/components/spend/by-run.tsx"
      to: "src/lib/ui/copy.ts STOPPED_REASON"
      via: "stoppedReasonOf() maps the key to a sentence"
      pattern: "STOPPED_REASON\\["
    - from: "src/components/runs/run-auto-refresh.tsx"
      to: "next/navigation router.refresh"
      via: "setTimeout loop with visibilitychange pause"
      pattern: "router.refresh"
---

<objective>
The shared run chrome the run report, preset detail and /spend all use: the extracted status badge,
the live-refresh island (D-17), the /spend stopped-reason leak fix and run links, and the nav rule
that lights Presets on /runs.

Purpose: UI-SPEC Rules 35, 36, 41 and § 0. Built before the pages so three later plans import one
implementation instead of three.

Output: two new components, three edited modules, two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. No scratch `.ts` in the repo.
</pinned_launcher>

<interfaces>
`src/components/spend/by-run.tsx` L43–75 (`TONE_CLASS`, `TONE_VARIANT`, `RunStatusBadge` with `'text-xs font-semibold'`), L104–106
(`stoppedReasonOf` returns the raw key — the leak), L28–31 (desk Table / phone ItemGroup with distinct testids).
`src/components/flags/flag-badge.tsx` L17: `FLAG_BADGE_SIZING = 'h-auto px-2 py-1 text-sm font-semibold tabular-nums'`.
`src/components/app-shell/app-sidebar.tsx` L45–51 (`NavItem`), L136–138 (`isNavActive(pathname, base)`), call sites L177, L195 and the mobile tab bar.
`src/components/preset-editor/use-live-estimate.ts` L71–113 (the repo's one timer: `useEffect` + `setTimeout` + `clearTimeout` cleanup).
`src/components/preset-detail/run-drawer.tsx` L164 (`router.refresh()` usage).
From 04-07: `RUN_KIND_LABEL`, `StoppedReason`, `STOPPED_REASONS` (run-tone.ts); `STOPPED_REASON`, `RUN_REPORT_LIVE_LINE(time)`,
`RUN_REPORT_REFRESH_NOW`, `RUN_REPORT_REFRESHING`, `RUN_REPORT_REFRESH_FAILED(time, lastTime)`, live-announcement strings (copy.ts).
From 04-09: `runs.kind`, `runs.stopped_reason`.
`src/server/queries/budget.ts` L314–363 `readSpendByRun` / `getSpendByRun` → `RunSpend`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract RunStatusBadge, fix the /spend leak, add run links and kind, light Presets on /runs</name>
  <files>src/components/runs/run-status-badge.tsx, src/components/spend/by-run.tsx, src/server/queries/budget.ts, src/components/app-shell/app-sidebar.tsx, tests/unit/run-chrome.test.tsx</files>
  <read_first>
    - `src/components/spend/by-run.tsx` (whole file), `src/server/queries/budget.ts` L240–365, `src/components/app-shell/app-sidebar.tsx` (whole file)
    - any existing test that renders ByRun or calls `isNavActive` (`grep -rln "isNavActive\|ByRun\|by-run" tests/unit`)
  </read_first>
  <behavior>
    - "the run status badge is one component sized as a flag badge": `RunStatusBadge({ status: 'partial' })` has `data-status="partial"`, the label `RUN_LABEL.partial`, and class includes `text-sm` and `font-semibold`, not `text-xs`; `running` renders a `Spinner` with class `motion-reduce:animate-none`
    - "the spend view renders stopped reasons as sentences": a partial run with `stoppedReason: 'exceeded_estimate'` renders `STOPPED_REASON.exceeded_estimate` and the DOM text contains no `exceeded_estimate`
    - "the spend view links each run to its report": `spend-run-link-{runId}` has `href="/runs/{runId}"` (desk: the name cell; phone: the card) — exactly one element per testid per viewport tree
    - "the run report lights Presets": `isNavActive('/runs/0f…', '/presets', ['/runs'])` is true; `isNavActive('/runsx', '/presets', ['/runs'])` is false
  </behavior>
  <action>
1. Lift `TONE_CLASS`, `TONE_VARIANT`, `RunStatusBadge` into `src/components/runs/run-status-badge.tsx` (server-safe,
   no client directive), replacing `'text-xs font-semibold'` with `FLAG_BADGE_SIZING`, adding
   `data-testid` (default `run-status-badge`) and `data-status`, and a 16px `Spinner` with
   `motion-reduce:animate-none` inside the `running` badge. `by-run.tsx` imports it.
2. `stoppedReasonOf` → `STOPPED_REASON[run.stoppedReason as StoppedReason] ?? null` (unknown keys render
   nothing rather than the raw key).
3. `readSpendByRun` selects `r.kind` and types `stoppedReason` as `StoppedReason | null`; `RunSpend`
   gains `kind: RunKind`. The run name cell (desk) and card (phone) become `next/link` to `/runs/{id}` with
   `data-testid={`spend-run-link-${run.id}`}` on exactly one element per layout.
4. `NavItem` gains `alsoActiveUnder?: readonly string[]`; `isNavActive(pathname, base, also?: readonly string[])`
   returns true for `base` or any `also` prefix (same `=== || startsWith(x + '/')` rule); the Presets entry of
   `LEADS_NAV` sets `alsoActiveUnder: ['/runs']`; every call site passes `item.alsoActiveUnder`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the run status badge is one component" && $PNPM test:unit -t "the spend view renders stopped reasons as sentences" && $PNPM test:unit -t "the spend view links each run to its report" && $PNPM test:unit -t "the run report lights Presets" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function RunStatusBadge" src/components/spend/by-run.tsx` returns nothing (lifted, not forked)
    - `grep -n "text-xs" src/components/runs/run-status-badge.tsx src/components/spend/by-run.tsx` returns nothing
    - `grep -n "alsoActiveUnder: \['/runs'\]" src/components/app-shell/app-sidebar.tsx` matches
    - Existing `/spend` and nav tests stay green
  </acceptance_criteria>
  <done>One badge, no raw stopped keys on /spend, runs are links, /runs lights Presets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RunAutoRefresh — the live refresh island with its fake-timer proofs</name>
  <files>src/components/runs/run-auto-refresh.tsx, tests/unit/run-auto-refresh.test.tsx</files>
  <read_first>
    - `src/components/preset-editor/use-live-estimate.ts` L71–113
    - 04-UI-SPEC.md § Screen 1 "Live refresh contract", § Accessibility "run report's live region", Rules 11 and 36
    - `tests/unit/stale-estimate.test.tsx` (fake timers + `next/navigation` mock in the dom lane)
  </read_first>
  <behavior>
    Props: `{ status: RunStatus; renderedAtMs: number; truncatedCount: number }` (the server passes `Date.now()` at render as `renderedAtMs`).
    - "the live refresh polls every 5 seconds while running": status `running` → `router.refresh` called at 5000 ms, again at 10000 ms
    - "the live refresh stops on a terminal status": re-render with `complete` → no further calls, and `vi.getTimerCount() === 0`
    - "the live refresh pauses while hidden and refreshes on return": `visibilityState` `hidden` → no calls past 20 s; back to `visible` → one immediate call, then the 5 s cadence resumes
    - "the live refresh clears its timer on unmount": unmount → `vi.getTimerCount() === 0`
    - "a refresh that does not land within 10 seconds shows the refresh-failed line": `renderedAtMs` unchanged 10 s after a refresh → `run-updated-at` shows `RUN_REPORT_REFRESH_FAILED(...)`; a new `renderedAtMs` clears it
    - "refresh now refreshes immediately and resets the cadence": click `run-refresh-now` at 3 s → call at 3 s, next at 8 s; while waiting, the button shows `RUN_REPORT_REFRESHING` at the same width
    - "the live region announces only transitions": `run-live-status` text changes on `queued → running` and on `truncatedCount 0 → 3`, and does NOT change when only `renderedAtMs` changes
  </behavior>
  <action>
`'use client'` island; no data of its own. Renders, for `queued|running` only: the
`run-updated-at` line (`RUN_REPORT_LIVE_LINE(formatLocal(renderedAt, { timeStyle: 'medium' }))` or the
failed line) and the outline `run-refresh-now` button (h-11); always renders the visually hidden
`role="status" aria-live="polite" aria-atomic="true"` `run-live-status` region. Timer: one `setTimeout`
chain (never `setInterval`), cleared on terminal status, on hidden, and on unmount; a 10 s watchdog
timer started at each refresh, cleared when `renderedAtMs` changes. Use `useRouter()` from `next/navigation`.
Constants `REFRESH_MS = 5000`, `REFRESH_TIMEOUT_MS = 10_000` exported for the test.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the live refresh" && $PNPM test:unit -t "refresh now refreshes immediately" && $PNPM test:unit -t "the live region announces only transitions" && $PNPM typecheck && $PNPM lint</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all seven tests
    - `grep -n "setInterval" src/components/runs/run-auto-refresh.tsx` returns nothing
    - `grep -n "visibilitychange" src/components/runs/run-auto-refresh.tsx` matches
  </acceptance_criteria>
  <done>The live refresh obeys Rule 36 and never blanks a number.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB machine keys → rendered text | `stopped_reason`, `kind`, `status` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05 | Information disclosure | `/spend` stopped reason | mitigate | mapped through `STOPPED_REASON`; unknown key renders nothing (Rule 35) |
| T-4-02 | Denial of service | refresh polling | mitigate | 5 s cadence only while queued/running, paused when hidden, stopped on terminal; one RSC re-render per tick |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint` green
</verification>

<success_criteria>
- Shared run chrome exists once with its timer contract proven
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-14-SUMMARY.md`
</output>
