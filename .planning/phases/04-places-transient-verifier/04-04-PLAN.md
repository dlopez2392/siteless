---
phase: 04-places-transient-verifier
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/places/place-types.ts
  - src/seed/data/clusters.json
  - src/lib/estimate/assumptions.ts
  - src/lib/estimate/estimate.ts
  - src/lib/estimate/expand-cells.ts
  - src/server/queries/presets.ts
  - tests/unit/place-types.test.ts
  - tests/unit/estimate.test.ts
  - tests/unit/outlet-counts.test.ts
autonomous: true
requirements: [PLACE-01, PLACE-04]

must_haves:
  truths:
    - "Every placesTypes entry in clusters.json is a Table A type in a committed snapshot of Google's type table; general_contractor (Table B only) is removed, watched red first (M48)"
    - "The estimate is type-aware: requestsLo = Σ types × PAGES_LO and requestsHi = Σ types × PAGES_HI × FAN_OUT over the cells (D-18)"
    - "The RGV baseline re-pins at 442 / 3,978 requests and a full RGV sweep exceeds the $50 cap at admission, while McAllen × home services fits (D-18)"
    - "RUN_CEILING_MULTIPLIER = 2 is a committed constant beside FAN_OUT; the ceiling is ceil(2 × requestsHi) requests, never dollars (D-15, D-18)"
    - "The estimator can price a subset of cells (a partition) through an optional cell filter"
  artifacts:
    - path: "src/lib/places/place-types.ts"
      provides: "Table A snapshot (478 types) with source URL + fetch date, isTableAType()"
      exports: ["PLACES_TABLE_A", "isTableAType"]
    - path: "src/lib/estimate/assumptions.ts"
      provides: "RUN_CEILING_MULTIPLIER and the stale-run minutes"
      contains: "RUN_CEILING_MULTIPLIER"
    - path: "tests/unit/place-types.test.ts"
      contains: "every placesTypes entry is a Table A type"
  key_links:
    - from: "src/lib/estimate/estimate.ts"
      to: "src/seed/data/clusters.json"
      via: "SeedTables.clusters → placesTypes.length per cell"
      pattern: "placesTypes"
    - from: "src/lib/estimate/expand-cells.ts"
      to: "04-13 plan-run / 04-03 partition"
      via: "exported cellKey(clusterKey, unitId) using the U+0000 separator"
      pattern: "export function cellKey"
---

<objective>
Make the cost model honest about Places types (D-18) and make the seeded type list valid before
anything can send it (Claude's Discretion: "validate every placesTypes entry … before the first
paid call").

Purpose: FAN_OUT was per cell while each cluster searches 6–7 types, so D-15's 2× ceiling would
stop even the D-04 run (04-RESEARCH Pitfall 2). danlo accepted the consequence in D-18: the RGV
baseline moves from 612 to ~3,978 requests and a full RGV sweep is refused at admission.

Output: the Table A snapshot, a corrected `clusters.json`, a type-aware estimator, the ceiling
constant, re-pinned cost-model tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. No scratch `.ts` in the repo.
</pinned_launcher>

<interfaces>
`src/lib/estimate/expand-cells.ts` (current):
```ts
export type SeedTables = { cities: CitiesFile; counties: CountiesFile; outletCounts: OutletCountsFile; geoPresets: GeoPresetsFile };
export type Cell = { clusterKey: ClusterKey; unitKind: UnitKind; unitId: string; expectedOutlets: number; expectedOutletsExact: number };
const SEP = '\u0000';               // private today
export function expandCells(spec: PresetSpec, seed: SeedTables): Cell[];
```
`src/lib/estimate/estimate.ts` (current, L73–88):
```ts
const requestsLo = Math.ceil(cellCount * PAGES_LO * FAN_OUT);
const requestsHi = Math.ceil(cellCount * PAGES_HI * FAN_OUT);
const free = freeRemaining(ESTIMATE_SKU, ctx.unitsUsedThisPeriod);
const costMicroUsdLo = priceRequests(ESTIMATE_SKU, requestsLo, free).microUsd;
```
`src/seed/types.ts`: `ClusterSeed { key; displayName; sortOrder; naicsRanges; placesTypes: string[]; placesTypesNote: string }`, `ClustersFile { source; measuredAt; clusters: ClusterSeed[] }`.
`src/server/queries/presets.ts` L74: `const SEED: SeedTables = { cities, counties, outletCounts, geoPresets };`
`tests/unit/estimate.test.ts` L64: the same SEED literal; L94/L160/L175 pin `612` and `9144 / 612`.
`tests/unit/outlet-counts.test.ts` L144–147: every cluster has ≥1 placesType and its note contains `'Phase 4 validates every type'`.
`PRICE_BOOK.ts_enterprise`: 35,000 µUSD/request, 1,000 free/month. Cap default 50,000,000 µUSD.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Table A snapshot, the named validity test (red on general_contractor), and the corrected seed</name>
  <files>src/lib/places/place-types.ts, tests/unit/place-types.test.ts, src/seed/data/clusters.json, tests/unit/outlet-counts.test.ts</files>
  <read_first>
    - `src/lib/budget/price-book.ts` L1–18 (header: a published external table committed as data, with source URL and fetch date)
    - `src/seed/data/clusters.json` (whole file)
    - `tests/unit/outlet-counts.test.ts` L130–150
    - 04-RESEARCH.md § Validated place types
  </read_first>
  <behavior>
    - "every placesTypes entry is a Table A type": for every cluster, every `placesTypes` entry satisfies `isTableAType` — RED while `general_contractor` is present, naming it
    - "the Table A snapshot is the published table": `PLACES_TABLE_A.length === 478`, contains `plumber`, `roofing_contractor`, `hair_salon`, `car_repair`, and does NOT contain `general_contractor`
  </behavior>
  <action>
1. Build `src/lib/places/place-types.ts`: fetch the raw HTML of
   `https://developers.google.com/maps/documentation/places/web-service/place-types` at the desk
   (curl into the session scratchpad, never into the repo), parse Table A's type names, and commit
   them as `export const PLACES_TABLE_A: ReadonlySet<string> = new Set([...])` (sorted, one per line)
   plus `export function isTableAType(t: string): boolean`. Header: source URL, the page's "Last
   updated" date, the fetch date, "Table A = 478, Table B = 36 (04-RESEARCH, raw HTML parse)", and
   "Table A values may be used as `includedType`; Table B values may NOT be used in a request."
   🔴 The URL goes in a comment only — `tests/unit/no-network.test.ts` fails a live host in `src/`.
   If the parsed count is not 478, STOP and report the count (the page changed; research is stale).
2. Write `tests/unit/place-types.test.ts` with the two tests above (import `clusters.json` the way
   `outlet-counts.test.ts` does). Run — the first test must go RED naming `general_contractor`.
   Quote the red output in the SUMMARY (M48's target).
3. Edit `clusters.json`: remove `"general_contractor"` from `home_services`. Replace every cluster's
   `placesTypesNote` with: "Validated against the Places API (New) Table A snapshot in src/lib/places/place-types.ts (2026-09-23). general_contractor was removed: it is Table B only and cannot be sent as includedType. Phase 4 validates every type before any paid call; tests/unit/place-types.test.ts enforces it."
4. `tests/unit/outlet-counts.test.ts` L147: change the expected substring to `'Phase 4 validates every type'`
   only if the new note no longer contains it — the note above keeps it, so the test should stay
   green unchanged. Confirm by running it.
5. Do NOT add the optional Table A extras (`beauty_salon`, `massage`, …) — danlo did not ask for them.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "every placesTypes entry is a Table A type" && $PNPM test:unit -t "the Table A snapshot is the published table" && $PNPM test:unit tests/unit/outlet-counts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "general_contractor" src/seed/data/clusters.json` returns 0
    - `grep -n "export function isTableAType" src/lib/places/place-types.ts` matches
    - `grep -n "https://" src/lib/places/place-types.ts` matches only on comment lines
    - The SUMMARY quotes the red run naming `general_contractor`
  </acceptance_criteria>
  <done>The seeded types are provably sendable; 26 types remain (6 + 6 + 7 + 7).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type-aware estimate, the ceiling constant, cellKey export, and the re-pinned cost model</name>
  <files>src/lib/estimate/assumptions.ts, src/lib/estimate/estimate.ts, src/lib/estimate/expand-cells.ts, src/server/queries/presets.ts, tests/unit/estimate.test.ts</files>
  <read_first>
    - `src/lib/estimate/assumptions.ts` (whole file — every constant is named, documented, "UNMEASURED BY DESIGN")
    - `src/lib/estimate/estimate.ts` (whole file)
    - `src/lib/estimate/expand-cells.ts` L1–80 and L180–260
    - `tests/unit/estimate.test.ts` (whole file — L84–185 are the pinned numbers)
    - `src/server/queries/presets.ts` L60–80
  </read_first>
  <behavior>
    Re-pinned tests (keep their names; values change):
    - `cost model: the RGV baseline priced over the real seeded cell list` (free exhausted): `cells` 68, **`typeSearches` 442**, `requestsLo` **442**, `requestsHi` **3978**, `costMicroUsdLo` **15_470_000**, `costMicroUsdHi` **139_230_000**, and `costMicroUsdHi` is now **greater than** `CAP` (comment: "D-18: a full RGV sweep is refused at admission; a partition or one city × one cluster fits").
    - `cost model: the RGV county baseline`: `cells` 16, `requestsHi` **936** (4 × 26 × 9), `costMicroUsdHi` **32_760_000**.
    - `cost model: Texas exceeds the cap and says so`: `cells` 1016, `requestsHi` **59_436** (254 × 26 × 9), `costMicroUsdHi` **2_045_260_000** ((59_436 − 1_000) × 35_000).
    - `free allowance: 68 requests`: the direct-pricing half unchanged; the estimator half becomes early-month RGV baseline `requestsHi` 3978, `freeRemaining` 1000, `costMicroUsdHi` **104_230_000** ((3978 − 1000) × 35_000), and free-exhausted **139_230_000**.
    - `texas multiplier is computed, not a constant`: `m === 59436 / 3978` and `Number(m.toFixed(1)) === 14.9`.
    New tests:
    - `cost model: McAllen × home services fits the free allowance`: 1 cell, `typeSearches` 6, `requestsHi` 54, early-month `costMicroUsdHi` 0; `Math.ceil(RUN_CEILING_MULTIPLIER * 54) === 108`.
    - `the estimate can price a subset of cells`: the RGV baseline with `onlyCells: (c) => c.clusterKey === 'home_services'` gives `cells` 17 and `requestsHi` 17 × 6 × 9 = 918.
    - `cellKey joins cluster and unit with U+0000`: `cellKey('home_services', '48215\u0000McAllen') === 'home_services\u000048215\u0000McAllen'`.
    🔴 Every number above is the planner's arithmetic from the seeded files. Run the tests; if any
    value differs, the SEED is the truth — re-derive it by hand in the SUMMARY (show the sum) before
    committing a different number. Never adjust a number to make a test pass without that derivation.
  </behavior>
  <action>
1. `assumptions.ts` gains, in the file's documented style:
```ts
/**
 * D-15 / D-18. A run may issue at most this many times its estimate-high REQUESTS before it stops
 * as `partial` with stopped_reason `exceeded_estimate`. Requests, never dollars: inside the free
 * allowance estimate-high is $0.00, and 2 × $0 would stop a run on its first paid page.
 * Stored per run as runs.ceiling_requests = ceil(RUN_CEILING_MULTIPLIER × requestsHi).
 */
export const RUN_CEILING_MULTIPLIER = 2;
/** A `queued` run the workflow never picked up is failed as `never_started` after this long. */
export const RUN_NEVER_STARTED_AFTER_MINUTES = 15;
/** A `running` run whose heartbeat is this stale is failed as `abandoned`. */
export const RUN_ABANDONED_AFTER_MINUTES = 30;
```
   Re-document `FAN_OUT`: "tiles per (cell × Places type) — D-18 made the estimate type-aware; still UNMEASURED BY DESIGN, trued up on the D-04 run and the Phase 6 invoice."
2. `expand-cells.ts`: `SeedTables` gains `clusters: ClustersFile`. Export
   `export const CELL_KEY_SEP = '\u0000';` (keep `SEP` as an alias of it) and
   `export function cellKey(clusterKey: string, unitId: string): string { return clusterKey + CELL_KEY_SEP + unitId; }`,
   and `export function placesTypesFor(clusterKey: string, seed: SeedTables): readonly string[]` which
   throws naming the key when the cluster is unknown.
3. `estimate.ts`: `estimatePreset(spec, ctx, opts?: { onlyCells?: (c: Cell) => boolean })`. Filter the
   cells first. `typeSearches = Σ placesTypesFor(cell.clusterKey).length`.
   `requestsLo = Math.ceil(typeSearches * PAGES_LO)`, `requestsHi = Math.ceil(typeSearches * PAGES_HI * FAN_OUT)`.
   Add `typeSearches` to `EstimateRange`. Everything else (free allowance, pct) unchanged.
4. `presets.ts`: import `clusters.json` and add it to `SEED`. `tests/unit/estimate.test.ts`: add
   `clusters` to its SEED literal, then re-pin every number per the behavior block. Run red first
   (the old 612 assertions fail), record in SUMMARY, then re-pin.
5. Grep `tests/` and `src/` for any other `SeedTables` literal (`grep -rn "SeedTables = {" src tests`)
   and add `clusters` there too.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "cost model" && $PNPM test:unit -t "the estimate can price a subset of cells" && $PNPM test:unit -t "cellKey joins cluster and unit" && $PNPM test:unit -t "texas multiplier" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const RUN_CEILING_MULTIPLIER = 2" src/lib/estimate/assumptions.ts` matches
    - `grep -n "typeSearches" src/lib/estimate/estimate.ts` matches
    - `grep -n "3978\|3_978" tests/unit/estimate.test.ts` matches and `grep -n "612" tests/unit/estimate.test.ts` returns nothing
    - `grep -n "export function cellKey" src/lib/estimate/expand-cells.ts` matches
    - PASS list names `cost model: McAllen × home services fits the free allowance`
  </acceptance_criteria>
  <done>The estimate counts types, the ceiling constant exists, and every displayed Phase 2 number now derives from 26 real types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed JSON → Places request | a type string becomes `includedType`; an invalid one is a 400 at best |
| estimate → admission hold | the hold and the ceiling derive from this arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Denial of service (financial) | estimate under-count | mitigate | type-aware formula with `Math.ceil`; committed cost-model test with exact numbers; ceiling = ceil(2 × requestsHi) |
| T-4-10 | Tampering | `includedType` values | mitigate | only Table A snapshot values are sendable; named test M48; 04-12's builder also refuses a non-Table-A type at build time |
| T-4-12 | Tampering | ceiling constant | accept | a committed constant; changing it reds the named test and needs review |
</threat_model>

<verification>
- `$PNPM test:unit` green; `$PNPM typecheck`, `$PNPM lint` green
- SUMMARY records the red runs (general_contractor; the old 612 pins)
- 🔴 Flag for danlo in the SUMMARY: the preset screens now show the D-18 numbers (RGV baseline ~$104 early-month), and "Run full sweep" on the RGV baseline will be refused at admission — that is D-18's accepted consequence, not a regression
</verification>

<success_criteria>
- 26 valid types; a type-aware estimate; RUN_CEILING_MULTIPLIER committed beside FAN_OUT
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-04-SUMMARY.md`
</output>
