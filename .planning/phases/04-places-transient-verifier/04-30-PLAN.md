---
phase: 04-places-transient-verifier
plan: 30
type: execute
wave: 10
depends_on: [04-29]
files_modified:
  - docs/deploy.md
  - .planning/phases/04-places-transient-verifier/deferred-items.md
autonomous: false
requirements: [PLACE-01, PLACE-02, PLACE-03, PLACE-04, PLACE-05, PLACE-06]

must_haves:
  truths:
    - "Migrations 0026–0029 are applied to production exactly once, after a separate read-only pre-flight and danlo's explicit approval; production's journal goes from 26 rows to 30 and a second migrate is a verified no-op"
    - "Production's catalog proves the retention barrier and tenancy: authenticated holds SELECT only on the seven Places tables and nothing on place_coordinates; the purge is executable by siteless_cron only; every new app.* writer is a definer; the signal view is security_invoker; runs_one_active_per_org exists"
    - "The app is deployed with PLACES_MODE unset (= off) and CRON_SECRET set; the build log names the intended commit; /api/health is 200; the cron route refuses without the secret and purges with it (D-02, D-12)"
    - "The deployed e2e suite passes — including /sources' transient card and the rewritten spend spec — and no spec clicks a run confirm (Rule 39)"
  artifacts:
    - path: "docs/deploy.md"
      provides: "Phase 4 production notes: the four migrations, CRON_SECRET, PLACES_MODE default, the queued-runs precondition"
      contains: "CRON_SECRET"
  key_links:
    - from: "production journal"
      to: "drizzle/meta/_journal.json"
      via: "30 rows after, 26 before — matched by name, not version"
      pattern: "0029_places_writers"
---

<objective>
Apply the phase's schema to production behind a human gate, deploy with Places still switched off,
and prove the deployed app — the repo's established production procedure (03-21), extended with the
cron secret and a second-run no-op check.

Purpose: nothing Google-facing can be enabled (04-31) until production carries the retention barrier,
the meter functions and the executor. `PLACES_MODE` stays `off` through this plan.

Output: production migrated and deployed; `docs/deploy.md` updated; phase `deferred-items.md` started.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/deploy.md
@.planning/phases/03-free-data-spine-entity-resolution/03-21-PLAN.md
@.planning/CONVENTIONS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
🔴 `$PNPM db:migrate:prod` is the ONLY path to production (`scripts/db.ts --target=prod`, session pooler on 5432). Never Supabase MCP
`apply_migration`. Never re-run a mutating command for its output — read the catalog instead. Separate read-only pre-flight BEFORE any write.
🔴 Vercel team `team_8zjV46sJxQDsVzikNQa1JaO2`; production URL `https://siteless-iota.vercel.app` (03-21). Never assume a push deployed —
the build log naming the commit is the evidence.
</pinned_launcher>

<tasks>

<task type="auto">
  <name>Task 1: Read-only production pre-flight</name>
  <files>.planning/phases/04-places-transient-verifier/deferred-items.md</files>
  <read_first>
    - `docs/deploy.md` (whole), `scripts/db.ts`, `.planning/phases/03-free-data-spine-entity-resolution/03-21-SUMMARY.md` (the pre-flight format)
    - `drizzle/0026_*.sql` … `drizzle/0029_places_writers.sql`
  </read_first>
  <action>
In ONE `begin read only` transaction on production (owner credential from `.env.local`, never printed), record:
1. `select count(*) from drizzle.__drizzle_migrations` (expect 26) and the last applied hash/created_at;
2. `select org_id, status, count(*) from runs where status in ('queued','running') group by 1,2` — 0027 fails stale `queued` rows before creating the unique index; report how many will be touched;
3. `select rolname from pg_roles where rolname in ('siteless_cron','app_user')`;
4. `select count(*) from businesses` (Phase 3 left it 0 — context only);
5. `select term from industry_terms where kind = 'places_type' and term = 'general_contractor'` (the stale seed row — inert: nothing reads Places types from `industry_terms`);
6. `select version()` (expect PostgreSQL 17.x).
Start `.planning/phases/04-places-transient-verifier/deferred-items.md` with: the stale `general_contractor` industry_terms row (inert; a cleanup
migration is optional), and the still-deferred preset delete path / `presets.spec.ts` teardown (owed since Phase 2; not picked up in Phase 4).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && test -f .planning/phases/04-places-transient-verifier/deferred-items.md</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records all six pre-flight results with the transaction marked read-only
    - No write was issued (the transaction was `read only`)
  </acceptance_criteria>
  <done>danlo has the facts the approval needs.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Approve the production migration and deploy</name>
  <files>docs/deploy.md</files>
  <read_first>
    - Task 1's pre-flight results
  </read_first>
  <decision>Apply 0026–0029 to production and deploy the phase with PLACES_MODE off?</decision>
  <context>
- **0026_places_tables** — eight tables, the `runs` columns.
- **0027_places_grants_triggers** — fails stale `queued` runs as `never_started` (pre-flight count shown), creates `runs_one_active_per_org`,
  `businesses_latlng_idx`, SELECT-only grants, NO grant on `place_coordinates`, the append-only trigger, `siteless_cron`, the signal view.
- **0028_places_meter_retention** — release, planning/progress, purge (siteless_cron only), transient stats.
- **0029_places_writers** — the numeric-only features CHECK and the three writers.
Deploy: `CRON_SECRET` generated and set in Vercel Production; `PLACES_MODE` left unset (the app treats it as `off`); no Google key exists yet.
The daily cron starts purging (there is nothing to purge). Nothing can call Google after this deploy.
  </context>
  <options>
    <option id="apply">
      <name>Apply 0026–0029 and deploy</name>
      <pros>Production gains the retention barrier, meter functions and executor; unblocks 04-31</pros>
      <cons>Grants and the role are irreversible without a follow-up migration</cons>
    </option>
    <option id="hold">
      <name>Hold</name>
      <pros>Nothing changes</pros>
      <cons>04-31 and 04-32 cannot run; the phase cannot close</cons>
    </option>
  </options>
  <acceptance_criteria>
    - danlo answered `apply` or `hold`
  </acceptance_criteria>
  <resume-signal>Reply `apply` or `hold`</resume-signal>
  <action>Present Task 1's pre-flight results and the four migrations; wait for apply or hold. No write happens before the reply.</action>
  <verify>Human checkpoint — the reply is recorded verbatim in the SUMMARY; no automated command applies.</verify>
  <done>danlo's reply is recorded and the plan proceeds (or stops) exactly as it directs.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate once, verify from the catalog, prove the no-op, set env, deploy, smoke, deployed e2e</name>
  <files>docs/deploy.md</files>
  <read_first>
    - `docs/deploy.md`, `tests/e2e/_required-env.ts`
  </read_first>
  <action>
Only after `apply`.
1. `$PNPM db:migrate:prod` — once. Record the applied list.
2. Post-flight (catalog, not "the script ran"): journal = 30 rows ending `0029_places_writers`; `information_schema.tables` has the eight tables;
   `role_table_grants` for `authenticated`: `SELECT` only on the seven, nothing on `place_coordinates`; `has_column_privilege('authenticated','runs','ceiling_requests','UPDATE') = false`;
   `pg_proc` for `release_reservation, plan_run_searches, mark_run_search, purge_expired_place_coordinates, places_transient_stats, record_places_page,
   record_change_check, decide_place_attachment` all `prosecdef = t`; `has_function_privilege('authenticated','app.purge_expired_place_coordinates(text)','EXECUTE') = false`
   and `…('siteless_cron',…) = true`; `pg_class.reloptions` of `business_place_signal` contains `security_invoker=true`; `pg_indexes` has
   `runs_one_active_per_org`; `pg_constraint` has `pa_features_numeric` and `pc_expiry_within_30_days`.
3. Second-run no-op: `$PNPM db:migrate:prod` again → nothing applied; the journal count is still 30. (This is the orchestrator-required
   idempotency proof, read from the catalog after it.)
4. Env: generate `CRON_SECRET` with `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` piped straight into
   `vercel env add CRON_SECRET production` (never echoed into the transcript or a file); confirm `vercel env ls production` lists `CRON_SECRET` and does
   NOT list `PLACES_MODE` or `GOOGLE_PLACES_API_KEY`. Record the project's Fluid compute setting (research A10).
5. Deploy (`vercel deploy --prod --yes --scope team_8zjV46sJxQDsVzikNQa1JaO2` from the branch, as 03-21). Read the build log: it must name the
   intended commit sha. Smoke: `/api/health` 200; `curl -s -o /dev/null -w '%{http_code}' https://siteless-iota.vercel.app/api/cron/purge-places` → 401;
   the same with `Authorization: Bearer <secret>` (read into a shell variable from `vercel env pull` into a scratch file outside the repo, then deleted) → 200 with `{"ok":true,…}`.
6. Deployed e2e: `E2E_BASE_URL=https://siteless-iota.vercel.app $PNPM test:e2e` (the full suite; `preset-detail` and `runs` self-skip as non-local;
   `budget-banner` keeps its self-skips). Confirm `sources.spec.ts` asserts the transient card and `spend.spec.ts` never clicks a run.
7. `docs/deploy.md` gains a Phase 4 section: the four migrations, the queued-runs precondition 0027 handles, `CRON_SECRET` (Production, ≥16 chars,
   how to rotate), `PLACES_MODE` defaults off and how it is changed (link `docs/runbooks/places.md`), the Fluid compute note.
🔴 Any failure: stop and report. A half-applied set is fixed by a NEW migration, never a hand edit.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && E2E_BASE_URL=https://siteless-iota.vercel.app $PNPM test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - Production journal = 30 rows ending `0029_places_writers`, before and after the second migrate
    - Every catalog check in step 2 recorded with its result
    - The Vercel build log names the intended sha; `/api/health` 200; cron route 401 without the secret, 200 with it
    - `vercel env ls production` shows `CRON_SECRET` and neither `PLACES_MODE` nor `GOOGLE_PLACES_API_KEY`
    - The deployed e2e run passes and names `sources` and `spend` tests in its PASS list
  </acceptance_criteria>
  <done>Production carries the phase, verified from the catalog, deployed with Places off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → production database | four migrations cross once, behind a human gate |
| operator → Vercel env | the cron secret is created and stored |
| CI/e2e → production | the suite authenticates as a real user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-16 | Tampering | `db:migrate:prod` | mitigate | read-only pre-flight; human approval; `scripts/db.ts` gate; catalog post-flight; second-run no-op proven from the catalog |
| T-4-08 | Spoofing | cron secret | mitigate | generated with CSPRNG, piped into Vercel without echo; smoke proves 401 without it |
| T-4-01 | Information disclosure | secrets in transcripts | mitigate | values never printed; scratch pull file outside the repo, deleted |
| T-4-02 | Denial of service (financial) | premature calls | mitigate | `PLACES_MODE` unset (= off) and no key exists; verified via `vercel env ls` |
| T-4-09 | Denial of service (financial) | deployed e2e | mitigate | no spec clicks a run confirm (04-26/04-27) |
</threat_model>

<verification>
- Catalog checks, env listing, build-log sha, smoke results and deployed e2e PASS names recorded in the SUMMARY
</verification>

<success_criteria>
- Production and local carry the identical Phase 4 schema; the app is live with Places off and the purge scheduled
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-30-SUMMARY.md`
</output>
