---
phase: 04-places-transient-verifier
plan: 26
type: execute
wave: 6
depends_on: [04-04, 04-07, 04-13, 04-14, 04-22]
files_modified:
  - src/server/actions/queue-run.ts
  - src/components/preset-detail/run-drawer.tsx
  - src/lib/ui/copy.ts
  - src/app/(app)/presets/page.tsx
  - tests/unit/ui-maps.test.ts
  - tests/db/queue-run.test.ts
  - tests/e2e/preset-detail.spec.ts
  - tests/e2e/spend.spec.ts
autonomous: true
requirements: [PLACE-04, PLACE-01]

must_haves:
  truths:
    - "Phase 2's Run button now starts the workflow: queueRun takes kind full_sweep | partition | change_check, refuses in PLACES_MODE off (and non-check kinds in ids_only) BEFORE any reservation, and calls start(placesSweep) only after its transaction commits (D-02, D-14, D-16)"
    - "Admission is sized from the same planner the workflow uses: a type-aware estimate over exactly the run's cells, ceiling_requests = ceil(RUN_CEILING_MULTIPLIER × requestsHi) stored at insert, and a unit with no geometry is refused with a sentence (D-15, D-18)"
    - "One active run per org: a second run is a 'conflict' result naming the running one; stale queued/running rows are failed as never_started/abandoned first"
    - "If start() fails after commit, the hold is released and the run is failed never_started, so 'nothing was charged' is true"
    - "The drawer navigates to /runs/{id} on success (no toast) and shows mode-refused / already-running alerts; PHASE4_RUN_NOTICE is retired from all six places (Rule 38)"
    - "No e2e spec can ever click a run confirm against a deployed app (Rule 39); the confirmed-run assertion moved to a DB-lane test"
    - "After next build, the workflow manifest registers placesSweep (Pitfall 11)"
  artifacts:
    - path: "src/server/actions/queue-run.ts"
      contains: "start(placesSweep"
    - path: "tests/db/queue-run.test.ts"
      contains: "a confirmed run creates a runs row on the current version"
  key_links:
    - from: "src/server/actions/queue-run.ts"
      to: "src/workflows/places-sweep/workflow.ts placesSweep"
      via: "start() from workflow/api after the withOrg transaction commits"
      pattern: "from 'workflow/api'"
    - from: "src/components/preset-detail/run-drawer.tsx"
      to: "/runs/{runId}"
      via: "router.push on ok"
      pattern: "router.push"
---

<objective>
Wire the executor behind the Run button (D-14), with the mode refusal (D-02), the three run kinds
(D-16), the request ceiling (D-15/D-18) and the one-active-run rule; retire the Phase 2 notice and
make the e2e suite incapable of starting a billed run (UI-SPEC Rules 38, 39).

Purpose: this is the moment the product can call Google. Everything that makes that safe must be in
this same plan — which is why the CI-spend hazard (spend.spec.ts L128–133) is fixed here, not later.

Output: `queue-run.ts`, `run-drawer.tsx`, the notice retirement across its blast radius, a DB-lane
action test, two e2e rewrites, the manifest proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:db -t "<name>"` / `$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME.
🔴 Never run the e2e suite against a deployed URL from this plan; `preset-detail.spec.ts` runs locally only (its `TARGET_IS_LOCAL` self-skip).
</pinned_launcher>

<interfaces>
`src/server/actions/queue-run.ts` (whole file, planner-read): `requireOrg()` first; hand-built claims `{ o:{id}, sub, role }` (fine — nothing role-gated is called);
`queueRunInputSchema = z.strictObject({ searchVersionId: z.uuid() })`; version → spec via `readReferenceIndex`/`specInputOfVersion`/`resolveSpec`;
`readCurrentPeriod`, `readUnitsUsedThisPeriod`; `computeEstimate(...).costMicroUsdHi`; `holdMicroUsd = hi > 0 ? BigInt(hi) : 1n`;
`insert into runs (org_id, search_version_id, status) … returning id`; `app.reserve_budget(...)`; refused → `status='refused', stopped_reason='budget_cap_reached'`;
returns `ok({ runId, reservationId, pctAfter, at80, notice: PHASE4_RUN_NOTICE })`.
04-04: `estimatePreset(spec, ctx, { onlyCells })`, `RUN_CEILING_MULTIPLIER`, `RUN_NEVER_STARTED_AFTER_MINUTES` (15), `RUN_ABANDONED_AFTER_MINUTES` (30), `cellKey`.
04-13: `cellsForRun(spec, seed, kind, now)`, `missingGeometry(cells, spec, shapes)`. 04-22: `placesSweep`. `start` from `workflow/api` returns `{ runId }`.
04-07 copy: `RUN_MODE_REFUSED(mode)`, `RUN_ALREADY_IN_PROGRESS`, `RUN_OPEN_RUNNING`, `RUN_NO_GEOMETRY(names)`, `RUN_START_FAILED`, `RUN_DRAWER_*`.
`PHASE4_RUN_NOTICE` references (PATTERNS blast-radius note 1): `copy.ts` L106, `run-drawer.tsx` L31 + L320–321, `queue-run.ts` L11 + L216,
`presets/page.tsx` L8 + L80, `tests/unit/ui-maps.test.ts` L26–27 + L155, `tests/e2e/preset-detail.spec.ts` L341.
`run-drawer.tsx` L69–103 (`useIsDesk`), L152–178 (confirm path), `gap-3` at L220 and L300 (Rule 41). `tests/e2e/spend.spec.ts` L128–133 (clicks `run-preset` → `run-confirm`).
DB-lane action tests: `tests/db/review-actions.test.ts` (mocks for `requireOrg`, `vi.doMock('@/db/with-org')` savepoint double); `vi.doMock('workflow/api', …)` reaches the action (it is not step code).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: queueRun — mode refusal, kinds, planner-sized admission, one active run, start() after commit</name>
  <files>src/server/actions/queue-run.ts, tests/db/queue-run.test.ts</files>
  <read_first>
    - `src/server/actions/queue-run.ts` (whole), `src/lib/places/plan-run.ts`, `src/workflows/places-sweep/workflow.ts`
    - `tests/db/review-actions.test.ts` L1–130
    - 04-RESEARCH.md § Pattern 1 (start after commit), § Pattern 6
  </read_first>
  <behavior>
    (`vi.doMock('@/env', …)` per test for `PLACES_MODE`; `vi.doMock('workflow/api', () => ({ start: vi.fn(async () => ({ runId: 'wrun_test' })) }))`)
    - `queueRun refuses in off mode before any reservation` — `fail('mode_refused', …)`; zero `runs` and zero `cost_reservations` rows added; `start` not called
    - `queueRun refuses a full sweep in ids_only mode` — same; and `change_check` in `ids_only` succeeds
    - `a confirmed run creates a runs row on the current version` (moved from e2e, Rule 38) — `enterprise`, `full_sweep`: one `queued` row on the version with `kind`, `estimate_requests_hi`, `estimate_micro_usd_hi`, `ceiling_requests = ceil(2 × requestsHi)`, `requested_by = 'user_reviewer_A'`, `workflow_run_id = 'wrun_test'`, and an admission reservation for the run; `start` called once with `[{ runId, clerkOrgId: 'org_A' }]` AFTER the row is committed
    - `queueRun stores the partition index for a partition run`
    - `a change check holds one micro-dollar on ts_essentials`
    - `queueRun refuses a second active run` — `fail('conflict', RUN_ALREADY_IN_PROGRESS, { reason:'busy', runningRunId })`; no second reservation
    - `queueRun fails stale runs before inserting` — a `queued` row 20 min old → `failed/never_started`; a `running` row with a 40-min-old heartbeat → `failed/abandoned`; the new run is admitted
    - `queueRun refuses a version without geometry` — Texas counties → `fail('validation', RUN_NO_GEOMETRY(…))`, no row
    - `queueRun releases the hold when the workflow cannot start` — `start` rejects → run `failed/never_started`, its reservation `released_at` set, no ledger row, `fail('unexpected', RUN_START_FAILED)`
  </behavior>
  <action>
1. Input: `z.strictObject({ searchVersionId: z.uuid(), kind: z.enum(['full_sweep','partition','change_check']).default('full_sweep') })`.
2. After `requireOrg()` and parsing, BEFORE any transaction: `const mode = env.PLACES_MODE; if (mode === 'off' || (mode === 'ids_only' && kind !== 'change_check')) return fail('mode_refused', RUN_MODE_REFUSED(mode));` (M28's action-side twin).
3. In the transaction, first: the two stale-run updates (`never_started` for `queued` older than 15 min; `abandoned` for `running` with `coalesce(heartbeat_at, started_at) < now() - 30 min`, both setting `finished_at = now()`); then `select id from runs where status in ('queued','running') limit 1` → `busy`.
   Version → spec (unchanged); `const { cells, partitionIndex } = cellsForRun(spec, seed, kind, now)`; `missingGeometry(cells, spec, shapes)` non-empty → `no_geometry`;
   `estimatePreset(spec, ctx, { onlyCells: (c) => keys.has(cellKey(c.clusterKey, c.unitId)) })`; `sku = kind === 'change_check' ? 'ts_essentials' : ESTIMATE_SKU`;
   for `change_check` the cost is 0 (hold 1 µUSD); `ceiling = Math.ceil(RUN_CEILING_MULTIPLIER * e.requestsHi)`;
   `insert into runs (org_id, search_version_id, status, kind, partition_index, estimate_requests_lo, estimate_requests_hi, estimate_micro_usd_lo, estimate_micro_usd_hi, ceiling_requests, requested_by) values (…) returning id`;
   reserve (with `sku`); refused path unchanged. A `23505` on `runs_one_active_per_org` (race) → `busy`.
4. After commit: `try { const wf = await start(placesSweep, [{ runId, clerkOrgId: orgId }]); await withOrg(claims, tx => tx.execute(sql\`update runs set workflow_run_id = ${wf.runId} where id = ${runId}\`)); } catch { await withOrg(claims, async tx => { await tx.execute(sql\`select app.release_reservation(${reservationId}::uuid)\`); await tx.execute(sql\`update runs set status='failed', stopped_reason='never_started', finished_at=now() where id=${runId} and status='queued'\`); }); return fail('unexpected', RUN_START_FAILED); }`
5. Return `ok({ runId, reservationId, pctAfter, at80 })` — `notice` removed. Update the header comment: the reservation is now the admission hold that `beginRun` releases (04-22).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "queueRun" && $PNPM test:db -t "a confirmed run creates a runs row on the current version" && $PNPM test:db -t "a change check holds one micro-dollar" && $PNPM test:unit tests/unit/server-actions-guard.test.ts && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all nine tests
    - In `queue-run.ts`, the `start(placesSweep` call appears after the `runInTransaction()` await (source order)
    - `grep -n "PHASE4_RUN_NOTICE" src/server/actions/queue-run.ts` returns nothing
  </acceptance_criteria>
  <done>The Run button starts a metered, ceilinged, mode-checked workflow run — or explains why not.</done>
</task>

<task type="auto">
  <name>Task 2: Drawer kinds and navigation; retire PHASE4_RUN_NOTICE everywhere; the manifest proof</name>
  <files>src/components/preset-detail/run-drawer.tsx, src/lib/ui/copy.ts, src/app/(app)/presets/page.tsx, tests/unit/ui-maps.test.ts</files>
  <read_first>
    - `src/components/preset-detail/run-drawer.tsx` (whole), `src/app/(app)/presets/page.tsx`, `tests/unit/ui-maps.test.ts`
    - 04-UI-SPEC.md § Screen 2 "The run drawer", Rules 38, 40, 41
  </read_first>
  <action>
1. `RunDrawer` gains `kind: 'full' | 'partition' | 'check'` (maps to the action's `full_sweep | partition | change_check`) and optional
   `partition?: { index: number; isoWeek: number; mondayIso: string; sundayIso: string; cells: number; totalCells: number }`. Titles, the
   partition "Cells" row, the check's "Cost $0.00 — IDs-only searches are free" row and note, confirm labels ("Reserve budget & start the sweep" /
   "Reserve budget & run week {w}" / "Start the free change check") and "Starting…" busy state from `copy.ts`. The trigger is rendered by the
   caller (04-27) — the drawer takes `trigger` as a prop if it does not already. On `ok`: `router.push(\`/runs/${result.data.runId}\`)`, no toast.
   Branches: `budget_refused` → inherited `run-refused`; `mode_refused` → destructive `run-mode-refused` with "Reload this preset"
   (`router.refresh()`); `conflict/busy` → an `Alert` with `RUN_ALREADY_IN_PROGRESS` and a link to `/runs/{runningRunId}`; else inherited `run-error`.
   Replace `gap-3` at L220 → `gap-2` (radio rows) and L300 → `gap-4` (footer stack).
2. Retire `PHASE4_RUN_NOTICE`: delete it from `copy.ts`; remove its import and rendering from `run-drawer.tsx` (and `data-testid="run-phase4-notice"`)
   and from `presets/page.tsx`; remove its assertions from `ui-maps.test.ts` (L26–27, L155). `grep -rn "PHASE4_RUN_NOTICE\|run-phase4-notice" src tests` must end empty
   except `tests/e2e/preset-detail.spec.ts`, which Task 3 rewrites.
3. `$PNPM build`, then locate the generated workflow manifest (`find src/app/.well-known/workflow -name "manifest.json"`) and confirm it
   registers `placesSweep` (Pitfall 11: discovery follows imports from route/page files to `start()`; our `start()` lives in a server action
   reached through a client drawer). If it does not, add the documented explicit registration for `withWorkflow` and record which one worked.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit tests/unit/ui-maps.test.ts && $PNPM typecheck && $PNPM lint && $PNPM build && grep -rl "placesSweep" src/app/.well-known/workflow</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "PHASE4_RUN_NOTICE" src tests/unit` returns nothing
    - `grep -n "router.push" src/components/preset-detail/run-drawer.tsx` matches; `grep -n "gap-3" src/components/preset-detail/run-drawer.tsx` returns nothing
    - The generated workflow manifest contains `placesSweep` (quote the matching line in the SUMMARY)
  </acceptance_criteria>
  <done>The drawer runs all three kinds and lands on the report; the Phase 2 notice is gone; the workflow is registered.</done>
</task>

<task type="auto">
  <name>Task 3: Make the e2e suite incapable of starting a run (Rules 38, 39)</name>
  <files>tests/e2e/spend.spec.ts, tests/e2e/preset-detail.spec.ts</files>
  <read_first>
    - `tests/e2e/spend.spec.ts` (whole — L128–133 is the hazard), `tests/e2e/preset-detail.spec.ts` (whole — the `TARGET_IS_LOCAL` self-skip L36–45, L341–352)
    - 04-UI-SPEC.md Rules 38, 39
  </read_first>
  <action>
1. `spend.spec.ts`: delete every click on `run-preset` / `run-confirm`. Replace with: open `/spend` → By run; if the list has zero rows,
   `test.skip(true, 'no runs exist on this deployment yet — this spec never creates one (UI-SPEC Rule 39)')`; otherwise, for every listed
   row, `spend-run-link-{runId}` has `href` `/runs/{runId}`. Add a header comment: "This spec must never click a run confirm against a
   deployed app: once PLACES_MODE is enterprise that is a billed Places sweep from CI."
2. `preset-detail.spec.ts` (local-only by its existing self-skip): remove the `run-phase4-notice` assertion and the "drawer closes on the same
   page with version-row-2-used-by = 1" block (L341–352). Replace with: open the drawer from `run-preset`, click `run-confirm`, and assert
   `run-mode-refused` is visible (local `PLACES_MODE` is `off`) and the URL did not change. 04-27 replaces this again with the off-state
   assertions once the notice and disabled actions exist.
3. `grep -rn "run-confirm" tests/e2e` must list only `preset-detail.spec.ts` (local-only); record the grep in the SUMMARY.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM typecheck && grep -rn "run-confirm" tests/e2e</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "run-confirm\|run-preset" tests/e2e/spend.spec.ts` returns nothing
    - `grep -n "run-phase4-notice" tests/e2e/preset-detail.spec.ts` returns nothing
    - `grep -rln "run-confirm" tests/e2e` lists only `tests/e2e/preset-detail.spec.ts`
  </acceptance_criteria>
  <done>CI can no longer start a run anywhere; the confirmed-run proof lives in the DB lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → queueRun | version id and kind are untrusted |
| server action → Vercel Queue | `start()` hands the workflow its tenant input |
| CI → deployed app | e2e specs authenticate as a real user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Denial of service (financial) | admission | mitigate | mode refusal before any reservation; planner-sized hold; `ceiling_requests` stored; one active run per org; stale-run reclaim |
| T-4-06 | Tampering (cross-tenant) | workflow input | mitigate | `clerkOrgId` from server-verified `requireOrg()` only; the version is read under RLS before the run row exists |
| T-4-09 | Denial of service (financial) | CI e2e | mitigate | `spend.spec.ts` never clicks a run; `preset-detail.spec.ts` is local-only and asserts the refusal; grep proof |
| T-4-02 | Denial of service (financial) | `start()` failure | mitigate | hold released and run failed `never_started`, so the UI's "nothing was charged" is true |
| T-4-14 | Spoofing | workflow registration | mitigate | manifest proves the only registered entry is `placesSweep`; handlers reachable only via Vercel Queues |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM test:db`, `$PNPM test:workflow`, `$PNPM typecheck`, `$PNPM lint`, `$PNPM build` green; manifest line quoted; branch + HEAD printed
</verification>

<success_criteria>
- The product can start a Places run only through a mode-checked, metered, ceilinged admission — and CI cannot start one at all
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-26-SUMMARY.md`
</output>
