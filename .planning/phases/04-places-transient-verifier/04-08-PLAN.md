---
phase: 04-places-transient-verifier
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/places/google-maps-tag.tsx
  - src/app/globals.css
  - tests/unit/google-maps-tag.test.tsx
  - tests/unit/no-map.test.ts
autonomous: true
requirements: [PLACE-06]

must_haves:
  truths:
    - "One server-safe GoogleMapsTag renders exactly 'Google Maps' as text in a span with translate=\"no\" and data-testid=\"google-maps-attribution\" (D-11, UI-SPEC Rule 29)"
    - "--google-attribution is a painted literal: #5E5E5E on :root and #FFFFFF on .dark, mapped through @theme inline; .google-maps-attribution sets Roboto, sans-serif at 14px/400, normal style and spacing, nowrap"
    - "No map library, map iframe, static map or tile URL exists anywhere in the app (§3.2.3(e) / §B.14.2, Rule 31)"
  artifacts:
    - path: "src/components/places/google-maps-tag.tsx"
      exports: ["GoogleMapsTag"]
    - path: "tests/unit/no-map.test.ts"
      contains: "no map library or map embed anywhere in the app"
  key_links:
    - from: "src/components/places/google-maps-tag.tsx"
      to: "src/app/globals.css .google-maps-attribution"
      via: "className"
      pattern: "google-maps-attribution"
---

<objective>
The Google Maps attribution mark (PLACE-06, D-11) as one component with pinned text and styles, and
the no-map guard (Rule 31).

Purpose: every Places-derived value the UI renders must carry this tag in its container; building
it first lets the five surface plans import one implementation. Google's policy fixes its colour
(#5E5E5E / white) and font (Roboto, sans-serif fallback, 12–16sp, 400).

Output: `GoogleMapsTag`, one token + one class in `globals.css`, two tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. No scratch `.ts` in the repo.
</pinned_launcher>

<interfaces>
`src/components/business-detail/fields-and-sources.tsx` L114–121 — a source tag is text, never a badge:
```tsx
function Tag({ field, 'data-testid': testid }: { field: FieldView<unknown>; 'data-testid': string }) {
  return <span data-testid={testid}>{sourceTagOf(field)}</span>;
}
```
`src/app/globals.css`: `--warning` is declared as a literal on `:root` (L67) and `.dark` (L117) and mapped
in `@theme inline` (L135, L158: `--color-warning: var(--warning);`). Tailwind v4 arbitrary values use
`[var(--x)]`, never `[--x]` (Rule 1). A literal token belongs on `:root`/`.dark`; composed ones go on `*`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GoogleMapsTag and its token/class</name>
  <files>src/components/places/google-maps-tag.tsx, src/app/globals.css, tests/unit/google-maps-tag.test.tsx</files>
  <read_first>
    - `src/components/business-detail/fields-and-sources.tsx` L27–33 and L93–121
    - `src/app/globals.css` L35–160
    - `tests/unit/closed-badge.test.tsx` (a small dom-lane component test to copy)
    - 04-UI-SPEC.md § Color → "The new token", Rule 29
  </read_first>
  <behavior>
    - "the google maps tag is exact text with translate=no": renders a `span` whose `textContent === 'Google Maps'` (case-sensitive), `translate="no"`, `class` includes `google-maps-attribution`, `data-testid="google-maps-attribution"`
    - "the google maps tag is not a badge": the span has no `data-slot="badge"` and no role
    - "the attribution token is painted in both themes": reading `src/app/globals.css` as text, the `:root` block contains `--google-attribution: #5E5E5E;`, the `.dark` block contains `--google-attribution: #FFFFFF;`, `@theme inline` contains `--color-google-attribution: var(--google-attribution);`, and the `.google-maps-attribution` rule contains `font-family: Roboto, sans-serif`, `font-size: 14px`, `font-weight: 400`, `font-style: normal`, `letter-spacing: normal`, `white-space: nowrap`, `color: var(--color-google-attribution)`
  </behavior>
  <action>
1. `src/components/places/google-maps-tag.tsx` (no client directive — server-safe, importable from RSC and client islands):
```tsx
/** D-11 / PLACE-06. Google's text attribution, exactly "Google Maps" (policy page, 2026-09-23).
 *  Text, never a badge (Rule 22). One implementation (Rule 29). The date beside it is a SIBLING
 *  muted span rendered by the caller, so this colour and font never extend to it. */
import { GOOGLE_MAPS_TAG } from '@/lib/ui/copy';
export function GoogleMapsTag() {
  return (
    <span translate="no" className="google-maps-attribution" data-testid="google-maps-attribution">
      {GOOGLE_MAPS_TAG}
    </span>
  );
}
```
   🔴 `GOOGLE_MAPS_TAG` is added by 04-07 (wave 2). This plan is wave 1, so for now write the literal
   `'Google Maps'` inline with a `// 04-07 moves this literal into copy.ts as GOOGLE_MAPS_TAG` comment,
   and 04-07's executor switches the import (04-07 Task 2 already defines the constant; it adds the
   one-line import change here as part of its "no inline strings" sweep). Record the handoff in the SUMMARY.
2. `globals.css`: `--google-attribution: #5E5E5E;` in `:root`, `--google-attribution: #FFFFFF;` in `.dark`,
   `--color-google-attribution: var(--google-attribution);` in `@theme inline`, and one rule:
```css
.google-maps-attribution {
  font-family: Roboto, sans-serif;
  font-size: 14px;
  font-weight: 400;
  font-style: normal;
  letter-spacing: normal;
  white-space: nowrap;
  color: var(--color-google-attribution);
}
```
   🔴 No `@font-face`, no font import, no package — devices without Roboto take Google's permitted fallback.
3. Tests per the behavior block (dom lane for the component; plain `readFileSync` for the CSS).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the google maps tag is exact text with translate=no" && $PNPM test:unit -t "the attribution token is painted in both themes" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "translate=\"no\"" src/components/places/google-maps-tag.tsx` matches
    - `grep -n "use client" src/components/places/google-maps-tag.tsx` returns nothing
    - `grep -c "google-attribution" src/app/globals.css` ≥ 4
    - `grep -n "@font-face\|fonts.googleapis" src/app/globals.css` returns nothing
  </acceptance_criteria>
  <done>The tag exists once with pinned text, token and class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: no-map guard</name>
  <files>tests/unit/no-map.test.ts</files>
  <read_first>
    - `tests/unit/no-google-credential.test.ts` (two-sided walker style; after 04-02 it imports `./_walk` — if 04-02 has not landed yet in this wave, write a local 8-line walker and leave a `// 04-02 _walk` TODO-free note in the SUMMARY; do not import a file that may not exist)
    - 04-UI-SPEC.md Rule 31
  </read_first>
  <behavior>
    - "no map library or map embed anywhere in the app": `package.json` dependencies + devDependencies contain none of `leaflet`, `react-leaflet`, `maplibre-gl`, `mapbox-gl`, `@vis.gl/react-google-maps`, any `@react-google-maps/*`, `pigeon-maps`, `ol`; and no file under `src/` (`.ts`, `.tsx`, `.css`, `.json`) matches `maps/embed`, `staticmap`, `tile.openstreetmap`, `api.mapbox.com`, `maplibre`, or `<iframe` on a line that also contains `map`. Two-sided: the scan found > 50 files and includes `src/app/globals.css`.
  </behavior>
  <action>Write the test; make it pass (it should pass today). Then apply the mutation from Rule 31 locally — add `"leaflet": "1.9.4"` to `package.json` dependencies — run the test, confirm it goes red NAMING `leaflet`, revert with `git checkout package.json`, confirm `git diff --stat` is empty. Record both runs in the SUMMARY.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "no map library or map embed anywhere in the app"</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names `no map library or map embed anywhere in the app`
    - The SUMMARY quotes the red run naming `leaflet` and the clean `git diff --stat` after revert
  </acceptance_criteria>
  <done>A map cannot enter the app unnoticed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Google Maps Platform Terms | attribution and no-non-Google-map obligations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-13 | Repudiation (legal: Terms §3.2.3(e), §B.14.2) | map rendering | mitigate | `no-map.test.ts` fails on any map package or embed pattern; mutation-checked in this plan |
| T-4-13 | Repudiation (legal: attribution) | `GoogleMapsTag` | mitigate | exact text, `translate="no"`, painted policy colours in both themes; computed-style e2e probe in 04-28 |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint` green
</verification>

<success_criteria>
- One attribution component with pinned text and styles; a mutation-proven no-map guard
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-08-SUMMARY.md`
</output>
