---
phase: 04-places-transient-verifier
plan: 25
type: execute
wave: 5
depends_on: [04-07, 04-08, 04-21]
files_modified:
  - src/components/business-detail/google-check.tsx
  - src/components/business-detail/detach-dialog.tsx
  - src/app/(app)/businesses/[id]/page.tsx
  - tests/unit/google-check.test.tsx
  - tests/unit/detach-dialog.test.tsx
  - tests/unit/no-internal-leak.test.ts
autonomous: true
requirements: [PLACE-06, PLACE-02]

must_haves:
  truths:
    - "Business detail gains a 'Google Maps check' card: the business-level signal (true if any attached listing's latest observation lists a website) as a sentence by host class, tagged 'Google Maps · {date}' in the source column (D-08, D-09, D-11)"
    - "Each listing renders by status: attached (score, own signal, tag, open-on-Maps, detach), tentative (pending, NO website sentence, review link), rejected/detached (who and when, no tag) (D-05, D-10)"
    - "The observation history is collapsed by default and lists each check newest first with a link to its run (D-10)"
    - "'Detach this listing' always opens a destructive confirmation and calls no server action until confirm (Rule 42)"
    - "No component reads or renders a Places coordinate; the Location field keeps its durable source (Rule 32)"
  artifacts:
    - path: "src/components/business-detail/google-check.tsx"
      exports: ["GoogleCheck"]
    - path: "src/components/business-detail/detach-dialog.tsx"
      exports: ["DetachDialog"]
  key_links:
    - from: "src/components/business-detail/google-check.tsx"
      to: "src/components/places/google-maps-tag.tsx"
      via: "one tag per Places-derived row container ([data-places-content])"
      pattern: "GoogleMapsTag"
    - from: "src/components/business-detail/detach-dialog.tsx"
      to: "src/server/actions/detach-listing.ts"
      via: "onClick inside useTransition, only on confirm"
      pattern: "detachListing"
---

<objective>
The business detail's Google Maps check (UI-SPEC § Screen 5): the Places-derived website signal with
its attribution, the per-listing states, the check history, and the detach confirmation.

Purpose: PLACE-06 / D-11 — attribution wherever a Places-derived signal renders; this is the screen
Phase 7's triage card will inherit. D-08's "any attached listing" rule is what danlo sees here.

Output: two components, the page wiring, three tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. Never run `npx shadcn init`.
</pinned_launcher>

<interfaces>
04-21: `BusinessDetail.google: GoogleCheckView = { signal: { hadWebsiteUri; hostClass; observedMs } | null; listings: Array<{ attachmentId; placeId;
status: 'attached'|'tentative'|'rejected'; reason; score; tieBusinessId; decidedBy; decidedMs; latest: { hadWebsiteUri; hostClass; observedMs; runId } | null }>;
history: Array<{ observationId; placeId; observedMs; hadWebsiteUri; hostClass; runId }> }`; `detachListing({ attachmentId })`.
04-07: `signalSentence(hostClass)`, `mapsUrlFor`, copy `BUSINESS_GOOGLE_*`, `DETACH_*`, `TOAST_DETACHED`.
04-08: `GoogleMapsTag`.
Analogs: `src/components/business-detail/fields-and-sources.tsx` L93–121 (`FieldRow` three-column desk grid / phone three-line stack; source tag as text),
`merge-history.tsx` (per-row history), `src/components/sources/confidence-distribution.tsx` (the Collapsible),
`unmerge-dialog.tsx` + `tests/unit/unmerge-dialog.test.tsx` (destructive confirmation), actor display names via
`src/app/(app)/businesses/[id]/actor-names.ts`. Page: `src/app/(app)/businesses/[id]/page.tsx` L66–143 (sections order).
`tests/unit/no-internal-leak.test.ts` — its registry/greps.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GoogleCheck card and its placement on the page</name>
  <files>src/components/business-detail/google-check.tsx, src/app/(app)/businesses/[id]/page.tsx, tests/unit/google-check.test.tsx</files>
  <read_first>
    - `src/components/business-detail/fields-and-sources.tsx`, `merge-history.tsx`, `src/components/sources/confidence-distribution.tsx`
    - `src/app/(app)/businesses/[id]/page.tsx`, `actor-names.ts`
    - 04-UI-SPEC.md § Screen 5, § States (Empty rows), Rules 22, 28, 30, 32
  </read_first>
  <behavior>
    - `the google check shows the business signal with the Google Maps tag and date` — `business-google-signal` with `data-host-class="business_site_dead"` renders "Website listed — a dead Google site (business.site)"; its row container (`data-places-content`) holds exactly one `google-maps-attribution` and a SEPARATE muted date span
    - `a tentative listing shows no website sentence` — `business-google-listing-{id}` `data-status="tentative"` shows the Tentative badge, "Pending review — scored 87…", a `/review?kind=google` link, a tag, and none of the six signal sentences
    - `rejected and detached listings show who and when and carry no tag`
    - `one attached listing renders once` — exactly one attached listing and nothing else → the signal row carries the listing's actions and there is no separate listing row
    - `the empty google check says not checked yet` — `business-google-empty` with the city and cluster from the business
    - `only tentative listings read as no confirmed listing yet` — row 1 "No confirmed listing yet — one is pending review." with no tag
    - `the check history is collapsed and newest first` — `business-google-history-toggle` present only when > 1 observation; opening it lists rows newest first, each with a `/runs/{runId}` link and a tag
  </behavior>
  <action>
`google-check.tsx` (server component; the Collapsible and the detach trigger are client children): `Card` `business-google`, title
"Google Maps check"; row 1 (label "Website on Google" / value `signalSentence` / source column `<GoogleMapsTag /> · {date}` right-aligned on
desk, line 3 on phone) using the `FieldRow` grid; rows 2…n per listing in the order attached (newest observation first) → tentative →
rejected/detached; attached rows get "Open this listing on Google Maps" (outline, 44px, external, new tab, `rel="noopener noreferrer"`,
href `mapsUrlFor(placeId, business.displayName, business.city)`) and the `DetachDialog` trigger; history in a Collapsible closed by default.
Every Places-derived row sits in a `data-places-content` container with exactly one tag. Never read or render a coordinate.
`page.tsx`: insert `<GoogleCheck google={detail.google} business={…} />` between "Fields and sources" and "Source records".
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the google check" && $PNPM test:unit -t "a tentative listing shows no website sentence" && $PNPM test:unit -t "one attached listing renders once" && $PNPM test:unit tests/unit/business-detail.test.tsx && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all seven tests; the existing business-detail tests stay green
    - `grep -n "GoogleMapsTag" src/components/business-detail/google-check.tsx` matches
  </acceptance_criteria>
  <done>The business screen shows the Places signal, listings and history with attribution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DetachDialog and the never-rendered-coordinates guard</name>
  <files>src/components/business-detail/detach-dialog.tsx, tests/unit/detach-dialog.test.tsx, tests/unit/no-internal-leak.test.ts</files>
  <read_first>
    - `src/components/business-detail/unmerge-dialog.tsx`, `tests/unit/unmerge-dialog.test.tsx`, `tests/unit/no-internal-leak.test.ts` (whole files)
    - 04-UI-SPEC.md § Copy Table → Detach dialog, § Error (detach failed), Rules 32, 42
  </read_first>
  <behavior>
    - `the detach trigger calls no server action until confirm` (Rule 42) — `business-google-detach-{id}` opens `business-google-detach-dialog`; 0 calls; `business-google-detach-confirm` → 1 call
    - `the detach dialog keeps the listing attached on failure` — a failed result shows the detach-failed `Alert` inside the dialog with "Try again" / "Reload this business"; dialog stays open
    - `keep it attached dismisses without detaching` — `business-google-detach-dismiss` → 0 calls
    - `places coordinates are never rendered` — no file under `src/components/` or `src/app/` mentions `place_coordinates` or `placeCoordinates`, and the `GoogleCheckView` fixture used by the google-check test has no `lat`/`lng` keys
  </behavior>
  <action>`detach-dialog.tsx` copies `unmerge-dialog.tsx` exactly in structure (desk Dialog / phone Drawer via `useIsDesk`, destructive Alert body with the detach copy, destructive confirm "Detach this listing" → busy "Detaching…", outline dismiss "Keep it attached", `useTransition`, toast `TOAST_DETACHED(name)` on success only, `router.refresh()` after close). Add the never-rendered test to `no-internal-leak.test.ts` beside its existing registry.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the detach trigger calls no server action until confirm" && $PNPM test:unit -t "the detach dialog" && $PNPM test:unit -t "places coordinates are never rendered" && $PNPM typecheck && $PNPM lint && $PNPM build</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all four tests; `no-internal-leak` stays green overall
    - `grep -n "<form" src/components/business-detail/detach-dialog.tsx` returns nothing
    - `$PNPM build` exits 0
  </acceptance_criteria>
  <done>Detaching is confirmed, reversible-safe on failure, and coordinates cannot reach any component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored derived signals → screen | host classes, scores, dates |
| browser → detach action | untrusted id |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-13 | Repudiation (legal: attribution) | Google check rows | mitigate | one `GoogleMapsTag` per Places-derived row container; tentative/rejected rows show no Places value |
| T-4-04 | Information disclosure (legal retention) | coordinates | mitigate | never read (no grant) and never rendered (named repo grep) |
| T-4-06 | Tampering | detach | mitigate | confirmation required; server action re-checks org (04-21) |
| T-4-11 | Tampering | Maps link | mitigate | `mapsUrlFor` with encoded components; `noopener noreferrer` |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint`, `$PNPM build` green
</verification>

<success_criteria>
- The Places signal is visible, attributed and correctable on the business screen
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-25-SUMMARY.md`
</output>
