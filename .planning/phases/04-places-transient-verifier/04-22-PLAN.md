---
phase: 04-places-transient-verifier
plan: 22
type: execute
wave: 5
depends_on: [04-01, 04-05, 04-11, 04-13, 04-16, 04-18, 04-19]
files_modified:
  - src/workflows/places-sweep/workflow.ts
  - src/workflows/places-sweep/steps.ts
  - src/lib/places/check-tile.ts
  - src/server/queries/preset-spec.ts
  - src/server/queries/presets.ts
  - tests/workflow/_seed.ts
  - tests/workflow/places-sweep.test.ts
autonomous: true
requirements: [PLACE-03, PLACE-04, PLACE-05]

must_haves:
  truths:
    - "placesSweep is a Vercel Workflow: beginRun → one searchTile/checkTile step per (Places type × tile) → finishRun, resumable across crashes and deploys (D-14)"
    - "beginRun claims the run (queued → running), releases the admission hold with no ledger row, and plans the root searches with the same planner admission used; a change check substitutes the stored leaf tiles (D-15, D-16)"
    - "A saturated tile subdivides and the run completes, reporting truncation (criterion 3); a refused reservation ends it partial/budget_cap_reached; the request ceiling ends it partial/exceeded_estimate; a daily-quota 429 ends it partial/google_daily_quota without retry (D-19, M52); a 400 fails it without retry"
    - "A per-minute 429 / 5xx / timeout is a RetryableError with a bounded maxRetries; a budget stop is never thrown (M33)"
    - "No step input or output, nor any file the workflow world writes, contains Places text (Pitfall 1a, M45)"
    - "finishRun sets the terminal status, stopped reason, finished_at and cost from the ledger only while the run is still 'running', settles any leftover in-flight reservation as charged, and emits one run-level event"
  artifacts:
    - path: "src/workflows/places-sweep/workflow.ts"
      exports: ["placesSweep", "SweepOutcome"]
    - path: "src/workflows/places-sweep/steps.ts"
      exports: ["beginRun", "searchTile", "checkTile", "finishRun"]
    - path: "tests/workflow/places-sweep.test.ts"
      contains: "a saturated tile subdivides and the run completes"
  key_links:
    - from: "src/workflows/places-sweep/workflow.ts"
      to: "src/workflows/places-sweep/reducer.ts"
      via: "initialQueue / nextSearch / applyResult / failReasonOf / finishVerdict"
      pattern: "applyResult"
    - from: "src/workflows/places-sweep/steps.ts"
      to: "src/lib/places/search-tile.ts runSearchTile + check-tile.ts runCheckTile"
      via: "'use step' wrappers converting fail results into FatalError / RetryableError"
      pattern: "RetryableError"
---

<objective>
The executor (D-14): the repo's first Vercel Workflow, its four steps, and the workflow-lane proofs
that run the real compiled workflow in-process against msw and the local database.

Purpose: criteria 3, 4, 5 end to end. The research spike proved `@workflow/vitest` runs here and that
msw reaches step code while `vi.mock` does not — so this lane seeds a real org in the local DB and
serves Places from msw.

Output: `workflow.ts`, `steps.ts`, the preset-spec extraction if needed, a seed helper, the lane tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:workflow -t "<name>"`, never `-- -t`; read the PASS-list NAME. No scratch `.ts` in the repo.
</pinned_launcher>

<interfaces>
Workflow API (workflow@4.8.9, bundled docs + spike): `"use workflow"` / `"use step"` directives; `import { FatalError, RetryableError } from 'workflow'`;
`new RetryableError(message, { retryAfter: ms | '1m' | Date })`; `stepFn.maxRetries = 3` (default 3; 0 = no retry);
step args/returns are serialized by value; a FatalError's message reaches the workflow's `catch` as `(e as Error).message` (spike-verified);
`start(flow, args)` from `workflow/api` returns a run with `await run.returnValue`; never put `"use server"` in a module a workflow or step imports;
workflow code must hold no Node I/O (`fs`, `pg`, `fetch` only inside steps).
04-05 reducer: `initialQueue`, `nextSearch`, `applyResult`, `failReasonOf`, `finishVerdict`, `PlannedSearch`, `SearchResult`.
04-18: `runSearchTile(input, search, { mode, shapes })`, `SweepInput`, `TileStepResult`; 04-19: `runCheckTile(input, search, { mode })`.
04-13: `planRootSearches({ spec, seed, shapes, kind, now })`. 04-16: `withWorkerOrg`, `settleInFlight`. 04-11: `app.release_reservation`, `app.plan_run_searches`.
`src/server/queries/presets.ts` exports used by `queue-run.ts`: `getSeedTables()`, `readReferenceIndex(tx)`, `specInputOfVersion(clusterIds, geoKind, geoPayload, index)`, `resolveSpec(specInput, index, name)`.
`app.emit_event(text, uuid, text, jsonb)` — read its exact parameter order in `drizzle/0011_*.sql` before calling.
`@workflow/builders` maps `server-only` to an empty module in step bundles; `next/*` and `@clerk/*` are NOT safe to pull into a step.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: steps.ts and workflow.ts (+ a step-safe preset-spec module)</name>
  <files>src/workflows/places-sweep/steps.ts, src/workflows/places-sweep/workflow.ts, src/lib/places/check-tile.ts, src/server/queries/preset-spec.ts, src/server/queries/presets.ts</files>
  <read_first>
    - `src/server/queries/presets.ts` (whole file — list its imports; anything from `next/*` or `@clerk/*` must not reach a step)
    - `src/server/actions/queue-run.ts` L64–115 (how the version becomes a spec)
    - 04-RESEARCH.md § Pattern 1, § Anti-Patterns, Pitfalls 1, 5, 11
    - `src/lib/places/search-tile.ts`, `check-tile.ts`, `src/workflows/places-sweep/reducer.ts`
  </read_first>
  <action>
1. If `presets.ts` imports anything from `next/*` or `@clerk/*`, move `getSeedTables`, `readReferenceIndex`, `specInputOfVersion`
   and `resolveSpec` (and their private helpers) into `src/server/queries/preset-spec.ts` (`import 'server-only'`, no Next/Clerk import)
   and re-export them from `presets.ts` so no caller changes. If `presets.ts` is already step-safe, create `preset-spec.ts` as a
   one-line re-export so steps have a stable, reviewable import path. Record which case applied.
2. Dedupe: `check-tile.ts` imports `SweepInput` and `TileStepResult` from `search-tile.ts` instead of its local copies.
3. `steps.ts` (every exported function starts with `'use step';`):
   - `beginRun(input: SweepInput): Promise<{ kind: 'not_runnable' } | { kind: 'runnable'; runKind: RunKind; searches: PlannedSearch[] }>` —
     one `withWorkerOrg` txn: read `runs r join search_versions v join searches s` for `input.runId` (zero rows → `throw new FatalError('places_request_rejected')`
     — a run this org cannot see is never executed); `queued` → `update runs set status='running', started_at=now(), heartbeat_at=now() where id=… and status='queued'`;
     `running` → continue (a replay of this step); anything else → `not_runnable`. Release the admission hold: for each
     `select id from cost_reservations where run_id = … and settled_at is null and released_at is null` → `select app.release_reservation(id)`.
     Build the spec exactly as `queue-run.ts` does (via `preset-spec.ts`), then `planRootSearches({ spec, seed, shapes, kind: r.kind, now: new Date(r.created_at_ms) })`
     (the run's own creation instant, so a partition keeps the week it was queued in). For `change_check`, replace each root by the stored leaves
     `select tile_key, quad_path, depth, south, west, north, east from place_tiles where tile_key like <root-prefix> || '%' and is_leaf` (as `ids_only` TileSpecs with the root's shape) when any exist.
     Then `app.plan_run_searches(runId, <jsonb with dbSafe unitId>)` → merge `search_id` → `PlannedSearch[]`.
   - `searchTile(input, search): Promise<SearchResult>` — `const r = await runSearchTile(input, search, { mode: env.PLACES_MODE, shapes })`;
     `r.kind === 'fail'` → `r.retryable ? throw new RetryableError(r.reason, { retryAfter: r.retryAfterMs ?? 2000 }) : throw new FatalError(r.reason)`; else return `r`.
     `searchTile.maxRetries = 3;`
   - `checkTile(input, search)` — same around `runCheckTile`; `checkTile.maxRetries = 3;`
   - `finishRun(input, v: { status: 'complete' | 'partial' | 'failed'; reason: StopReason | FailReason | null }): Promise<SweepOutcome>` —
     one txn: for each `run_searches` row of the run with `inflight_reservation_id is not null` → `settleInFlight` semantics (settle as charged);
     `cost = sum(cost_ledger.micro_usd)`; `update runs set status = …, stopped_reason = …, finished_at = now(), cost_micro_usd = … where id = … and status = 'running'`;
     one `app.emit_event('places_run_finished', runId, 'runs', jsonb_build_object('status', …, 'reason', …, 'calls', calls_count))`;
     return `{ status, reason }` (or the row's existing terminal status when the guard matched nothing).
   Geo shapes: `import shapes from '@/seed/data/geo-shapes.json'` inside steps.ts only (never in workflow.ts).
4. `workflow.ts`:
```ts
import { beginRun, searchTile, checkTile, finishRun } from './steps';
import { initialQueue, nextSearch, applyResult, failReasonOf, finishVerdict, type FailReason } from './reducer';
export type SweepOutcome = { status: 'complete' | 'partial' | 'failed' | 'not_runnable'; reason: string | null };
export async function placesSweep(input: { runId: string; clerkOrgId: string }): Promise<SweepOutcome> {
  'use workflow';
  const plan = await beginRun(input);
  if (plan.kind === 'not_runnable') return { status: 'not_runnable', reason: null };
  let state = initialQueue(plan.searches);
  let failure: FailReason | null = null;
  try {
    for (let s = nextSearch(state); s; s = nextSearch(state)) {
      const r = plan.runKind === 'change_check' ? await checkTile(input, s) : await searchTile(input, s);
      state = applyResult(state, s, r);
      if (state.stopped) break;
    }
  } catch (e) {
    failure = failReasonOf(e);
  }
  return finishRun(input, finishVerdict(state, failure));
}
```
   Header: "D-14. Orchestration only — deterministic, no Node I/O. Every side effect is a step. Scale path past ~2,000 events: child workflows per cell (04-RESEARCH § Alternatives)."
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM typecheck && $PNPM lint && $PNPM build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "'use step'" src/workflows/places-sweep/steps.ts` = 4 and `grep -c "'use workflow'" src/workflows/places-sweep/workflow.ts` = 1
    - `grep -nE "from 'node:|from '@/db|fetch\(|geo-shapes" src/workflows/places-sweep/workflow.ts` returns nothing
    - `grep -rn "from 'next/\|from '@clerk/" src/server/queries/preset-spec.ts` returns nothing
    - `grep -n "maxRetries = 3" src/workflows/places-sweep/steps.ts` matches twice
    - `$PNPM build` exits 0
  </acceptance_criteria>
  <done>The workflow and its steps compile under Next and hold side effects only in steps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The workflow-lane proofs (real compiled workflow, msw, local DB)</name>
  <files>tests/workflow/_seed.ts, tests/workflow/places-sweep.test.ts</files>
  <read_first>
    - The research spike's `sweep.integration.test.ts` shape (start → `await run.returnValue`), `vitest.workflow.config.ts` (04-01)
    - `tests/db/_places-fixtures.ts`, `tests/db/_merge-fixtures.ts` (`seedOvertureSide` — reusable with a committed client), `tests/unit/msw/places.ts`
  </read_first>
  <behavior>
    `_seed.ts`: an OWNER `pg` client on `TEST_DATABASE_URL` creates a dedicated org (`clerk_org_id` `org_wf_<random>`), the five `PLACES_SPINE`
    businesses, a McAllen × home_services search + version, and a `queued` run with `ceiling_requests` and an admission hold reserved under that
    org's claims; `teardownOrg(orgId)` deletes every row of the org in FK order (events, ledger, reservations, place_*, run_*, runs, versions,
    searches, businesses, source_records, budget_periods, orgs). Committed transactions — the workflow opens its own connections.
    msw routes match on `includedType` and on whether the rectangle equals McAllen's bbox from `geo-shapes.json` (root) or not (child).
    - `a saturated tile subdivides and the run completes` — plumber root → saturated 3 pages; children → `places-child-12`; other types → empty ⇒ `{ status:'complete' }`; root `saturated` + `subdivided`; children `done`; `calls_count` = ledger rows = requests seen by msw; no reservation of the run left open
    - `a refused reservation ends the run partial` — cap below one Enterprise price ⇒ `{ status:'partial', reason:'budget_cap_reached' }`; zero Places requests
    - `the run stops at its request ceiling` — `ceiling_requests = 4` with the saturated route ⇒ `partial/exceeded_estimate`; planned searches remain listed
    - `google daily quota stops the run` (M52) — daily 429 on the first request ⇒ `partial/google_daily_quota`; exactly ONE request reached msw
    - `a rejected request fails the run without retry` — 400 ⇒ `failed/places_request_rejected`; exactly one request
    - `a change check run diffs the stored leaf tiles` — pre-seeded leaf tiles + members, kind `change_check`, IDs-only routes ⇒ `complete`; `change_verdict` written per leaf; every request's mask is `places.id,nextPageToken`
    - `the admission hold is released when the run begins` — the queue-time reservation has `released_at` set and no ledger row carries its id
    - `no step returns Places content` (M45) — after the saturated + match-page runs, read every file the workflow world wrote under `.workflow-vitest/` and `.workflow-data/` (whichever exist) plus `JSON.stringify(await run.returnValue)`; none contains any `PLACES_SENTINELS` string
  </behavior>
  <action>Write `_seed.ts` and the eight tests (`beforeAll` seed / `afterAll` teardown per test org; `resetPlaces()` after each). Watch M52 fail first: locally map `daily_quota` to a retryable fail in `search-tile.ts`, run, read the red NAME (more than one request / not partial), revert, `git diff --stat src/` empty. Record it.</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:workflow -t "a saturated tile subdivides and the run completes" && $PNPM test:workflow -t "google daily quota stops the run" && $PNPM test:workflow -t "no step returns Places content" && $PNPM test:workflow</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all eight tests plus `the workflow lane pins zone, locale and a fake Places key`
    - After the lane, `select count(*) from orgs where clerk_org_id like 'org_wf_%'` on the test DB is 0 (teardown works)
    - SUMMARY records M52's red-then-revert
  </acceptance_criteria>
  <done>The executor is proven end to end in-process against msw and a real database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel Queue → workflow/step handlers | step inputs come from `start()` in a server action (04-26), never from a client |
| steps → workflow event log | every step argument and return is persisted (run + 7 days on Pro) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-05 | Information disclosure (legal) | event log / local world files | mitigate | steps return ids/counts/enums only; lane test scans every world file for sentinels (M45) |
| T-4-06 | Tampering (cross-tenant) | beginRun / every step | mitigate | `withWorkerOrg` from the start input; beginRun refuses a run its org cannot see; writers re-check org |
| T-4-02 | Denial of service (financial) | retries / loops | mitigate | budget stops are results (never thrown, M33); daily quota never retried (M52); `maxRetries = 3` bounded; request ceiling; tiling floors |
| T-4-03 | Elevation / Tampering | pinned deployments | mitigate | finishRun and every reservation guard on `status = 'running'`; an operator's `failed` is never overwritten |
| T-4-02 | Denial of service (financial) | leftover in-flight reservations | mitigate | finishRun settles them as charged (pessimistic), so a failed run never under-ledgers |
</threat_model>

<verification>
- `$PNPM test:workflow`, `$PNPM test:db`, `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint`, `$PNPM build` green
- Print branch + HEAD after the build
</verification>

<success_criteria>
- A real compiled workflow completes, stops and fails exactly as D-14/D-15/D-19 require, with no Places text in its event log
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-22-SUMMARY.md`
</output>
