---
phase: 04-places-transient-verifier
plan: 07
type: execute
wave: 2
depends_on: [04-03, 04-05]
files_modified:
  - src/lib/ui/copy.ts
  - src/lib/ui/run-tone.ts
  - src/lib/ui/places-format.ts
  - src/components/places/google-maps-tag.tsx
  - tests/unit/ui-maps.test.ts
  - tests/unit/places-format.test.ts
  - .planning/phases/04-places-transient-verifier/04-UI-SPEC.md
autonomous: true
requirements: [PLACE-06, PLACE-03]

must_haves:
  truths:
    - "Every string in 04-UI-SPEC § Copy Table lives in src/lib/ui/copy.ts (Executor Rule 5); no Phase 4 component spells a user-facing sentence inline"
    - "Stopped reasons are machine keys in the DB and sentences on screen: STOPPED_REASON covers every StoppedReason the executor can write, enforced by a drift test (Rule 35)"
    - "The research-round copy gaps are settled and recorded as a UI-SPEC amendment: google_daily_quota, places_request_rejected, places_unavailable, places_key_missing, never_started, abandoned, 'a run is already in progress', the no-geometry refusal, and 'this run stops at {price} · {n} requests' (D-18, D-19)"
    - "Host classes render as words, never colours (D-09); the signal sentence and the short label are one map each over all six classes"
    - "places-format.ts is server-safe (no client directive) and its chips read the matcher's numeric feature keys defensively"
  artifacts:
    - path: "src/lib/ui/run-tone.ts"
      exports: ["STOPPED_REASONS", "StoppedReason", "RUN_KINDS", "RunKind", "RUN_KIND_LABEL"]
    - path: "src/lib/ui/places-format.ts"
      exports: ["signalSentence", "hostClassLabel", "placesChips", "PlacesChip", "mapsUrlFor", "HOST_CLASS_ORDER"]
    - path: "src/lib/ui/copy.ts"
      contains: "STOPPED_REASON"
  key_links:
    - from: "src/lib/ui/run-tone.ts StoppedReason"
      to: "src/workflows/places-sweep/reducer.ts StopReason | FailReason"
      via: "compile-time assertion in tests/unit/ui-maps.test.ts"
      pattern: "StopReason"
    - from: "src/lib/ui/places-format.ts placesChips"
      to: "src/lib/places/match.ts PlaceFeatures keys"
      via: "city / sab / listingPhone / listingLocation read as 0|1"
      pattern: "listingLocation"
---

<objective>
Land every Phase 4 user-facing string, the run-kind and stopped-reason maps, and the Places
formatters in the server-safe UI modules, once, so the five UI plans only import.

Purpose: UI-SPEC Rule 5 (copy lives in `copy.ts`) and Rule 35 (stopped reasons are never rendered
raw). Several UI plans run in parallel later; one owner of `copy.ts` avoids a five-way merge on it.
Research open item 4 left copy gaps — this plan settles them in the UI-SPEC's tone and records them
as an amendment.

Output: `copy.ts` Phase 4 section, `run-tone.ts` unions/maps, `places-format.ts`, drift tests,
a UI-SPEC amendment section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-CONTEXT.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:unit -t "<name>"`, never `-- -t`; read the PASS-list NAME. No scratch `.ts` in the repo.
</pinned_launcher>

<interfaces>
From 04-03: `src/lib/places/host-class.ts` — `HOST_CLASSES = ['none','business_site_dead','social','directory','platform_subdomain','other'] as const; type HostClass`.
From 04-05: `src/workflows/places-sweep/reducer.ts` — `type StopReason = 'budget_cap_reached' | 'exceeded_estimate' | 'google_daily_quota'`, `type FailReason = 'places_request_rejected' | 'places_unavailable' | 'places_key_missing'`.
From 04-06 (same wave as 04-05; structural contract, do not import): `PlaceFeatures = Features & { city?: 0|1; sab?: 0|1; listingPhone?: 0|1; listingLocation?: 0|1 }` where `Features` has numeric `name, phone, address, distance, cluster, nameSim, distanceM|null`, `signals: string[]`, optional `rule`.
Existing: `src/lib/ui/run-tone.ts` L31–72 (`RUN_STATUSES`, `RUN_TONE`, `RUN_LABEL`; server-safe header L1–22);
`src/lib/ui/review-format.ts` L26–124 (`ReviewChip = { key: 'phone'|'name'|'distance'|'zip'|'cluster'; label; agrees }`, `reviewChips()` reading `features` defensively via `num()`);
`src/lib/ui/copy.ts` (L35, L106 `PHASE4_RUN_NOTICE` — 🔴 leave `PHASE4_RUN_NOTICE` in place: 04-26 retires it in the task that wires the executor, UI-SPEC Rule 38);
`src/lib/time.ts` `formatLocal`, `formatCount`; money via the existing `formatUsd`.
`tests/unit/ui-maps.test.ts` walks `src/lib/ui/` for the client-boundary token — comments in these files must not spell it.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run-tone.ts unions and maps, with the drift assertions</name>
  <files>src/lib/ui/run-tone.ts, tests/unit/ui-maps.test.ts</files>
  <read_first>
    - `src/lib/ui/run-tone.ts` (whole file)
    - `tests/unit/ui-maps.test.ts` (whole file — the existing drift rule over RUN_STATUSES)
    - 04-UI-SPEC.md Rule 35 and Open Question 19
  </read_first>
  <behavior>
    - "every stopped reason the executor can write has copy": for every member of `STOPPED_REASONS`, `STOPPED_REASON[r]` is a non-empty string that does not contain `_` (never the raw key)
    - "every run kind has a label": `RUN_KIND_LABEL` covers `full_sweep | partition | change_check` with exactly "Full sweep" · "This week's partition" · "Change check"
    - "the reducer's stop and fail reasons are stopped reasons": a compile-time assertion `type _A = Assert<StopReason | FailReason extends StoppedReason ? true : false>` plus a runtime list check
  </behavior>
  <action>
In `run-tone.ts`, beside `RUN_STATUSES`:
```ts
export const STOPPED_REASONS = ['budget_cap_reached','exceeded_estimate','google_daily_quota',
  'places_request_rejected','places_unavailable','places_key_missing','never_started','abandoned'] as const;
export type StoppedReason = (typeof STOPPED_REASONS)[number];
export const RUN_KINDS = ['full_sweep','partition','change_check'] as const;
export type RunKind = (typeof RUN_KINDS)[number];
export const RUN_KIND_LABEL: Record<RunKind, string> = { full_sweep: 'Full sweep', partition: "This week's partition", change_check: 'Change check' };
```
(`STOPPED_REASON` sentences live in `copy.ts`, Task 2; `ui-maps.test.ts` imports both.)
Add the three tests; run them red until Task 2 lands `STOPPED_REASON`, then green.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "every run kind has a label" && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const STOPPED_REASONS" src/lib/ui/run-tone.ts` matches with all eight keys
    - `grep -n "StopReason | FailReason" tests/unit/ui-maps.test.ts` matches
  </acceptance_criteria>
  <done>Run kinds and stopped reasons are typed unions with a drift test against the executor's own types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Phase 4 copy section, including the settled gaps, and the UI-SPEC amendment</name>
  <files>src/lib/ui/copy.ts, tests/unit/ui-maps.test.ts, src/components/places/google-maps-tag.tsx, .planning/phases/04-places-transient-verifier/04-UI-SPEC.md</files>
  <read_first>
    - `src/lib/ui/copy.ts` (whole file — naming style, parameterized strings as functions, e.g. `RUN_REFUSED(capMicroUsd)`)
    - 04-UI-SPEC.md § Copywriting Contract, § States (Empty, Error), § Copy Table — every row
  </read_first>
  <behavior>
    - "every stopped reason the executor can write has copy" (Task 1's test) turns green
    - "phase 4 copy matches the UI-SPEC copy table": exact pins for `GOOGLE_MAPS_TAG === 'Google Maps'`, `RUN_FULL_SWEEP === 'Run full sweep'`, `RUN_PARTITION === "Run this week's partition"`, `RUN_CHECK_CHANGES === 'Check for changes (free)'`, `REVIEW_ACTION_NOT_THIS === 'Not this business'`, `REJECT_CONFIRM === 'Never attach this listing'`, `DETACH_CONFIRM === 'Detach this listing'`, `SOURCES_TRANSIENT_TITLE === 'Google Places (transient)'`, `RUN_REPORT_TITLE === 'Run report'`, `SECOND_WALL_SET_BADGE === 'Set'`, and `RUN_TRUNCATION_HEADING(1)` starts with `'1 tile still hit'` while `RUN_TRUNCATION_HEADING(3)` starts with `'3 tiles still hit'`
  </behavior>
  <action>
1. Append a `// ─── Phase 4 (04-UI-SPEC § Copy Table) ───` section to `copy.ts` containing EVERY row of the
   UI-SPEC Copy Table and the § States Empty/Error tables, verbatim, named by screen prefix:
   `GOOGLE_MAPS_TAG`, `RUN_REPORT_*`, `RUN_*` (actions, confirms, kind/cost lines), `PRESET_*`,
   `PLACES_MODE_NOTICE_OFF_*` / `PLACES_MODE_NOTICE_IDS_ONLY_*`, `RUN_DRAWER_*`, `REVIEW_GOOGLE_*`,
   `REVIEW_FILTER_*`, `REJECT_*`, `BUSINESS_GOOGLE_*`, `DETACH_*`, `SOURCES_TRANSIENT_*`,
   `SECOND_WALL_SET_*`, `TOAST_*`. Every `{placeholder}` becomes a function parameter; money through
   `formatUsd`, counts through `formatCount`, instants through `formatLocal` — no `Intl` here.
2. `export const STOPPED_REASON: Record<StoppedReason, string>` — the short sentence (recent runs,
   `/spend` By-run sub-line):
   - `budget_cap_reached`: "Stopped at the monthly cap — nothing past it was charged."
   - `exceeded_estimate`: "Stopped at twice the estimate."
   - `google_daily_quota`: "Stopped at Google's daily limit — the rest can run after it resets."
   - `places_request_rejected`: "Google rejected a request, so the run stopped."
   - `places_unavailable`: "Google Places didn't answer, so the run stopped."
   - `places_key_missing`: "No Google Places key is set for this deployment."
   - `never_started`: "Never started — its budget hold was released."
   - `abandoned`: "Stopped reporting progress and was marked abandoned."
3. The run-report alert copy for the reasons the UI-SPEC did not write (the gaps):
   - `RUN_STOP_DAILY_QUOTA(quota, n, m)`: "Stopped at Google's daily limit of {quota} requests. Google refused the next request, so nothing past the limit was charged. The {n} tiles already searched are complete; the {m} not searched yet can run tomorrow — Google's daily quota resets at midnight Pacific time." (warning; action "Open spend view")
   - `RUN_FAILED_ERROR: Record<'places_request_rejected'|'places_unavailable'|'places_key_missing', string>` — the `{error}` clause inside the inherited failed alert: "Google rejected a request as malformed (HTTP 400)" · "Google Places didn't answer after retrying" · "no Google Places API key is set for this deployment, so nothing was sent".
   - `RUN_NEVER_STARTED(presetName)`: "This run never started. The workflow didn't pick it up within 15 minutes, so Siteless released its budget hold — nothing was charged. Start it again from its preset." → "Open {preset name}"
   - `RUN_ABANDONED(cost, presetName)`: "This run stopped reporting progress for 30 minutes, so Siteless marked it abandoned. Every request that left was reserved and ledgered first, so {cost} above is exactly what it cost. Start the run again from its preset; the tiles it reached are listed below." → "Open {preset name}"
   - `RUN_ALREADY_IN_PROGRESS`: "Another run is already in progress for this organization. Siteless runs one at a time so two sweeps can't race for the same budget. Nothing was reserved and nothing was charged." → `RUN_OPEN_RUNNING` "Open the running run"
   - `RUN_NO_GEOMETRY(unitName)`: "Siteless has no map outline for {unit}, so it can't tile it for Google Places. The 17 seeded RGV cities, the four RGV counties and radius searches can run. Nothing was reserved and nothing was charged."
   - `RUN_ESTIMATE_LINE(lo, hi, ceilingUsd, ceilingRequests)`: "Estimated {lo}–{hi} · this run stops at {ceilingUsd} · {n} requests" (D-18: the ceiling is enforced on requests; the dollar figure can be $0.00 inside the free allowance, so both are shown).
4. Handoff from 04-08: `src/components/places/google-maps-tag.tsx` carries the literal `'Google Maps'`
   with a comment naming this plan. Replace it with `import { GOOGLE_MAPS_TAG } from '@/lib/ui/copy';`
   and `{GOOGLE_MAPS_TAG}`, and remove the comment. `$PNPM test:unit -t "the google maps tag is exact text with translate=no"` must stay green.
5. Append to `04-UI-SPEC.md`, before § Checker Sign-Off, a section
   `## Amendment 1 — copy gaps settled at planning (2026-09-23)` listing each string above with its
   tone (warning / destructive / muted) and the line: "Settles 04-RESEARCH Open Question 4. Same
   voice rules: name the number, say what happened to the money and the data, end with a way out."
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "every stopped reason the executor can write has copy" && $PNPM test:unit -t "phase 4 copy matches the UI-SPEC copy table" && $PNPM test:unit tests/unit/ui-maps.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export const STOPPED_REASON: Record<StoppedReason, string>" src/lib/ui/copy.ts` matches
    - `grep -n "## Amendment 1" .planning/phases/04-places-transient-verifier/04-UI-SPEC.md` matches
    - `grep -n "PHASE4_RUN_NOTICE" src/lib/ui/copy.ts` still matches (retired in 04-26, not here)
    - PASS list names both new tests
  </acceptance_criteria>
  <done>All Phase 4 copy exists in one server-safe module, the gaps are settled and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: places-format.ts — signal sentences, host-class labels, Google chips, Maps URL</name>
  <files>src/lib/ui/places-format.ts, tests/unit/places-format.test.ts</files>
  <read_first>
    - `src/lib/ui/review-format.ts` (whole file — the defensive `num()` reader and chip shape)
    - 04-UI-SPEC.md § Screen 3 (Google listing card, "Open this listing on Google Maps" href), § Screen 5 (signal sentences), Rule 30
  </read_first>
  <behavior>
    - "signal sentence for every host class": `signalSentence('none') === 'No website listed'`, `'business_site_dead'` → "Website listed — a dead Google site (business.site)", `'social'` → "Website listed — a social page, not a website", `'directory'` → "Website listed — a directory page, not a website", `'platform_subdomain'` → "Website listed — a site-builder subdomain", `'other'` → "Website listed — own domain, not checked yet"
    - "host class labels are fixed and ordered": `HOST_CLASS_ORDER` = `['other','social','directory','platform_subdomain','business_site_dead']` with labels "Own website (not yet checked)" · "Social page" · "Directory page" · "Site-builder subdomain" · "Dead Google site (business.site)"
    - "google chips read the numeric features": `{ sab: 1, city: 1, listingPhone: 0, listingLocation: 0, nameSim: 0.84, name: 32, phone: 0 }` → chips include `service-area business`, `city match`, `no phone on the listing`, `no location on the listing`, `name 0.84`; a malformed `features` (string, null, `{ nameSim: 'x' }`) returns `[]` or drops the bad chip and never throws
    - "the maps link is built from the place id and our own names only": `mapsUrlFor('ChIJabc', 'Ortiz & Sons', 'McAllen')` === `'https://www.google.com/maps/search/?api=1&query=Ortiz%20%26%20Sons%2C%20McAllen&query_place_id=ChIJabc'`
  </behavior>
  <action>
Implement per the behavior block. `PlacesChip = { key: 'phone' | 'name' | 'distance' | 'zip' | 'cluster' | 'city' | 'sab' | 'no_listing_phone' | 'no_listing_location'; label: string; agrees: boolean }`.
Chip labels come from `copy.ts` (Task 2): "city match", "service-area business", "no phone on the listing",
"no location on the listing"; the inherited ones reuse `review-format.ts`'s label helpers.
`mapsUrlFor` encodes every component with `encodeURIComponent`; it never takes Google text as input
(Rule 30 — the name and city are the SPINE's). No client directive, no `server-only`, no `Intl`.
Header comment: "Every file that imports this module must also import GoogleMapsTag (Rule 28; enforced by 04-28's repo walk)."
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "signal sentence for every host class" && $PNPM test:unit -t "google chips read the numeric features" && $PNPM test:unit -t "the maps link is built from the place id" && $PNPM test:unit tests/unit/ui-maps.test.ts && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all four places-format tests
    - `grep -n "encodeURIComponent" src/lib/ui/places-format.ts` matches
    - `ui-maps.test.ts`'s client-directive walk over `src/lib/ui/` stays green
  </acceptance_criteria>
  <done>Places-derived values have one formatter each, server-safe and defensive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB machine keys → screen | `runs.stopped_reason` and `host_class` become sentences here |
| stored `features` jsonb → chips | tenant-scoped JSON rendered as text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-11 | Tampering | `mapsUrlFor` | mitigate | built only from the stored place_id + the spine's own name/city, every component `encodeURIComponent`-ed; fixed origin |
| T-4-05 | Information disclosure (legal) | formatters | mitigate | inputs are enums, numbers and spine fields only; no formatter accepts Google text (Rule 30) |
| T-4-13 | Repudiation / legal | attribution | mitigate | module header binds importers to GoogleMapsTag; 04-28's repo walk enforces it |
</threat_model>

<verification>
- `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint` green
</verification>

<success_criteria>
- Every Phase 4 string and map exists once, with drift tests; the copy gaps are settled in the UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-07-SUMMARY.md`
</output>
