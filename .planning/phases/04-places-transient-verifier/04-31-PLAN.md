---
phase: 04-places-transient-verifier
plan: 31
type: execute
wave: 11
depends_on: [04-19, 04-29, 04-30]
files_modified:
  - docs/runbooks/google-quota.md
  - src/lib/budget/second-wall.ts
  - src/components/budget/second-wall-card.tsx
  - tests/unit/second-wall-card.test.tsx
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [BUDG-03]

must_haves:
  truths:
    - "danlo has created the Google Cloud project, enabled Places API (New), attached billing, set Places API (New) → Requests per day = 100, and created a key restricted to Places API (New) (D-03)"
    - "The key exists in .env.local and in Vercel Production under GOOGLE_PLACES_API_KEY, names only in .env.example, never NEXT_PUBLIC_, and PLACES_MODE in production is still off (D-02, D-03)"
    - "Claude verifies the key with ONE free IDs-only call made through the product's own meter and client (reserved, ledgered at $0 with units 1) — never a raw curl that bypasses criterion 5 (D-03)"
    - "BUDG-03 is closed: the second-wall card reads 'Google Cloud daily quota — set' with the Set badge, REQUIREMENTS.md marks BUDG-03 complete (Phase 4), and STATE.md drops the blocker (UI-SPEC § 6)"
  artifacts:
    - path: "src/lib/budget/second-wall.ts"
      exports: ["GOOGLE_QUOTA_SET_ON", "GOOGLE_QUOTA_REQUESTS_PER_DAY"]
    - path: "docs/runbooks/google-quota.md"
      contains: "Places API (New)"
  key_links:
    - from: "src/components/budget/second-wall-card.tsx"
      to: "src/lib/budget/second-wall.ts GOOGLE_QUOTA_SET_ON"
      via: "set state when non-null (static copy recorded by the checkpoint — the app cannot read GCP)"
      pattern: "GOOGLE_QUOTA_SET_ON"
---

<objective>
The D-03 human-action checkpoint: danlo sets up Google Cloud from the runbook; Claude verifies the key
with one free, metered IDs-only call and closes BUDG-03 (carried from Phase 2).

Purpose: the per-API daily quota is the independent second wall under Siteless's own meter
(PROJECT.md § Constraints → Budget). This is also the first real Google call of the project — which is
why 04-29 (D-01) precedes it.

Output: runbook steps, the second-wall "set" state, BUDG-03 closed, one verified free call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/runbooks/google-quota.md
@docs/runbooks/places.md
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
🔴 Never print, echo, commit or paste the key. Check for its PRESENCE only (exit codes). Production `PLACES_MODE` is NOT changed by this plan.
🔴 Abort this plan if 04-29 recorded `no`.
</pinned_launcher>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Runbook steps and the second-wall card's "set" state (constant still null)</name>
  <files>docs/runbooks/google-quota.md, src/lib/budget/second-wall.ts, src/components/budget/second-wall-card.tsx, tests/unit/second-wall-card.test.tsx</files>
  <read_first>
    - `docs/runbooks/google-quota.md` (the 100/day value, derivation and console path already there), `docs/runbooks/places.md` (04-17)
    - `src/components/budget/second-wall-card.tsx` (whole — inline strings, `text-xs` at L46), `src/components/flags/flag-badge.tsx` L17
    - 04-UI-SPEC.md § Screen 6, Rule 41
  </read_first>
  <behavior>
    - "the second wall reads not set while the date is null" — `Needs danlo` badge (now via copy + `FLAG_BADGE_SIZING`), unchanged meaning
    - "the second wall reads set once the checkpoint records a date" — with `GOOGLE_QUOTA_SET_ON = '2026-09-2x'`: title "Google Cloud daily quota — set", a `secondary` `Set` badge sized with `FLAG_BADGE_SIZING`, the body "Places API (New) → Requests per day = 100, set on {date}…", the console link unchanged
    - "the second wall card spells no inline strings" — its source contains no string literal longer than 3 words (all copy from `copy.ts`)
  </behavior>
  <action>
1. `google-quota.md` gains a "D-03 — first-time setup (danlo)" section: (1) create the Google Cloud project; (2) enable **Places API (New)**
   (not the legacy Places API); (3) attach billing; (4) Quotas → Places API (New) → **Requests per day = 100** (existing derivation);
   (5) Credentials → create API key → **API restrictions: Places API (New) only**; Application restrictions: **None** — Vercel functions
   have no fixed egress IP (so an IP restriction would break production) — the compensating controls are server-only use, the one
   sanctioned reader, the 100/day quota and the meter; (6) put the key in `.env.local` and Vercel **Production** as `GOOGLE_PLACES_API_KEY`
   (Preview only if danlo decides); never `NEXT_PUBLIC_`; (7) rotation: create a new key, swap the env var, redeploy, delete the old key.
   Also: the quota counts IDs-only requests too (research A3), so a change check spends quota even though it costs $0.
2. `src/lib/budget/second-wall.ts`: `export const GOOGLE_QUOTA_REQUESTS_PER_DAY = 100; export const GOOGLE_QUOTA_SET_ON: string | null = null;` with a header:
   "Static copy recorded by the D-03 checkpoint (04-31). The app cannot read Google Cloud and must not claim to."
3. `second-wall-card.tsx`: strings from `copy.ts` (`SECOND_WALL_*`, `SECOND_WALL_SET_*`); `text-xs` → `FLAG_BADGE_SIZING`; the set state when
   `GOOGLE_QUOTA_SET_ON !== null`. Tests per the behavior block (mock the constant module).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the second wall" && $PNPM typecheck && $PNPM lint</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names the three tests; `grep -n "text-xs" src/components/budget/second-wall-card.tsx` returns nothing
    - `grep -n "Application restrictions" docs/runbooks/google-quota.md` matches
  </acceptance_criteria>
  <done>The runbook is ready for danlo and the card can show "set" once a date is recorded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: danlo sets up Google Cloud and stores the key</name>
  <files>docs/runbooks/google-quota.md</files>
  <read_first>
    - `docs/runbooks/google-quota.md` § D-03 (Task 1)
  </read_first>
  <action>danlo performs D-03 steps 1–6 in the Google Cloud console and Vercel dashboard. Claude cannot: there is no gcloud CLI here and the console steps need danlo's Google account and billing.</action>
  <instructions>
1. Follow `docs/runbooks/google-quota.md` § "D-03 — first-time setup" steps 1–6.
2. Put the key in `.env.local` as `GOOGLE_PLACES_API_KEY=…` and in Vercel → Project → Settings → Environment Variables → Production. Do NOT set `PLACES_MODE` in Vercel.
3. Reply with: the date you set the quota, the quota value you see (must be 100), and "API restriction = Places API (New) only". Do not paste the key.
  </instructions>
  <verification>Claude checks (Task 3) the variable NAMES exist locally and in Vercel Production, then makes one free metered IDs-only call.</verification>
  <acceptance_criteria>
    - danlo replied with the date, "100", and the API restriction confirmation
  </acceptance_criteria>
  <resume-signal>Reply `done <YYYY-MM-DD> quota=100 restricted=places-api-new`</resume-signal>
  <verify>Human checkpoint — the reply is recorded verbatim in the SUMMARY; no automated command applies.</verify>
  <done>danlo's reply is recorded and the plan proceeds (or stops) exactly as it directs.</done>
</task>

<task type="auto">
  <name>Task 3: One free metered IDs-only call, then close BUDG-03 and deploy the card</name>
  <files>src/lib/budget/second-wall.ts, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - `scripts/record-places-fixtures.ts` (04-19 — the `--ids-only` single-call mode), `docs/deploy.md` § Phase 4
  </read_first>
  <action>
1. Presence only: `node -e "require('dotenv').config({path:'.env.local'});process.exit(process.env.GOOGLE_PLACES_API_KEY?0:1)"` → exit 0;
   `vercel env ls production` lists `GOOGLE_PLACES_API_KEY` and still does NOT list `PLACES_MODE`.
2. The verification call, LOCAL database, through the product's own meter and client:
   `$PNPM record:places --ids-only --max-requests=1 --version=<a local McAllen × home_services version id> --type=plumber --unit=city:48215/McAllen`
   (the recorder refuses without the D-01 row and against any non-local target). Record from the local DB: one `cost_ledger` row
   `sku 'ts_essentials', micro_usd 0, units 1` for that run, and the script's reported HTTP outcome `ok` with N ids. If the outcome is `rejected`
   or `no_key`, stop and report (key/API restriction wrong). No fixture is written in `--ids-only` mode unless `--out` is given.
3. `GOOGLE_QUOTA_SET_ON = '<danlo's date>'`; `$PNPM test:unit -t "the second wall"` green.
4. `REQUIREMENTS.md`: BUDG-03 → `[x]`, and its traceability row → `Phase 4 | Complete`. `STATE.md`: remove both BUDG-03 blocker lines; add
   "BUDG-03 closed <date> (04-31): 100/day quota set by danlo; key API-restricted to Places API (New); verified by one metered IDs-only call".
5. Deploy (production `PLACES_MODE` still unset): build log names the sha; `/api/health` 200.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:unit -t "the second wall" && grep -n "\[x\] \*\*BUDG-03\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The local ledger row for the verification call is recorded in the SUMMARY (sku, micro_usd 0, units 1)
    - `grep -n "BUDG-03 | Phase 4 | Complete\|BUDG-03.*Complete" .planning/REQUIREMENTS.md` matches
    - `vercel env ls production` shows `GOOGLE_PLACES_API_KEY` and not `PLACES_MODE`; the deployed build names the sha
    - No command output in the transcript contains the key
  </acceptance_criteria>
  <done>The second wall exists, is recorded in the product, and the key is proven with one free metered call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| danlo's GCP account → Vercel env / .env.local | the key crosses here, by danlo's hand |
| desk → places.googleapis.com | the project's first real Google call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Information disclosure | the key | mitigate | danlo stores it; Claude checks presence by exit code only; API-restricted to Places API (New); server-only; one sanctioned reader |
| T-4-02 | Denial of service (financial) | spend beyond the meter | mitigate | 100/day quota as the independent second wall (BUDG-03); verification call is IDs-only ($0) and metered |
| T-4-05 | Information disclosure (legal) | verification call | mitigate | gated by the D-01 row (recorder guard); IDs-only mask returns place ids only |
</threat_model>

<verification>
- Key presence (names only), one ledgered $0 call, BUDG-03 closed in REQUIREMENTS/STATE, the card deployed
</verification>

<success_criteria>
- BUDG-03 is satisfied and visible; the key works; production still cannot call Google until 04-32 flips the mode
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-31-SUMMARY.md`
</output>
