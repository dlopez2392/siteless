---
phase: 04-places-transient-verifier
plan: 21
type: execute
wave: 4
depends_on: [04-07, 04-09, 04-15]
files_modified:
  - src/server/actions/record-listing-decision.ts
  - src/server/actions/detach-listing.ts
  - src/server/queries/review-queue.ts
  - src/server/queries/businesses.ts
  - tests/db/listing-actions.test.ts
  - tests/db/review-queue-google.test.ts
  - tests/unit/business-detail.test.tsx
autonomous: true
requirements: [PLACE-02]

must_haves:
  truths:
    - "Tentative Google listings join the existing review queue as a second item kind in ONE score-ordered queue, filterable all / duplicates / google (D-05, UI-SPEC OQ 3)"
    - "'Same business' confirms a tentative listing (attached, reason confirmed); 'Not this business' rejects it so it never re-attaches; a detach turns an attached listing into a rejected one — each through app.decide_place_attachment, with the actor from Clerk claims (D-05, UI OQ 6)"
    - "An already-decided listing is a conflict result, another org's listing is not-found, and requireOrg() is the first statement of both actions (T-3-10 pattern)"
    - "A Google queue item carries the spine side, the score, the numeric features and, for a tie, the other business's display name and score — and nothing Google-authored (Rule 30)"
    - "The business detail read returns the business-level signal (attached-only, D-08), every listing with its status and latest observation, and the observation history — and never a coordinate (Rule 32)"
  artifacts:
    - path: "src/server/actions/record-listing-decision.ts"
      exports: ["recordListingDecision"]
    - path: "src/server/actions/detach-listing.ts"
      exports: ["detachListing"]
    - path: "src/server/queries/review-queue.ts"
      exports: ["ReviewFilter", "GoogleListingView", "ReviewQueue", "readReviewQueue", "listReviewQueue"]
    - path: "src/server/queries/businesses.ts"
      exports: ["GoogleCheckView", "readGoogleCheck"]
  key_links:
    - from: "src/server/actions/record-listing-decision.ts"
      to: "app.decide_place_attachment"
      via: "withOrg(orgClaims()) → select * from app.decide_place_attachment($1, 'confirm'|'reject')"
      pattern: "decide_place_attachment"
    - from: "src/server/queries/businesses.ts readGoogleCheck"
      to: "business_place_signal view + place_attachments + place_observations"
      via: "inside getBusinessDetail's single withOrg"
      pattern: "business_place_signal"
---

<objective>
The server side of the Google listing review and detach (D-05, D-08; UI-SPEC OQ 3 and 6): two
actions, the review queue's Google item kind, and the business-detail Google check read.

Purpose: a tentative listing is excluded from every verdict until a human confirms it, and a wrong
auto-attach needs a human reversal path. Both screens (04-24 review, 04-25 business) consume these.

Output: two actions, two query extensions, two DB test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-UI-SPEC.md
@.planning/phases/04-places-transient-verifier/04-PATTERNS.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:db -t "<name>"`, never `-- -t`; read the PASS-list NAME.
</pinned_launcher>

<interfaces>
`src/server/actions/record-review-decision.ts` (whole file is the analog): `await requireOrg(); const claims = await orgClaims();`
zod `safeParse` → `fail('validation', …)`; `withOrg(claims, …)`; `pgFailure(err)` → `42501` → `fail('not_found', NOT_FOUND('pair'))`,
`55000` → `fail('conflict', …, { reason: 'already_decided' })`; `revalidatePath('/review')`, `revalidatePath('/businesses')`; `ok({...})`.
`src/server/actions/unmerge-business.ts` — the detach analog. `src/server/actions/_result.ts`, `_pg.ts`.
`tests/unit/server-actions-guard.test.ts` walks `src/server/actions/` and requires `requireOrg()` first.
`src/server/queries/review-queue.ts`: `ReviewQueue = { top: CandidatePairView | null; remaining: number; ingested: … }`,
`readReviewQueue(tx)`, `listReviewQueue(claims)` (one `withOrg`), `CandidateSideView` (the spine side reader), `REVIEW_BAND_FLOOR = 80`.
`src/server/queries/businesses.ts`: `readBusinessDetail(tx, id)` (L457), `getBusinessDetail(claims, id)` (L695, one `withOrg`).
04-15: `app.decide_place_attachment(p_attachment uuid, p_decision text) returns table (business_id uuid, place_id text, status text)` — `22023` bad decision, `42501` foreign, `55000` already decided.
04-09: `business_place_signal (org_id, business_id, had_website_uri, host_class, observed_at, listings)`.
From 04-07 copy: `NOT_FOUND`, `REVIEW_GOOGLE_DECISION_FAILED`, `REVIEW_GOOGLE_ALREADY_DECIDED`, `DETACH_FAILED`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: recordListingDecision and detachListing</name>
  <files>src/server/actions/record-listing-decision.ts, src/server/actions/detach-listing.ts, tests/db/listing-actions.test.ts</files>
  <read_first>
    - `src/server/actions/record-review-decision.ts`, `src/server/actions/unmerge-business.ts` (whole files)
    - `tests/db/review-actions.test.ts` (whole file — the `vi.doMock('@/db/with-org')` savepoint double and the `requireOrg`/`orgClaims` mocks)
  </read_first>
  <behavior>
    - `same business confirms a tentative listing` — `recordListingDecision({ attachmentId, decision: 'attached' })` → ok; row `attached/confirmed`; `decided_by = 'user_reviewer_A'`
    - `not this business rejects a tentative listing` — `decision: 'rejected'` → `rejected/rejected`
    - `skip records nothing` — `decision: 'skip'` → ok, row unchanged
    - `a listing already decided elsewhere is a conflict` — second decision → `{ ok:false, code:'conflict', detail: { reason:'already_decided' } }`
    - `another org's listing is not found` — `CLAIMS_B` → `code 'not_found'`
    - `a malformed listing id is a validation failure` — `attachmentId: 'x'` → `code 'validation'`, no DB call
    - `detach rejects an attached listing and drops it from the signal` — `detachListing({ attachmentId })` → `rejected/detached`; `business_place_signal` loses the row when it was the only attached listing
    - `detach refuses a tentative listing` — `55000` → `conflict`
  </behavior>
  <action>
`record-listing-decision.ts` (`'use server'`): input `z.strictObject({ attachmentId: z.uuid(), decision: z.enum(['attached','rejected','skip']) })`;
`skip` → `ok({ remaining })` without a write; otherwise `select business_id, place_id, status from app.decide_place_attachment(${id}, ${decision === 'attached' ? 'confirm' : 'reject'})`;
map `42501` → `not_found`, `55000` → `conflict` (`REVIEW_GOOGLE_ALREADY_DECIDED`, `{ reason: 'already_decided' }`), else `unexpected` (`REVIEW_GOOGLE_DECISION_FAILED`);
`revalidatePath('/review')`, `revalidatePath(\`/businesses/${businessId}\`)`; return `ok({ remaining, businessName })` where `businessName` is the spine `display_name` (for the toast — never Google text).
`detach-listing.ts` (`'use server'`): input `z.strictObject({ attachmentId: z.uuid() })` → `decide_place_attachment(id, 'detach')`; same mapping (`DETACH_FAILED`); revalidates the business and `/review`.
Both: `await requireOrg()` first, then `orgClaims()` (never hand-built claims).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "confirms a tentative listing" && $PNPM test:db -t "a listing already decided elsewhere is a conflict" && $PNPM test:db -t "detach" && $PNPM test:unit tests/unit/server-actions-guard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names all eight tests; `server-actions-guard` green over the two new files
    - `grep -n "await requireOrg()" src/server/actions/record-listing-decision.ts src/server/actions/detach-listing.ts` matches in both as the first statement of the exported function
  </acceptance_criteria>
  <done>Both human decisions exist as guarded actions over the one definer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The Google item kind in the review queue</name>
  <files>src/server/queries/review-queue.ts, tests/db/review-queue-google.test.ts</files>
  <read_first>
    - `src/server/queries/review-queue.ts` (whole file) and every test importing it (`grep -rln "review-queue" tests`)
    - 04-UI-SPEC.md § Screen 3 (remaining counts, filter, tie reason)
  </read_first>
  <behavior>
    - `the review queue orders both kinds by score` — a pending pair at 90 and a tentative listing at 92 → `top.kind === 'google'`; with filter `duplicates` → `pair`; with `google` → `google`
    - `the review queue counts both kinds` — `counts = { pairs: 1, google: 1 }`, `remaining` = the filtered total
    - `the google item carries the tie partner's name and score` — a tie row → `tie = { businessId, displayName: 'Rio Roofing Co', score }`
    - `the google item carries the spine side and numeric features only` — `business` is a `CandidateSideView` of the spine business; `features` is the stored object; no field named like `websiteUri`, `formattedAddress`, `nationalPhoneNumber`
    - `existing pair behaviour is unchanged` — the Phase 3 queue tests still pass with `top.kind === 'pair'` added
  </behavior>
  <action>
`export type ReviewFilter = 'all' | 'duplicates' | 'google';`
`export type GoogleListingView = { kind: 'google'; attachmentId: string; placeId: string; score: number; reason: 'score' | 'tie'; features: unknown; business: CandidateSideView; tie: { businessId: string; displayName: string; score: number } | null };`
`ReviewQueue` becomes `{ top: (CandidatePairView & { kind: 'pair' }) | GoogleListingView | null; remaining: number; counts: { pairs: number; google: number }; ingested: … }`.
`readReviewQueue(tx, filter = 'all')`: the top candidate is the higher of (best pending pair, best `tentative` attachment ≥ `REVIEW_BAND_FLOOR`) by score, tie-broken by kind (`pair` first) then id; filtered kinds are skipped.
`listReviewQueue(claims, filter = 'all')` keeps ONE `withOrg`. Update every existing test expectation that deep-equals `top` to include `kind: 'pair'`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "the review queue" && $PNPM test:db -t "the google item" && $PNPM test:db tests/db/review-actions.test.ts && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names the five tests; Phase 3 review tests stay green
    - `grep -n "export type ReviewFilter" src/server/queries/review-queue.ts` matches
  </acceptance_criteria>
  <done>One queue, two kinds, one ordering, filterable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The business-detail Google check read</name>
  <files>src/server/queries/businesses.ts, tests/db/listing-actions.test.ts, tests/unit/business-detail.test.tsx</files>
  <read_first>
    - `src/server/queries/businesses.ts` L279–460 and L695–710 (`BusinessDetail`, `readBusinessDetail`, `getBusinessDetail`)
    - 04-UI-SPEC.md § Screen 5
  </read_first>
  <behavior>
    - `the google check reads the business signal and every listing` — one attached (latest obs `none`), one tentative, one rejected → `signal = { hadWebsiteUri:false, hostClass:'none', observedMs }`; `listings` ordered attached (newest observation first) → tentative → rejected/detached, each with `status`, `reason`, `score`, `decidedBy`, `decidedMs`, and `latest` (null for tentative-only display rules is the UI's job — the read returns what exists)
    - `the google check history is newest first and names its run` — two observations → two history rows with `runId`, newest first
    - `the google check never selects coordinates` — `JSON.stringify(view)` has no `lat`/`lng` keys, and the SQL text contains no `place_coordinates`
    - `a business with no listing has an empty google check` — `signal null`, `listings []`, `history []`
  </behavior>
  <action>
`export type GoogleCheckView = { signal: { hadWebsiteUri: boolean; hostClass: HostClass; observedMs: number } | null;
  listings: Array<{ attachmentId: string; placeId: string; status: 'attached' | 'tentative' | 'rejected'; reason: string; score: number;
    tieBusinessId: string | null; decidedBy: string | null; decidedMs: number | null;
    latest: { hadWebsiteUri: boolean; hostClass: HostClass; observedMs: number; runId: string } | null }>;
  history: Array<{ observationId: string; placeId: string; observedMs: number; hadWebsiteUri: boolean; hostClass: HostClass; runId: string }> };`
`readGoogleCheck(tx, businessId)` reads `business_place_signal`, `place_attachments` and `place_observations` only. `BusinessDetail` gains
`google: GoogleCheckView`, filled inside `readBusinessDetail` (so `getBusinessDetail` stays ONE `withOrg`).
Every existing `BusinessDetail` fixture (`grep -rn "BusinessDetail" tests/unit`) gains
`google: { signal: null, listings: [], history: [] }` so `next build` (which type-checks tests) stays green.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && $PNPM test:db -t "the google check" && $PNPM test:db tests/db/provenance-render.test.ts && $PNPM typecheck</automated>
  </verify>
  <acceptance_criteria>
    - PASS list names the four google-check tests; `provenance-render` (the business-detail DB test) stays green
    - `grep -n "place_coordinates" src/server/queries/businesses.ts` returns nothing
  </acceptance_criteria>
  <done>The business screen can render the Places signal and its history without touching coordinates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → server actions | attachment ids and decisions are untrusted input |
| tenant session → review/business reads | RLS-scoped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-06 | Tampering (cross-tenant) | listing actions | mitigate | `requireOrg()` first; claims from `orgClaims()`; definer re-reads under the org (42501 → not_found); zod `z.uuid()` + enum |
| T-4-06 | Repudiation | decisions | mitigate | `decided_by` = Clerk `sub` via the definer; `log_event` row per status change |
| T-4-04 | Information disclosure (legal retention) | business read | mitigate | never selects `place_coordinates` (named test) |
| T-4-05 | Information disclosure (legal) | queue item | mitigate | carries spine fields + numeric features only (named test) |
</threat_model>

<verification>
- `$PNPM test:db`, `$PNPM test:unit`, `$PNPM typecheck`, `$PNPM lint` green
</verification>

<success_criteria>
- Tentative listings are reviewable, reversible and tenant-safe on the server side
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-21-SUMMARY.md`
</output>
