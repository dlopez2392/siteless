---
phase: 04-places-transient-verifier
plan: 33
type: execute
wave: 13
depends_on: [04-28, 04-32]
files_modified:
  - docs/measurements/04-gate-mutations.md
  - docs/measurements/04-screens/README.md
  - .planning/phases/04-places-transient-verifier/04-VALIDATION.md
autonomous: false
requirements: [PLACE-01, PLACE-02, PLACE-03, PLACE-04, PLACE-05, PLACE-06, BUDG-03]

must_haves:
  truths:
    - "Every gate mutation M26–M53 was applied to the live code or database, turned EXACTLY its named test red (the name read, not the exit code), was reverted, and the tree/catalog diffed back clean — logged one row each"
    - "The five Phase 4 screens were screenshotted on the built app in both themes (run report running and partial; preset detail in off, ids_only and enterprise; a Google review item; the business Google card; the /sources transient card) and danlo approved them (UI-SPEC § Theme, Rule 8)"
    - "04-VALIDATION.md is closed: per-task map filled, nyquist_compliant true, sign-off recorded"
  artifacts:
    - path: "docs/measurements/04-gate-mutations.md"
      provides: "M26–M53, one row each: mutation, target, command, red test name, revert proof"
      contains: "M53"
  key_links:
    - from: "docs/measurements/04-gate-mutations.md"
      to: "04-RESEARCH.md § Gate mutations"
      via: "the same IDs and target test names"
      pattern: "M26"
---

<objective>
The phase gate: run the twenty-eight gate mutations, review both-theme screenshots of the real screens,
and close VALIDATION — the Phase 3 closing pattern (03-22).

Purpose: "A green suite proves nothing a mutation check hasn't." Each guard in this phase — the SAB flag,
the mode gate, reserve-before-call, the ceiling, the retention barrier, the purge's role, sticky rejects,
tie handling, attribution, the step-return scan — is proven to be the thing that turns its test red.

Output: `docs/measurements/04-gate-mutations.md`, `docs/measurements/04-screens/`, a closed `04-VALIDATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-places-transient-verifier/04-RESEARCH.md
@.planning/phases/04-places-transient-verifier/04-VALIDATION.md
@docs/measurements/03-gate-mutations.md
</context>

<pinned_launcher>
Every shell command starts with `cd /c/Users/danlo/prospector &&`. `$PNPM` =
`node /c/Users/danlo/AppData/Local/pnpm/store/v11/links/@/pnpm/12.5.1/2097cbb8d3fdcf63cc8ef1bd7c2ab483d4f3ab1ae34974b1097f9a70a69a4422/node_modules/pnpm/bin/pnpm.mjs`.
`$PNPM test:<lane> -t "<name>"`, never `-- -t`. 🔴 Read the failing test's NAME on every run — a `-t` that matches nothing exits 0.
DB mutations are applied to the LOCAL test database only (never production), reverted, and verified from `pg_get_functiondef` /
`information_schema.role_table_grants` / `pg_constraint`, not from "the script ran". Code mutations end with `git diff --stat` empty.
</pinned_launcher>

<tasks>

<task type="auto">
  <name>Task 1: Run M26–M53 and log them</name>
  <files>docs/measurements/04-gate-mutations.md</files>
  <read_first>
    - 04-RESEARCH.md § Gate mutations (the table: ID, mutation, test that must turn red)
    - `docs/measurements/03-gate-mutations.md` (the log format)
    - The SUMMARYs of 04-02 … 04-28 (mutations already run once there — re-run each here for one consolidated log)
  </read_first>
  <action>
For each row M26…M53 of 04-RESEARCH § Gate mutations: apply the mutation exactly as written; run the lane and filter that holds the named test
(unit / db / workflow); confirm the red run NAMES that test (and, where the row says so, only that one); revert; prove the revert (`git diff --stat`
empty, or the catalog read for DB mutations). Targets, by plan: M26/M27/M49/M51 (04-12), M28/M29/M34/M46 (04-16), M30/M31 (04-05), M32/M36 (04-18),
M33/M45/M52 (04-22), M35/M47 (04-03), M37 (04-09), M38/M39/M53 (04-11), M40 (04-15), M41/M42 (04-06), M43 (04-09), M44 (04-28), M48 (04-04), M50 (04-17).
Where a mutation SURVIVES (suite stays green): do not "strengthen the test" by reflex — first ask what else is already doing the job (the lesson
from Phase 3's BOM strip), record the answer, and propose either removing the dead guard or a test that isolates it. Log every row as
`| M# | mutation | file / object | command | red test NAME (or SURVIVED + analysis) | revert proof |`.
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && grep -c "^| M[2-5][0-9]" docs/measurements/04-gate-mutations.md && git diff --stat -- src drizzle</automated>
  </verify>
  <acceptance_criteria>
    - 28 rows (M26…M53), each with a red test NAME or a SURVIVED analysis
    - `git diff --stat -- src drizzle` is empty after the task
    - DB mutations carry a catalog read proving the revert
  </acceptance_criteria>
  <done>Every guard in the phase is proven to be load-bearing, or its redundancy is explained.</done>
</task>

<task type="auto">
  <name>Task 2: Both-theme screenshots of the real screens on the built app</name>
  <files>docs/measurements/04-screens/README.md</files>
  <read_first>
    - 04-UI-SPEC.md § Theme (the screen list), Rule 8; memory rule: a hidden Chrome window makes every measurement lie — assert `innerHeight > 0` and visibility
    - `tests/e2e/runs.spec.ts` (04-28 — the local seeding it uses)
  </read_first>
  <action>
`$PNPM build && $PNPM start` locally (record the PID), seed the local DB as `runs.spec.ts` does (plus one `running` and one `partial/exceeded_estimate`
run with a truncated tile, a tentative listing, an attached business), and capture at 1280×800 and 390×844, light and dark:
run report (running; partial), preset detail with `PLACES_MODE` = off / ids_only / enterprise (restart `next start` per mode with the env set
for that process only), `/review?kind=google` Google item, `/businesses/{id}` Google card (with history open), `/sources` transient card.
Save to `docs/measurements/04-screens/<screen>-<mode>-<viewport>-<theme>.png` and list them in `README.md` with what each proves. For every
screenshot, first assert `window.innerHeight > 0` and the page is visible. Stop exactly the recorded PID(s).
  </action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && ls docs/measurements/04-screens/*.png | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - ≥ 32 screenshots (8 screen states × 2 viewports × 2 themes), each listed in the README
    - No screenshot shows a raw stopped-reason key, a map, or a Google-derived value without the "Google Maps" tag
  </acceptance_criteria>
  <done>The real screens exist as reviewable evidence in both themes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: danlo reviews the screens</name>
  <files>docs/measurements/04-screens/README.md</files>
  <read_first>
    - `docs/measurements/04-screens/README.md`
  </read_first>
  <what-built>The run report, preset actions in all three modes, the Google review item, the business Google card and the transient source card — built app, both themes, desk and phone.</what-built>
  <how-to-verify>
1. Open `docs/measurements/04-screens/README.md` and page through the images.
2. Check: truncation warning is amber with an icon and a sentence; the cost is the biggest number; "Google Maps" sits beside every Google-derived value and reads grey in light / white in dark; the off mode shows the notice and no accent button; the transient card is dashed and separate from the ledger.
3. Reply `approved` or list what to change.
  </how-to-verify>
  <acceptance_criteria>
    - danlo replied `approved`, or the listed changes were made and re-shot
  </acceptance_criteria>
  <resume-signal>Type `approved` or describe issues</resume-signal>
  <action>Point danlo at docs/measurements/04-screens/README.md and wait for approved or a change list.</action>
  <verify>Human checkpoint — the reply is recorded verbatim in the SUMMARY; no automated command applies.</verify>
  <done>danlo's reply is recorded and the plan proceeds (or stops) exactly as it directs.</done>
</task>

<task type="auto">
  <name>Task 4: Close 04-VALIDATION</name>
  <files>.planning/phases/04-places-transient-verifier/04-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/04-places-transient-verifier/04-VALIDATION.md`, `docs/measurements/04-gate-mutations.md`
  </read_first>
  <action>Fill every row of the Per-Task Verification Map's status from the plan SUMMARYs (✅ / ❌ / ⚠️); tick the Wave 0 checklist; tick the sign-off list; set `nyquist_compliant: true`, `wave_0_complete: true`, `status: complete`; write "Approval: approved <date> — M26–M53 logged in docs/measurements/04-gate-mutations.md; screens approved by danlo".</action>
  <verify>
    <automated>cd /c/Users/danlo/prospector && grep -n "nyquist_compliant: true" .planning/phases/04-places-transient-verifier/04-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `nyquist_compliant: true` and `status: complete` in the frontmatter; no `⬜ pending` rows remain
  </acceptance_criteria>
  <done>The phase's validation contract is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutation runs → local DB and tree | deliberate temporary breakage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-16 | Tampering | mutations leaking into commits | mitigate | `git diff --stat` empty after every code mutation; catalog read after every DB revert; never against production |
| T-4-05 | Information disclosure | screenshots | mitigate | taken of the local DB seeded with synthetic data; no Google text can appear (none is stored) |
</threat_model>

<verification>
- 28 mutation rows; ≥ 32 screenshots approved; VALIDATION closed
</verification>

<success_criteria>
- Phase 4 is gate-complete and ready for /gsd-verify-work and /gsd-secure-phase
</success_criteria>

<output>
After completion, create `.planning/phases/04-places-transient-verifier/04-33-SUMMARY.md`
</output>
