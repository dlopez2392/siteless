node_modules/
.next/
.env
.env.*
!.env.example
.DS_Store
*.log
.vercel
/.next/
/out/
/coverage/
/playwright-report/
/test-results/
/drizzle/meta/_journal.json.bak
*.tsbuildinfo
tests/e2e/.auth/
# Workflow DevKit (04-01, D-14). The local world and the test world write run state
# here; `.swc/` is the SWC plugin cache; the .well-known tree is generated by
# `next build` / `next dev` (the builder also writes a nested `.gitignore *` inside it,
# this line documents it at the top level). 04-RESEARCH Pitfall 6.
.workflow-data/
.workflow-vitest/
.swc/
src/app/.well-known/workflow/
